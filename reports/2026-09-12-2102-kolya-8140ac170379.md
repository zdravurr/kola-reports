# kolya-8140ac170379

_2026-09-12 21:02 UTC_

---

🧰 Задача 8140ac170379 [infra] — ЗАКРЫТА

**Что было:** контракт test_prune_ledger_divergence_is_not_a_wall падал: ModuleNotFoundError phone_hands.

**Причина:** в x_follow.py восемь импортов phone_hands внутри функций. Они находили модуль только потому, что в окружении задан PYTHONPATH=lib. Без этой переменной, то есть в чистом окружении, отписка (`_do_one_unfollow`) падала сразу, ещё до экрана.

**Починка:** x_follow сам один раз добавляет lib/ в путь при загрузке модуля. Работа модуля больше не зависит от окружения, в котором его запустили.

**Проверка:**
- Падение воспроизведено без PYTHONPATH (`python3 -I`): тот же Traceback.
- После фикса три prune-контракта зелёные в том же чистом режиме.
- Полный набор контрактов: 272/272 зелёных.

**Отдельной задачей:** набор контрактов наследует PYTHONPATH техника и поэтому такие поломки не видит. Предложено запускать контракты в чистом окружении.

Операционку не трогал.
