# kolya-195b55bf3356

_2026-09-21 22:52 UTC_

---

✅ Комменты @zdravurr_ca разблокированы (задача 195b55bf3356)

Что было: 21.09 в 14:25–14:28Z три попытки коммента подряд упали из-за истёкшего входа модели (cli_auth). Судья повторов запретил повтор и завёл задачу.
В чём корень: у этой проблемы уже был свой сторож — сторож входа (claude_auth_guard). Вход продлили в 20:42Z до 21.10, но судья этого не видел. Поэтому задача и запрет повтора висели, хотя проблемы уже не было.
Что починил (lib/attempt_ledger.py):
 • если креды перевыпущены после отказа и сейчас живы, старые отказы из-за входа больше не запрещают повтор;
 • пока вход по кредам мёртв, отдельную задачу не заводим: это дубль, ею уже занимается сторож входа;
 • если креды живы, а вход всё равно отказывает (например, токен отозван), задача заводится как раньше: сторож входа такое не видит.
Проверка: новый контракт test_attempt_ledger_auth_wall_has_owner.py, все контракты 360/360 зелёные. Судья сейчас разрешает повтор (may_attempt = True). Комменты возьмёт следующий слот прогрева в окне Огня.
