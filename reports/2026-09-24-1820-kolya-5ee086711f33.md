# kolya-5ee086711f33

_2026-09-24 18:20 UTC_

---

**Задача 5ee086711f33 — «Огонь не производит комменты @zdravurr: CLI понимания недоступен»**

**Что оказалось.** CLI не падал. В 16:41 UTC одна попытка коммента оборвалась: `stuck_repeat`, то есть одна и та же витрина поста разобрана 4 раза подряд, а композер так и не открылся. Это защита screen_sense от повторов на одном посте. Заход сам перешёл к следующему посту и оставил ещё 1 коммент. Отсюда 5 комментов из 6.

**Корень.** Сторож ogon_quota считал текст «понимание недоступно» признаком падения транспорта CLI при любой причине внутри скобок. Поэтому стена одного поста получила ярлык «умер CLI, лечить вход» и подняла техника.

**Починка** (`lib/ogon_quota.py`):
- транспортом теперь считаются только настоящие причины: `cli_auth/limit/timeout/rc/empty`, «вход мёртв», «модель недоступна»;
- для `stuck_repeat`, `solve_deadline`, `sense_blind` и похожих заведён отдельный класс `sense_screen_wall`: «композер не открылся: стена витрины (CLI жив)». Словарь очереди относит его к `ui_unreachable`.

**Проверка.** На живом журнале @zdravurr сегодняшний отказ получает класс `sense_screen_wall` → `ui_unreachable`. Новый контракт `test_sense_wrapper_stuck_repeat_is_not_transport.py`, весь набор 371/371 зелёный.

Прогрев, мост и вход исправны, от Босса ничего не требуется.
