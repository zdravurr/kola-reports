# kolya-7da864e289a6

_2026-08-23 14:42 UTC_

---

# 7da864e289a6 — выравнивание версий openclaw: упёрлось в root, путь передан дальше

**Итог: не починил. Не из-за незнания как — знаю точно как; упёрлось в права.** Породил задачу
`33bf5795aa47` [capability_missing] с готовым пошаговым runbook. Решения от Босса не жду и не прошу —
задача в очереди и подхватится тем, у кого есть права.

## Что подтвердил (двумя прямыми вызовами, не по памяти)
- `npm --version` — отказ по allowlist сессии.
- `systemctl --user status openclaw-gateway` — отказ по allowlist сессии.
- Обходной двери в `workspace/scripts` нет: grep по `systemctl`/`npm ` даёт только упоминания
  в `gateway_log.py` и аудитах, исполняющей обёртки не существует.

То же самое видела сессия `9229865b06f6`. Два раза подряд одна стена — это не флап, это стена.

## Что выяснил НОВОГО (этого в уликах не было)
`ExecStart` юнита: `/usr/bin/node /usr/lib/node_modules/openclaw/dist/index.js gateway --port 18789`.
Гейтвей запускается из **root-owned системного префикса** `/usr/lib/node_modules` — песочница сессии
не даёт туда даже `ls`. Значит дело **не только в allowlist**: даже выдай мне `npm`, от `botuser`
запись в этот префикс не пройдёт. Апгрейд требует **root (`sudo npm -g`)**, один прогон.

Своими руками расширять себе allowlist или брать права я не стал — это ровно тот забор, который
не обходят.

## Мина, которую надо снять ПЕРВОЙ (проверил, взведена)
`~/.config/systemd/user/openclaw-gateway.service.d/zz-debug-log.conf` (создан 23.08 13:31), содержимое:
```
[Service]
Environment=OPENCLAW_LOG_LEVEL=debug
```
Сейчас неактивна (reload не делали). Но `daemon-reload` без её снятия уводит гейтвей в debug-логи
навсегда. В runbook она снимается шагом 2, до установки.

## Что останется ПОСЛЕ выравнивания версий (не смешивать)
`missing scope: operator.admin` — 1694 раза за 3 ч на сборке, которая методы знает (до рестарта 13:28
23.08). Версиями это не лечится: это scope токена диригента, т.е. отдельное решение о правах.
Заводить отдельной задачей после апгрейда, а не подшивать сюда.

## Чего я НЕ делал
- Контракты не гонял: кода/конфига не менял ни строки, гонять их не по чему.
- Drop-in не удалял: без `daemon-reload` смысла нет, а лезть правкой вне воркспейса ради
  полумеры — хуже, чем оставить одну явную запись в runbook.
