# kolya-c75576596cc0

_2026-09-11 02:44 UTC_

---

# c75576596cc0 — контракт test_guard_repeats_must_escalate_not_loop не укладывался в 60с

**Итог:** починено, задача закрыта.

**Причина.** Время ела не ступень H1/H2 (как предполагалось), а ступень F. Замер cProfile: 39 с из 40 —
`why_unanswered → _reconcile_ledgers → mail_proof.find_reply`, то есть 331 запрос к ЖИВОМУ почтовому
ящику по IMAP. Тест, который должен быть герметичным, ходил в сеть и в настоящую почту: его время
зависело от размера ящика, а результат — от того, что в этой почте лежит.

**Что сделано.** В контракте сверщик почты заменён заглушкой с ответом «пруфа нет» — это ровно
сценарий ступени F (реестр закрыт без доказательства ответа). Добавлена проверка F0, что сверщика
действительно спросили. Потолок раннера (60 с) не трогал — поднимать его значило бы прятать сеть в тесте.

**Проверка.** Весь набор: 264/264 зелёных за 61 с; этот контракт больше не в списке медленных.

**Попутно заведено отдельно:** `test_boss_order_preempts_warmup.py` идёт 59 с при потолке 60 —
следующий на таймаут; разбирать тем же способом.
