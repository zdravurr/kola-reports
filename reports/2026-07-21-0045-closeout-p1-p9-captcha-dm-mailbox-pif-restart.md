# closeout-p1-p9-captcha-dm-mailbox-pif-restart

_2026-07-21 00:45 UTC · финальный close-out_

**Titan pid 72507 / Solana pid 72522 — проверены ДО и ПОСЛЕ рестарта, НЕ ТРОНУТЫ (оба active,
7д аптайм). Workspace `0a5ad96..80a3efa` (6 коммитов) запушен в kola-workspace-backup.**

Работал по пунктам: сделал → живая проверка → закрыл → следующий.

---

## P1 — CAPTCHA-ПОЛИТИКА ✅ ЗАКРЫТ (`233bef9`)
`captcha_solve.py` теперь несёт ОБЕ ветки как СТАНДИНГ В КОДЕ: (1) токен-путь; (2) если площадка
отвергла (session-bound виджет) → IN-SESSION: читаем challenge ГЛАЗАМИ, координаты решают ЛЮДИ
(2captcha CoordinatesCaptcha), тапаем плитки НАСТОЯЩИМ human_tap, verify ФАКТОМ экрана.
Запрет «не тапать плитки» СНЯТ (твой приказ). `solve_coordinates`/`solve_in_session`/`handle_captcha`.
**Живьём:** оба решателя аутентифицируются (2captcha $4.97, capsolver $9.99); токен-путь прогнан
сквозняком — оба честно отказали демо-цели, код вернул CaptchaUnsolved с обоими текстами.
Полный tile-verify требует РЕАЛЬНОЙ капчи на площадке — не воспроизвести по требованию (честно).

## P2 — X DM УДАЛЕНЫ ЦЕЛИКОМ ✅ ЗАКРЫТ (`43a21a5`, −717 строк)
X DM = спам. Снято: файл `dm_reply_one.py`; весь DM-блок в zdravurr_auto/ca_auto — `_send_dm`
(с синтетик-кликом `b.click()`), `check_and_reply_dms`, генерация ответов, разлочка X-Chat; оба
вызова (entry-rule + CLI `dm`); ссылки в daily_self_audit. **Проверено, что ничто живое не
зависит — ПО ВСЕМ ТРЁМ каналам:** import (звались только из этих 2 файлов), cron (нет),
subprocess (нет). Оба auto-файла парсятся.

## P3 — КОНВЕРСИЯ СИНТЕТИК-КЛИКОВ ✅ ЗАКРЫТ (`33f20bc`) — с честной отсрочкой live-пруфа
Расследование живьём: в ЖИВОМ X-warmup синтетик-кликов НЕТ. Кнопки Post/Reply — через eyes.act;
13 `real_tap` — kernel-тап с джиттером (реальное input-событие). Унифицировал: `real_tap`
делегирует в `human_tap.tap` (случайная точка + dwell + kernel). **Офлайн-пруф:** 8 тапов → 7/8
разных точек. 2 JS-`.click()` — в МЁРТВОМ `z_warmup_continuous` (presence off с 02.07), под
жирным гвардом «конвертировать перед воскрешением».
🔴 **Честно:** live-пруф на РЕАЛЬНОМ постинге НЕ сделан — сейчас 00:xx, ближайший прогрев 10:00 UTC
(~10ч), лишний прогон ты запретил. Правка безопасна (тот же kernel + рандомизация), уедет в бой
следующим плановым заходом.
**Остаток синтетик-кликов в живых путях: 0.**

## P4 — РАЗДЕЛЕНИЕ ЯЩИКОВ ✅ ЗАКРЫТ (`806e1fa`)
🔴 **Риск доказан живой IMAP-сверкой:** в ТВОЁМ личном ящике 2r — **34 письма твоего личного
eBay**. scan() применял все сигналы к обоим ящикам → твоя будущая ЛИЧНАЯ покупка ушла бы к Боре
как наша закупка. Фикс `SIGNAL_MAILBOX`: eBay/OfferUp/Nextdoor/FB → **ТОЛЬКО 4r**.
**Замер по площадкам:** 2r → ebay 34(личные), cl 2, ig 5, x 5; 4r → offerup 63, nextdoor 53,
facebookmail 18, ebay 1(наш welcome). **CRAIGSLIST — задокументированное исключение:** аккаунт
заведён на 2r, мигрировать нельзя без переоформления живых объявлений; риск назван.
**Доказано:** eBay ищется 2 раза (4r×2), а не 4 — твой личный eBay лида БОЛЬШЕ НЕ создаст.

## P5 — PIF-СТОРОЖ ✅ ЗАКРЫТ (`0cf4b41`)
Отпечаток Canary 202607 (lynx_beta) истекает **2026-08-19** → тогда FB/Messenger/OfferUp/Play
падают на Play Integrity (выглядит как «приложение сломалось»). Дата была только у тебя в голове —
теперь в файле. `lib/pif_expiry.py`: считает дни, предупреждает за 21 день (<7 срочно), крон 8:20.
**Живьём:** 29 дней, PIF+autopif4 на месте; форс-предупреждение доставлено тебе (msg 1408).
Авто-обновление через штатный autopif4 — только по явному `--refresh` (смена отпечатка вживую
может сломать прошедшие проверку приложения).

## P6 — ДЕТЕКТОР МЁРТВЫХ АККАУНТОВ ✅ ЗАКРЫТ (`80a3efa`)
Удаляет follow-сиды навсегда → «не должен лгать». Логика JS здорова (переписана 20.07 контрольным
опытом). 🔴 **Живая проверка исполнения упёрлась в замороженную вкладку** — вне активной сессии
cdp_js отдаёт None (доказано: даже `1+1`→None). Именно поэтому ты сказал «на следующей Ogon-сессии».
**Найдена и закрыта дыра:** None→{} тихо скипал как «нет кнопки» — слепой детектор молчал. Теперь
None → явное «ДЕТЕКТОР НЕ ПРОЧИТАЛ — сид НЕ трогаю». Ложного «мёртв» из слепоты быть не может
(доказано таблицей разбора). Живое исполнение JS подтвердится на плановом прогреве (self-логи).

## P7 — ДВА САМО-ЗАКРЫВАЮЩИХСЯ ХВОСТА ⏳ ЖДУТ ПЛАНОВЫХ ПРОГОНОВ
(a) Post-vs-Schedule-post — хук `совпало НЕТОЧНО → переспрос` на месте; (b) OfferUp-ad — хук
`ПОМЕХА → закрываю` на месте. Оба само-логируются на следующих плановых прогонах (X 10:00 UTC;
OfferUp — при появлении ad). В ЭТОЙ сессии не закрыть без лишнего анти-бан-бёрна — по твоему же
запрету. Механизмы готовы и ждут.

## P8 — eBay: НЕ ТРОГАТЬ ✅ ЗАПАРКОВАН
Маркер `ebay.parked=true` в registrations.json + память (грузится каждую сессию). Ничего не
листил, выплаты не трогал, «sold» не верифицировал. ⚠️ Явно записал: снятие капча-запрета (P1)
eBay НЕ разблокирует — отдельный park, снимаешь только ты.

## P9 — ЧИСТЫЙ РЕСТАРТ + ПОЛНАЯ ВЕРИФИКАЦИЯ ✅ ЗАКРЫТ
Стек размечен перед касанием: gateway (systemd --user), inbox-watcher=push-демон (systemd --user),
dirigent (docker). **Titan/Solana — СИСТЕМНЫЙ systemd, отдельно; PM2 пуст (не они).**
Рестарт ТОЛЬКО OpenClaw: `docker restart dirigent` + `systemctl --user restart gateway/inbox-watcher`.
**Titan/Solana PID до = после: 72507 / 72522, оба active.**
Всё вернулось живым: gateway HTTP **200**; Dirigent Up + **3 полосы live** (procurement/warmup/sales),
лог обрабатывает лиды; push-демон рестартнут (heartbeat 0м); mail-watcher идёт (00:30); Ogon окно
(06-22 ET, сейчас 20:37 ET) + квота «норма выдержана»; **capability_audit: ВСЁ ЖИВО (11/11)**;
supervision pass доставлен. **eyes-понимание после рестарта — ЖИВОЙ пруф:** прочитал экран
(uidump, 31 элемент), screen_sense опознал «FB news feed, pinned Marketplace banner», on_target=true.
git синхронен с origin.

---

## ЧЕСТНО НЕ ЗАКРЫТО ЖИВЬЁМ (ждёт плановых прогонов, не в этой сессии — по запрету на лишний бёрн)
1. **P3:** конвертированный real_tap на РЕАЛЬНОМ постинге — подтвердится на прогреве 10:00 UTC.
2. **P6:** валидное исполнение cdp_js(_STATE_JS) на живой вкладке (gone=true на мёртвом, false на
   живом) — на следующей Ogon-сессии (вне сессии вкладка заморожена, доказано).
3. **P7:** оба хвоста — на следующих плановых прогонах (X 10:00; OfferUp опортунистично).
4. **P1:** полный tile-tap-verify — только на реальной капче площадки.
5. `?? listing_check` — стрей-JPEG (до сессии) в корне репо, не мой, помечен, не удалён.

**Итог:** P1,P2,P4,P5,P6,P8,P9 — ЗАКРЫТЫ (P4/P9 доказаны живьём end-to-end). P3 — код+офлайн-пруф,
живой постинг на след. прогреве. P7 — механизмы готовы, ждут плановых прогонов. Titan/Solana целы.
