# self-improve 8 flags applied 24jul

_2026-07-24 15:05 UTC_

---

🛠️ САМО-РАЗБОР 24.07 — 8 ФЛАГОВ ИЗ АУДИТА ПРИМЕНЕНЫ И ПРОВЕРЕНЫ ЖИВЬЁМ

Взял флаги, которые разбор описывал сутками, но НЕ применял. Каждый — фикс + пруф. Titan/Solana НЕ ТРОНУТЫ. Коммиты через commit-gate (токен «для своих», гейт честно алертнул).

📦 Коммиты: 7e6198a · af4cf68 · 228c471 (workspace)

━━━ P1.1 — stale /root/titan-bot ━━━
БЫЛО: sys.path.insert(0,'/root/titan-bot') в report_due.py + turn_report_watchdog.py + dirigent_enforce._escalate. /root/titan-bot = 0700 root → из-под botuser НЕДОСТИЖИМ, поэтому мёртвый fallback годами читался как «стале-путь не починен» (🔴🔴 6 прогонов/сутки).
СТАЛО: fallback обёрнут в `if os.geteuid()==0` — botuser его вообще не трогает, root сохраняет страховку. dirigent_enforce._escalate ДОБАВЛЕН родной botuser-путь notify (раньше знал ТОЛЬКО titan-bot → надзор-с-зубами ловил нарушение и молча не мог доложить).
ПРУФ: launcher_path_audit EXIT=0, «✅ все пути живые, stale не найдено».

━━━ P1.2 — стоп-правило «3+ цикла → стоп+эскалация» ━━━
ПРИЧИНА (докопался): правило БЫЛО в коде (lib/fix_repeat_guard), но задеплоено 23.07 ПОСЛЕ утреннего прогона → 24.07 = его ПЕРВЫЙ запуск, все счётчики законно стартовали с n=1, поэтому cars_ev (10 задокументированных циклов) показал n=1 и «снова не сработало».
СТАЛО: добавил _history_cycles() — счётчик СИДИТСЯ из числа прошлых секций «Само-разбор» в LEARNINGS.md проекта, так что правило, подключённое в середине залипшего цикла, эскалирует СРАЗУ. escalate_to_boss теперь ОТВЕЧАЕТ на вопрос петли (резолвит абс. путь LEARNINGS.md — посылка «файла нет» была ложной). Хендлер при stop гасит recurring/фиксы и в ДАЙДЖЕСТЕ (не только в LEARNINGS) — «10-й цикл» больше не всплывёт.
ПРУФ: тест n=1→2→3→стоп+эскалация; живьём для cars_ev — ОДИН пуш Боссу (msg 1662), n=5, escalated=True; `fix_repeat_guard status` → «🛑 cars_ev: 1 фикс в стопе».

━━━ P1.3 — двойная печать launcher_path_audit ━━━
БЫЛО: каждый 🔴 в launcher_audit.log дважды (свой stdout-список + эхо notify_boss того же алерта).
СТАЛО: 🔴 идут ТОЛЬКО через notify-блок, 🟡 — только stdout. Дедуп находок по (sev,file,path) уже был.
ПРУФ: синтетический прогон — a.py в логе РОВНО 1 раз (было 2).

━━━ P1.4 — провиженинг: пустой лог ≠ green ━━━
БЫЛО: health решал LLM по ПУСТОМУ лог-ошибок → green, пряча, что после капча-фикса 17.07 НИ ОДНОГО сабмита не было.
СТАЛО: детерминированный provisioning_activity_health() считает РЕАЛЬНЫЕ сабмиты (счётчик отдельно от ошибок); 0/24ч → перебивает green на yellow. gmail_signup_with_spoof.sh пишет маркер signup_start.
ПРУФ: пустой лог → yellow/0; 1 сабмит → green/1.
⚠️ БОССУ: аккаунт НЕ создан — только eBay от 18.07 (parked). Сама провиженинг-цепочка сабмитов сейчас ручная (не крон), потому и тихо.

━━━ P1.5 — порог X @zdravurr (фин) 12→26ч ━━━
СТАЛО: 26 в ПЕРСИСТЕНТНОМ гейте post_watchdog.ACCOUNTS (там же FB=26). Утренний аудит больше не хардкодит (был 14) — читает порог из гейта (единый источник, дрейф невозможен).
ПРУФ живьём: `post_watchdog --dry` → «X @zdravurr (фин): … (порог 26ч)».

━━━ P1.6 — FB SoCal EV «тихо скипается 10+ сут» ━━━
РАЗОБРАЛ противоречие с «16ч ✅»: ✅ давал РУЧНОЙ пост 23.07 16:57; последний АВТО-пост — 20.07. last_post_ts теперь (а) всегда отбрасывает ОТОЗВАННЫЕ ложные посты (post_PUBLISHED_RETRACTED — призрак «Tag people» 23.07), (б) в аудите берёт automated_only, чтобы ручная добивка не маскировала мёртвую автоматику. Гейт по-прежнему считает ручной пост (без холостого автопостинга); аудит показывает 110ч → 🔴.
ПРУФ: gate=21.9ч (тихо, не долбит), audit=110ч 🔴 (правда вскрыта).
⚠️ БОССУ (не однострочник): корень тишины — навигация к КОМПОЗЕРУ группы сломана в katana-сборке FB (litho-невидимый композер). Нужен ремонт через СЛОЙ ЗРЕНИЯ, не патч.

━━━ P1.7 — экранные драйверы без замка ━━━
СТАЛО: dirigent_voice._push_photo_to_phone (adb-пуш) и pif_expiry (зонд+refresh) обёрнуты в phone_lock.hold — fail-closed, зонды атомарны. daily_self_audit.LOCK_REFS += "phone_lock" (канонический мьютекс экрана не был в белом списке аудита).
ПРУФ: check_screen → 🟢 «все 12 контролируемых экранных драйвера через lock/priority» (было 🔴 по двум).

━━━ P1.8 — необъявленные драйверы + inbox_sweep ━━━
СТАЛО: объявил 7 драйверов в AGENTS.md RANK-4 (main_liveness, lead_guarantee, dirigent_voice → A; cl_renew_bike_phone, coverage_eyes_sweep → B; pif_expiry → D; agent_capacity → E). inbox_sweep «перестал логировать» = ЛОЖНЯК: его */15-крон снят НАМЕРЕННО (FB→push, OU→email; cron_guard.py:142), сам свип жив (раз/сутки из self_improve), а аудит резолвил несуществующий scripts/inbox_sweep.py вместо projects/sales/. Починил резолв путей.
ПРУФ: check_declarations → 🟢 «все объявлены + логируют; новых нет».

━━━ P2 — СВЕЖИЕ АУДИТЫ ━━━
• launcher_path_audit: EXIT=0, чисто.
• daily_self_audit: Экран/локи 🟢 (было 🔴), Драйверы 🟢 (было 🟡).
• capability_audit (system_director, read-only): 0 сломанных, 13×✅, «ВЕРДИКТ: ВСЯ СИСТЕМА ЖИВА».
• Инвариант песочницы: root-овых файлов в workspace — 0.

ЧЕСТНО: два флага теперь показывают yellow/red НЕ потому что сломаны, а потому что так ПРАВИЛЬНО — провиженинг (0 сабмитов) и FB-авто-пост (мёртв на нав-дефекте) больше НЕ маскируются под зелёное. Это и просил Босс: пустой прогон = не green. Оба корня — на твоё решение (см. ⚠️ выше).
