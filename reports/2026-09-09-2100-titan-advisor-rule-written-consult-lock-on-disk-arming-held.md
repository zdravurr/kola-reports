# titan-advisor-rule-written-consult-lock-on-disk-arming-held

_2026-09-09 21:00 UTC_

---

# 🔴 ТИТАН — ПРАВИЛО СОВЕТНИКА ЗАПИСАНО В КАНОН (2 из 10, +1.52R В ЕГО ПОЛЬЗУ). ЗАМОК НА ДВОЙНУЮ КОНСУЛЬТАЦИЮ — НА ДИСКЕ, НЕ ЗАГРУЖЕН. ВООРУЖЕНИЕ ГЕЙТА — НЕ ВЫПОЛНЕНО: ОКНО «ИЗ ПЛОСКОГО» ЗАКРЫТО ЖИВЫМ SHORT vpos 105.

**2026-09-09 21:00 UTC · Титан `16b851d` на диске, в рантайме по-прежнему `6fa5d45` (PID 705634, с 14:52:36) · `openitems_guard` EXIT=0 до и после · Mercury-SOL не тронут — ни байта, ни чтения, ни рестарта**

---

## 🔴 ЧТО РЕШАТЬ ТЕБЕ

1. **Вооружение книжного гейта НЕ сделано — по твоему же условию.** «Подтвердить плоско перед рестартом;
   если позиция открыта — сказать и ОСТАНОВИТЬСЯ». Позиция открыта: **SHORT vpos 105** (0.0019 BTC @ 78 263.2,
   стоп 79 295.1 на бирже), оба зонда BingX, список ошибок пуст. `config.py` не тронут. Процедура a–d
   записана в канон `§0.BOOK-GATE-ARMING`; окно откроется на закрытии 105. **Если хочешь вооружить
   поверх открытой позиции — это отдельный приказ, я его не выдумывал.**
2. **Правило советника теперь существует вне твоей головы** — `§0.EXIT-ADVISOR-RULE`, в деньгах.
   Текущее состояние: **2 из 10, Σ +1.5237R = +$2.00 в пользу советника.** Контрфакт vpos 104
   РАЗРЕШИЛСЯ в 15:11 UTC — удержанная позиция упёрлась бы в исходный стоп (−1.10R). Контрфакт vpos 101
   (раньше не считался): трейл 19:00 01.09, +0.45R — советник отстал на 0.16R.
3. **Замок на двойную консультацию написан, доказан исполнением, закоммичен `16b851d` — и НЕ ЗАГРУЖЕН**
   по той же причине: рестарта из плоского нет. Загрузится тем же рестартом, что и вооружение.
   Не пропущу этого в следующей сессии: канон говорит «ON DISK, NOT LOADED» в шапке.

---

# 1. ВООРУЖЕНИЕ ГЕЙТА — ОСТАНОВЛЕНО НА ШАГЕ (b), КАК ПРИКАЗАНО

`openitems_guard` первым делом: **EXIT=0** (HEAD 6fa5d45, 11 значений сходятся).

| проверка 20:39 UTC | результат |
|---|---|
| `virtual_positions status='open'` | **1 — vpos 105, SHORT**, открыта 19:45:28 UTC |
| `exit_pending` / `breakeven_jobs` | 0 / 0 |
| BingX зонд 1 + зонд 2 (`_probe_positions`, `unified+raw`) | **SHORT 0.0019 @ 78 263.2** |
| список ошибок зондов | **[] — пусто** |
| открытые ордера | стоп 2097773401682325504, SHORT, trigger 79 295.1 |
| last | 78 272.0 (позиция около нуля) |

**Шаги (a), (c) НЕ выполнялись.** `.bak` config.py не снимал, строку не флипал — флаг на диске под
работающим воркером сделал бы таблицу состояния канона лживой, ровно тот класс, ради которого сторож
существует. AST-сверка того, что `config.py` не изменился: `git diff 6fa5d45 -- titan-bot/config.py` пуст.

**(d) Счётчик ревью ПРОДОЛЖАЕТСЯ:** **10/200** (8 LONG / 2 SHORT), 0 отказов, с нуля от рестарта
07.09 13:29:26. Предрегистрация без изменений: **LONG 4.26 % / SHORT 3.81 % / перекос 1.12×; тревога
выше 5 % на любой стороне или перекос выше 2×.** Граница «dryrun-строки / вооружённые строки» будет
записана в канон меткой времени вооружающего рестарта — сейчас записывать нечего, рестарта не было.

Для следующей сессии в каноне лежит полная процедура: `.bak` → одна строка → AST только `BOOK_GATE_DRYRUN`
(нетронуты `SL_ATR_MULT` 2.25, `TRAIL_MULT_ATR` 1.6875, EMA-конверт, `LONG_PARTIAL_ENABLED` False,
`CONFLUENCE_SCORE_THRESHOLD` 3.0, `EXIT_ADVISOR_DRYRUN` False, три откаченных значения каскада, оба
промпта, размер 30×5) → плоско обоими зондами → рестарт → значения из байткода → строка загрузки →
guard EXIT=0 → метка границы в канон. `16b851d` грузится тем же рестартом.

---

# 2. ПРАВИЛО СОВЕТНИКА — ЗАПИСАНО В `§0.EXIT-ADVISOR-RULE`, В ДЕНЬГАХ

**Популяция:** каждое живое закрытие `close_reason='ai_exit'` с **vpos 101** (первое под кэпом 1 200 и
четырьмя фактами 30.08). **Мера:** реализованный R советника минус R контрфакта «держать до трейла или
стопа» на 1m-свечах BingX в порядке `bardir`, правила `virtual_trader` (исходный стоп; взвод на +1R →
стоп на breakeven = вход × (1 ± 0.002); после взвода трейл `water_mark × (1 ∓ trail_pct)`; net = gross −
0.0005 × size × (вход + выход)) — метод §3c отчёта 15:00. **Правило остановки: на 10 закрытиях, если
Σ(R советника − R контрфакта) ОТРИЦАТЕЛЬНА → `EXIT_ADVISOR_DRYRUN = True`. Положительна → остаётся,
следующее ревью на 20.** Неразрешённый контрфакт не считается, пока не упрётся в стоп или трейл.

## 2a. Состояние: 2 из 10

| # | vpos | сторона | закрыта | советник R (net $) | контрфакт | контрфакт R (net $) | Δ R | Δ $ |
|---|---|---|---|---|---|---|---|---|
| 1 | 101 | SHORT | 09-01 18:00 | +0.2956 (+0.57) | **трейл** 77 124.0 в 19:00 01.09: взвод 18:39 (low 76 574.3 ≤ +1R 76 641.6), water_mark 76 372.5 в 18:45, трейл 0.984 % | +0.4509 (+0.87) | **−0.1553** | −0.30 |
| 2 | 104 | LONG | 09-09 08:30 | +0.5757 (+0.79) | **стоп** 78 350.1 в 15:11 09.09 (low 78 272.9); +1R 79 874.7 не напечатан — максимум после выхода 79 737.7 в 08:36 | −1.1033 (−1.51) | **+1.6790** | +2.30 |
| | | | | | | **Σ** | **+1.5237** | **+2.00** |

vpos 104: в отчёте 15:00 контрфакт был ОТКРЫТ (14:54, −0.219R по рынку). **Разрешился в 15:11 UTC об
исходный стоп** — 733 свечи 08:30→20:42, взвод так и не напечатан. vpos 101: 5 000 свечей с 18:00 01.09;
после выхода советника цена ушла ещё на 900 USD вниз, взвела трейл и вернулась — трейл вышел бы на
77 124.0. Здесь советник обрезал бегуна на 0.16R; на 104 — спас 1.68R. Оба реплея воспроизводимы из
строки БД + BingX (`cf101.py`, `cf104.py` в scratchpad, метод выше).

## 2b. Старая популяция — рядом, для контраста, НИКОГДА не суммировать

vpos 87–98, десять закрытий `ai_exit` под двухфразным промптом: **Σ net −$5.848, ΣR −2.6048**
(87 −0.82 · 88 −0.53 · 89 +2.30 · 90 −0.61 · 91 −0.64 · 92 −1.31 · 93 −0.15 · 96 −1.34 · 97 −2.29 · 98 −0.47).
Тот же механизм, другой инструмент. Записано в канон рядом с правилом с этой оговоркой.

## 2c. Два дефекта рассуждения на vpos 104 — записаны как ФАКТЫ, не как починки

Закрывший вердикт (trades 31113, 08:30:19, `close` 0.72):
1. **«1H expired via TTL (360min since entry, now 222min old)»** — промпт: 1H поставлен за 12.8 ч ДО входа
   (≈16.5 ч на момент закрытия); **222 мин — возраст ПОЗИЦИИ**, не сигнала. Число сфабриковано/перепутано.
2. **«opposing wall collapsed to 16th pct»** — промпт даёт 16-й перцентиль как NOW без entry-значения и
   прямо пишет «NONE of these is a change over time». Вывод об изменении из величины, помеченной как не-изменение.
(Мелкое: `vol_15m` 2.52 → 0.39 приписан 5m — числа верны, таймфрейм нет.)
**Тот же класс, что четыре дефекта читаемости промпта, починенные 31.08 (`§0.PROMPT-PAIRING`) — но в этот раз
ПРОМПТ БЫЛ ВЕРЕН, и модель всё равно прочла его неверно.** Второй вердикт на той же позиции (31115) не
сделал ни одной из этих ошибок. Ничего не менял: судит советника правило выше, в деньгах.

---

# 3. ДВОЙНАЯ КОНСУЛЬТАЦИЯ — ЗАМОК НАПИСАН, ДОКАЗАН, ЗАКОММИЧЕН, НЕ ЗАГРУЖЕН

## 3c. Сколько раз это случалось

| | |
|---|---|
| строк консультаций (`exit_ai_dryrun`) в БД | **173**, 26.07 22:06 → 09.09 20:00 |
| пары ≤10 с на ОДНОЙ открытой vpos | **ровно 1**: 31113 / 31115, 4.0 с, vpos 104 |
| то же при окне 60 с | 1 |
| `CLOSING at market` в журнале (с 07.09 02:20) | 2, обе vpos 104 |
| впустую | **1 вызов модели + 1 попытка закрытия** — пока |

Журнал 08:30, дословно (обрезано): `08:30:10.57 WEBHOOK_IN 15m 'Bullish I-BOS'` → `08:30:16.90 WEBHOOK_IN 15m
'Bullish S-CHOCH'` → `08:30:19.12 [EXIT-ADVISOR-LIVE] close=True 0.72` → `08:30:19.37 [EXIT-ADVISOR-ACT]
vpos=104 CLOSING at market` → `08:30:22.10 VIRTUAL CLOSE vpos=104 reason=ai_exit` → `08:30:23.82
[EXIT-ADVISOR-LIVE] close=True 0.72` (второй) → `08:30:24.07 [EXIT-ADVISOR-ACT] vpos=104 CLOSING at market` →
`08:30:24.35 [ADAPTER] 🔴 close requested but NO live position found — nothing sent`.

## 3a. Замок

`main.py`, вокруг `consult_exit_advisor` — единственной точки входа консультации (все четыре вызывающих:
5m Group B, armed_exit, 15m exit-confirm, hourly в поллере — идут через неё, поллер живёт в том же процессе,
`workers=1`). Один `threading.Lock` **на vpos id**, берётся `blocking=False`, отпускается в `finally`. Вторая
консультация при первой в полёте получает **ОТКАЗ с причиной**, не очередь:
`{'decide': 'refused_inflight', 'close': False, 'confidence': 0.0, 'reason': …}` + строка
`[EXIT-ADVISOR-REFUSED] vpos=… trigger=…` — без вызова модели, без строки в `trades`, без вердикта, без
закрытия. `_advisor_says_close` для отказа — False. Форма `virtual_trader._entry_lock` (`f30e074`, 04.06 —
не 08.08: у входного замка эта дата), два названных отличия: на позицию, а не глобальный; не ждёт 2 с, а
отказывает сразу. Побочное: отказанная HOURLY-консультация всё равно штампует `exit_advisor_last_ts` — тот
час судит вебхучный вердикт.

```diff
--- main.py (6fa5d45)
+++ main.py (16b851d)
@@ -1,4 +1,5 @@
 import os, ccxt, requests, sqlite3, time, re, json, builtins, subprocess, traceback, bisect
+import threading   # 2026-09-09: the per-position exit-consultation lock (see consult_exit_advisor)
@@ consult_exit_advisor @@
+_exit_consult_inflight = {}                      # vpos id -> threading.Lock
+_exit_consult_inflight_guard = threading.Lock()  # protects the dict itself, never held long
+
+def _exit_consult_lock_for(vpos_id):
+    with _exit_consult_inflight_guard:
+        lk = _exit_consult_inflight.get(vpos_id)
+        if lk is None:
+            lk = _exit_consult_inflight[vpos_id] = threading.Lock()
+        return lk
+
 def consult_exit_advisor(vpos_row, symbol, side, exit_signal_name, trigger):
-    ctx = _build_exit_context(dict(vpos_row), symbol, side, exit_signal_name, trigger)
-    advice = claude_advisor.consult_for_close_rich(ctx)
-    ... persist, print ...
-    return advice
+    try:
+        _vid = dict(vpos_row).get('id')
+    except Exception:
+        _vid = None
+    _lk = _exit_consult_lock_for(_vid) if _vid is not None else None
+    if _lk is not None and not _lk.acquire(blocking=False):
+        _why = (f"an exit consultation is already in flight for vpos {_vid} ({symbol} {side}); "
+                f"this one (trigger={trigger}, signal={exit_signal_name!r}) is REFUSED, not queued — "
+                f"no model call, no verdict, no close")
+        print(f"[EXIT-ADVISOR-REFUSED] vpos={_vid} {symbol} {side} trigger={trigger} — {_why}", flush=True)
+        return {'decide': 'refused_inflight', 'close': False, 'confidence': 0.0, 'reason': _why, 'raw': None}
+    try:
+        ctx = _build_exit_context(dict(vpos_row), symbol, side, exit_signal_name, trigger)
+        advice = claude_advisor.consult_for_close_rich(ctx)
+        ... persist, print — byte-for-byte the old body, indented one level ...
+        return advice
+    finally:
+        if _lk is not None:
+            _lk.release()
```

## 3d. AST — изменение ограничено точкой входа консультации

Старый и новый `main.py`: **156 → 160 стейтментов верхнего уровня; 155 из 156 старых узлов
байт-в-байт по `ast.dump`.** Добавлено 4: `import threading`, `_exit_consult_inflight`,
`_exit_consult_inflight_guard`, `_exit_consult_lock_for`. Изменён 1: `consult_exit_advisor`. Удалено 0.
`virtual_trader.py` не тронут (0 упоминаний замка — стоп и трейл его не знают). `py_compile` ✓.
Бэкап `main.py.bak_consultlock_20260909T204800Z`.

## 3b. Доказано исполнением: удерживаемый замок НЕ МОЖЕТ задержать стоп

Реальный текст четырёх узлов вытащен из `main.py` через AST и исполнен со стабами (main.py НЕ импортировался —
он поднимает Flask и трогает боевую БД); стаб модели спит 3 с, как настоящий вызов:

| шаг | результат |
|---|---|
| A: консультация vpos 104 в полёте (3 с) → B: вторая на 104 через 0.2 с | **отказ за 0.08 мс**, `decide=refused_inflight`, `close=False`, `_advisor_says_close`=False, модель вызвана 1 раз |
| C: консультация vpos 105 (другая сторона) при удерживаемом 104 | **допущена** — замок на позицию, не глобальный |
| тик поллера при удерживаемом замке: hourly-консультация (отказ) → проверка стопа `last ≤ 78 350.1` | **стоп сработал, тик 0.07 мс**, A всё ещё в полёте |
| A закончила → D: новая консультация 104 | допущена, модель вызвана, 3.00 с |
| модель бросила исключение | замок отпущен (`finally`) |
| строка без `id` | консультация без замка, без падения |

**7 из 7 утверждений прошли.** Стоп и трейл — секции 3 и 4 поллера, после hourly-блока, замка не читают;
единственное, что может держать замок, — консультация, и ждать её никто не умеет: `blocking=False`.

**Коммит `16b851d` (`/root`, только `titan-bot/main.py`). 🔴 НЕ ЗАГРУЖЕН** — рантайм исполняет `6fa5d45`.
Применится тем же рестартом из плоского, что и вооружение (§1).

---

# 4. ЗАПИСАНО, НЕ СДЕЛАНО

**(a) Ложная тревога `[VPOS-FILL] 🚨 … MANUAL ACTION REQUIRED` 08:30:20.771.** Рыночное закрытие советника
ушло 08:30:19.4; поллер заполнения в 08:30:20.8 увидел позицию GONE при неисполненном стопе и закричал;
`_do_close` записал строку в 08:30:22.1. Рук не требовалось. Класс — «тревога о состоянии, которое само
разрешилось»; шаблон — резолвер Mercury-SOL 08.08 (предикат на стадию тревоги, положительное чтение биржи,
никогда по возрасту, fail-closed). **Не строил.** В каноне `§0.RECORDED-NOT-ACTED (a)`.

**(b) `NeedDaemonReload=yes`.** Юнит и drop-in Титана не менялись (май/июль). В 13:38:37 UTC в
`/etc/systemd/system` добавлен `snap-ffmpeg\x2d2404-156.mount` — обновление snap ffmpeg. Не Титана.
`daemon-reload` не запускал. В каноне `§0.RECORDED-NOT-ACTED (b)`.

---

# 5. КАНОН И ПРОВЕРКИ

* `OPEN-ITEMS.md` (бэкап `OPEN-ITEMS.md.bak_advisorrule_20260909`): шапка → `16b851d` с абзацем «ON DISK, NOT
  LOADED» + «гейт приказан, не вооружён: окно закрыто vpos 105»; новые `§0.EXIT-ADVISOR-RULE`,
  `§0.BOOK-GATE-ARMING`, `§0.CONSULT-LOCK`, `§0.RECORDED-NOT-ACTED`. Таблица состояния — без изменений
  (config.py не менялся). `openitems_guard`: EXIT=0 в начале; EXIT=1 сразу после коммита (шапка 6fa5d45
  против 16b851d — ровно та рассинхронизация, для которой он существует); шапка обновлена → **EXIT=0**.
* Титан в рантайме: PID 705634, NRestarts=0, `6fa5d45`, vpos 105 SHORT под биржевым стопом.
* Mercury-SOL: ни байта, ни чтения, ни рестарта. Ссылка на его отчёт 08.08 — из kola-reports, не из его кода.

**Итог: правило советника записано в деньгах и стоит 2/10, +1.52R за советника (контрфакт 104 разрешён
стопом в 15:11); замок на двойную консультацию доказан и закоммичен `16b851d`, но не загружен; вооружение
гейта остановлено на шаге (b) по твоему условию — SHORT vpos 105 открыт; счётчик 10/200 идёт, предрегистрация
стоит; две записи без действия.**

Титан `16b851d` на диске · `6fa5d45` в рантайме.
