# session-final-esim-eyes-targeting-coverage-irena

_2026-07-20 18:26 UTC_

---

# Сессия 20.07 (часть 3+4) — СВОДНЫЙ ОТЧЁТ

**Titan / Solana НЕ ТРОНУТЫ** — проверено: `titan.service` pid **72507**, HEAD **`6c35b9d`** (как до
сессии), `mercury-sol` pid 72522 жив. Ни один файл обоих ботов не открывался на запись.

Пуш: **`6da54d4..1530c31`**, незапушенных коммитов **0**.

---

## 🔴 0. СНАЧАЛА — ИСПРАВЛЕНИЕ МОЕЙ ЖЕ КАРТИНЫ

Ты остановил меня словами «зациклился на OfferUp». **Я на OfferUp не заходил ни разу.**
Тот заход — `worker3`, поднятый `lead_guarantee` по лиду `lead-offerup-a` (журнал: назначения
1/4, 2/4, 3/4). Мои две попытки взять экран были на **ЧТЕНИЕ** тредов, обе не получили замок и
**ничего не тапали**.

Но проверил я это только когда ты остановил. **Считать чужой заход своим — такая же подмена
факта, как всё остальное в этом списке.** При этом след оказался прав по сути: там был живой
дефект, который я бы не нашёл, продолжая ждать замок.

---

## 1. 🔴 ГЛАЗА ТАПАЛИ В НАШЕ ЖЕ ОТПРАВЛЕННОЕ СООБЩЕНИЕ (`f555f50`)

**Диагноз по факту, с экрана.** Снимок + дамп 40190 байт, тред **JoeJoe Cee**, `com.offerup`.
Цель «Reply» совпала с **нашим же ранее отправленным сообщением**:

> «Sorry for the slow reply — the sectional is gone…» — **167 символов**,
> `class=android.widget.Button`, `box=[206,1555][1028,2041]`

Слово `reply` сидело внутри оборота **«slow reply»**. Глаза честно доложили «вижу цель» и
**трижды тапнули в тело переписки**. Тапы по тексту подняли меню выделения **«Copy text»**,
залипшее поверх треда — дальше любой тап уходил в него. Отсюда три `verify_failed` и вид «цикла».

**Настоящий композер был на экране всё это время и ни разу не стал целью:**
`EditText box=[149,2186][935,2279]`, content-desc «Message…».

### Глаза были ИСПРАВНЫ — и это часть диагноза
| проверка | факт |
|---|---|
| `eyes_health.ensure()` | **(True, 'ok:кадр 40186 байт')** |
| свежий дамп | с 1-й попытки, 40190 байт, совпадает со снимком |
| adb | `100.85.178.10:5555 device` |
| заморозка вкладки / рекламная карточка | **нет** |
| замок экрана | держал `xwarmup` (плановый заход Огня 16:00, бюджет 3600с) |

Сломано было **не зрение, а прицеливание**. Fail-closed по глазам тут не сработал бы — по всем
своим проверкам способность здорова. **Слепота и меткость — разные способности, сторож был
только на первую.**

### Родство с сегодняшним же `8f59fd3`
Утром цель «Post» совпала с обёрткой во весь экран — закрыл **площадью** (>50% ⇒ не кнопка).
Здесь пузырь **15%** экрана, потолок площади его не ловит. **Дефект тот же, размер другой.**
Латая частный случай, я не назвал общий.

### Починка — два независимых признака
1. **ДЛИНА.** Подпись контрола коротка, переписка длинна. Порог от длины цели (6×, но ≥40).
2. **КЛИКАБЕЛЬНОСТЬ.** `clickable`/`enabled` **выбрасывались при парсинге** — единственный факт,
   отличающий кнопку от пузыря, до матчера не доезжал вообще.

⚠️ **Честно: признак (2) в ЭТОМ инциденте НЕ спас бы** — узел-обёртка имел `clickable="true"`.
Ловит именно **длина**. Проверил, а не предположил: в дампе ОБА узла с этим текстом
(Button `clickable=true`, TextView `clickable=false`) — отвергаются **оба, по длине**.

**Проверено на том самом захваченном дампе:** узел-ловушка → `score 0.0`;
`Back` / `Navigate to profile` / `Message…` → `score 3.2` (не сломаны).
Меню снято `KEYCODE_BACK`, экран вернулся в читаемый инбокс (второй снимок).

**Подтверждение делом:** после починки заход прочитал 5 тредов подряд, а на «Mando» с ТРЕМЯ
тредами-тёзками встал **fail-closed** («не угадываю, какой наш») вместо тапа наугад.

---

## 2. 🔴 JEENU: НОМЕР ОТПРАВИЛИ МЫ — ПОДТВЕРЖДЕНО ГЛАЗАМИ (`f25268e`)

Сначала нашёл в журнале (`watcher.log:4144`), **потом прочитал тред на экране**. Совпало:

> **THEM (Jeenu):** «Hi Alex, is this still available?»
> **ME:** «Yes, still available! You interested?»
> **ME:** «Awesome, glad you're interested! 🙌 **Easiest is to text me at `<наш eSIM>`** — we'll
> lock in a time that works for both of us and I'll send you the exact pickup spot once we set it.»

* **КТО:** мы. `CARRIED` = наш исходящий; на экране это `ME`. **Не** покупатель со своим номером.
* **КОГДА:** `2026-07-01 16:05:17 UTC`.
* **КОНТЕКСТ:** он спросил «ещё доступно?», мы подтвердили и **сами предложили наш eSIM** как
  контакт для координации выдачи.
* **ПОСЛЕДСТВИЕ:** **Jeenu после этого не ответил** — тред обрывается на нашем сообщении.

Номер = наш eSIM (`registrations.json`). В реестре владельцев его нет и быть не может:
велик → Радомир, вещи Зины → Зина. **Jeenu не писал** (твой приказ).

### Причина — это был НЕ сбой кода, а ПИСАНАЯ ПОЛИТИКА
`.kola_state/bike_sale_strategy.md:61,77` предписывал буквально: «Контакт покупателю = только мой
Pixel `<eSIM>`». 01.07 агент **исполнил действующую инструкцию**. Ты отменил её 11.07
(MEMORY.md:22, тогда же появился `_pickup_phone`), но **сам файл никто не поправил** — и он ещё
**девять дней** продолжал предписывать утечку. Отмена жила в голове и в одной функции, а не в
источнике, который читают.

### Дыра пережила починку 11.07 и была открыта ДО СЕГОДНЯ
`_pickup_phone` только **выбирает** номер для промпта, зная две ветки: велик → Радомир,
**всё остальное → Зина молчаливым дефолтом**. А номер **в готовом тексте не проверял никто**:
`_strip_location` прямо декларировал «телефон — НЕ трогаем», текст пишет LLM свободно и может
взять номер откуда угодно. **Между «какой номер выбран» и «какой уехал человеку» не стояло ни
одной проверки.**

### Починка
`_phone_guard` — проверка **по факту отправляемого текста**; источник истины — **реестр
владельцев**. Нет в реестре ⇒ не уедет. Известна вещь ⇒ номер обязан принадлежать владельцу
**именно этой** вещи. **Fail-closed:** реестр не прочитан ⇒ все номера под запрет.

Врезано в `_strip_location` — **единственную** функцию, через которую уже проходят все пути
наружу (все выходы `generate_reply` + граничные `_guard_outgoing` в messenger_app_send /
fb_messenger_send / dm_reply_one). Отдельный гард пришлось бы врезать в каждый сендер, и
следующий новый его бы не позвал — ровно так `lucas_send.py` обошёл гард геолокации.

| вход | выход |
|---|---|
| eSIM + вещь известна | номер владельца велика (подставлен) |
| eSIM без контекста | `(number on request)` |
| номер Зины в треде **велика** | заменён на номер Радомира |
| законный номер владельца | **без изменений** |
| `$40, 2019 model, zip 92688` | **не тронуто** |

Проверено и на **реальной границе отправки** `messenger_app_send._guard_outgoing` — той самой
функции, что несла бы сообщение Jeenu.

---

## 3. 🔴 ДОКУМЕНТИРУЯ УТЕЧКУ PII, Я СОЗДАЛ ВТОРУЮ (`1530c31`)

Pre-push secret guard **зарубил пуш**: в комментарии к `_phone_guard` я вписал **настоящий номер
eSIM прямо в код**. То есть, объясняя утечку телефона покупателю, отправил тот же номер в
публичный репозиторий. Это **дословно** та ошибка, которую уже чинил `7b20b52` — и чинил её я же.

**Гард был прав, я — нет. Не обходил и не ослаблял.** Номер вычищен, остались хвост и ссылки на
улики в gitignored-файлах. Правило записано над блоком: *документируя утечку PII, не создавай
вторую.* После правки: `git grep` по номеру пуст, гард работает как прежде.

**Знать про ловушку недостаточно, чтобы в неё не наступить** — сегодня подтвердилось дважды.

---

## 4. ФАНТОМ + ДЕТЕКТОР МЁРТВЫХ АККАУНТОВ (`9922624`)

**«Price Negotiable» закрыт.** Не человек — **строка из текста объявления**, разобранная как имя.
Улика в `history_tail` его же карточки: переписка про секционный диван и обращение **«Hi Daniel!»**
⇒ **дубль треда Daniel**. Открытых обещаний не висело.

Списка `_NON_PERSON` не хватило: там служебные имена (конечное множество), текст объявления
бесконечен. Добавлено **правило**: имя из ≥2 слов, где **каждое** слово из словаря объявлений.
Граница осознанная: одиночные слова не судим — «Price», «Free», «Newman» реальные фамилии.
Проверено в обе стороны: отваливаются `Price Negotiable`/`Firm On Price`/`Must Go`/`$40 Firm`;
проходят Jeenu B., Irena, Greg, Mahmood, Sam, James, Alexander, Ann B., Newman Price.

**Детектор мёртвых аккаунтов врал В ОБЕ СТОРОНЫ.** Контрольный опыт:

| | шапка UserName | текст страницы |
|---|---|---|
| @WholeMarsBlog | нет | «unable to show this account… deleted», 404 |
| @InsideEVs (**живой**) | **нет тоже** | живое био, 90.2K подписчиков |

1. `!header` — **не улика вообще**: у живого разлогиненного профиля шапки тоже нет. Конъюнкт,
   одинаково истинный для живого и мёртвого, **создавал лишь впечатление строгости**.
2. Набор фраз **не покрывал реальный мёртвый аккаунт** («unable to show» не искали) ⇒
   по-настоящему мёртвый @WholeMarsBlog получил бы вердикт «ЖИВ».

Теперь арбитр — **текст страницы**, решение двустороннее: `dead_txt` И НЕ `alive_txt` (счётчики
Followers/Following, кнопка подписки, лента). **Fail-safe:** неоднозначность ⇒ НЕ gone (вердикт
удаляет сид **навсегда**). 6 случаев, включая оба реальных замера и точный сценарий ложного
срабатывания 17.07 — сошлись.
⚠️ Честно: доказана **логика** (регулярки извлечены из исходника); исполнение JS в браузере
живьём — к проверке на ближайшем заходе Огня.

---

## 5. 🔴 ПОЧЕМУ МУЛЬТИВАРКУ НИЧТО НЕ ПОМЕТИЛО — ДВА КОРНЯ (`d74a44e`, `93c97fa`)

### Корень 1: ЗАПИСИ ТОВАРА НЕ СУЩЕСТВОВАЛО
Она жила **только** в `sell_tasks.json` как черновик. В **реестре товаров** её не было вообще —
а реестр то единственное, что читают все сторожа и весь путь ответа покупателю. Опубликуй я её
как есть: `_item_for()` не нашёл бы фактов; **`_is_gone()` физически не смог бы сказать «её
забрали»** (сценарий журнального столика буква в букву); `handoff_phone` нет — на закрытии нечего
дать покупателю. **Запись заведена ДО публикации.**
`unconfirmed: [brand, model, capacity]` — Зина их не подтверждала, покупателю не называем.

### Корень 2: СТОРОЖ СЛЕДИЛ ЗА ОДНОЙ ВЕЩЬЮ НА ОДНОЙ ПЛОЩАДКЕ
`listing_supervisor.py` не сломан — он **никогда не умел** нужного:
```
b = d.get("bike_giant_rainier", {})   # ОДНА вещь, ЖЁСТКО В КОДЕ
if b.get("url"): check_cl(b["url"])   # ОДНА площадка (CL)
```
Мультиварку он не пропустил — **не мог увидеть**. И это **маскировалось под здоровье**:
ежедневное зелёное «CL bike: live (ok)» в те самые дни. Даже маркер живости — слово «giant».

**Вторая ложь, тише:** у OfferUp и FB в `live_listings.json` стоит `"status": "live"` — но это
**запись дня публикации**, не перепроверявшаяся. Читается как «сейчас висит».

**`lib/listing_coverage.py`** — матрица вещь × площадка **от реестра** (новая вещь под надзором
автоматически). `claimed_stale` **никогда не засчитывается** за покрытие. Честная граница: с
сервера доказуем только CL; FB/OfferUp/eBay app-gated ⇒ требуют **глаз**, и это само по себе
дефицит. Крон 09:40 UTC. **Первый прогон — 7 пробелов:**

| вещь | CL | OfferUp | FB | eBay |
|---|---|---|---|---|
| велик | ✅ LIVE (curl) | ⚠️ заявлено, не проверено | ⚠️ заявлено, не проверено | ⛔ MISSING |
| мультиварка | ⛔ MISSING | ⛔ MISSING | ⛔ MISSING | ⛔ MISSING |

---

## 6. ЖИВОЕ ЧТЕНИЕ ТРЕДОВ (после починки глаз)

### 🟡 ИРЕНА — ТОЧКА СДЕЛКИ, РЕШАЕШЬ ТЫ
> ME: «Good evening, is this still available?»
> THEM: «Yeah it is, are you intrested?»
> ME: «would you take **$340** for it?»
> THEM: «The lowest i can do is **$370**»
> THEM (**новое**): «I can do **$350**, if you are still intrested.»

Она **сбросила 370 → 350**. Мы стоим на **340**. **Разрыв $10.**
Я **не торговался и ничего не обещал** — она продавец Beelink, цена твоя.
CRM хранил только «$370» — правду сказал **экран**, как и должно быть.

### Закупочные треды (прочитано 3 из 6)
* **GREG** (Ioniq): «Clean title firm price» — тайтл чистый, цена **твёрдая**, торга не будет.
* **MAHMOOD** (Sonata): «yes it's available, this is clean title excellent condition» — ждёт нас.
* **MANDO** (Ioniq PHEV): **отказ**. На предложение обмена на Durango — «I don't want something
  that is worth more plus I only want cash». Только наличные; листинг уже «No longer available».

Ничего не обещал, цен не называл, встреч не назначал.

---

## 7. НАДЗОР И ЗДОРОВЬЕ

* **Огонь по ПРОДУКТУ жив:** `ogon_quota` — zdravurr **1/1 постов, 1 фоллов**;
  zdravurr_ca **1/1, 1**. Дефицит 0. Починка замороженных вкладок держится.
* **Кроны проверены, не со слов:** `ogon_quota` `17 * * * *`, `capability_audit` `0 */4`,
  новый `listing_coverage` `40 9 * * *`.
* **Обещания: открыто 4. Сироты: ноль.**
  `michelle` (159ч) — **НЕ долг**: запаркована тобой, и `lead_guarantee` **знает про парковку**
  (`is_parked`, включая нормализацию `irena`/`irena-irena`). Заподозрил ложный долг и
  **проверил прежде чем докладывать** — подозрение не подтвердилось.
  `str76`/`a` — в работе worker3. `david` — закрыт.
* **Сервисы:** titan, mercury-sol, kola-bridge, gemini_bridge — все `active running`.
* ⚠️ **Не выдаю за дефект системы:** публикация отчёта из-под моей сессии падала на
  `/tmp/kola_report_publish.lock` (`Permission denied` при euid=0 — воспроизвёл). Это **артефакт
  песочницы моей сессии**, не production-путь: под `botuser` открывается штатно. Публикую как botuser.

---

## ⛔ ЧТО НЕ СДЕЛАНО — ЧЕСТНО

| задача | статус |
|---|---|
| **Sam, James, Alexander** | ⛔ заход убит таймаутом на Sam (5 из 8 прочитано) |
| **Публикация мультиварки** (FB/OfferUp/CL) | ⛔ не начата (запись и черновики ГОТОВЫ) |
| **Опись покрытия ГЛАЗАМИ** | ⛔ не начата (серверная часть сделана, 7 пробелов найдены) |

**Не кручусь** — по твоему правилу остановился и отмечаю.

**Мой мусор:** убитый процесс оставил метку `fb_read:sam`, из-за неё `fb_warmup` уступал экран
**4 раза** (17:30–17:32), затем TTL снял её сам. Ущерба нет, но это моя недоделка: скрипт
обязан снимать метку и при смерти.

Отдельно: я снова наступил на **само-матчинг `pgrep`** (сторожевой цикл поймал сам себя и
крутился час, показывая «процесс жив»). Тот же капкан, что описан в отчёте 20.07 утром.

---

## РЕШЕНИЯ ЗА ТОБОЙ

1. **ИРЕНА: $350.** Берём / держим 340 / встречное 345?
2. **Мультиварка** — публиковать сейчас? (тред «Мультиварка» у тебя на ручной паузе —
   публикацию это не запрещает, но отмечаю.)
3. Дочитывать Sam / James / Alexander следующим заходом?

## УРОК ДНЯ
Все дефекты этой части — **один класс**: проверка удовлетворялась не тем фактом, который
декларировала. «Цель найдена» = узел СОДЕРЖИТ слово · «аккаунт снесён» = нет шапки · «листинги
живы» = жив один листинг одной вещи · «номер выбран верно» = верный номер уехал · «покупатель» =
строка объявления.
Новое за сегодня: **я дважды повторил ошибку, которую сам же и чинил** (PII в исходник; патч
частного случая вместо класса). Знания о ловушке недостаточно — спасает только живой прогон и
чтение настоящего вывода.
