# kolya-a83ba46af3be

_2026-09-03 12:33 UTC_

---

# a83ba46af3be [infra] — красный контракт на чтении инбокса

**Итог: чинить было нечего — красноты уже нет, и я это проверил не галочкой, а мутациями. Защита «свип читает ВЕСЬ список, а не один экран» жива.**

## Что было заявлено
Единственный красный контракт в наборе (203/204 на 03.09): тест разошёлся с кодом `projects/sales/inbox_sweep.py` в двух местах — фейк `_Phone.see()` не принимал kwarg, который код шлёт, и `read_ou_rows_tree_all` возвращал не 3 значения при распаковке в 3.

## Что оказалось на самом деле
Обе рассинхронизации к моему заходу уже сняты — задачу закрывал не я, её закрыло время:

- тест (`tests/test_sweep_reads_whole_inbox_not_one_screen.py`, mtime **08:02:51Z**, через 9 минут после заведения задачи в 07:53Z) распаковывает **четыре** значения;
- код (`inbox_sweep.py:425`) возвращает ровно `order, pages, truncated, anchor`;
- фейк `_Phone.see(ui=..., screenshot=...)` принимает ровно те kwargs, что код передаёт на строке 295;
- более поздняя правка `inbox_sweep.py` (**11:53:19Z**, задача `0531ad2fc447` — «верх списка не отрисован» → pull-to-refresh) синхронность **не сломала**.

Полный набор: **207/207 зелёных за 51с**.

## Почему я не поверил зелёной галочке
«Контракт зелёный» и «контракт что-то охраняет» — разные вещи, а охраняет он самое денежное: чтение инбокса. Прогнал две мутации в памяти (`.kola_state/tech_tasks/a83ba46af3be.mutcheck.py`):

1. **потолок листания = 1** (читатель берёт ровно один экран, как до фикса) → тест краснеет: `потерян тред: kristopher`;
2. **anchor всегда объявляет верх доказанным** (верх «по надежде», а не по факту упора) → тест краснеет: `верх «доказан» на списке, который не переставал ехать`.

Обе пойманы. То есть сценарий «свип прочитал один экран вместо всего списка, и никто не заметил» контракт по-прежнему ловит.

В `inbox_sweep.py` я **не менял ни строки**.

## Найдено попутно — заведено отдельной задачей `5c366a54b9ae`
Этот контракт и `test_unrendered_top_of_inbox_is_not_a_read_list` оба пишут **боевой** леджер `.kola_state/ou_unread_raw.json` (`isw.UNREAD_F`), полагаясь на backup/restore вокруг. Набор гоняется **по 8 разом** — два теста топчут один боевой файл, и параллельный боевой свип может влезть между backup и restore. Это тот же класс, что разобранный 03.09 корень «контракт мерцает в параллельном наборе». Сейчас оба зелёные — это скрытая мина мерцания, не текущая краснота, поэтому отдельной задачей, а не правкой задним числом.

## Что от Босса не требуется
Ничего. Деловых решений тут нет, работа не блокирована.
