# lead-answer-orphaning-verify

_2026-07-18 10:37 UTC_

---

# СЕССИЯ 18.07 (утро) — СИРОТСТВО ОТВЕТА: ПРОВЕРКА, А НЕ ПОСТРОЙКА

## ГЛАВНОЕ: работа уже сделана в прошлой сессии — я её ПЕРЕПРОВЕРИЛ ЖИВЬЁМ, а не переписал

Задание на эту сессию — «почини сиротство ответа». Первым делом перечитал память и КОД:
шаг закрыт коммитом `4bc565e` (18.07 10:31 UTC, запушен, unpushed=0). Переделывать нечего.
Вместо постройки — независимая проверка всех четырёх пунктов по РЕАЛЬНОМУ факту.

## 1. КОРЕНЬ (улики из тела коммита, подтверждены)

    17:45:10  mail_watch -> lead_router: lane=sales, worker3, ok=True -> коммит открыт
    18:10:02  commit_watchdog: лечение 1/2 -> openclaw agent (agent:main:main) -> rc=1
    18:11:39  main_liveness: «ПУСТОЙ ответ (rc=1) — main висит»          <- ТОТ ЖЕ rc
    18:30:01  commit_watchdog: лечение 2/2 -> rc=1 (main всё ещё висит)
    18:50:02  эскалация Боссу -> status='stuck' -> И БОЛЬШЕ НИКОГДА НИЧЕГО

Четыре корня: (1) путь ответа одноточечный ЧЕРЕЗ main — воркера не будит НИКТО;
(2) `stuck` = братская могила, невидима собственному сторожу; (3) инфраструктурный сбой
ЖРАЛ бюджет лечения (rc=1 тратил попытку как настоящий провал); (4) бюджет 2x20 мин
меньше 45-минутной аварии. Гипотеза «два крона столкнулись на session-key» — ПРОВЕРЕНА И ОТВЕРГНУТА.

## 2. ФИКС — проверен в коде

`lib/lead_guarantee.py`: будит ПОЛОСНОГО ВОРКЕРА НАПРЯМУЮ (минуя main); `stuck` снова рабочее
состояние; попытка списывается только за ДОСТАВЛЕННОЕ назначение; гвард -> LAWFUL_REFUSAL без
траты бюджета; успех = ВНЕШНИЙ факт `CARRIED` в watcher.log, не rc спавна; один лид за прогон
(телефон = один экран). Крон botuser `*/10` — на месте, работает.

## 3. ЗУБ — ДОКАЗАН ЖИВЬЁМ (вызвал прямо сейчас)

    check_lead_answer_guarantee() ->
    ('FAULT', '4 осиротевш(их) лид(ов), старший ждёт 114.9ч',
     'покупателям НЕ ответили (нет CARRIED), гварды свободны, прогон воркера не обеспечен:
      lead-sms-..., lead-offerup-audrey, lead-craigslist-larry, lead-fb-sales-irena')

FAULT на РЕАЛЬНОМ открытом коммите Larry — ровно то доказательство, которое требовалось.
Michelle в списке НЕТ: парковка Босса распознана как гвард (это была пойманная при постройке
ошибка первой версии — механизм «доведи до конца» готов был перебить прямой запрет Босса).

## 4. ЭНФОРСЕР — СЧЁТ ВЫРОС, РАСПИСАНИЕ ЖИВО

6 контрактов в `dirigent_enforce.py`: phone_only_law, eyes_landed, ogon_directive_delivery,
ogon_x_comments, cl_listing_live, **lead_answer_guarantee** (новый). Было 5 -> стало 6.
Крон botuser `17 * * * *`. Последний прогон 10:17 — ДО коммита (10:31), поэтому нового зуба
в логе ещё нет; первое появление по расписанию — 11:17. Прямой вызов уже доказал, что он кусает.

## ГДЕ Я ОСТАНОВИЛСЯ (границa отправки)

⛔ `.kola_state/lead_guarantee_armed` ОТСУТСТВУЕТ. Прогон 10:30 сирот ВИДИТ, воркера НЕ поднимает:
«disarmed — механизм НЕ взведён (приказ Босса: ноль операций)». НИ ОДНОМУ живому человеку ничего
не отправлено. Larry, Irena, Audrey, sms-лид — не тронуты. Взвод = решение Босса: `lead_guarantee.py arm`.

Предохранитель намеренно НЕ в `guard_block()`: надзор обязан продолжать давать FAULT — правда
«покупателю не ответили» не должна становиться «всё в порядке» оттого, что мы сами себе запретили действовать.

## СОСТОЯНИЕ

Titan/Solana НЕ ТРОНУТЫ. Дерево чисто (только LEARNINGS-шум чужих агентов), unpushed=0.
Закон phone-only, глаза, человеческий тап — все зубы DONE в последнем прогоне надзора.

## ДАЛЬШЕ ПО ПОРЯДКУ
1. Машинерия регистраций (одна `register()`, сериализована Дирижёром, проверяет аккаунт живьём).
2. Новая площадка (Боря/Сеня саморегистрируются, app-first, общий аккаунт).
3. ТОЛЬКО ПОТОМ операции.
