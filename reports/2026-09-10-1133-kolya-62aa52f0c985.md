# kolya-62aa52f0c985

_2026-09-10 11:33 UTC_

---

# 62aa52f0c985 [infra] — патруль cl_chat_watch на слот 3,33: НЕ ПРИМЕНЕНО, нужна одна твоя команда

## Итог одной строкой
Правка верна и готова, но **установка расписания у меня за гейтом**: `crontab <файл>` в сессии
техника отвечает дословно `This command requires approval` — разрешено только чтение `crontab -l`.
Токен обхода мне не передан, обходить гейт wrapper'ом/spool-файлом не стал. Задача НЕ закрыта,
припаркована на 12 ч (`wait_kind=gate`), чтобы не жечь сессию каждый тик.

## Что проверил (правка применима без доработок)
- `diff` бэкап↔новый = **ровно одна замена**: закомментированная 26.08 строка
  `#STOPPED26AUG_EVENTDRIVEN 3,33 … cl_chat_watch.py` → 9 строк комментария + живая строка
  `3,33 * * * * run_guarded.sh clsweep /usr/bin/timeout 180 python3 projects/sales/cl_chat_watch.py`.
  Больше в файле не изменено ничего (293→302 строки сходятся).
- **Живой crontab побайтно равен бэкапу** ⇒ бэкап актуален, чужих правок с момента подготовки
  не было, применение ничего чужого не затрёт, откат честный.
- Слот **:03/:33 в живом кроне свободен** (единственный сосед — fbsweep на 12,42).
- Полоса подтверждена по коду, а не на слово: `run_guarded.sh:142` → `*sweep*) KOLA_LANE=detect`,
  значит метка `clsweep` действительно уступает экран ждущему покупателю.
- Премиса задачи подтверждена: двери для Craigslist в событийном пуше правда нет —
  `node_notif_watch.py:51` знает только orca/mlite/instagram/offerup/google.messaging, а `parse()`
  ещё и требует Android `category=msg`, чего web-push Chrome не даёт. Строить дверь для Chrome
  не на чем: ни одного образца CL-уведомления в логах нет. Возврат патруля — единственный
  обоснованный фикс, а не «удобный».

## Что сделать (одна команда, откат рядом)
```
crontab /home/botuser/.openclaw/workspace/.kola_state/crontab.new_clchat_20260910
crontab -l | grep clsweep        # должна показать живую строку 3,33
# откат: crontab /home/botuser/.openclaw/workspace/.kola_state/crontab.bak_before_clchat_return_20260910
```
Канал Craigslist-чата слеп с 26.08 (15 суток): `cl_chat_watch.log` обрывается на 2026-08-26T11:03,
слова «craigslist» в `node_notif_watch.log` нет ни разу.

## Чего не делал
Контракты не гонял: код я не менял ни строкой — менять было нечего, вся правка это одна строка
расписания, которую гейт не пустил. Задачу не закрывал: закрыть её без применения расписания было
бы ложной галочкой.
