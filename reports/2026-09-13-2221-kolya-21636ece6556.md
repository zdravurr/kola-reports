# kolya-21636ece6556

_2026-09-13 22:21 UTC_

---

🧰 21636ece6556 — долг follow-back получил карантин (как у отписки)

**Что было.** Долг по взаимным (принятые, но без подписки) перебирался целиком в каждом заходе: отказ (skip) не тратил бюджет сессии, поэтому цикл шёл по всем подряд. С 06.09 было 13 хэндлов и 297 отказов. Корень этих 13 уже закрыт в dc6e82471746, но любой следующий вечный отказ опять съедал бы минуты.

**Что сделано** (`projects/social_x/x_followback.py`):
- по каждому хэндлу долга копится счётчик срывов подряд (`backlog_fail` в состоянии). Подписка легла или «уже подписаны» — счётчик обнуляется;
- сорвавшийся уходит в хвост очереди. После 3 срывов подряд он уходит в отвод на 72 ч. Отвод не вечный: решение «принят» в силе, а отказ мог быть временным;
- в логе видно две строки: «ДОЛГ В ОТВОД на 72ч: N срывов подряд» (плюс событие `follow_back_backlog_quarantine`) и в каждом заходе «ДОЛГ В ОТВОДЕ — K хэндлов: @x (×n, ещё Hч)»;
- попутно закрыта дыра: цикл новых подписчиков сохранял копию состояния из начала захода и стёр бы счётчик. Теперь после разбора долга состояние перечитывается.

**Анти-бан не тронут:** лимиты, стоп и скрининг прежние, число действий в сутки не выросло.

**Контракт:** `tests/test_followback_debt_eternal_refusal_quarantined.py` проверяет хвост, отвод, истечение отвода, обнуление при успехе и что счётчик переживает заход. Старый контракт долга зелёный.


**Прогон:** весь набор 283/283 зелёный (48 с), 13.09 22:2x UTC.
