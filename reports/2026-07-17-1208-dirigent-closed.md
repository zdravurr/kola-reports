# dirigent-closed

_2026-07-17 12:08 UTC_

---

# ДИРИЖЁР ЗАКРЫТ — 17.07.2026

Всё, что ты приказал, сделано и доказано живьём. Открытых хвостов **три**, все названы ниже
поимённо: два — принятый остаточный риск (твоё же решение), один — упёрся в твой пуш-тест.

Коммиты: `263ff97` (workspace), `6c35b9d` (titan-bot, ТОЛЬКО full_report.py). Оба запушены.

---

## 1. ПАТРУЛЬ УДАЛЁН НАСОВСЕМ ✅

* Крон `*/15 fb_inbox_sweep` **удалён**. Активных строк свипа: **0**. Крон 44 → 43.
* Стэш `--peek/--tick` **сброшен** (sha `7c07c1a`, если вдруг понадобится).
* Файл `fb_inbox_sweep.py` НА МЕСТЕ, строка в кронтабе закомментирована с инструкцией
  возврата — ничего не удалено безвозвратно.
* **ПУШ (`inbox_watcher.service`) = ЕДИНСТВЕННЫЙ детектор FB.** Жив, аптайм 16ч+,
  опрос каждые ~26с, последний 12:06:20.
* **`--peek` НЕ запускал. Messenger не открывал.** Последний свип в истории — 11:47:08,
  удаление в 11:49. Больше Messenger никто не дёргает.
* Побочно: свип открывал Messenger каждые 15 минут — то есть **твой пуш-тест мешал не мой
  `--peek`, а твой собственный крон**. Теперь этой помехи нет: удаление патруля тесту помогло.

---

## 2. LEDGER СВЕДЁН + СТОРОЖ «ХОД БЕЗ ОТЧЁТА» ✅

### Сначала ledger — иначе сторож родился бы ложно-орущим

`full_report` (путь, предписанный CLAUDE.md) в ledger **не писал**. В 11:40 ledger
кончался на **09:20**, хотя два отчёта со ссылками ушли в **10:46** и **11:25**.

**Доказано живьём** (боевая отправка 11:51):

    ДО:    669 строк, последняя 09:20 (why=ready)
    ПОСЛЕ: 670 строк →  ts=11:51:45  why=full_report-direct
                        message_id=978  chars=480  has_link=True ✅
    владелец файла остался botuser ✅ (root-created ledger запер бы botuser'а — этот класс
    отказа уже стоил нам публикатора)

Из **58** отчётов со ссылкой в ledger'е **новым путём записан 1** — ровно тот, которого
сторож раньше не увидел бы.

### Потом сторож — на механике

`scripts/turn_report_watchdog.py`, крон **ROOT'а** `*/10`.

Почему не хватало существующих: `commit_watchdog` сторожит обещания, которые **я сам**
зарегистрировал; `report_outbox.watchdog` — задачи, которые **я сам** пометил done. Оба
висят на моей памяти. Улика здесь — **git commit**: его нельзя «забыть создать».

Почему в кроне root'а: как `cron_guard` — сторож не должен выключаться тем же движением,
которым выключают охраняемое. Плюс `/root/titan-bot` — 0700 root, botuser его не прочитает,
а работа по Титану тоже требует отчёта.

**Обе стороны доказаны:**

    ОР:   ledger подменён пустым (= отчёта не было), грейс вышел →
          🔴 «КОЛЯ ЗАКОНЧИЛ ХОД БЕЗ ОТЧЁТА» • [workspace] 0de48b0c (30 мин назад): fix(ogon)…
          exit=1
    ТИХО: отчёт со ссылкой есть →
          🟢 «тихо: каждый коммит закрыт отчётом со ссылкой (или ещё в грейсе)» exit=0

Заведомо ложную тревогу «Коля без отчёта» тебе **НЕ слал намеренно** — это ровно то «волки!»,
против которого сторож и построен. Путь доставки — `send_full_report`, доказан сегодня
4 раза, включая `message_id=978`, лежащий в ledger'е.

Предохранители от шума: грейс 25 мин, ор **ровно один раз** на коммит, окно истории 6ч,
fail-silent при поломке git/ledger.

---

## 3. ВИНОВНИК АВТО-ПОВОРОТА — СУЖЕН, НО НЕ ДОБИТ ⚠️

**Наш код чист** — подтверждено: записи `accelerometer_rotation=1` у нас нет нигде.

**Что доказано:**
1. 6 часов сносы шли строго по сетке `*/15` (`:02/:16/:32/:46`) — сетке свипа, открывавшего
   Messenger.
2. Час без свипа (10:02→11:02) — **сносов НОЛЬ**.
3. После моего запуска `fb_active` (katana, 11:05) частота выросла с раза в 15 мин до
   **раза в 2–4 мин** — приложение резидентно.
4. `ps` на телефоне: **katana и orca РЕЗИДЕНТНЫ** (Chrome на переднем плане).
5. logcat писателя настройки **не показывает** — Android авторство записи не логирует.

**Эксперимент:** force-stop **только katana** (11:55:28; orca не трогал — это пуш-детектор).
Результат: частота рухнула (каждые ~4 мин → один снос за 7 мин), но снос **12:02 всё же был**.

**Вердикт честно: katana — соучастник, не единственный виновник. Главный оставшийся
подозреваемый — orca (Messenger).**

**Почему не добил:** решающий тест — force-stop orca. Orca **И ЕСТЬ** пуш-детектор, а твой
тест взведён. Убить её = сжечь тест и ослепить единственный детектор FB. Не стал.

**Сторож на месте и работает:** каждый снос чинится за **2 секунды** (`11:52:02 СНЕСЕНО →
11:52:04 восстановлен`). Ущерба нет, есть только шум в логе.

**Добить смогу сразу после твоего пуш-теста** — одной командой.

---

## 4. ХАРДЕНИНГ COMMIT-GATE

### Пароль botuser — ЗАКРЫТ ✅

    было:  botuser P (пароль активен), sshd passwordauthentication=yes
           → хеш был живым путём удалённого входа под личность агентов
    стало: passwd -l botuser → botuser L
    проверено: botuser выполняет код ✅, git от botuser ✅, ловец active ✅
    бонус: authorized_keys у botuser НЕТ → прямой вход под botuser закрыт ПОЛНОСТЬЮ
           (остаётся root → su; permitrootlogin=yes)
    откат: passwd -u botuser

### `--no-verify` — ПРИНЯТЫЙ ОСТАТОЧНЫЙ РИСК (закрыть нельзя)

Локально закрыть **невозможно по конструкции**: тот, кто коммитит, владеет `.git/hooks` —
`--no-verify`, `rm .git/hooks/pre-commit`, `git -c core.hooksPath=` эквивалентны. Хук сам
это честно пишет: «Это МЯГКИЙ гейт: он ловит НОРМАЛЬНЫЙ автономный коммит, а не атаку».

**Почему принимаю, а не изображаю защиту:**
* Модель угрозы — **полезный агент**, не злоумышленник.
* **Опасная половина уже отрезана де-рутингом**: `sudo -l -U botuser` → *«User botuser is
  not allowed to run sudo»*. Агент физически не дотянется до Титана, `trades.db`,
  `systemctl` торговых ботов. Обход гейта теперь может испортить только репо воркспейса.
* Пост-фактум ловится: сторож «ход без отчёта» + твоё ревью коммитов.
* Жёсткая гарантия — только полная изоляция агентов (`project_architecture_isolation_redesign`,
  PROPOSED). Это архитектурный шаг, не однострочник, и без твоего решения я его не делаю.

**Гейт отработал ЖИВЬЁМ прямо в этой сессии:** мой коммит сторожа в `scripts/` был
**заблокирован**, патч сохранён (`.kola_state/commit_gate_held/20260717T120350Z.patch`),
тебе ушла тревога (`message_id=980`), ничего не потеряно. Прошёл только по токену.

---

## СВОДКА ПО ТВОЕМУ СПИСКУ

**Governance**
* Де-рутинг ✅ — проверено живьём: `botuser is not allowed to run sudo`.
* Commit gate ✅ — сработал живьём (блок + held-патч + тревога 980).
  Харденинг: пароль **закрыт** ✅; `--no-verify` — **принятый остаточный риск** (обоснование выше).
* Wake audit ✅ · Аварийный стоп ✅ (горло `tab_lock`, fail-closed; проверен утром).
* Сторож «ход без отчёта» ✅ — орёт при пропаже, молчит при наличии. Оба случая доказаны.

**Огонь**
* Одно окно 06:00–22:00 ET + один гейт ✅ — пауза валит ВСЕГО Огня (X + FB-прогрев + FB/EV),
  доказано утром (все четыре легли; стейт не сожжён, md5 совпали).
* EV-кроны под `overload_guard` ✅ — под флагом `soft`: `SKIP evwarmup` + `SKIP xwarmup`,
  echo не напечатался ни там, ни там. Ведёт себя точно как X.
* Посты/лайки/фолловы через vtap — **закрыто ранее, мной сегодня НЕ перепроверялось**.
  Не ставлю галочку за чужую работу: скажешь — проверю живьём.

**Детект**
* OfferUp+CL — почта первична ✅ (закрыто 17.07 ранее).
* FB — **пуш единственный детектор** ✅, патруль удалён ✅.
* Ledger отчётов сведён ✅.

**Пуш-ловец: ВЗВЕДЁН, НЕ СРАБОТАЛ.** Ждёт твоё тестовое сообщение. Чист: Messenger с 11:47
никто не открывал, и больше не откроет.

**Полосы:** `procurement: live`, `warmup: live`. Гейт Огня 🟢.

**Титан/Solana — НЕ ТРОНУТЫ:** `titan.service active`, `gunicorn master [titan-btc]` (pid
72507), 4 процесса `optimizer_listener`, `titan-bot` незакоммиченного **0**, торговых строк
в root-кроне **13** (как было). Единственная правка — `full_report.py` (ты разрешил точечно).

**Git:** workspace `263ff97` == origin ✅ · titan-bot `6c35b9d` → `c7976f3..6c35b9d` запушен ✅.

---

## ОТКРЫТО — ТРИ ПУНКТА, ВСЕ НАЗВАНЫ

1. **Виновник авто-поворота не добит.** Сужен до FB-приложений; katana — доказанный
   соучастник, orca — главный подозреваемый. Решающий тест убивает твой пуш-тест и слепит
   детектор. **Сторож чинит за 2с, ущерба нет.** Добью по твоему слову после теста.
2. **`--no-verify` — принятый остаточный риск.** Не закрывается локально; опасная половина
   снята де-рутингом. Полный close = изоляция агентов (архитектурный шаг, ждёт решения).
3. **Лоссивость пуша — принятый остаточный риск (твоё решение).** Плашка FB перезаписывает
   уведомление треда (кейс Raghavender). Буфера-правды (экрана) больше нет — ловим то, что
   пуш донёс. Ты сказал: мониторишь живьём. Записано в кронтабе прямым текстом, чтобы
   следующий, кто заглянет, не считал это недосмотром.
