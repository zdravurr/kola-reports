# Report-link rule saved to memory + structurally enforced via hooks

_2026-07-15 14:55 UTC_

---

# ПРАВИЛО «НЕТ ССЫЛКИ = НЕ СДЕЛАНО» — СОХРАНЕНО В ПАМЯТЬ + ВШИТО СТРУКТУРНО (15.07.2026)

Приказ Босса: сделать структурно НЕВОЗМОЖНЫМ закончить задачу без ссылки на отчёт. Выполнено на двух уровнях.

## 1. ЖЁСТКОЕ ПРАВИЛО В ДОЛГОВРЕМЕННОЙ ПАМЯТИ (грузится каждую сессию)
- `feedback_full_report_all_diagnostics_to_telegram.md` усилен: наверху теперь блок «🔴 ЗАДАЧА НЕ ЗАКОНЧЕНА, ПОКА НЕ ДОСТАВЛЕНА ССЫЛКА. НЕТ ССЫЛКИ = НЕ СДЕЛАНО. Ссылка — ЭТО результат, не код. Публиковать ПЕРВЫМ действием по завершении, ДО слова "готово". Ловлю себя на "готово" без ссылки → СТОП, сначала ссылка.» + зафиксировано нарушение 15.07 (ход шагов и карта контейнеров — в терминал без ссылки).
- `MEMORY.md` (индекс, читается каждую сессию): строка №1 переписана в «🔴🔴🔴 НЕТ ССЫЛКИ = НЕ СДЕЛАНО».

## 2. СТРУКТУРНОЕ ПРИНУЖДЕНИЕ — ДВА ХУКА в ~/.claude/settings.json (хост/Kolya)
Механизм на маркер-файле `/tmp/claude_report_due` — срабатывает ТОЛЬКО на мои tool-вызовы (кроны и разговорные ходы не задевает):
- **PostToolUse(Bash):** если команда содержит `git … commit` в titan-bot/openclaw-workspace → СТАВИТ маркер («должен отчёт»). Если команда содержит `report_publish` / `send_full_report` / `.publish(` → СНИМАЕТ маркер.
- **Stop:** если маркер стоит (закоммитил, но не опубликовал) → возвращает `{"decision":"block", ...}` и НЕ даёт закончить ход, пока не опубликую отчёт со ссылкой. Маркера нет → тихо пропускает (разговорные ходы не блокируются).

## ПРОВЕРЕНО (9 тестов, все прошли)
- commit-команда → маркер СТАВИТСЯ; publish-команда → маркер СНИМАЕТСЯ; `ls` → тихо.
- Stop с маркером → валидный JSON block с напоминанием; Stop без маркера → тихий выход 0.
- Прогнал РЕАЛЬНЫЕ строки хуков из settings.json (не приближения) — все зелёные. theme/model сохранены.

## ПОЧЕМУ ЭТО РЕШАЕТ ПРОБЛЕМУ
Раньше правило было «поведенческим» — я его забывал. Теперь: закоммитил работу и пытаюсь закончить без публикации → сам харнесс МЕНЯ ОСТАНАВЛИВАЕТ и заставляет сперва дать ссылку. Забыть физически нельзя.

Оговорка: чтобы хук активировался в ТЕКУЩЕЙ сессии, может понадобиться один раз открыть `/hooks` (перечитает конфиг) или перезапуск; в новых сессиях подхватывается сам.
