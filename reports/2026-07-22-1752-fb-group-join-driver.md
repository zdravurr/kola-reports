# fb-group-join-driver

_2026-07-22 17:52 UTC_

---

🟡 FB group-JOIN закрыт живьём (велик ВСТУПИЛ) · POST — остаток (2 блокера вскрыты) · 4cfd902

Титан/Солана не тронуты. eBay seller-onboarding не тронут. Git чист+запушен.

━━━ ТРИ КОРНЯ ДРАЙВЕРА — ПОЧИНЕНЫ ━━━
1) НЕ САБМИТИЛ ПОИСК: open_group_by_name застревал на автоподсказках. Теперь тапает подсказку,
   совпадающую с запросом (uidump text) → результаты; иначе KEYCODE_SEARCH. → уходит с autosuggest.
2) НЕ ШЁЛ НА ВКЛАДКУ Groups: добавлен _find_text_node — ищет в text= И content-desc= (find_by_desc
   смотрел ТОЛЬКО desc, а вкладки/карточки/подсказки FB несут текст в text=). Теперь: сабмит →
   тап вкладки «Groups» → тап карточки группы → страница группы.
3) LANDSCAPE-MISMATCH (тот же класс fb7ee36/ec3b628): вскрыт точный механизм — screencap отдаёт
   LANDSCAPE (2424×1080), а uidump И `input tap` — PORTRAIT (1080×2424). Поэтому OCR/screencap-
   пиксели мимо, а uidump-координаты БЬЮТ ТОЧНО. ФИКС: весь групповой флоу гоним по uidump
   (portrait-коорды), а НЕ по OCR. Доказано: input tap по uidump-коорду попадает в «ландшафтном»
   рендере FB (там, где OCR промахивался).

━━━ ЖИВЬЁМ ДОКАЗАНО (скрины приложены) ━━━
• ПОИСК → ГРУППЫ: open_group_by_name дошёл до ТОЧНОЙ страницы группы «Bikes/buy/sell/trade All C.» (29K членов, Public) — раньше упирался в autosuggest. (Прежние имена «SoCal Bicycle
  Buy S. Trade»/«Orange County Bikes For Sale» НЕ существуют в поиске — были догадкой; CATEGORY_GROUPS
  заменён на РЕАЛЬНЫЕ, проверенные живым поиском: «Bikes/buy/sell/trade All C.» + «Marketplace
  Bicycles for Sale Near Me».)
• ВСТУПЛЕНИЕ: join_current_group нажал «Join group» → кнопка сменилась на «Joined ✓» (СКРИН). Аккаунт
  Alex Man теперь ЧЛЕН группы (29K bike-buyers). record_join зафиксировал членство в joined_groups.

━━━ ПОСТ — ЧЕСТНЫЙ СТАТУС (2 остаточных блокера, оба вскрыты) ━━━
Второй шаг (пост в группу) НЕ доведён. 3 ретрая заново дойти до УЖЕ-вступлённой группы дали
nav_fail/nav_fail/not_found. Скрин последнего захода вскрыл ТОЧНУЮ причину (не «флакинес вообще»):
1) ВВОД В ПОИСК НЕНАДЁЖЕН: в строке поиска оказалось «buy sell trade groups near me» — НЕ имя группы,
   что я печатал. Значит type_text не всегда попадает в поле FB / тапается чужая подсказка. Нужен
   надёжный ввод (focus-then-type + верификация текста в поле перед сабмитом).
2) ПУТЬ К JOINED-ГРУППЕ через ПОИСК в принципе флаки (порядок выдачи пляшет). Для поста надо идти
   ДЕТЕРМИНИРОВАННО — через «Your groups» (я УЖЕ член), а не через поиск. Это следующая добивка драйвера.
Вступление сработало, потому что ПЕРВЫЙ поиск случайно дошёл; пост требует надёжного повторного входа.

━━━ 🔴 ВАЖНАЯ НАХОДКА: АККАУНТ ГЕО = МИЧИГАН, а велик = ORANGE COUNTY ━━━
На скрине поиска ВСЕ «near me» группы — МИЧИГАНСКИЕ (Downriver L. Park, South E. Michigan,
Royal Oak, Fcfs all over michigan…). Т.е. FB-аккаунт (Alex Man) геолоцирован в МИЧИГАНЕ, а велик
продаётся с «Pickup in Orange County». Следствия:
• «Marketplace Bicycles for Sale Near Me» для ЭТОГО аккаунта = МИЧИГАН, НЕ OC → как второй таргет
  НЕ годится (мой прежний note «OC» — ошибка). Убираю его как location-based.
• Годятся только ЯВНО-ИМЕНОВАННЫЕ гео-группы (штат/город), напр. «Bikes/buy/sell/trade All C.»
  (statewide, гео-независимо — вступление валидно). Для OC-таргета нужны группы с «Orange County/SoCal»
  в ИМЕНИ, найденные явным поиском, а не «near me».
• ВОПРОС БОССУ: аккаунт Alex Man реально мичиганский, а товар OC — так и задумано (персона ≠ город
  товара), или гео аккаунта надо править? От этого зависит выбор bike-групп.

Коммит 4cfd902 (запушен). ВСТУПЛЕНИЕ в реальную CA-bike-группу доказано живьём + драйвер-фикс закрыт.
Пост НЕ доведён — остаток: (1) надёжный ввод в FB-поиск, (2) вход в joined-группу через «Your groups».
Плюс всплыл гео-микс Мичиган/OC — нужно решение Босса по таргету перед второй группой.
