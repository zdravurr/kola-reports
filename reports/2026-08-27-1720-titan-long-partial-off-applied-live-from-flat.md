# ПАРТИАЛ ТИТАНА ВЫКЛЮЧЕН · ЖИВЬЁМ, С ПЛОСКОЙ ПОЗИЦИИ · ОДНА СТРОКА

**2026-08-27 17:20 UTC** · `openitems_guard` — **EXIT=0** до правки и **EXIT=0** после
(`titan-bot HEAD 3888504`, 11 значений).
🔴 **Mercury-SOL НЕ ТРОНУТ:** в его дерево я не заходил, файлы не читал, БД не опрашивал,
венью не трогал. `NRestarts=0`, старт **24.08 13:29:27** — тот же, что и до задачи.

---

## 0. ЧТО СДЕЛАНО, ОДНОЙ СТРОКОЙ

```diff
- LONG_PARTIAL_ENABLED = True     # kill switch — False restores the current rule exactly
+ LONG_PARTIAL_ENABLED = False    # kill switch — True restores the 1/3-at-+1R behaviour EXACTLY
```

Коммит **`3888504`**. Применено **с плоской позиции**, рестарт **17:14:07 UTC**.
Взвод 0.75R **НЕ портирован** и не будет — см. §4.

🔴 **И ГЛАВНОЕ, ЧТО НАДО СКАЗАТЬ СРАЗУ: ПРАВКА ВЕРНА, НО ПОЧТИ ИНЕРТНА.
ПАРТИАЛ СРАБАТЫВАЛ ЗА ВСЮ ИСТОРИЮ БОТА РОВНО ДВА РАЗА.** Подробно в §6 — это ровно тот случай,
который ты просил назвать прямо сейчас, а не «обнаружить через месяц».

---

## 1. 🔴 ПРОВЕРКА ПЛОСКОСТИ — ПЯТЬ ПУНКТОВ, ВСЕ ПРОЙДЕНЫ

Эта проверка дважды блокировала правку, поэтому она сделана целиком, а не «на глаз».

### 1a. `virtual_positions` со `status='open'`

```
row count: 0
```

Разбивка всех статусов: `closed` 66 · `archived_pre_geometry_fix` 6 · **`open` — НОЛЬ.**

### 1b. Отложенные выходы

| таблица | строк | вердикт |
|---|---|---|
| `breakeven_jobs` | **0** | чисто |
| `exit_pending` | **1** | 🔴 **разобрано ниже** |

Строка нашлась, и я её не замолчу:

```
side=LONG  armed_at=2026-08-27T09:00:08.735347+00:00
           expires_at=2026-08-27T15:00:08.735347+00:00   source_signal='Exit Signal'
```

**Она ПРОСРОЧЕНА на 2 ч 08 мин** (проверка в 17:08:48 UTC). `state_machine.is_exit_armed()`
на просроченной строке уходит в ветку `datetime.now(timezone.utc) >= expires` → `clear_exit_pending(side)`
→ **`return None`**. Ни один потребитель не может её зажечь; единственный, кто вообще способен
закрыть по ней (`main.py:3539 _execute_armed_exit`), вызывается только по живому арму.

⚠️ **Я НЕ ВЫЗЫВАЛ `is_exit_armed()`** — она удаляет строку, а проверка не должна менять состояние.
Просрочка доказана арифметикой. Строку удалит сам бот при первом чтении.

### 1c. 🔴 BingX — ОБЕ ПРОБЫ, И ОБЕ ОБЯЗАНЫ СОЙТИСЬ

| проба | результат |
|---|---|
| `fetch_positions(['BTC/USDT:USDT'])` | **0 записей**, ни одной с `contracts != 0` → **FLAT** |
| сырой `swapV2PrivateGetUserPositions({'symbol':'BTC-USDT'})` | `code=0`, `data` **0 записей** → **FLAT** |
| **согласие** | ✅ **ОБЕ ПЛОСКИЕ И СОГЛАСНЫ** |

### 1d. Открытые ордера — остаточный `STOP_MARKET` был бы признаком нечистого закрытия

```
ccxt fetch_open_orders(BTC/USDT:USDT) : 0 order(s)
raw  swapV2 openOrders                : code=0  count=0
```

**Ноль. Остаточных стопов нет.**

### 1e. Вердикт

**Ничего не открыто. Условие «не применять под стоящей сделкой» выполнено** — правка LONG-only,
и живого лонга, вошедшего по старому контракту, не существует. **Проверка повторена ещё раз
непосредственно перед рестартом** — результат тот же.

---

## 2. ПРИМЕНЕНИЕ

### 2a. `.bak`

`/root/titan-bot/config.py.bak_longpartial_off_20260827` — md5 совпал с оригиналом
(`dcb47ff042adb461fd956dd600fcac39`) в момент снятия.

### 2b. Одна строка + обоснование в комментарии

В комментарий вписаны: измеренные цифры (+43.40R / +2.75R), что это **kill switch**,
что **`True` восстанавливает сегодняшнее поведение ТОЧНО**, что шорты не затронуты
по устройству, что взвод 0.75R **не портируется** и почему, и **честная граница замера**
(семнадцать — один день и один тренд, эффективное n ближе к 5–6).

### 2c. 🔴 AST-ПРОВЕРКА — НИ ОДНА ФУНКЦИЯ НЕ ИЗМЕНИЛАСЬ

```
=== FUNCTIONS ===   0 function name(s); bodies differing: 0
=== CLASSES  ===    set equal: True   bodies differing: []
=== IMPORTS  ===    identical: True  (1 statements)
=== MODULE-LEVEL ASSIGNMENTS ===
  added: none   removed: none
  CHANGED VALUES: ['LONG_PARTIAL_ENABLED']
     LONG_PARTIAL_ENABLED:  Constant(value=True)  ->  Constant(value=False)

🔴 VERDICT: PASS — exactly one constant changed, zero code changed
```

И на уровне дерева: `git status --porcelain titan-bot/` → **`M titan-bot/config.py`** и больше
ничего. `main.py`, `virtual_trader.py`, `breakeven_worker.py`, `claude_advisor.py`,
`risk_manager.py` — **чисты**.

### 2d. 🔴 ПОДТВЕРЖДЕНО НЕТРОНУТЫМ

| константа | до | после |
|---|---|---|
| `SL_ATR_MULT` | 2.25 | **2.25** |
| `TRAIL_MULT_ATR` | 1.6875 | **1.6875** |
| `TRAIL_ATR_TF` | 1h | **1h** |
| `EMA_ENVELOPE_GATE_ENABLED` / `_TFS` / `_REQUIRED_DIR` / `_FAIL_OPEN` | True / ['1h','15m'] / Expanding / True | **идентично** |
| каскад 21.08 (a) `HTF_NEUTRAL_REQUIRE_15M_DRYRUN` | True | **True** |
| каскад 21.08 (b) `CONFLUENCE_FLAT_THRESHOLD` | 3.0 | **3.0** |
| каскад 21.08 (c) `HTF_REARM_COOLDOWN_MINUTES` / `HTF_REARM_DRYRUN` | 60 / False | **60 / False** |
| `CONFLUENCE_SCORE_THRESHOLD` | 3.0 | **3.0** |
| оба промпта советника | — | **файлы не тронуты (git clean)** |
| `EXIT_ADVISOR_DRYRUN` | False | **False** |
| риск-гейты (`DXY_HALT_DRYRUN`, `MACRO_BLACKOUT_MINUTES`, `RISK_PER_TRADE_PCT`, `MAX_MARGIN_PCT_PER_TRADE`) | True / 30 / 0.02 / 0.3 | **идентично** |
| `LIVE_TRADING_ENABLED` | True | **True** |
| `ORDER_ADAPTER_LIVE` | True | **True** |
| **размер позиции** `LIVE_FIXED_MARGIN_USDT` × `LEVERAGE` | 30.0 × 5 | **30.0 × 5 = $150** |

```
total changed across the guarded+related set: 1  ->  PASS (only the kill switch)
```

### 2e. 🔴 РЕСТАРТ С ПЛОСКОЙ И ПРОВЕРКА В РАНТАЙМЕ

**Загрузочные строки, дословно:**

```
Aug 27 17:14:07  systemd[1]: Started titan.service - Titan BTC/USDT trading bot.
Aug 27 17:14:13  [TITAN][ORDER-MODE] 🔴 LIVE ORDERS — REAL MONEY: orders ARE sent to BingX
Aug 27 17:14:13  [TITAN][ORDER-MODE]   LIVE_TRADING_ENABLED = True
Aug 27 17:14:13  [TITAN][ORDER-MODE]   ORDER_ADAPTER_LIVE   = True
Aug 27 17:14:13  [TITAN][ORDER-MODE]   sizing: margin $30 x 5 = $150 notional per entry
Aug 27 17:14:17  [TITAN][RECONCILE-XDB] ✅ exchange and DB agree for BTC/USDT:USDT:
                                          0 exchange position(s), 0 open row(s)
Aug 27 17:14:19  [STOP-CLEANUP] no orphaned orders for LONG BTC/USDT:USDT
Aug 27 17:14:19  [STOP-CLEANUP] no orphaned orders for SHORT BTC/USDT:USDT
Aug 27 17:14:19  [RECONCILE] done
```

**Рестарт подтверждён с плоской позиции самим ботом: `0 exchange position(s), 0 open row(s)`.**

🔴 **А ТЕПЕРЬ — ЧТО ИМЕННО ЗАГРУЗИЛ ПРОЦЕСС. Не «файл на диске», а исполняемый байткод.**

`__pycache__/config.cpython-312.pyc` **записан в 17:14:09 UTC** — через две секунды после старта
службы (17:14:07). То есть **его скомпилировал сам загружающийся воркер** из исходника
`mtime=17:10:19, size=65383`, и заголовок `.pyc` эти же `mtime`/`size` и несёт (`VALID: True`).
Это не артефакт с прошлого раза. Читаю константы **прямо из этого объекта**:

```
=== CONSTANTS READ FROM THE BYTECODE THE RUNNING WORKER IMPORTED ===
  LONG_PARTIAL_ENABLED               = False 🔴 <-- THE CHANGE
  LONG_PARTIAL_LEVEL_R               = 1.0
  SL_ATR_MULT                        = 2.25
  TRAIL_MULT_ATR                     = 1.6875
  TRAIL_ATR_TF                       = '1h'
  CONFLUENCE_SCORE_THRESHOLD         = 3.0
  CONFLUENCE_FLAT_THRESHOLD          = 3.0
  HTF_NEUTRAL_REQUIRE_15M_DRYRUN     = True
  HTF_REARM_COOLDOWN_MINUTES         = 60
  HTF_REARM_DRYRUN                   = False
  EMA_ENVELOPE_GATE_ENABLED          = True
  EXIT_ADVISOR_DRYRUN                = False
  LIVE_TRADING_ENABLED               = True
  ORDER_ADAPTER_LIVE                 = True
  LEVERAGE                           = 5
  LIVE_FIXED_MARGIN_USDT             = 30.0
  MAX_POSITIONS_PER_SIDE             = 1
  TREND_REVERSAL_EXIT_DRYRUN         = True

  TRAIL_ARM_R present in module namespace? False

  ✅ ASSERTION PASSED: LONG_PARTIAL_ENABLED is False in the loaded bytecode.
```

Плюс цепочка происхождения: cwd мастера и воркера — `/root/titan-bot`; никакой другой
`config.py` импортироваться не может; исходник записан **17:10:19**, служба стартовала **17:14:07**.

⚠️ **Точная формулировка того, что доказано:** я прочитал константы из **скомпилированного
объекта, который создал сам процесс при загрузке**, а не выковырял значение из его RAM.
Инъекцию в живой процесс на реальных деньгах я не делал сознательно. Это сильнейшее
доказательство, доступное без вмешательства, и я называю его границу.

---

## 3. 🔴 ДОКАЗАТЕЛЬСТВО, НА КОТОРОМ СТОИТ ПРАВКА

Реплей на **собственной книге Титана** — семнадцать лонгов 19.08, порядок `bardir`,
1m-свечи, трейл 1.6875/2.25 = 0.75×1R, тейкер 0.0005 с обеих ног. Стенд откалиброван по
опубликованному прогону 22.08 **с точностью 0.6 %**.

| конфигурация | ΣR (n=17) | среднее | против «сегодня» |
|---|---|---|---|
| взвод 1.00R, партиал **ON** (было) | +102.54 | +6.032 | — |
| взвод 1.00R, партиал **OFF** (стало) | 🔴 **+145.94** | **+8.585** | 🔴 **+43.40** |

Под реальным `MAX_POSITIONS_PER_SIDE = 1` — то, что Титан взял бы на самом деле:
**+7.34R → +10.09R, `+2.75R` на двух позициях.**

На Mercury-SOL тот же партиал снят **14.08** и был **единственной монотонной осью** всего
того свипа: меньше берём — лучше результат, на каждом шаге.

**Шорты не затронуты по устройству.** На трёх шортах 19.08 все четыре конфигурации дают
идентичные **−3.44R**: их MFE были +0.06, +0.06, +0.04R — они не дошли ни до какого уровня взвода.

---

## 4. 🔴 ЧТО НЕ ПОРТИРОВАНО — ВЗВОД 0.75R, И ЭТО ЗАФИКСИРОВАНО

**`TRAIL_ARM_R` в дереве Титана ОТСУТСТВУЕТ.** Проверено: имени нет ни в одном модуле,
и в пространстве имён загруженного байткода его нет (`present? False`). Оно встречается
**единственный раз — в моём новом комментарии, как прозаическое утверждение о том, что его нет.**

**+1R остаётся ЗАШИТЫМ в обоих сайтах:**

```python
# virtual_trader.py:1782
r_dist = _one_r_distance(fill, original_sl)          # без множителя

# breakeven_worker.py:754
r_dist = abs(entry_price - orig_sl)                  # без множителя
```

**Почему отказано:** собственный замер дал **−15.11R** на семнадцати против **ВЕРХНЕЙ**
оценки пользы **+2.78R** на всей книге n=63. Механизм — ровно жалоба оператора: ранний взвод
не просто двигает стоп, он **запускает трейл от БОЛЕЕ НИЗКОГО водяного знака** и на трендовом
ходе срезает бегуна. **Партиал-OFF и взвод-0.75R разделимы, и выжил только один.**

---

## 5. 🔴 ГРАНИЦА КОГОРТЫ ЗАПИСАНА В КАНОН

В `OPEN-ITEMS.md` добавлено:

**(а)** шапка переведена на `HEAD 3888504` с явным предупреждением, что это
**изменение торгового поведения**, а не тулинг, и со ссылкой на новый раздел
(прежняя строка сохранена для аудита).

**(б)** новый раздел **`§0.LONGPARTIAL — THE LONG-EXIT-CONTRACT BOUNDARY — 2026-08-27 17:14:07 UTC`**,
рядом с существующей границей 1R от 04.08, тем же языком:
**«LONG-исходы до и после этого мгновения — НЕ ОДНА ВЫБОРКА. Не объединять, не усреднять,
не продлевать ни один LONG-счётчик через эту черту.»**

**(в)** две новые строки внутри машинно-проверяемой ограды `RUNTIME-STATE`:
`LONG_PARTIAL_ENABLED = False` и `TRAIL_ARM_R = ABSENT — и намеренно`.

**Сторож после всех правок: `EXIT=0`, `titan-bot HEAD 3888504`.**

### 5b. 🔴 ЭКСПЕРИМЕНТ КАСКАДА: LONG-ПОЛОВИНА ТЕПЕРЬ СМЕШАНА, SHORT-ПОЛОВИНА ЧИСТА

* **34 из 65** закрытых позиций — **ШОРТЫ, правкой не затронуты.** SHORT-половина эксперимента
  с 21.08 остаётся **чистой непрерывной выборкой**.
* **31 из 65** — **ЛОНГИ, эта половина теперь СМЕШАНА:** её LONG-плечо охватывает два разных
  контракта выхода, и LONG-вывод эксперимента нельзя читать как одну выборку.

---

## 6. 🔴 БАЗА ЗАМОРОЖЕНА · И ЧЕСТНЫЙ РАЗМЕР ЭФФЕКТА

66 закрытых строк; `id=33` исключена (нет `initial_risk_usdt`); ещё 6 в `archived_pre_geometry_fix`
и в книгу не входят.

| срез | n | ΣR | среднее R | медиана R | винрейт | медиана холда |
|---|---|---|---|---|---|---|
| **ВСЁ** | **65** | **−4.711** | −0.0725 | −0.2795 | **25/65 = 38.46 %** | 5.92 ч |
| **LONG** (теперь смешан) | **31** | **−9.217** | −0.2973 | −0.3065 | **9/31 = 29.03 %** | 5.22 ч |
| **SHORT** (остаётся чист) | **34** | **+4.506** | +0.1325 | −0.0864 | **16/34 = 47.06 %** | 6.95 ч |

**LONG по тому, что закрыло — срез, на который правка действует:**

| закрыл | n | ΣR | среднее R | винрейт | медиана холда |
|---|---|---|---|---|---|
| `sl` | 16 | −9.747 | −0.6092 | 1/16 | 3.03 ч |
| `trail` | 4 | **+2.574** | +0.6435 | 4/4 | 7.19 ч |
| `ai_exit` | 5 | −2.770 | −0.5539 | 0/5 | 4.01 ч |
| `external` | 6 | +0.726 | +0.1210 | 4/6 | 14.96 ч |

**SHORT, заморожен для чистого сравнения:** `sl` 12 / −10.008 · `trail` 11 / **+14.581** ·
`ai_exit` 5 / +0.165 · `external` 5 / −0.248 · `post_entry_critical` 1 / +0.015.

### 6b. ЧТО ДОЛЖНО ИЗМЕНИТЬСЯ — предрегистрация

Лонг, дошедший до **+1R**, теперь несёт **полный** размер к трейлу вместо сдачи трети на взводе.
Наблюдаемо: `realized_partial_usdt` перестаёт писаться, `partial_taken` остаётся 0/NULL на каждом
новом лонге, а реализованный R победившего лонга должен встать **ближе к своему MFE**, чем давала
прежняя смешанная цифра.

### 6c. 🔴 И СКОЛЬКО РАЗ ЭТО ВООБЩЕ СРАБОТАЕТ — ПРЯМО СЕЙЧАС, А НЕ ЧЕРЕЗ МЕСЯЦ

**ПАРТИАЛ СРАБОТАЛ ЗА ВСЮ ИСТОРИЮ БОТА ДВА РАЗА.**

| vpos | side | закрыт | MFE | реализованный R | закрыл | партиал |
|---|---|---|---|---|---|---|
| **82** | LONG | 2026-07-27 | +2.242R | +1.039 | trail | $18.91 |
| **94** | LONG | 2026-08-17 | +1.802R | +0.865 | trail | $0.24 |

Три знаменателя, все три:

* **2 из 31** лонгов всей закрытой книги;
* **2 из 10** лонгов, закрытых с момента включения партиала 26.07 — **20 %**;
* **2 из 2** лонгов, которые в том окне вообще дошли до +1R — то есть **механизм срабатывал
  КАЖДЫЙ раз, когда был применим; применим он был дважды.**

> 🔴 **ВЫВОД, КОТОРЫЙ НАДО СКАЗАТЬ ПРЯМО: ПРАВКА ВЕРНА, НО В КОРОТКОМ ОКНЕ ПОЧТИ ИНЕРТНА.**
> На наблюдаемом темпе она задевает примерно **ОДИН лонг в две недели**. Тот, кто через месяц
> посмотрит на книгу Титана, **НЕ ДОЛЖЕН** приписывать этой правке никакое движение ΣR, не
> проверив сперва, **сколько лонгов за интервал дошло до +1R**: если ноль или один — правка
> не объясняет **ничего**, и движение пришло откуда-то ещё.
> Улики будут копиться **месяцами**. Не перетюнивать раньше тех ~30 чистых лонгов, о которых
> просил ещё исходный комментарий.

---

## 7. СОСТОЯНИЕ ПОСЛЕ

| | |
|---|---|
| `titan.service` | `active (running)`, `NRestarts=0`, старт **17:14:07 UTC** |
| открытых позиций Титана | **0** |
| ошибок/трейсбеков в журнале после рестарта | **нет** |
| `openitems_guard` | **EXIT=0**, `titan-bot HEAD 3888504` |
| **`mercury-sol.service`** | `active (running)`, **`NRestarts=0`**, старт **24.08 13:29:27** — **не тронут** |

**Откат — одна строка:** `LONG_PARTIAL_ENABLED = True` + рестарт с плоской.
`.bak`: `config.py.bak_longpartial_off_20260827`.
