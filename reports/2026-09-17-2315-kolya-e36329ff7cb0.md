# kolya-e36329ff7cb0

_2026-09-17 23:15 UTC_

---

# Отчёт: «ПУСТОЙ ХОД: delegation_guarantee будит worker3, и тот молчит» (e36329ff7cb0)

## Что было на самом деле
Воркер **не молчал**. Ходы 22:58 / 23:00 / 23:08 по лидам Lolis и Steven назвали
измеренную стену ровно теми же словами, что и ходы 22:12/22:14, которые судья принял:
«`ph.see()` снова таймаут 40с», «устройственный слой Pixel не отвечает уже ~1ч40м»,
«Pixel-нода stale, экранный слой (screencap/uiautomator/input) мёртв».

Разница — только в **форме отказа**. Словарь судьи знал три формы, и все три про
ДЕЙСТВИЕ хода: «не могу отправить», «не могу войти», «не успел / таймаут убил job».
Здесь действия нет и быть не может: инструмент, которым только и читается тред, МЁРТВ,
и ход честно сказал об этом, не выдумывая себе попытку. Четвёртая форма была безымянной →
честный BLOCKED падал в EMPTY, сторож объявлял «ВОРКЕР РАЗБУЖЕН И НЕ СДЕЛАЛ НИЧЕГО»
ходу, который назвал причину раньше и точнее самого сторожа, и заводил на него
техзадачу — **18-й раз**.

## Что починил
`lib/turn_outcome.py`:
1. `_DEAD_LAYER_RX` — отказ по **смерти слоя**: мёртв / не отвечает / недоступен / stale /
   не поднимается — но обязательно ПРИ техническом слое (слой, нода, мост, экран,
   screencap, uiautomator, phone_hands, `ph.see`, ssh, adb, tailnet, Pixel, bridge),
   и именно в этом порядке.
2. `names_blocker` принимает отказ, названный ЛИБО бессилием хода, ЛИБО смертью слоя.
3. У этой формы **замер — сам мёртвый слой**, а не обязательно цифра: «экранный слой
   Pixel мёртв, нода stale» числа не содержит, но называет именно то, обо что ход
   расшибся. Такая строка теперь читается как измеренная стена наравне с `tab_lock`.

**Факт после:** все три хода — BLOCKED с причиной, которую назвал сам ход; она уезжает
в `blockers.jsonl`, переспрашивается каждые 20 мин и опровергается первой же успешной
отправкой покупателю.

**Границы целы:** молчание человека («покупатель Steven не отвечает 2 часа», «продавец
недоступен») блокером НЕ становится; голое «всё мёртво» без слоя — НЕ блокер; своё
удавшееся действие с замком причиной не становится; прежние три формы и успешный ход
судятся слово в слово как вчера.

Контракт: `tests/test_dead_layer_refusal_is_a_named_blocker.py` (14 проверок, обе стороны
границы, тела двух живых ходов дословно). **Набор 323/323 зелёных за 50с.**

## Честно: покупатели этим НЕ отвечены
Починен судья, а не телефон. Под ним стоит живая стена, и она реальная:
**устройственный слой Pixel мёртв ~2 часа** — `see/screencap/uiautomator/input` уходят
в таймаут 40–85с шесть подъёмов подряд, `rebridge` поднимает только браузер-мост и
устройственный слой не чинит. Из-за неё стоит ВСЯ очередь sell-лидов fb_sales:
**Lolis, Phuong, Dai, Scott, Steven** — ни один тред не читается и не отвечается.

Владельца у этой стены не было — завёл отдельной задачей **b3e6a9a75810**.
Решений от тебя не требуется.
