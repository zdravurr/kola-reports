# kolya-aba3c7c6b50d

_2026-08-23 17:56 UTC_

---

# Инбокс FB: прочитать НЕ УДАЛОСЬ — blocked, а не «никто не написал»

**Коротко:** приоритет клиента, ради которого задачу переоткрыли, **работает** — экран у прогрева
отбирается. Но упёрлись в другое: один прогон чтения инбокса **длиннее жизни моей сессии**, поэтому
до списка тредов я так и не дошёл. **Есть ли новое входящее и от кого — по-прежнему НЕИЗВЕСТНО.**

## Что подтвердилось (починка прошлой сессии жива)
- метка `fb_inbox:look kind=buyer ttl=900` ставится и держится;
- держателя экрана **вытеснили**: `[tab_lock] ВЫТЕСНЯЮ держателя «xwarmup» (pid=906511, держит 45с):
  ждёт ПОКУПАТЕЛЬ fb_inbox:look`;
- ложного REAP чужой метки больше нет — снимаются только осиротевшие (по мёртвому pid).

## Обо что встали
Три прогона `platform_look.py facebook` (17:52–17:58), каждый >9 минут, ни один не дошёл до тредов:
ожидание экрана ~45–140 с, затем навигация «Marketplace» **пикселями (OCR)** — в uidump элемента нет.
Окно сессии (10 мин, SIGTERM) кончается раньше, и процесс умирает вместе с сессией:
`REAP who=fb_inbox:look — владелец pid=906636 мёртв`. Сколько ни повторяй — синхронно из сессии
инбокс прочитать нельзя в принципе.

Побочно: при выводе не в терминал скрипт почти ничего не пишет (за 9 минут — 153 байта, одна строка
про tab_lock). **Вердикта нет в логе вообще**, так что даже успевший прогон нечего предъявить.

## Что дальше (уже заведено, Босса ждать не нужно)
Задача-наследник: гонять `platform_look` **отвязанно от сессии** (setsid/systemd-user), с записью
вердикта («новых входящих нет» / «входящее от КОГО») в `.kola_state/platform_look/<площадка>.json`
с меткой времени. Тогда задача вида [platform_look] закрывается чтением файла за секунды, а не
удержанием экрана десять минут. Родитель aba3c7c6b50d закрыт как тупик по этому пути.

## Чего я НЕ делал
Кода не трогал, контракты не гонял (нечего было менять), операционку не трогал.
