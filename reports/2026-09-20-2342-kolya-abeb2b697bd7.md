# kolya-abeb2b697bd7

_2026-09-20 23:42 UTC_

---

# abeb2b697bd7 — резерв зрения: обжора больше не слепит чужих через общий перелив

**Что было сломано.** С 11.09 у каждого критичного потребителя зрения есть своя квота резерва. Но перелив сверх общего потолка оставался одной общей кучей на всех: `критичных < eff + резерв`. Свип инбокса сделал 63 подъёма и в одиночку выбрал эту кучу (124 > 117). После этого ослепли ВСЕ критичные, включая прогревы с целой своей квотой. Это тот же дефект «кто первый взял», только этажом выше.

**Что починено** (`lib/source_day_budget.py`, новая функция `_crit_ceiling`):
- Перелив теперь поимённый. Общий потолок критичных = `eff + резерв × число критичных имён` (имя = полоса:скрипт).
- Каждое имя сверх `eff` тратит только свой резерв (`mine < own_limit`), поэтому перебор ограничен вкладом каждого имени.
- `allow` и `free_at` считают потолок одной функцией, час освобождения совпадает с решением.

**(а) Законны ли 63 подъёма у свипа.** Да: крон `12,42 * * * *` даёт 48 заходов в сутки, в среднем ~1.3 кадра на заход. Один и тот же экран по кругу он не крутит. Сверх общей доли свип теперь упирается в свой резерв (сейчас 40/12) и ждёт с часом освобождения.

**Проверка.**
- Контракт `test_day_budget_reserves_sight_for_the_critical_path.py`, раздел 4, переписан. Свип выгоняет критичных за прежнюю кучу `eff + резерв`. Прогрев с целой квотой видит. Сам обжора и прогрев с выбранной квотой ждут.
- Набор контрактов: 351/351 зелёных.
- Живой журнал сейчас: 71/93, потолок критичных 213, прогревы 5/12 и 4/12 — зрение есть.
