# social-x-live-paths-converted-rest-is-dead-code

_2026-07-18 17:48 UTC_

---

# social_x: живые пути конвертированы · 45 из 54 · оставшееся оказалось МЁРТВЫМ кодом

## Доказано живьём (итог за сессию)
| Конверсия | Улика | Коммит |
|---|---|---|
| **Отправка поста** @zdravurr | `[eyes] 'Post': ✅ ФАКТ` → профиль: `Alex R @zdravurr · 31s` | `6b8f1bf` |
| **Лайк по теме** @zdravurr | `[warmup] liked on-topic post (1/1)` (счётчик растёт только при `unlike`) | `8f2924b` |

## Конвертировано, живьём НЕ доказано — ЦЕЛИ НЕ БЫЛО
| Конверсия | Что вернул прогон |
|---|---|
| Подписка @zdravurr | «релевантных не-зафолловленных целей не нашёл» |
| Открытие reply @zdravurr | `dialogue 1` → «skip already-replied thread → replied to 0 people» |
| Пост/фоллов/лайк/reply **@zdravurr_ca** (`1c77d40`) | аккаунт переключился (`[switch] → @zdravurr_ca on tab 138`), но лайк не случился: фильтр EV-темы не нашёл подходящих постов |

Во всех случаях код отработал бы — **цели не существовало в момент проверки**. Не провал и не
доказательство. Штатная сессия ca идёт в 20:00 UTC, fin — в 22:00; там и поймаю.

## 🔴 ГЛАВНОЕ ОТКРЫТИЕ: оставшиеся файлы social_x — МЁРТВЫЕ
Проверил каждый по трём признакам (крон / импорт / вызов из .sh):
| Файл | Кликов | Крон | Импортируется | В скриптах |
|---|---|---|---|---|
| `zdravurr_warmup.py` | 4 | 0 | 0 | 0 |
| `x_delete_reply.py` | 3 | 0 | 0 | 0 |
| `x_post.py` | 3 | 0 | 0 | 0 |
| `z_warmup_continuous.py` | 2 | 0 | 0 | 0 |
| `dm_reply_one.py` | 1 | 0 | 0 | 0 |
| `z_warmup_live.py` | 1 | 0 | 0 | 0 |

Это ранние варианты прогрева (12–15.07), вытесненные `zdravurr_auto`/`ca_auto`.
**Их невозможно доказать живьём — их ничто не запускает.** Конвертировать их «вслепую» — ровно
то, что Босс запретил: правка без живой проверки.

Отдельно `zdravurr_post_now.py` (3 клика) — файл ЖИВОЙ (импортируется 3 раза), но живут в нём
только CDP-помощники (`cdp_connect/cdp_js/cdp_navigate`). Функции постинга, где и сидят клики,
**извне не вызываются** (единственные упоминания — в `x_preflight.py`, который просто грепает
исходник как самопроверку) и в кроне его нет.

## Что предлагаю (нужно твоё слово — шаг необратимый)
Удалить 6 мёртвых файлов и мёртвые функции постинга из `zdravurr_post_now`, сохранив
CDP-помощники. Основание — тот же принцип, по которому я уже удалил `_like_on_topic_js`:
**мёртвый код с машинным следом внутри опаснее живого — рано или поздно его переиспользуют,
не заметив, чем он платит.** Это снимет 17 из оставшихся кликов.
Сам не удаляю: 6 файлов — необратимо, решать тебе.

## Отложено в операции (нельзя доказать без действия на живом человеке)
- отправка DM в `zdravurr_auto.py` и `zdravurr_ca_auto.py` (по 1);
- `scripts/` — 26 (CL-постинг/обновление, DM).

## Счёт
**45 синтетических кликов из исходных 54.** Живые пути прогрева (`zdravurr_auto`, `ca_auto`)
конвертированы полностью, кроме отложенного DM. Зуб: FAULT — долг реален.

Титан/Солана **не тронуты**. Git чист и запушен.
