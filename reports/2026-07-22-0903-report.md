# report

_2026-07-22 09:03 UTC_

---

🧠 САМООБУЧЕНИЕ 22.07 09:00 UTC (разбор 24ч)
⚙️ Свои баги — чиню сам, до автоматизма, без костылей и без запроса добра.
Здоровье: 🟢0 🟡2 🔴4
🔴 Красные: fb_social, infra, sales, social_x

♻️ Повторы:
• [cars_ev] kola-self-improve-daily: 8-й цикл подряд один и тот же неподтверждённый фикс (идемпотентная запись в LEARNINGS.md); стоп-правило (3+ цикла → эскалация) описано, но фактически ни разу не сработало — вместо пуша Боссу пишется очередная слепая запись в лог
• [fb_social] Мета-цикл 7-е сутки (16-22.07): фикс SSH-таймаута написан в плане, но невносим — нет доступа к коду из песочницы (ls /mnt и find заблокированы)
• [fb_social] Нулевой постинг: 0 post_PUBLISHED за сутки, долг растёт
• [fb_social] Пустой 24ч-лог = ложный green: ошибок нет, потому что сессии не стартуют, а не потому что здорово
• [infra] Ложный 🔴 'sys.path:scripts/autonomy/self_improve_daily.py: insert → НЕТ WS/sales' — флаппинг с 10.07, печатается дважды в одном ране (нет дедупа), уже 9+ дней обесценивает красный цвет
• [infra] Битый путь /root/titan-bot размножился: было turn_report_watchdog.py → стало report_due.py + turn_report_watchdog.py (чинят находку, а не класс)

🔧 Трив-фиксы (применю в сессии):
• [cars_ev] Каждую команду в хендлере выполнять одним вызовом (без ';'/'&&'/grep-цепочек), т.к. составные команды режутся апрувом в кроне независимо от allow — это и есть причина, по которой прошлые проверки 'файла нет' были ложными.
• [infra] НЕМЕДЛЕННО в этой сессии понизить проверку sys.path-insert по WS/sales до WARN без 🔴 (дедлайн исчерпан, 9-й день, без условий); retry/recheck добавить отдельно потом
• [infra] Дедуп находок по ключу (файл+путь+причина) в пределах рана; не печатать 'найдено N (🔴0)' без содержимого — молчать или выводить сами находки
• [infra] grep -rn 'titan-bot' scripts/ и разом вычистить/починить ВСЕ вхождения (/root/titan-bot не существует), а не по одному файлу в день
• [provisioning] R._cdp(): выбирать активную вкладку + Page.bringToFront с переподключением сокета, не брать tabs[0]
• [provisioning] Санити-чек координат тапа: точка обязана лежать в пределах фактического размера экрана, иначе fail, а не тихий тап

🛠 В сессии (нужен разбор):
• [cars_ev] Форсировать стоп-правило в коде хендлера, а не в тексте лога: счётчик повторов одного фикса >=3 → PushNotification Боссу с одним вопросом (где лежит LEARNINGS.md / создать по явному пути) и НЕМЕДЛЕННЫЙ выход без записи новой секции. Сейчас правило существует только как рекомендация внутри лога и потому не исполняется.
• [cars_ev] Снять ложную посылку 'файла нет': LEARNINGS.md по факту читается (его хвост подан в этот прогон через SessionStart-хук), значит путь резолвится в среде сессии. Зафиксировать абсолютный путь из окружения хука в конфиге крона и прекратить генерировать фиксы, исходящие из 'файл не существует'.
• [fb_social] ЕДИНСТВЕННЫЙ пункт — открыть канал доставки фикса: смонтировать /mnt/volume в песочницу ИЛИ добавить путь в allowed working directories ИЛИ выдать ls+find+Edit по пути ИЛИ назначить внешнего исполнителя правок. Технику (remote timeout, BatchMode, health-check, гейт post_PUBLISHED, user_rotation) НЕ повторять — описана 7 раз, остаётся планом до появления доступа
• [infra] all_ok = AND по ВСЕМ компонентам, включая node; node=stale → all_ok=False, node в regressed[], рестарт → repaired[]; вызывать auto-repair и при пустом regressed[] если all_ok=False
• [provisioning] В health-логике отделять 'ошибок нет' от 'прогонов не было': сутки без единого submit → yellow, не green (счётчик реальных прогонов, а не только ошибок)
• [sales] РЕАЛЬНО применить (не только предложить) порог X @zdravurr (фин) 12→26ч в едином персистентном конфиге порогов; логировать список каналов+порогов при старте тика; пометить фикс применённым лишь после подтверждения в логе, что порог=26ч

📈 X-контент (разбор 12 постов за сутки):
• Лучший охват: zdravurr_ca «Zdravurr C. is live — EV dealer SoCal. A Tesla is more than a car. It is a hedge.» — views=206, кратный отрыв от всего остального
• Что заходит: Заходит короткое заявление-манифест с чётким хуком и контрастом ('more than a car — it is a hedge'), а не аналитические простыни. У fin высокий score/views даёт пост с вопросом-фреймворком (DXY 100) — вовлечение тянут простые бинарные тезисы и открытый вопрос
• Посты завтра: Резать длину: один афористичный тезис + одна цифра + провокация/вопрос в конце. Убрать обрезанные 'умные' абзацы (Circle/Clarity Act) — они дают views без вовлечения. Первую строку делать самодостаточным хуком
• Комменты завтра: Заканчивать конкретным вопросом с цифрой-якорем ('твой таргет по DXY?', 'под сколько брал бы Polestar 2?') вместо расплывчатого 'What's your timeline?' — просить назвать число/срок, это поднимает reply-rate

📥 Свип инбокса не удался: client_pending — yielded

🧩 Стек генерации:
• ✅ генерация: Opus 4.8 (CLI/подписка)
• ✅ Gemini=только память (генерация закрыта, HTTP 429)

🔧 Фиксы 04.07:
• ✅ notif_watch = dumpsys-first
• ✅ translate_ru placeholder-гейт
• ✅ FB-capture мукс (kola_tap)
• ✅ inbox_sweep thread-guard
• ✅ X CDP-мост alive
• ✅ кроны: inbox_sweep + bridge-keepalive
• 🔴 inbox-watcher service active
• ✅ orca: авто-снос restore-модалки
• ✅ orca: общий предохранитель go_home_generic
• 🔴 покупатель: draft всегда есть (не дёргаю Босса по 'available?')

📮 X-постинг (таб+dedup+свежесть):
• ✅ X: живой page-таб есть (не sw.js)
• ✅ X: find_tab фильтрует type==page
• ✅ X: dedup порог ≥3 (моно-ниша не глохнет)
• ✅ X: нет оборванных постов за 24ч (гейт завершённости)

🚦 Прогревы (гейт перед стартом дня):
• 🔴 X @zdravurr (фин): посл. пост 16ч (порог 14)
• ✅ X @zdravurr_ca (EV): посл. пост 10ч (порог 30)
• 🔴 FB группа SoCal EV: посл. пост 57ч (порог 28)
• ✅ X CDP-мост (Chrome на Pixel)
• ✅ FB прогрев (Alex Man) guard=yellow

(код из крона сам не правлю — фиксы применяю в сессии; журнал в LEARNINGS)
