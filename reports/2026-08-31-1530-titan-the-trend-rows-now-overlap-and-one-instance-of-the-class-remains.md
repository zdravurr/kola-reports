# ТИТАН: СТРОКИ ТРЕНДОВ ТЕПЕРЬ ПЕРЕСЕКАЮТСЯ. И ОДИН ЭКЗЕМПЛЯР ТОГО ЖЕ КЛАССА ОСТАЛСЯ — НАЗВАН, НЕ ТРОНУТ

**2026-08-31 15:30 UTC** · `openitems_guard` — **EXIT=0 до и после**. HEAD `c66a900` → **`9c40a4f`**.
🔴 **Правка ПРИМЕНЕНА. Только читаемость: ярлыки и значения. Ни порога, ни инструкции, ни совета.**
🔴🔴 **Mercury-SOL НЕ ТРОНУТ: в дерево не входил, не читал, не импортировал.**
Титан **ПЛОСКИЙ** (0 открытых) при правке и при рестарте — проверено дважды.

**Основание — мой отчёт 15:20 сегодня, §1d пункт 4.** Я его нашёл, назвал и оставил Боссу.
**Решение принято: чинить.** Это третья правка одного класса за день — и, как показывает §4,
**не последняя.**

---

# §1. ПОЧЕМУ ЭТО НЕ «НОВЫЕ ФАКТЫ» — И ПОЧЕМУ БОСС ПРАВ

Заголовок блока обещает **«Regime at ENTRY vs NOW»**. Что было под ним:

| таймфрейм | ENTRY | NOW | пара? |
|---|---|---|---|
| 1d | ✅ | ❌ | нет |
| 4h | ✅ | ❌ | нет |
| 1h | ✅ | ❌ | нет |
| 15m | ❌ | ✅ | нет |
| 5m | ❌ | ✅ | нет |

🔴 **ПЕРЕСЕЧЕНИЕ ПУСТОЕ. Ни одного таймфрейма, который был бы на обеих строках.**
Заголовок обещал сравнение, которого в блоке нет, а `1h=bear` рядом с `15m=neutral`
читается как разворот, которого никто не измерял. **Ровно та же поломка, что с ADX15m.**

## 1a. ПОКРЫТИЕ — ЗАМЕРЕНО, КАК И ДЛЯ `srv_adx_15m` (84/84)

| колонка | сторона | заполнено |
|---|---|---|
| `trades.trend_15m` | ENTRY | **82 / 84** |
| `trades.trend_5m` | ENTRY | **82 / 84** |
| `trades.trend_4h` / `trend_1h` | ENTRY | 82 / 84 |
| `trades.trend_1d` | ENTRY | 🔴 **63 / 84** — у 21 входа его нет |
| `smart_exit_dryrun_samples.trend_15m_live` | NOW | 313 / 313 |
| `smart_exit_dryrun_samples.trend_5m_live` | NOW | 313 / 313 |
| 🔴 **любая колонка тренда 1d/4h/1h** | **NOW** | 🔴 **НЕ СУЩЕСТВУЕТ ВООБЩЕ** |

**Пустых строк (`''`) нет ни в одной колонке — проверено отдельно, чтобы `''` не прошло за значение.**

**NULL печатается словами, соседний таймфрейм НЕ подставляется:**
```python
def _tr(k):
    v = e[k] if k in e.keys() else None
    return v if v not in (None, '') else 'not recorded'
```
**Двадцать один вход без `trend_1d` теперь честно скажет `1d=not recorded`, а не одолжит `4h`.**

## 1b. 🔴 NOW-ЗНАЧЕНИЙ ДЛЯ 1d/4h/1h НЕ СУЩЕСТВУЕТ — И ЭТО СКАЗАНО, А НЕ ОПУЩЕНО

`smart_exit_dryrun_samples` **не несёт ни одной колонки тренда 1d/4h/1h** (313 строк из 313).
Значит эти три **никогда** не смогут получить пару в момент консультации.
**Молчание — это ровно то, что породило оба сегодняшних дефекта.** Поэтому отсутствие названо:

```
  🔴 UNPAIRED: 1d / 4h / 1h are ENTRY-ONLY. No current value for those three
  timeframes is sampled at consultation time, so the 'Now' 15m and 5m readings
  are NOT their continuation and no change across them may be inferred.
  15m and 5m are the only trends that appear on BOTH lines.
```

**Форма взята та же, что уже применяется в блоке книги и в примечании про окно ADX:
факт отсутствия плюс запрет на вывод — и ни слова о том, что делать с позицией.**

## 1c. ДО И ПОСЛЕ НА ДВУХ ПОЗИЦИЯХ, ВСКРЫВШИХ КЛАСС

**vpos 90** (та, где смешались таймфреймы ADX):
```
БЫЛО   At entry: regime=TREND 1d=neutral 4h=bear 1h=bear ADX1h=23.0 ADX15m=39.9
       Now:      15m=neutral 5m=neutral ADX1h=25.9 ADX15m=49.5

СТАЛО  At entry: regime=TREND 1d=neutral 4h=bear 1h=bear 15m=bear 5m=bear ADX1h=23.0 ADX15m=39.9
       Now:      15m=neutral 5m=neutral ADX1h=25.9 ADX15m=49.5
       🔴 UNPAIRED: 1d / 4h / 1h are ENTRY-ONLY. …
```
🔴 **Теперь видно настоящее движение: `15m: bear → neutral`, `5m: bear → neutral`.**
И это **подтверждает** то, что советник тогда написал («5m/15m shifted to neutral, were SHORT at
entry») — но раньше проверить его слова по промпту было НЕЧЕМ. Теперь есть.

**vpos 96** (та, где склеились две стены):
```
БЫЛО   At entry: regime=TREND 1d=bull 4h=bull 1h=bull ADX1h=13.3 ADX15m=25.1
СТАЛО  At entry: regime=TREND 1d=bull 4h=bull 1h=bull 15m=bull 5m=bull ADX1h=13.3 ADX15m=25.1
```
**`15m: bull → neutral`, `5m: bull → neutral` — пара, которой не было.**

---

# §2. ЧТО НЕ ИЗМЕНИЛОСЬ

## 2a. СИСТЕМНЫЙ ПРОМПТ
```
sha256 ПОСЛЕ: 3d709571e17ff4051362c5a50496c74c0223b29a062eb2c94b2a5de09655670b
совпадает с эталоном задания 3d709571e17ff4051362c5a50496c74c… : True
```

## 2b. НИ ПОРОГА, НИ ИНСТРУКЦИИ, НИ СОВЕТА
Добавлено: пять значений трендов на строку входа, слово `not recorded` на месте NULL,
и абзац `UNPAIRED`, говорящий только о том, **чего в данных нет**. **Ни одного слова о позиции.**

## 2c. 🔴 ВСЕ ЧЕТЫРЕ ФАКТА 30.08 И ПЯТЫЙ ОТ 31.08 — ПОБАЙТОВО ТЕ ЖЕ

Полный дифф промпта, прогнанный **через настоящий код** с подменённым `_call`
(**обращений к API — ноль**), на двух реальных сохранённых позициях:

| позиция | дифф |
|---|---|
| **vpos 96** (НЕ взведена) | **один участок — блок режима. Больше ничего.** |
| **vpos 100** (ВЗВЕДЕНА) | **один участок — блок режима. Больше ничего.** |

| блок | идентичен? |
|---|---|
| **Trailing-stop arithmetic** (ПЯТЫЙ факт, 31.08) | ✅ **ДА** |
| **Cost of closing** (факт «a», 30.08) | ✅ **ДА** |
| **If you HOLD / STOP_MARKET** (факты «b», «c», 30.08) | ✅ **ДА** |
| **Since the previous consultation** (факт «d», 30.08) | ✅ **ДА** |

## 2d. AST, КОНСТАНТЫ, `.bak`, КОНТРАКТЫ
```
main.py           : изменена РОВНО ОДНА функция — _build_exit_context
                    добавлен вложенный помощник _tr
claude_advisor.py : 🔴 НЕ ИЗМЕНЁН ВООБЩЕ (changed=[] added=[] removed=[])
git diff          : ОДИН файл. config.py вне диффа.
.bak              : main.py.bak-trendpair-20260831-152212
                    claude_advisor.py.bak-trendpair-20260831-152212
контракты         : 172/172 зелёных (sudo -u botuser, 34с)
```

| константа | значение | тронута? |
|---|---|---|
| `SL_ATR_MULT` 2.25 · `TRAIL_MULT_ATR` 1.6875 · `TAKER_FEE_RATE` 0.0005 | — | **нет** |
| `BREAKEVEN_BUFFER_PCT` 0.001 · `EXIT_ADVISOR_DRYRUN` False | — | **нет** |
| `HTF_CASCADE_ENABLED` True + два каскадных флага | — | **нет** |
| `LONG_PARTIAL_ENABLED` False · `CONFLUENCE_SCORE_THRESHOLD` 3.0 | — | **нет** |

---

# §3. ПРИМЕНЕНО

**3a. ПЛОСКИЙ — 0 открытых, проверено в секунду перед рестартом.** Останавливаться было не о чем.

**3b.**
```
titan.service  : ActiveState=active  NRestarts=0  старт 2026-08-31 15:24:31 UTC
boot line      : [TITAN][ORDER-MODE] 🔴 LIVE ORDERS — REAL MONEY: orders ARE sent to BingX
                 [TITAN][ORDER-MODE]   LIVE_TRADING_ENABLED = True
загружено ли   : mtime main.py 15:22:39 < старт процесса 15:24:32 ⇒ поднят НОВЫЙ файл
в журнале      : ошибок и трейсбеков — 0
openitems_guard: EXIT=0 ПОСЛЕ
коммит         : 9c40a4f, запушен
```

---

# §4. 🔴 ПОСЛЕДНЯЯ СВЁРТКА — И ОНА НАХОДИТ ЕЩЁ ОДИН ЭКЗЕМПЛЯР ТОГО ЖЕ КЛАССА

Отрендерил весь промпт целиком и прошёл по каждому блоку с одним вопросом:
**обещает ли заголовок или формулировка связь, которой в значениях нет?**

## 🔴 НАЙДЕНО: СТРОКА `Volume:` — ТОТ ЖЕ ДЕФЕКТ, ЧТО ТОЛЬКО ЧТО ЧИНИЛИ

```
Regime at ENTRY vs NOW
  At entry: … 15m=bull 5m=bull ADX1h=13.3 ADX15m=25.1
  Now:      … 15m=neutral 5m=neutral ADX1h=15.9 ADX15m=27.4
  Volume:   vol_1h=3.37  vol_15m=2.21  ATR change vs entry=+15.0%
```

**Строка `Volume:` стоит ВНУТРИ блока «Regime at ENTRY vs NOW» и несёт два значения
ТОЛЬКО ЗА СЕЙЧАС — `vol_1h` и `vol_15m` — без входной пары и без пометки о её отсутствии.**
Дельтой явно назван лишь третий элемент, `ATR change vs entry`.

🔴 **И входные данные СУЩЕСТВУЮТ И ЗАПОЛНЕНЫ ПОЛНОСТЬЮ:
`trades.srv_vol_ratio_1h` — 84/84, `trades.srv_vol_ratio_15m` — 84/84.**
**Ровно та же форма, что была у `trend_15m` (82/84) и у `srv_adx_15m` (84/84) до их починки.**

⚠️ **НЕ ТРОНУТО — задание сказало: доложить, дальше в этом проходе не чинить.**
Записано в канон как **§0.PROMPT-PAIRING**, ждёт решения Босса, как ждала строка трендов.

## ЧТО ПРОВЕРЕНО И ОКАЗАЛОСЬ ЧИСТЫМ

| блок | вердикт |
|---|---|
| `Order book NOW vs AT ENTRY` | ✅ 2 из 3 строк имеют пару, третья (противоположная стена) **помечена** «NOW only» |
| `Order-book PERCENTILE scale` | ✅ все четыре помечены `NOW`, объявлены разными объектами, настоящие дельты названы |
| `Cost of closing` | ✅ «34 % от −0.48R» — отношение величин, арифметика сходится |
| `Peak so far (MFE) / Giveback from peak` | ✅ giveback = MFE − нереализованное, тождество выполняется |
| `ENTRY THESIS` (тиры) | ✅ каждая строка несёт свой ТФ и слова «at entry» |
| `Recent 5m structure` | ✅ один объект, помечен «CONTEXT ONLY» |
| `If you HOLD` | ✅ один объект, все величины текущие и названы |

## ⚠️ И ОДНА МЕЛОЧЬ ПОМЕНЬШЕ, НАЗЫВАЮ ЧЕСТНО

`Since the previous consultation (1.0h into the position)` — в скобках стоит `prev_age_h`,
то есть **возраст ПРЕДЫДУЩЕГО замера**, а не промежуток между двумя. Читатель может принять
1.0h за интервал. **Двусмысленность, а не ложное утверждение** — величины под ней верны.
Записываю, но к классу «заголовок обещает несуществующую связь» не отношу.

---

# ВЕРДИКТ

1. 🔴 **СТРОКИ ТРЕНДОВ ПЕРЕСЕКАЮТСЯ. 15m и 5m теперь на ОБЕИХ строках; 1d/4h/1h помечены
   ENTRY-ONLY, потому что NOW-значения для них НЕ СУЩЕСТВУЕТ (313/313 строк без такой колонки).**
   **Пустых мест не осталось: у каждой строки либо пара, либо явная пометка «пары нет».**
2. **Это не новые факты: `trend_15m`/`trend_5m` лежали в той же строке БД (82/84) и не выбирались.
   NULL печатается как `not recorded` — 21 вход без `trend_1d` теперь скажет это вслух,
   а не одолжит соседний таймфрейм.**
3. **Настоящая пара сразу дала пользу: у vpos 90 видно `15m: bear → neutral`, и слова советника
   впервые стало чем проверить по самому промпту.**
4. **`_CLOSE_SYSTEM_RICH` побайтово равен эталону задания. `claude_advisor.py` в этом проходе
   НЕ ИЗМЕНЁН ВООБЩЕ. Изменена одна функция, добавлен один вложенный помощник.
   Все четыре факта 30.08 и пятый от 31.08 — байт в байт, доказано полными диффами промпта
   на vpos 96 и vpos 100. Ни одна из восьми констант не тронута. Контракты 172/172.**
5. **Применено из ПЛОСКОГО состояния, рестарт 15:24:31, файл старше процесса, журнал чист,
   guard EXIT=0.**
6. 🔴 **СВЁРТКА НАШЛА ЕЩЁ ОДИН ЭКЗЕМПЛЯР КЛАССА: строка `Volume:` внутри блока
   «ENTRY vs NOW» несёт `vol_1h` и `vol_15m` только за СЕЙЧАС, без пары и без пометки.
   Входные `srv_vol_ratio_1h` и `srv_vol_ratio_15m` заполнены 84/84.**
   **НЕ ТРОНУТО по условию задания. Записано в канон, ждёт решения Босса.**
7. **Итог дня по этому классу: три дефекта найдены, три исправлены, четвёртый найден и назван.
   Все четыре — одной формы: ЗАГОЛОВОК ОБЕЩАЕТ СРАВНЕНИЕ, КОТОРОГО В ЗНАЧЕНИЯХ НЕТ.**

---

**Происхождение.** `trades.db` Титана — `mode=ro`, только SELECT: покрытие `trend_15m`/`trend_5m`
(82/84), `trend_1d` (63/84), `srv_vol_ratio_1h`/`srv_vol_ratio_15m` (84/84), отсутствие любой
колонки тренда 1d/4h/1h в `smart_exit_dryrun_samples` (313 строк). Диффы промптов — прогон
настоящего кода на строках позиций 96 и 100 с подменённым `_call`: обращений к API ноль,
ордеров ноль. Сравнение версий — `ast.dump` по каждой функции; системный промпт — `sha256`
литерала через `ast.literal_eval`. Mercury-SOL не открывался.
