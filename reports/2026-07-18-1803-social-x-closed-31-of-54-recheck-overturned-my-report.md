# social-x-closed-31-of-54-recheck-overturned-my-report

_2026-07-18 18:03 UTC_

---

# social_x закрыт: 31 из 54 · перепроверка перед удалением ОПРОВЕРГЛА мой же доклад

## 🔴 Сначала — поправка к тому, что я тебе доложил
Я сообщил: «все 6 файлов мертвы, 0/0/0». **Это было неверно.** Строгая перепроверка (крон
botuser+root, статические И динамические импорты, вызовы по имени файла из `.py/.sh/.service`)
нашла живые ссылки у двух. Первый греп искал `import X` и `X.py` и **не видел** ни
`run("X.py")`, ни `subprocess.run([... "path/X.py"])`.

Твоё требование «перепроверить перед удалением» окупилось буквально: без него я бы сломал
живое, имея на руках твоё одобрение.

## Удалено (вызывающих нет — проверено)
| Что | Почему безопасно |
|---|---|
| `zdravurr_warmup.py` (4 клика) | звал только `daily_warmup.py`, а его самого не зовёт никто |
| `x_post.py` (3) | только собственные usage-строки и упоминание в комментарии |
| `x_delete_reply.py` (3) | только usage-строка и совет в тексте другого скрипта |
| `z_warmup_live.py` (1) | ноль ссылок |
| `post_with_image()` + `main()` из `zdravurr_post_now.py` (3) | файл ОСТАЁТСЯ живым как библиотека CDP-помощников (3 импорта); `x_preflight` проверяет не его, а `zdravurr_auto`/`ca_auto` — у них свои копии, они целы |

## НЕ удалено, вопреки одобрению — сломало бы живое
- **`z_warmup_continuous.py`** (2 клика): его запускает `presence_tick.py` через `subprocess`.
  Крон `presence_tick` **закомментирован** 02.07 с пометкой «вернуть при воскрешении X» —
  удаление убило бы документированный путь восстановления.
- **`dm_reply_one.py`** (1 клик): `daily_self_audit.py` (ЖИВОЙ, в кроне) читает его **исходник**
  и проверяет наличие `read_labeled_history()`/`load_convo_card()`. Удаление → пустой
  `readfile` → аудит **каждый день** орал бы «РЕГРЕССИЯ к last-line-blind».
  Вечно орущий сторож = шум, это мы уже проходили.

## Проверено ПОСЛЕ удаления — не импорты, а работа
```
x_preflight:  🟢 ВСЁ ЧИСТО — можно постить/отвечать
живой прогрев: аккаунт @zdravurr → [warmup] liked on-topic post (1/1)
```
Все три зависимых скрипта (`cl_post_bike_phone`, `cl_renew_bike_phone`, `lib/register`)
парсятся, CDP-помощники на месте. Удалённое восстановимо из git history.

## Счёт: 31 из 54
| Где | Кликов | Статус |
|---|---|---|
| `scripts/cl_post_bike*.py`, `cl_renew_bike_phone` | 19 | операции (действие на живом объявлении) |
| `scripts/kola_dm_brushoff.py` | 2 | операции (сообщение человеку) |
| `scripts/autonomy/adb-watchdog.sh`, `enable-wadb.js` | 5 | инфра ADB, не поверхность площадок |
| DM в `zdravurr_auto` / `ca_auto` | 2 | операции |
| `z_warmup_continuous.py` | 2 | ждёт воскрешения presence-полосы |
| `dm_reply_one.py` | 1 | операции |

**Живые пути прогрева конвертированы полностью**, кроме отложенного DM.

## Что ещё не доказано живьём (жду штатных сессий)
подписка и открытие reply на @zdravurr; пост/лайк/фоллов/reply на @zdravurr_ca.
Сессии: ca в 20:00 UTC, fin в 22:00 — доложу, что из них реально исполнилось.

Титан/Солана **не тронуты**. Git чист и запушен (`349739a`).
