# cl-publish-blocked-pixel-ssh

_2026-07-15 23:27 UTC_

---

# CL публикация велика — ЧЕСТНЫЙ СТАТУС (финальный блокер НЕ снят) — 2026-07-15

Прямые ответы на твои 4 вопроса, без прикрас.

## 1. Резидент-прокси через Pixel поднялся? — НЕТ.
`:8899` (HTTP-прокси) и `:1080` (SOCKS) НЕ слушают. Прокси-цепочка не встала.

## 2. Листинг велика на Craigslist LIVE, с URL? — НЕТ, всё ещё PENDING.
Драфт СОЗДАН в этой сессии (скрины /tmp/cl_s10_final.png, cl_confirmed.png от 23:09), CL
прислал письмо-подтверждение — но публикация требует ВХОДА в аккаунт, а вход идёт через
резидент-прокси, который не поднялся. **Публичного URL НЕТ. Не выдумываю его.**

## 3. OfferUp — подключён / запощен / live? — НЕТ.
До OfferUp в этот заход НЕ дошёл (он шёл ПОСЛЕ живого CL-URL по приоритету). ADB жив
(OfferUp без litho-проблемы — реалистичен следующим), но сейчас статус: НЕ сделано.

## 4. Что именно падает на Pixel SSH (честно, без петли):
- ADB (транспорт №1) — ЖИВ: `adb ... echo` → ADB_OK.
- SSH к Termux sshd (:8022, транспорт №2, нужен для SOCKS-прокси) — ВИСНЕТ: свежие
  соединения не завершаются (и через тёплый mux, и «холодные» — обрубались таймаутом 25с).
- Вероятная причина: мои повторные `ssh -D 1080` набили MaxStartups у Termux sshd
  зависшими полу-сессиями (та самая известная беда «6 зависших sshd-сессий держат слоты»).
- Что сделал: через ADB убил и перезапустил sshd на Pixel (restarted). Финальную попытку
  SOCKS-туннеля ты прервал — подтвердить, ожил ли туннель, я не успел.

## ИТОГ ЧЕСТНО
Велик на CL = СОЗДАН, но PENDING. LIVE-URL НЕТ. Единственный оставшийся блокер — Pixel SSH
(:8022) для резидент-прокси; он — известный долг («recurring flaky point»), сейчас висит,
и мои -D попытки его усугубили. ADB жив, sshd перезапущен через ADB — следующий чистый заход:
1 попытка SOCKS через свежий sshd; если снова виснет — чинить Pixel-SSH как отдельный долг
(не долбить -D по кругу).

## ЧТО ПРОЧНО СДЕЛАНО РАНЬШЕ В СЕССИИ (не откатано)
- ✅ Playwright chromium переустановлен (жертва переезда №1).
- ✅ CL area-picker дрейф формы починен (OC по лейблу) — форма проходит до конца.
- ✅ eSIM-номер убран из объявления → CL email-relay → mail_watch → Сеня.
Эти три фикса закоммичены; CL-поток технически рабочий до шага «войти и опубликовать».

Titan + Solana НЕ тронуты.
