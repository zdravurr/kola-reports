# kolya-b2250fdef3b7

_2026-09-17 22:53 UTC_

---

# b2250fdef3b7 · судья подъёма зовёт ЗАВЕРШЁННЫЙ ход «ещё идёт» — ЗАКРЫТО

## Корень
`delegation_guarantee._delegated` мерил **жизнь** хода окном времени: свежее
`WORKER_TIMEOUT_S` (45 мин) — значит «работа идёт». Факт завершения всё это время лежал в
том же файле хода — трейлер нашей обёртки `# rc=… dur=…s`, который печатается уже ПОСЛЕ
смерти ребёнка. Судья его не читал.

**Улика 17.09:** ход `20260917T211004Z_lead_guarantee_retry_3122538.log` кончился в
21:17:40Z (`rc=0 dur=456s`), а в 21:41Z судья про него отвечал «штатный подъём ещё идёт:
worker3». Следствие: завершившийся **вхолостую** ход по СВОЕМУ же лиду держал лекаря
выключенным до 45 минут — покупатель ждёт, а лечить «нельзя, работа идёт».

Это отдельный корень от 0438034390cb: там ход судился по **предмету** (за кого он),
здесь — по **жизни** (кончился или нет).

## Что починено
- `lib/delegation_proof.py` — `woken_turns()` отдаёт `rc` и `ended` (= старт + `dur`) из
  трейлера; добавлены `turn_finished(name)` и `run_finished(run)` — тем же фактом судится
  наш механический подъём (у него в реестре лежит имя файла хода). `spawned_runs()` признак
  завершения (`ended`) носил уже давно — его просто никто не спрашивал.
- `scripts/delegation_guarantee.py` — `_delegated()` требует **отсутствия завершения** по
  всем трём источникам пруфа, а не только свежести.

**Граница сохранена:** трейлера нет → ход НЕ доказан завершённым (идёт / убит насмерть
SIGKILL / старше 24.08), и решает свежесть, как раньше. Крен «лучше лишняя тревога, чем
второй воркер у покупателя» не отменён — он ограничен знанием.

## Чем закреплено
`tests/test_finished_turn_is_not_a_live_wake.py` — 6 проверок: признак завершения доезжает
до судьи; у незавершённого хода он пуст; завершённый ход / завершённый спавн Дирижёра /
наш завершённый подъём не держат лекаря; идущий ход держит.

Набор контрактов: **321/321 зелёных** (50 с).
