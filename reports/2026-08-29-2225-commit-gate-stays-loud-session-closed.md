# COMMIT GATE остаётся громким — решение зафиксировано. Сессия закрыта

**29.08.2026, 22:25 UTC.** Правок в гейте — **ноль**. Контракты **155/155**,
git чист и запушен (`7d9e142`).

---

## 1. Подтверждаю: изменений НЕ БЫЛО

Не «я так помню», а проверяемо:

```
.git/hooks/pre-commit          2026-07-28 10:50:10
scripts/git_hooks/pre-commit   2026-07-28 10:49:55
diff → ИДЕНТИЧНЫ
git status scripts/git_hooks/  → 0 изменений
```

Хук не трогали **месяц**, живой файл байт-в-байт равен трекаемому исходнику.
Я его не глушил, не понижал и не буду.

## 2. Стоячее правило — записано

> **COMMIT GATE ОСТАЁТСЯ ГРОМКИМ.** Запись в money-path код — это «нужен человек»,
> а не самолечение. Обмен шума на слепоту в самом чувствительном месте не делается.

Правило закреплено **механически**, а не обещанием: контракт
`test_commit_gate_stays_loud` существует ровно затем, чтобы будущая сессия не приглушила
гейт тихо. Он стережёт то, что снимается одной строкой и потом не замечается:

* рот в сторону Босса — `notify_boss` на блоке и слово «заблокирован» в тексте;
* состав защищённых путей: `projects/sales/`, `lib/lead_router.py`,
  `lib/agent_capacity.py`, `lib/notify.py`, `scripts/git_hooks/`;
* удержание заблокированной правки (`commit_gate_held`) — «громко» не должно стать «дорого»;
* след авторизованного прохода (`commit_gate_pass.log`, метка `AUTHORISED`);
* **живой хук совпадает с трекаемым исходником** — иначе стерегли бы файл, который не бежит.

Это единственное, что я добавил. Поведение гейта не изменено ничем.

---

## 3. ⚠️ Факт, на котором должно стоять правило (проверил, а не вспомнил)

Формулировка решения звучала как «каждое срабатывание, **даже авторизованное**, продолжает
уведомлять». Сверил с кодом и с журналом доставок — **сегодня это не так**:

| сегодня | сколько | ушло в Telegram |
|---|---|---|
| 🛑 блоков | **4** | **4** |
| 🔓 авторизованных проходов по токену | **6** | **0** |

В хуке авторизованная ветка (строки 110–124) печатает в терминал, дописывает строку
`AUTHORISED` в `.kola_state/commit_gate_pass.log` и делает `exit 0`. **`notify_boss` в ней
нет** — он только в ветке блокировки. Так сделано **намеренно 28.07**: до того гейт умел
доказать лишь то, что ЗАБЛОКИРОВАЛ, и на вопрос «правка money-path прошла ревью или её
протащили токеном?» ответить было нечем — поэтому появился файл-след.

**Что это значит практически:** сегодня 6 записей в money-path код
(`inbox_watcher.py`, `lead_router.py`, `agent_capacity.py`, `dirigent_control.py`,
`screen_priority.py`, `x_tab_guard.py`) прошли **без сообщения в Telegram** — их видно
только в `commit_gate_pass.log` и в истории git.

**Ничего не добавляю.** Приказ был «оставить ровно как есть», и добавление рта — это
изменение, пусть и в сторону громкости. Если нужно, чтобы **и авторизованные проходы**
звонили — это одна строка в ветке 110–124, делаю по прямому слову.

---

## Итог дня

| проверка | факт |
|---|---|
| commit gate | **не тронут** (28.07, идентичен исходнику), правило закреплено контрактом |
| контракты | **155/155** зелёных за 33 с |
| git workspace | чисто, `HEAD == origin/master` (`7d9e142`) |
| git /root | чисто, `HEAD == origin/main` (`a661dd6`) |
| Титан | pid **1713083**, NRestarts=**0** — не тронут |
| SOL listener | pid **3521920**, NRestarts=**0** — не тронут |

### Три решения Босса за вечер — все исполнены
1. Пять закупочных автотредов закрыты навсегда (park `boss_decision` + CRM `lost`).
2. Второй писатель бейджей — не заводим; источник долга остаётся один.
3. Порог тревоги поднят: Боссу звонит только не починившееся само — **кроме commit gate**,
   который по этому решению остаётся громким всегда.
