# kolya-bbc750cf7342

_2026-09-12 21:41 UTC_

---

# bbc750cf7342 · инбокс OfferUp глазами — BLOCKED (экран не прочитан)

**Итог: экран НЕ прочитан. Это НЕ значит «никто не написал».** Задачу не закрываю.

## Что выяснил
- **Пуш «More for less than $50»** — это реклама OfferUp, а не покупатель. Правило для него уже стоит в `_OU_PRICE_BAIT` (`projects/sales/inbox_watcher.py`, правка 12.09 20:50). Сверяется вся строка целиком, поэтому «More for less than $50? Can you do 40 for both?» по-прежнему считается живым лидом. Проверил: `is_offerup_price_bait('More for less than $50')` → True. Этот случай проверяет контракт `tests/test_offerup_giveaway_push_dropped_as_noise.py` (строки 50–55). Значит, такие пуши больше не должны заново открывать эту задачу.
- **Глаза:**
  - Прогон 20:50:27 → `killed`. Он всё время ждал экран: экран держал прогрев `xwarmup` pid=1652531, и прогон умер, не дописав вердикт.
  - Новый отвязанный прогон 21:40:14 → `running`. Экран сейчас держит `boss_order:mercari_route_12sep` pid=1661353. Это работа Босса, она законно главнее взгляда. При этом `pixel.lock` всё ещё за pid=1652531 (тот же прогрев).
- В 10-минутное окно сессии вердикт не пришёл. Надзиратель прогона продолжит сам, и следующая сессия прочитает его через `platform_look.py offerup --read`.

## Что проверить дальше
- Если pid=1652531 (`xwarmup`) держит `pixel.lock` уже больше 50 минут, это отдельная поломка: прогрев не отпускает замок. Проверить не смог: смотреть процессы (ps, /proc) эта сессия не может без одобрения.
- Прошлый взгляд (11.09 21:50): 8 тредов, никто не ждёт.

Контракты не прогонял: кода не менял, правка и её контракт уже на месте.
