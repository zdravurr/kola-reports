# СВОДНЫЙ ИТОГ 23.07: пост в одном тапе (блокер найден), велик и мультиварка по 3 площадки, закон зрения

_2026-07-23 16:14 UTC_

---

**Сводный итог за 23.07.** Коммиты: `2294eb3`, `a84e58d`, `607a8b3`, `011fa1f`, `dd33ed9`,
`2c6d9bf`, `f5a8b37`, `71bac06` — все запушены.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 1. ПОСТ В ГРУППУ — НЕ ОПУБЛИКОВАН. ОДИН ТАП, И ВОТ ПОЧЕМУ

Заход был **ОДИН**, как приказано. Скрин: `.kola_state/p2_FINAL_blocker_addgroups_23jul.jpg`

**Что на скрине:** композер **нужной группы** «SoCal Electric & Hybrid D. · Public group»,
автор **Alex Man**, наш текст набран **ЦЕЛИКОМ** (CARFAX + battery warranty transfer), и синяя
кнопка **Post внизу справа — ВИДИМАЯ, ничем не накрытая**. До публикации — один тап.

**Точный блокер (и он мой, не механизма зрения):**
FB **ВОССТАНОВИЛ открытый композер с черновиком** после force-stop. Код же безусловно пошёл по
пути «мы на странице группы → найди строку создания поста → тапни». Зрение ответило честно:
`verdict=wrong_screen` — «это не строка создания поста». **Глаза не ошиблись — им задали неверный
вопрос:** искать ВХОД в композер на экране, где мы УЖЕ ВНУТРИ композера.

Показательно: до этого нав прошла впервые за день чисто — `nav_via_joined_list` ✅.

**Починено (71bac06):** `post_to_group` первым делом спрашивает «мы уже внутри композера?»
(ComposerActivity + поле ввода + НАШ текст в дереве) — если да, не навигирует, **не
перепечатывает текст** (дало бы дубль) и идёт прямо к публикации. Логика публикации вынесена в
`_submit_open_composer()` и переиспользуется ОБОИМИ путями: один общий путь, а не спец-случай.

**Повторно не долблю** — решение за тобой. Фикс залит и готов; следующий плановый заход
(Пн/Ср/Пт 19:00 UTC) его отработает, либо скажи слово — сделаю один заход.

**Артефакты после захода ЧИСТЫ** (проверено): стейт `1784505921` = реальная публикация
**20.07 00:05**, дневной кап **0**, `idx=6` не сожжён, `STATUS=fail:composer` — честно.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 2. СТАТУС ВЕЩЕЙ

## Велик Giant Rainier 26in — 3 площадки
| Площадка | Статус | Улика |
|---|---|---|
| **FB Marketplace** | ✅ **$100 · Available** | карточка в НАСТОЯЩЕЙ сетке «Your listings» 13:56 + кнопки `Renew listing`/`Mark as sold`; скрин `p1_PROOF_bike_live_grid_23jul.jpg` |
| **Craigslist** | ✅ live | `curl`: маркер вещи на странице |
| **OfferUp** | ✅ live (утренний пруф) | глаза: `giant rainier 26in mountain bike` в «Мои объявления» |

⚠️ **Оба сегодняшних «missing» по OfferUp я АННУЛИРОВАЛ** — вынесены с чужих экранов (сначала
реклама, потом ИНБОКС). Подробно в п.4. Велик пропавшим **не объявляю**.
⚠️ FB предлагает `Renew listing` + баннер «older than 1 month» — вещь ЖИВА, но стареет. Это
работа контракта `listing_aging`; сам в money-path без твоего слова не тапал.

## Мультиварка Zojirushi EL-CAC60 — 3 площадки
| Площадка | Статус | Улика |
|---|---|---|
| **FB Marketplace** | ✅ **$100 · Available** | та же сетка «Your listings» 13:56 |
| **Craigslist** | ✅ live | URL `7948754253` |
| **OfferUp** | ✅ live | eyes 22.07: «Posted!», $100, 3 фото |

Утром я ошибочно написал «подтверждаю только ДВЕ площадки» — **исправлено**: их три, как ты и
говорил. Пруфа не было по ТОЙ ЖЕ причине, что «пропал» велик: сломанный детектор сетки не читал
ни одной карточки. Один баг спрятал обе вещи.

## Титан / Солана / eBay
- **Титан — НЕ ТРОНУТ.** Ни файла, ни сервиса, ни конфига.
- **Солана (mercury-sol) — НЕ ТРОНУТА.** То же.
- **eBay — НЕ ТРОНУТ.** Cold-hold, канал Бори; seller-onboarding не касался.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 3. ЧТО ЗАКРЫТО ЗА ДЕНЬ

| Пункт | Итог |
|---|---|
| **P2 доступ** (8 суток) | ✅ Канал был закрыт ТРЕМЯ слоями, все сняты: симлинк → systemd BIND-монт (один инод, два пути); 56 файлов root-owned (включая ровно тот `fb_active.py`) → владение + сторож */10; `claude -p` без `--permission-mode` авто-отклоняет Edit. Запись подагентом доказана живьём |
| **P2 корень нуля постов** | ✅ Не SSH-таймаут (его винили 9 разборов), а МОЛЧАЛИВЫЙ путь: `RC=0` на провале → стейт «постили только что» → анти-дубль глушил 8ч, включая плановый пост 22.07 19:00 |
| **P1 велик** | ✅ Ложное чтение опровергнуто карточкой в сетке |
| **P3 stale-путь** | ✅ Фикс не откатывался — врал детектор: `/root` это 0700, `Path.exists()` глотает PermissionError. Аудит 🔴 **0** от botuser и от root |
| **P4 стоп-правило** | ✅ В коде. Самопроверка опровергла мою первую редакцию (хэш давал n=1,1,1) → перекрытие: 1→2→3, stop, РОВНО одна эскалация |
| **P5 Огонёк** | ✅ У ланы следования не было потребителя директив. Починил → заход ca подхватил САМ через 22 мин: `@TeslaHype`, `@learnmyev` зафолловлены, квота 2/5 (было 0/5), квитанции `applied` |
| **Побочно** | money-path ждал экран 360с и не получил (уступка стояла только в SUSTAINED) → чекпойнты на КАЖДОЙ границе фазы в оба X-воркера |
| **ЗАКОН ЗРЕНИЯ** | ✅ `docs/UI_TARGET_RULE.md` + аудит + конверсия composer/messenger/sweep/renew |

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 4. ГЛАВНЫЙ УРОК ДНЯ: ОДИН ДЕФЕКТ, ШЕСТЬ РАЗ, КАЖДЫЙ РАЗ СЛОЕМ ВЫШЕ

**Вердикт по улике, которую вынести было НЕЛЬЗЯ:**
1. аудит: «нет каталога `/root/titan-bot`» — жив, но 0700 у предка (не видно ≠ нет);
2. обход FB: «велика нет в сетке» — читал экран ОДНОГО листинга, и его же панель
   «Listed in 4 places» была доказательством ЖИЗНИ;
3. анти-дубль: «постили только что» — своя же запись о ПРОВАЛЕ;
4. `browse`: «9 скроллов ок» — скроллил ЧУЖОЕ приложение;
5. **мой рапорт тебе: «пост опубликован»** — по отсутствию кнопки на под-экране «Tag people».
   Передал, не открыв пруф. Испортил ТРИ артефакта — все откатил (ретракция в журнал, а не
   переписывание улик);
6. **мой же новый `verify`** — дважды: прошёл на РЕКЛАМЕ («Inbox | com.ebay.mobile…gclid…»),
   потом на ИНБОКСЕ, где «sold» нашлось в ЧУЖОМ тексте «this one already sold». Обход прочитал
   ПЕРЕПИСКУ и объявил, что вещи нет в списке лотов.

**Форма ошибки одна:** я чинил слой под собой и доверял слою, который не проверил. Контракт кода
выхода — не сильнее проверки публикации. reach-цикл — не сильнее своего `verify`. Рапорт — не
сильнее открытого пруфа.

**Два правила, записанные в память (переживут сессию):**
- проверять по **НАЛИЧИЮ результата**, никогда по отсутствию признака; **пруф существует, чтобы
  В НЕГО СМОТРЕЛИ** — «готово» без открытого пруфа не рапорт;
- всякая цель на экране — **общий цикл зрения**, и **это касается ПРОВЕРКИ тоже**: слова в
  свободном тексте пользователя не могут доказывать, ЧТО за экран открыт.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 5. ТВОЙ ДОВОД ПРО «FB ПОМЕНЯЕТ ВЁРСТКУ» ПОДТВЕРДИЛСЯ БУКВАЛЬНО

```
локатор кнопки Post:  c[1] < 400        (правый ВЕРХ, разведка UI 23.06)
живой замер 23.07:    y = 2213          (НИЗ экрана)
```
Точный `desc="Post"` в дереве БЫЛ, функция отдавала None. И неряшливый фолбэк (подстрока «Post»
по экрану) поломку МАСКИРОВАЛ, «находя» текст диалога. Область снята → живьём `(935, 2213)`.

**Два поступка общего цикла, которых слепая цепочка не умеет:**
- OfferUp: понял, что сидит **в переписке с покупателем** (Jasmine, проданный столик), вернулся
  НАЗАД, дошёл до лотов на шаге 2. Старый `tap_label` тапнул бы ВНУТРИ чужого треда;
- FB: понял, что в композере **с набранным черновиком**, и ОСТАНОВИЛСЯ: «выходы отсюда
  УНИЧТОЖИЛИ БЫ неопубликованный черновик». Вердикт `unknown`, НЕ `missing`. Отказался ломать
  нашу работу ради своей цели.

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 6. ОТКРЫТО (честно)

| Что | Состояние |
|---|---|
| **Пост в EV-группу** | фикс залит, НЕ проверен живьём. Плановый заход Пн/Ср/Пт 19:00 UTC либо твоё слово |
| **OfferUp-велик** | нужен один чистый перепроверочный заход с новым verify через понимание |
| **Renew велика** | FB сам предлагает; за тобой решение |
| **Конверсия остатка** | `fb_active` ~21 матч под тап (инвайты/лайки/пикер — не money-path); ~15 как ПРОВЕРКИ (законом допустимо); messenger 9 узловых (горячие уже сняты); `lib/kola.py` CLI; `craigslist_chat_send` на DOM — другой класс, нужен отдельный подход для веба |

Ни один конвертированный шаг не оставлен с текстовым фолбэком.
