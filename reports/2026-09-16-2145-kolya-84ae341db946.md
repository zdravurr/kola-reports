# kolya-84ae341db946

_2026-09-16 21:45 UTC_

---

# 84ae341db946 — сессия техника, 16.09.2026 (3-е открытие по Sergio)

## Что было
Покупатель Sergio (fb_sales, детект 20:51:54Z) не отвечен. Предыдущая сессия в 21:34
разобрала, ПОЧЕМУ сторож не называл помеху, починила сторожа и **назвала причину**:
завела `file --from 84ae341db946` отдельную задачу **b2a16c57d603** — «экран покупателя
заперт чужим tab_lock >20 мин, уступка (YIELD) приходит после смерти воркера».

Дальше случилось то, ради чего меня разбудили ТРЕТИЙ раз:
* 21:34:22 — заведена b2a16c57d603 (причина названа, лежит в очереди);
* 21:34:42 — 84ae341db946 закрыта;
* 21:35:04 — сторож ответа увидел ТОТ ЖЕ неотвеченный ответ Sergio и **переоткрыл** её;
* 21:40:06 — поднята НОВАЯ сессия техника (эта) — на симптом, чья причина лежала рядом
  в статусе NEW и никем не взята.

## Корень (починен)
**Симптом не исчезает, пока не починена причина** — значит каждый тик сторожа
переоткрывал задачу и жёг сессию на уже разобранное, а настоящая задача стояла
непринятой. Петля замкнута по построению.

`--from` намеренно не родство (`parent`): та задача живёт своей жизнью, её статус не
трогается. Но для ТРАТЫ это ровно тот класс, что уже известен очереди — «лимит подписки»
(22.08) и «стена, которую снимают руки» (07.09): **чинить ЗДЕСЬ нечего, ждать есть чего.**
Этого класса у порога подъёма не было.

## Что именно сделано (lib/tech_queue.py)
1. `cause_owned_by(task_id)` — открытые задачи, заведённые ИЗ этой (`file … --from <id>`:
   ссылка лежит в `component` и в улике «найдено по ходу задачи»). Родство `parent` сюда
   НЕ втянуто — там работает обратное правило 29.08 (`_reopen_parent_if_done`).
2. Гейт в `spawn_kolya` (последний рубеж перед тратой): есть живой владелец причины —
   **сессии нет**, на задаче остаётся метка `cause_owned_by=<id>` (молчание названо, а не
   слепое). Улику не прячем: задача не закрывается, `occurrences`/`last_seen` едут дальше.
3. Владелец причины закрыт — метка снимается, обычный путь возвращается (тот же урок, что
   у `_apply_wait`: срок принадлежит ПОСЛЕДНЕМУ замеру, иначе ожидание станет глухотой).
4. В ветке переоткрытия `file_task` звонок технику тоже не уходит, пока причиной владеет
   другая открытая задача: звонок — та же трата, что и сессия.

## Контракт
`tests/test_session_not_spawned_when_cause_owned_by_open_task.py` — 11 проверок:
владелец гасит подъём и назван; переоткрытие без звонка, но и без сокрытия улики;
закрытие владельца снимает метку; чужая задача ничего не гасит; правило 29.08 цело.

## Что осталось Боссу знать
Ответ Sergio по-прежнему не ушёл — его держит **b2a16c57d603** (чужой tab_lock и уступка
после смерти воркера). Она в очереди и теперь получит сессию вместо симптома.
