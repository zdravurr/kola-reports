# status-petlya-krutilas-vholostuyu-chestno-po-punktam

_2026-08-15 19:46 UTC_

---

# Статус: петля крутилась вхолостую. Честно, по пунктам

**15.08.2026, 19:50 UTC.**

---

## 1. Три задачи — состояние

| задача | вещь | состояние | попыток сессий |
|---|---|---|---|
| `ca7ff4f95bd2` | Coffee Table | **new** | 0 |
| `dcc2a402d0ef` | Writing Desk | **new** | 0 |
| `ecf35c93329e` | Sectional Sofa | **new** | 0 |

**Ни одна не в работе и ни одна не закрыта.** Счётчик попыток нулевой, потому что задачи
заводились до починки учёта (см. п.4).

## 2. Легла ли починка нав-бара

**Частично — и не до конца.** Правки сессии Коли лежат на диске (`lib/eyes.py`,
`lib/phone_hands.py` — а диск и есть то, что бежит), написаны два контракта, оба зелёные.

Корень она нашла верный, и он не тот, что называл я: сторож `vtap` отказывал в тапе по
законной кнопке нижней панели **приложения** (y≈2180–2280), приняв её за системный нав-бар
(начинается с 2298). Это класс — те же отказы в логе с 13.07.

**Но стена НЕ снята.** Последний контракт самой же сессии показывает:

```
verdict=target_unlocatable
причина: понимание называет цель «Marketplace tab (иконка-витрина с бейджем 1)
в нижнем таб-баре», но её бокс не даёт ни дерево, ни OCR, ни локатор по виду —
тапать нечем
```

То есть стало ЧЕСТНЕЕ (внятный вердикт вместо немого `max_steps`, за 2 круга вместо 6),
но конкретно иконка Marketplace по-прежнему не нащупывается.

## 3. Возобновилась ли заблокированная работа

**Нет.** Возобновление привязано к `resolve`, а ни одна задача не закрыта — значит триггер
не срабатывал ни разу. Исходный воркер отработал свои 2612 с (43 мин) по всем трём клеткам
и закончился `did=False` в 19:34:47. Это был первый проход, а не возобновление.

## 4. 🔴 Честно: петля КРУТИЛАСЬ

По задаче `ca7ff4f95bd2` поднялись **три сессии подряд**. Причина: `spawn_kolya` не менял
состояние задачи — она оставалась `new`, и прокачка честно подбирала её снова и снова.
Работа шла, а очередь считала, что за задачу никто не взялся.

Потолок кругов не спасал: `MAX_GOAL_CYCLES` — про ЦЕЛЬ Босса, а жглись сессии техника.
Другая ось, своего потолка у неё не было.

**Исправлено:** спавн ЗАБИРАЕТ задачу (`in_progress`) и считает попытки;
`MAX_SESSION_ATTEMPTS=3` на задачу, дальше не поднимаем вовсе. Три захода не помогли —
значит задача поставлена неверно, и нужен человек, а не ещё круг.

**Прогресс есть, результата нет.** За 20+ минут: найден настоящий корень (мой диагноз был
неверен), починен класс отказов, живших с 13.07, написаны два контракта. Снято объявлений —
**ноль**.

## 5. Сторож отчётов больше не орёт волком

Доставка **7076** легла в леджер как `kind='report'`, `has_link=false` → замок счёл её
отчётом, потерявшим ссылку. Но звонок технику — это **указатель на очередь** («вот id, вот
улики, вот команда показа»), ссылки на полный отчёт у него нет и быть не должно.

Это **третий** случай класса: `is_alert_body` знает виды тревог **поимённо**, и каждая новая,
не вписанная в него, рождает новый ложный алерт — так было с алертом о голой позиции и с
самим сторожем отчётности. Вписал `ТЕХЗАДАЧА`. Настоящий отчёт ссылку требовать не перестал.

---

## Состояние

| | |
|---|---|
| Коммиты | `a302221` (петля + классификатор) · до него `1c4b9d6`, `851a59d`, `7272e30` |
| Контракты | зелёные, включая два новых от сессии Коли |
| Задачи | 3 × `new`, потолок сессий теперь есть |
| Снято объявлений | **0** |
| Титан / Solana | ✅ не тронуты |

Что дальше по существу: иконку Marketplace нечем нащупать ни одним из трёх локаторов.
Это упирается в тот же вывод, что я давал раньше — нужен **браузерный путь**
(`mbasic/m.facebook.com`), где у элементов есть настоящий DOM. Скажешь — поставлю это
задачей в ту же очередь.
