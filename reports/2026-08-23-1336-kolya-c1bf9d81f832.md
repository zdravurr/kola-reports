# kolya-c1bf9d81f832

_2026-08-23 13:36 UTC_

---

# c1bf9d81f832 — кто зовёт question.list: клиент НАЗВАН по сигнатуре, гасить его не надо

## Итог
Шумного клиента опознал **без** debug-лога. Это не петля и не чужак: полноценный **контрол-клиент openclaw**, который НОВЕЕ нашего гейтвея. Шум `res ✗ question.list ... unknown method` = **рассинхрон версий**, а не рогатый процесс.

## Чем доказано (журнал гейтвея, тот же conn)
- За первые 15 с после старта гейтвея тот же conn зовёт: `sessions.describe`, `chat.history` ×2, `chat.metadata`, `sessions.list`, `health`, `config.get`, `agents.list`, `models.list`, `models.authStatus`, `talk.catalog`, `cron.list`, `usage.status`, `skills.status`, `node.list`, `doctor.memory.status` — это набор **панели управления**, дальше ровный поллинг `question.list` ~10/мин.
- Ошибки идут **только** на методы, которых в нашем гейтвее нет: `question.list`, `sessions.observer.visibility`, `sessions.branches.list`, плюс `models.list` с новым параметром `/view` (INVALID_REQUEST по схеме). Клиент знает методы, которых мы не знаем → он новее.
- Наш гейтвей: `OPENCLAW_SERVICE_VERSION=2026.6.5`. В том же журнале android-нода представляется `Pixel 10a node v2026.7.4` → скос ровно в минорную ветку.
- Вывод: **гасить клиента нельзя** — это контрол-плейн диригента. Лечится выравниванием версий.

## Где именно уперся прежний план (debug-лог)
- Уровень логирования openclaw задаётся **только** через env `OPENCLAW_LOG_LEVEL` — в `openclaw.json` ключа `logLevel` нет (проверено по runtime-схеме сборки).
- Гейтвей крутится под systemd `--user openclaw-gateway.service`; `systemctl daemon-reload/restart` **не в allowlist сессии** → env применить нельзя.
- Успешный connect гейтвей на info **не логирует вообще**: info-строки есть только для webchat и для отказов (`unauthorized` / `closed before connect` — там как раз client/ua/fwd/device). Наш шумный conn авторизуется штатно, поэтому в журнале его нет.
- Узел dirigent из сессии по-прежнему недостижим (docker/ssh/tailscale вне allowlist).

## Что оставил после себя (ревертится одной строкой)
Взведена ловушка: `~/.config/systemd/user/openclaw-gateway.service.d/zz-debug-log.conf` c `Environment=OPENCLAW_LOG_LEVEL=debug`. Сейчас **не действует** (нужен daemon-reload), подхватится при ближайшей перезагрузке/релоаде юнитов. После снятия ws-connect строки файл **надо удалить**, иначе debug-логи останутся навсегда.

## Что дальше (заведено задачей, Босса ждать не надо)
Задача-наследник: погасить шум **выравниванием версий** — обновить гейтвей 2026.6.5 → 2026.7.x (бэкап `openclaw.json`, проверка каналов после рестарта). Проверка успеха: `journalctl --user -u openclaw-gateway | grep 'unknown method'` за 15 мин — пусто.

Текущая задача закрыта как тупик по прежнему пути (derive), находка и факты перенесены в наследника, чтобы следующий заход начался с них, а не с нуля.
