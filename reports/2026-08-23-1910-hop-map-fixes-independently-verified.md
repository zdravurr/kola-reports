# Карта хопов: обе правки ПРОВЕРЕНЫ НЕЗАВИСИМО

**23.08.2026 19:10 UTC.** Сессия проверки. Титан/Solana НЕ ТРОНУТЫ.

## ЧТО БЫЛО НА ВХОДЕ

Задача Босса — применить две правки карты хопов (13 точек молчаливой потери).
Придя в дерево, я нашёл их **уже применёнными предыдущей сессией** (`session_01SdqmAjP1dzaeSikMhvN63o`),
коммиты `c8a50ef` (18:52 UTC) и `e782000` (18:57 UTC), отчёт опубликован 18:58.

Я НЕ переписывал работу. Я её **перепроверил живьём**, не веря сообщениям коммитов.
Ниже — только то, что я воспроизвёл СВОИМИ руками.

## ПОДТВЕРЖДЕНИЕ КАРТЫ (13 точек)

9 на пути покупателя (из 14 хопов), 3 на пути команды Босса (из 8), +1 между хопами (парковка).
Шесть из них — шесть ЧИТАТЕЛЕЙ одной строки `kind=route`, закрываются одной правкой.
Ещё две — второй. Пять независимы.
Главное: у закупочной полосы было **НОЛЬ судей** — 4388 `lane=sales`, 1427 `lane=self`,
**0 `lane=buying`** при $23 700, прошедших через неё, потому что закупочный путь
не звал `route_and_log`. А `BUYER_LANES` был набран руками: два мёртвых литерала
(`buy`, `procurement`) против двух живых непокрытых (`buying`, `social`).

## ПРАВКА 1 — КАНОН ПОЛОСЫ. ПРОВЕРЕНО ✅

**1. Выведение, а не набор.** `lead_router.LANES` несёт поле `debt`;
`answer_guarantee.BUYER_LANES = _derive_buyer_lanes()` → `{'buying','sales'}`.
Литерала рядом с читателем больше нет. При падении импорта — fail-open (судим ВСЁ + громкая жалоба).

**2. Кто будит воркера — заводит лид.** AST-контракт
`tests/test_lane_canon_every_wake_has_a_lead.py` — зеркало dry-контракта:
тот запрещает лид БЕЗ доставки, этот доставку БЕЗ лида. 6 путей проверено.

**3. У каждой полосы судья.** Долги (`buying`,`sales`) — лестница;
тихие (`self`,`social`) — `answer_guarantee.unowned_lanes()`, зовётся из тика
(`answer_guarantee.py:883`), крон `*/5`.

### СКВОЗНАЯ ПРОБА, ВОСПРОИЗВЕДЁННАЯ МНОЙ (изолированный `KOLA_STATE`)

```
1) lane=buying  method=lane_hint  worker=worker2
2) леджер: kind=route | platform=ebay | sender=Verify Seller 23aug | lane=buying | method=lane_hint
3) answer_guarantee.detected_leads видит buying: True
4) delegation_guarantee._worker_for('buying') → worker2
5) СТАРЫЙ рукописный фильтр {buy,procurement,sales} видит: 0
```

Строка 5 — цена дефекта одним числом: тот же самый факт старый сторож не видел ВООБЩЕ.

### КОНТРАКТ НЕ БУМАЖНЫЙ — ПРОВЕРЕНО ФАЛЬСИФИКАЦИЕЙ

Прогнал новый контракт по коду **ДО** правки (worktree на `c8a50ef~1`):
```
❌ FAIL: полоса 'sales' не отвечает на вопрос «долг перед человеком?»
rc=1
```
Ловит. На исправленном коде — `rc=0`.

## ПРАВКА 2 — АБСОЛЮТНЫЕ СРОКИ. ПРОВЕРЕНО ✅

`lib/hold_deadline.py` — ОДИН судья на три источника держания
(парковка 72 ч, отказ гварда 24 ч, исчерпанная лестница +24 ч).
Крон установлен: `37 */6`.

**Живая проба на двух реальных парковках:**
```
18:56:18Z [tick] просрочено 2 (парковка); к докладу 2
18:56:48Z [tick] просрочено 2 (парковка); к докладу 0
```
```
park::fb_sales:irena irena    age_s=2938513  → 34.0 суток
park::fb_sales:Ahmad Samarah  age_s= 523282  →  6.06 суток
```
Ровно две, обе настоящие, доклад ОДИН, повтор не чаще 24 ч. Впервые за 34 суток
держание сказало о себе само.

**Отдельно проверил то, что легко сломать молча:** штамп `told` ставится ТОЛЬКО
внутри `if delivered:` (`hold_deadline.py:283-285`). Неудачная доставка НЕ помечает
долг рассказанным — следующий тик повторит. Иначе у самого сторожа просрочек
завёлся бы факт без срока — ровно тот класс, что он чинит.

**Границу держит:** модуль НИЧЕГО НЕ СНИМАЕТ. Срок вышел — решает человек.

## КОНТРАКТЫ — ЖИВОЙ ПРОГОН

```
test_lane_canon_every_wake_has_a_lead.py     ✅ PASS  rc=0
test_dry_route_no_phantom_lead.py            ✅ PASS  rc=0
test_hold_deadline_every_hold_expires.py     ✅ PASS  rc=0
```

## 🔴 ЧТО Я НАШЁЛ СВОЕЙ ПРОВЕРКОЙ — И ЧЕГО НЕТ В ОТЧЁТЕ ПРЕДЫДУЩЕЙ СЕССИИ

**КОНТРАКТ, ЗАШИВШИЙ ПУТЬ К ДЕРЕВУ, НЕ УМЕЕТ СУДИТЬ ПРАВКУ ДО ЕЁ ПРИЁМА.**
`tests/test_lane_canon_every_wake_has_a_lead.py:39` — `WS = "/home/botuser/.openclaw/workspace"`,
абсолютный путь к ЖИВОМУ дереву.
Мой первый прогон по коду ДО правки дал **PASS** — и это была ЛОЖЬ: тест из worktree
молча импортировал ИСПРАВЛЕННЫЕ модули из живого дерева. Я поймал это только потому,
что ждал падения. Ждал бы зелёного — принял бы вакуумный успех за доказательство.

Класс: **тест, зашивший абсолютный путь, судит НЕ ТО, ЧТО ЕМУ ДАЛИ, а то, что лежит
на диске.** В worktree/CI/кандидатной ветке он зелёный ВСЕГДА, и зелень эта ничего не значит.
Это тот же класс, что «ФИКС НА ДИСКЕ ≠ ФИКС В РАБОТЕ», только с обратным знаком.

## ЧТО ОСТАЁТСЯ ОТКРЫТЫМ (честно)

1. **`lane=buying` живьём всё ещё НОЛЬ.** Живой журнал: 4388 sales / 1427 self / **0 buying**.
   Проба сделана в изолированном состоянии. Первый НАСТОЯЩИЙ закупочный лид — вот
   истинная проверка, и она ещё не наступала.
2. **Судья тихих полос в проде выдал 0 строк.** Молчит ПРАВДИВО (последняя `lane=self` —
   22.08 15:18, вне окна 6 ч), но на настоящей сироте после правки он ещё не работал.
3. **Пять независимых точек НЕ ТРОНУТЫ** этими правками: патруль OfferUp (выключен 17.07),
   `node_notif_watch` (не в кроне, журнал стоит с 14.07), двойная личность,
   разметка истории по геометрии, SIGTERM на пути команды Босса.

## НЕ ТРОНУТО

Титан и Solana — ноль обращений. Живой леджер не изменён (4388/1427/0 до и после моей пробы).
Задачи из очереди техника не подхватывал. Рабочее дерево `/root` чистое.
