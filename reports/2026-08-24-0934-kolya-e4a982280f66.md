# kolya-e4a982280f66

_2026-08-24 09:34 UTC_

---

# e4a982280f66 — репетиция писала в журнал, по которому судят живого покупателя

## Что было видно
Сторож `answer_guarantee` объявил по лиду Desideria техническую стену и приложил
«след воркера». Весь хвост рук в улике (34 строки) состоял из отказов
`[systext-guard] СЛУЖЕБНЫЙ ТЕКСТ В ТРЕД ПОКУПАТЕЛЯ`.

## Что оказалось на самом деле
Эти строки написал **контракт**, а не воркер. Автор назван в самой строке:
`test_system_text_never_reaches_buyer.py:138`. Времена — 09:16:04 и 09:18:31,
то есть **ВНУТРИ окон владения экраном за живого лида** (`fb_read:desideria`, pid=89660).

Почему дверь была открыта. 22.08 журнал рук уже переехал с прибитого гвоздём пути
на `KOLA_STATE` — и герметичные контракты течь перестали. Но контракт **о данных**
законно возвращает `KOLA_STATE` на боевой каталог (строка 30 того файла), чтобы
ЧИТАТЬ живые приказы Босса, — и вместе с правом читать молча получал право **писать**.
Тот же класс, что 488 фальшивых `kind=review` в `rr_audit` (23.08): подмена ПУТИ
не отличает «где моё состояние» от «я репетиция».

## Что починил
* `lib/phone_hands.py`: путь журнала считается **на каждой записи**, а не на импорте,
  и слушает **отдельную метку** `KOLA_CONTRACT=1` (плюс вход из `tests/`) — судья тот же,
  что у `rr_audit._is_rehearsal()`, своего мнения не заводим. Репетиция, целящаяся в
  боевой `phone_hands.log`, уводится в тень `.kola_state/phone_hands.contract.log` —
  и уводится **не молча**: печатает, куда ушла запись.
* `tests/test_system_text_never_reaches_buyer.py`: границу проводит сам протёкший
  контракт — слепок боевого журнала до первой печати, проверка в конце, что он не тронут.

## Проверка на живом
Полный прогон контрактов положил **8** строк `systext-guard` в тень; хвост боевого
журнала остался на живой строке 09:21:51 — репетиция его не тронула.
Контракты: **115/116**.

## Что осталось (заведено отдельно, не в этой истории)
* `a5b2c043a31b` — названная стена «цель «Got it thank you»» относится к треду **Брайана**
  (его вежливое закрытие, ответа не требует), а приписана Desideria: отбор строк смотрит
  ВРЕМЯ окна владения, но не ЦЕЛЬ строки. Остаток класса КОРЕНЬ 2 от 22.08.
* `73ab46157511` — красный контракт `test_escalation_loop_no_boss_courier.py`
  («после спавна задача не взята в работу»). Воспроизводится в одиночку, к этому пути
  отношения не имеет.

Босса ничего не спрашиваю: оба остатка — техника, они уже в очереди.
