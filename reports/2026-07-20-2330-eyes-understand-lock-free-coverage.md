# eyes-understand-lock-free-coverage

_2026-07-20 23:30 UTC · сессия «глаза, которые понимают»_

**Titan pid 72507 / Solana pid 72522 — ЖИВЫ 7д, НЕ ТРОНУТЫ. /root HEAD `6c35b9d` (как до сессии).**
Workspace `df4d0a2..0a5ad96` (4 коммита) запушен в kola-workspace-backup. Пре-пуш PII-скан прошёл.

Работал строго по пунктам: точка → живая проверка → закрытие → следующая.

---

## П.0 — КОНТЕКСТ ПЕРЕЗАГРУЖЕН
11 контрактов capability_audit (крон `0 */4`), ogon_quota на ПРОДУКТ (крон `17 * * * *`),
политика «только новые входящие» (`backlog_cutoff.py`, гвард в orphans), Ирена парк
`boss_only` — НЕ поднимал. Titan/Solana подтверждены живьём.
⚠️ Нашёл 2 коммита ПОСЛЕ отчёта 19:10, которых не было в брифе: `d1e8d91` (ложное самообвинение
замка) и `df4d0a2` (OCR-фолбэк глаз). Сталая сессия успела больше, чем показал отчёт.

## П.1 — ЗАМОК ЭКРАНА: ПРЕМИССА НЕ ПОДТВЕРДИЛАСЬ ✅ ЗАКРЫТ (`f2213d8`)
**Замок БЫЛ СВОБОДЕН** — проверено фактом, не заявлено: `fuser` пусто, записанный pid мёртв,
живой захват 0.00с, полный аудит оставляет замок свободным (`client_pending={}`).
`d1e8d91` уже починил корень (виновник — по факту владения, не по журналу). Проверил ВРАЖДЕБНО:
держателя, намеренно подписанного `who=capability_audit`, аудит назвал ПО ПРОЦЕССУ
(`hold_and_die.py`), не по метке. Убитый SIGKILL держатель → flock снят ядром, метка реап-нута,
замок берётся 0.0с. **Заклинить нечем.**

🔴 **Зато нашёлся РЕАЛЬНЫЙ долг того же класса — и он бил В ГЛАЗА.** Правило Босса «проверка не
имеет права оставить способность хуже, чем нашла» → прошёл все 11 контрактов. `cap_device_storage`
(при ≥95%) и `eyes_health._rung_storage` (БЕЗУСЛОВНО, каждый прогон) делали слепой
`rm -f /sdcard/ui_*.xml` — маска совпадает с тем, что пишет живой воркер
(`messenger_app_send.py:209`). Проверка глаз могла ОСЛЕПИТЬ глаза — тот самый rm-перед-съёмкой,
что ломал их 19.07. Починка ВОЗРАСТОМ (не замком — лестницу зовут из-под замка, вышел бы
дедлок): `lib/capture_purge.py`, кадр старше 5 мин. **Живьём на телефоне поймано 2 дефекта:**
`/sdcard` — СИМЛИНК, toybox find по нему не идёт (пустой список = «нечего чистить» = тихий no-op)
→ `/sdcard/.`; `ls/df` без `su` = Permission denied = тихий ноль. Стенд с assert: старый кадр
удалён, свежий «в полёте» ВЫЖИЛ.

## П.2 — ГЛАЗА, КОТОРЫЕ ПОНИМАЮТ ✅ ЗАКРЫТ (`e1c2c13`, `e946470`, `0a5ad96`)
Корень недели — цель искалась СОВПАДЕНИЕМ ПОДСТРОКИ. `lib/screen_sense.py`: модель смотрит
НАСТОЯЩИЙ кадр + цель словами → что за экран · тот ли · что мешает · что жать ПО СМЫСЛУ.
Текст стал ПОДСКАЗКОЙ. Два правила: координаты НЕ от модели (геометрию дают uidump/OCR, модель
выбирает смысл); FAIL-CLOSED (не понял/уверенность<0.55/не тот экран ⇒ СТОП, не тапаю вслепую).
Появилось: `where_am_i()` (вопрос, которого не было), `reach(goal, expect)` (помеха→закрыть→
заново · не туда→назад→сориентироваться · потолок шагов), 3-я ступень в `look`, снятие помех
в `act`. **Универсально ЧЕРЕЗ МЕХАНИЗМ:** `ph.tap_label/tap_text` понимают сами → все звонящие
(Marketplace/Inbox/Search и т.д.) получили даром, и завтрашний одиннадцатый тоже. `sense=True` —
умолчание. Греп: «найди-строку-или-сдайся» в живых путях не осталось.
🔴 Нашёл: `kola_vision.py` (Groq) МЁРТВ — 404, у Groq не осталось зрячих моделей; понимание на
`claude -p --model claude-opus-4-8`. Гейт коммита сработал на защищённом `phone_hands.py` и
доложил Боссу — исправная работа гейта; прошёл штатным токеном (не `--no-verify`), правка прямо
заказана п.2h.
🔴 **Пойман В ПРОДЕ 22:30:** «Post» совпал с «Schedule post» (иконка планировщика), verify
сказал ✅ по неверной причине, воркер записал «отправлено» — ПОСТ НЕ УШЁЛ. Прошло бы мимо
понимания-фолбэка (текст НАЙДЕН, просто чужой). `e946470`: неточное совпадение переспрашивается
пониманием. `0a5ad96`: `reach` путал ПРИБЫЛИ (on_target=true+stop) и ЗАСТРЯЛИ — успех
возвращался провалом.

## П.3 — ТРИ РЕАЛЬНЫХ СБОЯ
**3a ✅ ЗАКРЫТ (живьём).** @zdravurr «Alex R» (verified) — пост **«1h»** на ЖИВОМ профиле
(~22:05Z), совпал с логом (кнопка Post `[882,322,1042,404]`, vtap ok). Подтверждено МОИМИ
глазами по кадру. Кнопка «не видна» (df4d0a2 OCR-фолбэк) — резолвнута, пост лёг.
**3c ✅ ЗАКРЫТ (живьём).** FB «Your listings» (падал дважды) ДОСТИГНУТ `eyes.reach` за 4 шага,
каждый с where-am-i: FB feed→Marketplace→You-хаб→Your listings. Велик
**«Giant Rainier 26in Mountain Bike, $100 · Available»** — прочитан МОИМИ глазами. ПОСЛЕДНЯЯ
непроверенная клетка матрицы закрыта, `record_eyes(bike, fb_marketplace, live, proof)`.
🔴 launch FB чинился в 3 приёма: работает только через `su` (устройство мульти-юзер) + HOME-first
(Chrome перекрывал monkey — известный фикс 07-04).
**3b ⚠️ ЧАСТИЧНО.** Живой OfferUp-ad НЕ появился (OfferUp открылся сразу в My Listings — велик
там тоже Active). Механизм снятия помех ПОСТРОЕН и доказан на реальном оверлее (FB-карточка:
модель выбрала X, а НЕ Send — «Send пишет продавцу, не закрывает карточку»; ремап
dismiss_index→бокс крестика работает). Точный OfferUp-ad не воспроизведён по требованию.

## П.4 — КАК УМОЛЧАНИЕ ✅ ЗАКРЫТ (живьём)
Тот же код (screen_sense/reach), без спец-кейсов, водил ОБЫЧНЫЕ задачи: навигация Chrome
(омнибокс → ввод URL → выбор автоподсказки → профиль → скролл) и FB-навигация. **Where-am-i
спас от ошибки:** `am start` не сменил SPA, Chrome остался на чужом @themetashark — модель
ОТКАЗАЛАСЬ читать чужую ленту как @zdravurr и сказала «назад». Это капабилити, не три заплаты.

## П.5 — ПОЛНЫЙ АУДИТ ✅ ЗАКРЫТ
**10/11 контрактов ✅, один 🔴 = `warmup_output` (1/2).**
Классификация: **ОЖИДАЕМО, не долг, не сломанная проверка.** Дефицит = 1 ЗАКОННЫЙ dedup-скип
(near-duplicate theme, анти-повтор сработал верно) + 1 реальный пост (лёг, «1h» на профиле).
Квота=1 совпала с профилем ⇒ mis-target НЕ раздул счёт. Кандидат на будущее — считать
dedup-скип «законным нулём» (класс «отказ гварда ≠ провал»); квоту (благословение Босса) без
решения не трогал.
Мои фиксы п.1 зелены: замок «берётся 0.0с», место «23%» (purge чист), прицел «отработал 12×».
Здоровье: gateway/mail 10мин, push hb 0мин, Dirigent poll жив, inbox lane 2д, отчёты msg 1393.
**Titan 72507 / Solana 72522 — 7д, НЕ ТРОНУТЫ. git чист, запушен.**

---

## ЧЕСТНО НЕ ЗАКРЫТО ЖИВЬЁМ
1. `e946470` (Post vs Schedule post) на НАСТОЯЩЕМ композере X — нет кадра композера, нужен слот;
   следующий плановый прогрев закроет.
2. Живое снятие OfferUp-ad end-to-end — ad не появился (доказано на эквивалентном реальном оверлее).
3. `?? listing_check` — стрей-JPEG (19:02, ДО сессии) в корне репо; не мой, помечен, не удалён
   (не удаляю чужой артефакт вслепую).

**Итог:** 0,1,2,3a,3c,4,5 — ЗАКРЫТЫ живьём. 3b — механизм закрыт, точный ad не воспроизведён.
