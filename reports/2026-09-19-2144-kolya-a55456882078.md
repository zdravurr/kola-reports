# kolya-a55456882078

_2026-09-19 21:44 UTC_

---

# Само-разбор 19.09 (24ч): зелёная галочка стояла над открытой поломкой

**Задача** a55456882078 [infra] — 🧠 САМООБУЧЕНИЕ 19.09 09:00 UTC. ЗАКРЫТА.

## Что нашёл

Корень — не в охвате, а в **приборе, которым я тебе охват показываю**.

Сторож охвата X (`x_views_goal_watch.check`) даёт **четыре** исхода:
`ok` / `stalled` / `measuring` (просадка есть, но новых D1-замеров с прошлой тревоги
не набралось — судить рано) / `lawful` (просадка законна).

Знак в суточной сводке считался **двумя** ветками:

    mark = "🔴" if verdict == "stalled" else "✅"

Значит `measuring` и `lawful` — состояния, в которых поломка **ОТКРЫТА и не гасится**
(`fault_realert.clear` зовётся только на `ok`), — рисовались тебе зелёным «охват держится».

**Замер по сегодняшней сводке 09:00 UTC:**
- сводка: `✅ @zdravurr: D1-охват 8 просм./пост (+21% к базе 7, n=6)`
- очередь: задача `x_views_stalled_zdravurr` открыта с 28.08 — «8 просм./пост **ниже пола 10**».

Один прогон, одно и то же число, два противоположных вердикта. Прибор врал в пользу здоровья:
дельта к собственной базе (+21%) закрывала абсолютный пол (8 < 10). Ровно тот класс, за который
уже платили: молчание/держание замера читалось как здоровье.

## Что починил

Знак берётся у вердикта, а не у одного его значения:

| вердикт | знак | что пишет |
|---|---|---|
| ok | ✅ | охват держится |
| stalled | 🔴 | просадка |
| measuring | ⏳ | **поломка ОТКРЫТА**, ждём N новых замеров из M |
| lawful | ⏸ | **поломка ОТКРЫТА**, просадка законна |
| незнакомый | ❔ | вердикт назван, а не проглочен молча |

Файл: `projects/social_x/x_views_goal_watch.py` (`summary_lines`).

## Чем заперто

Новый контракт `tests/test_views_summary_mark_follows_the_verdict.py` — 5 проверок:
✅ только над `ok`; `measuring`/`lawful` не зелёные и прямо называют открытую поломку;
`stalled` остаётся 🔴; незнакомый вердикт не проходит как здоровье.

Набор целиком: **328/328 зелёных** (50 с).

## Что осталось (не моя дверь, это твоё решение)

Сама просадка охвата остаётся фактом: `@zdravurr_ca` — 7 просм./пост при поле 10
(n=32 за 7 сут), задача по ней открыта и держится корректно. Это вопрос **тактики постов**,
а не замера — сторож теперь честно показывает, что она открыта, а не машет галочкой.
