# 26.08 02:25 UTC — ТЕЛЕФОН НЕ В MESSENGER (СТОЯЛ, НЕ СКРОЛЛИЛ) + КОРЕНЬ ТРОЙНОГО ЛОЖНОГО РАСПАРКОВЫВАНИЯ ЗАКРЫТ

_2026-08-26 02:25 UTC · сессия техника · **Титан/Solana не тронуты**_

---

## 0. КОНТЕКСТ ПРИНЯТ

124/124 контракта были зелёными. Сторож исходящих стоит на всех 4 площадках. Прогрев Огня
стартует с чистого экрана и останавливается на чужом. George закрыт, действий не требует.
Waseem/Heather/Anita — фантомы, оставлены запаркованными правильно. Титан/Solana не трогались.

---

# 🔴 СРОЧНОЕ — ФАКТЫ ПРОТИВ ТОГО, ЧТО ВИДНО

**Телефон НЕ был в Facebook Messenger и НЕ скроллил треды покупателей. Он СТОЯЛ.**

### 1. Что на экране (снимок 02:01:40 UTC)

| замер | значение |
|---|---|
| `mCurrentFocus` | `com.android.chrome/…chrome.Main` |
| `topResumedActivity` | тот же Chrome, задача `t2898` |
| адресная строка | **`x.com/NYAutoMuseum`** |
| содержимое | профиль New York Auto Museum, пост про Pontiac Trans Am |
| экран | `mWakefulness=Awake`, батарея заряжается |

Messenger (`com.facebook.orca`) лежал в Recents **#3**, не на переднем плане. Больше того:
его `baseIntent` = `…orca/.auth.StartScreenActivity`, а у `com.facebook.katana` —
`.LoginActivity`. Это **экраны входа**, а не список переписок с покупателями.

### 2. Он не двигался — это доказано, а не предположено

Три снимка подряд, 02:03:08 / 02:03:20 / 02:03:32:

```
d9a9debd188927e0ba5dcb895eaa4866  shot_1.png
d9a9debd188927e0ba5dcb895eaa4866  shot_2.png
d9a9debd188927e0ba5dcb895eaa4866  shot_3.png
```

**Байт в байт одинаковые 36 секунд.** Скролла нет. Ни одного пикселя.

### 3. Какой процесс это делал — НИКАКОЙ

Живых процессов, работающих с экраном, на момент проверки **ноль**. `screen_priority.log`
показывает последнего владельца и его уход:

```
02:03:01 MARK    who=craigslist:peek kind=hold ttl=900 note=pid=1434804
02:03:01 ACQUIRE who=craigslist:peek waited=0.0 note=cl_chat_peek
02:03:23 RELEASE who=craigslist:peek held=21.8
02:03:23 CLEAR   who=craigslist:peek existed=True
```

`craigslist:peek` брал экран на 21.8 с ровно в окно моих снимков — и **кадр не изменился**,
то есть он работает через CDP, а не руками по экрану. Никто телефон не трогал.

### 4. Откуда взялся экран X

Это **остаток прогрева `@zdravurr_ca`**, слот 23:30 UTC (`Session start 23:33`). Сессия
отработала, опубликовала пост, и её лог обрывается в 23:57 — при `timeout 1800` от 23:30.
Chrome остался на последнем открытом профиле X. С тех пор — **2 часа неподвижности**.

### 5. Что сделано

Экран **возвращён на HOME** (02:05:25), подтверждено фактом:

```
mCurrentFocus=Window{83c3537 u0 com.google.android.apps.nexuslauncher/…NexusLauncherActivity}
```

Скриншот HOME снят, хэш отличается от прежнего кадра. Замок экрана в этот момент никем не
удерживался (последний `CLEAR` — 02:03:23, следующий плановый `craigslist:peek` — 02:33),
поэтому чужую сессию я не прерывал.

> **Итог по срочному: остановленной работы не было — нечего было останавливать.**
> Не «застрял в цикле» и не «в середине задачи»: **простой на брошенном экране**.

---

# ЗАДАЧА 7987a4fb532c — КОРЕНЬ НАЙДЕН И ЗАКРЫТ

## Улика: три роута ОДНОЙ СЕКУНДОЙ, 25.08 09:45:16–17

Дословно из лога свипа:

| кто | строка инбокса | чем это является |
|---|---|---|
| **Waseem** | `…, You can now rate each other` | служебная плашка площадки |
| **Anita** | `…, Anita reacted &#128077; to your message` | реакция эмодзи ФРАЗОЙ |
| **George** | `…, You: Not logged in · Please run /login, Seen by George Allen` | **наш собственный** текст |

Проверено независимо сейчас (26.08 02:14):

```
Waseem    message_text=''
Heather   message_text=''
Anita     message_text=''
```

**Входящего текста не существует ни у одного.**

## Корень — НЕ там, где кажется

Соблазнительный ответ — «`classify` слишком широк, сузить». **Это было бы регрессом,
который уже стоил сделки.** `classify` широк НАМЕРЕННО: плашка FB **перекрывает превью
последнего сообщения**, и ровно так 12.07 потеряли Raghavender — под
`You can now rate each other` лежало «Hey, Can I check this out today?». В файле это
записано явным запретом.

Настоящий корень: **одно слово `WAITING` читали ДВА потребителя как ДВА РАЗНЫХ утверждения.**

| потребитель | что он понимает под `WAITING` | сила утверждения |
|---|---|---|
| путь тревоги / чтения | «стоит ОТКРЫТЬ и прочитать» | слабое — и правильно |
| `drop_parked_unchanged` | «последним говорил **ПОКУПАТЕЛЬ**» | **сильное — и неправда** |

Второе записано в собственном комментарии гейта распарковки. Плашка, реакция и наш
собственный текст удовлетворяют первому утверждению и **не удовлетворяют второму** — а
гейт действовал по вердикту, который никогда не был доказательством входящего.

**Класс — наш давно названный: «ОДИН ФАКТ, НЕСКОЛЬКО СУДЕЙ» + «число едет со своим
ПРОИСХОЖДЕНИЕМ».** Вердикт ехал БЕЗ него.

## Лечение: не сужать вердикт, а заставить его везти происхождение

- `tail_evidence()` — чем именно строка доказывает вход:
  `unread | buyer_text | reaction | banner | nudge | ours | unknown`.
  Формы собраны **не из головы, а из 70 РЕАЛЬНЫХ строк** инбокса за 25–26.08.
- `INBOUND_EVIDENCE = ("unread", "buyer_text")` — только это настоящий вход покупателя.
- `classify_ex()` отдаёт вердикт **вместе с доказательством**; `classify()` не тронут.
- Гейт распарковки на первом взгляде **требует доказательства входа**, иначе только
  запоминает строку → `[parked-noevidence]`.
- Реакция **фразой** приравнена к реакции **иконкой** — дверь 22.08 (Марио) расширена,
  а не продублирована второй.

## Контракт: `tests/test_only_buyer_text_unparks.py`

8 пунктов на **дословных** строках из лога 25.08:

```
[1] три фантомные строки: входящего покупателя в них НЕТ ✅
[2] речь покупателя и бейдж непрочитанного проходят как вход ✅
[3] бейдж непрочитанного сильнее любой плашки ✅
[4] НЕ РЕГРЕСС: под плашкой по-прежнему идём ЧИТАТЬ тред ✅   ← Raghavender
[5] реакция фразой = реакция иконкой, обе не сообщения ✅
[6] пакет 25.08 парковку НЕ снимает, живой покупатель — снимает ✅
[7] НЕ РЕГРЕСС: изменение строки по-прежнему уводит тред Сене ✅ ← Turq/Seymour
[8] гейт трогает только ЗАПАРКОВАННЫЕ треды ✅
```

Прогон на сегодняшнем пакете печатает ровно то, чего не хватало:

```
[parked-noevidence] Waseem — доказательство=banner   → распарковку не открываю
[parked-noevidence] Anita  — доказательство=reaction → распарковку не открываю
[parked-noevidence] George — доказательство=ours     → распарковку не открываю
[parked-new]        Mario  — последним говорит ПОКУПАТЕЛЬ: роутю
```

**Полный прогон: 125/125 зелёных** (124 прежних + новый контракт).

Коммит **`ec5f0dd`**. Гейт коммитов на money-path пройден **токеном** — правку в этот
файл заказал ты задачей 7987a4fb532c; след авторизованного прохода записан в
`.kola_state/commit_gate_pass.log` (`AUTHORISED  projects/sales/fb_inbox_sweep.py`).

## 🔴 ЧЕСТНАЯ ГРАНИЦА: HEATHER ЭТИМ НЕ ЗАКРЫВАЕТСЯ

Из троих механическую починку получают **Waseem и Anita** (плюс George — тот же класс).
**Heather — нет, и я не стал делать вид, что да.**

Её строка — `Heather: Aww thank you!! we picked up a specialized bike today for 100 bucks
so my son is stoked!` — это **настоящий текст настоящего человека**. По твоему же
инварианту («новым входящим считается только реальный ТЕКСТ покупателя») она проходит
законно. Фантом она **по смыслу** (тёплое прощание, велик куплен в другом месте), а не по
форме. Механический судья формы её отличить не может и не должен.

`lead_router.is_leadworthy` её тоже пропускает: он ловит короткие вежливые концовки
(«got it thank you»), а у Heather — содержательная фраза.

**Закрывать её «фантомом» я не стал сознательно:** запись в `lead_phantom` матчится по
имени **навсегда и без срока** — это ослепило бы сторожа на реальную Heather, если она
напишет снова. Это ровно закон «у ФАКТА не было СРОКА». Решение — твоё.

## 🔴 ЖИВОЕ, ПРЯМО СЕЙЧАС: ЛОЖНАЯ ТРЕВОГА ВСЁ ЕЩЁ БЬЁТ

Починка закрывает **новые** ложные распарковки. Три **уже заведённых** счёта живы:

```
02:05:05 [tick] НАРУШЕНИЙ 3 (ступень уже отправлена) :: ['Waseem', 'Heather', 'Anita']
```

Бьёт **каждые 5 минут**, непрерывно с 25.08 — на момент отчёта это **~16.5 часов**, а окно
сторожа `LOOKBACK_S` = **14 суток**, то есть само оно погаснет только 08.09.
Это ровно тот фон, который учит не смотреть на тревоги.

Тихо гасить их я не стал: единственные инструменты — либо пометка «фантом» без срока
(см. выше), либо `reply_proof.RESOLVED`, который с 23.08 **намеренно** не считается
ответом покупателю. Оба меняют показания сторожа по живым именам — это твоё решение,
а не моё молчаливое.

---

# ПРОГРЕВ @zdravurr_ca — ОТВЕТ НА ВОПРОС

**Да, следующий слот отработал. Да, пост он выдал. Нет, недостачу он не догнал.**

| слот 25.08 UTC | исход |
|---|---|
| 13:00 | **упал** — `screen_priority.Preempted: полоса 'warmup' вытеснена: клиент ждёт экран (fb_inbox:look)`, ровно на тапе публикации |
| 20:00 | **упал** — `RuntimeError: Cannot get @zdravurr_ca tab: timed out` (CDP-мост) |
| **23:30** | ✅ **ПОСТ ЕСТЬ** — «Cybertruck's new-truck moves are dragging used comps down…», картинка `ev_2026-08-25_233355.png`, тап `Post` подтверждён ГЛАЗАМИ 23:34:57 (`[eyes] 'Post': ✅ ФАКТ подтвердил (ok)`) |

Ледджер `zdravurr_ca_log.jsonl` (независимая от лога запись) по дням:

```
2026-08-22  4    2026-08-24  2
2026-08-23  1    2026-08-25  1   ← пост от слота 23:30
```

Итог по 25.08: **1 пост из 3 слотов**. Красный `capability_audit` («@zdravurr_ca 0 из 1»)
снят слотом 23:30 — но сам аудит с **17:12 не перезапускался**, поэтому в его файле всё ещё
висит старая красная строка.

**Попутно замечено (не чинил — вне задачи):** `warmup_health.json` считает `last_ok` по
**завершению сессии**, а не по **факту поста**. Сессия 23:30 пост выдала, но упала в конце,
поэтому здоровье до сих пор думает, что `zdravurr_ca` последний раз был в порядке 21.08
(`broken_streak: 1`). Судья меряет не то, что оплачивает результат.

Слоты `@zdravurr_ca` на сегодня — 13:00 / 20:00 / 23:30 UTC, ни один ещё не наступил.

---

## ИТОГ

- **Срочное:** телефон был **в Chrome на x.com**, не в Messenger; **стоял неподвижно 36 с
  по хэшам**, ни один процесс его не вёл. Это остаток прогрева 23:30. Вернул на **HOME**.
- **7987a4fb532c закрыта:** корень — вердикт без происхождения, а не «широкий классификатор».
  Починено так, что защита Raghavender и Turq/Seymour цела. **125/125**, коммит `ec5f0dd`.
- **Честно не закрыто:** Heather (реальный текст — вне класса) и три **уже висящих**
  ложных счёта, которые бьют каждые 5 минут до 08.09. Оба — за твоим решением.
- **Прогрев:** слот 23:30 отработал и пост выдал; день закрыт **1 постом из 3**.
- **Титан/Solana не тронуты.**
