# Три приказа исполнены. Но по ходу МОЙ КОД отправил продавцу Tesla оффер, которого ты запретил

**17.08.2026, 18:30 UTC.** Все четыре пункта закрыты. **Титан / Solana — НЕ ТРОНУТЫ**
(pid Титана те же; SOL — те же, что после чужого рестарта в 14:58).

Начинаю с плохого, потому что оно важнее хорошего.

---

## 1. 🔴 ЧТО Я СЛОМАЛ: ОТПРАВЛЕННЫЙ ОФФЕР $8500

Ты сказал прямо: **«Ahmad specifically: do NOT reply — the Boss will decide on the Tesla
himself».** В **18:05:58** Боря отправил продавцу:

> «Hi! Thanks — would you take **$8500** for it? I can do a quick, easy pickup if that…»

по машине за $9,999. **Денег не двигали, обязательств не брали — но приказ нарушен, и
нарушен МОИМ кодом.** Как именно:

1. Я поставил стоп-защиту — запарковал тред ключом **`fb_sales:Ahmad Samarah`**.
2. Дирижёр спрашивает **`is_parked('fb_messenger', 'ahmad samarah')`**. Площадка написана
   ИНАЧЕ, `_pk` её не нормализовал → сравнение промахнулось **МОЛЧА**. Парковка «стояла» и
   НЕ ЗАЩИЩАЛА. Я проверил её СВОИМ написанием, получил `True` и успокоился.
3. В ту же минуту заработала ДРУГАЯ моя правка — **единый интейк**, который ВПЕРВЫЕ сделал
   этот лид видимым Дирижёру. Тред законно уехал в закупку → Боря → оффер.

🔴 **И `review_gate` Борю НЕ останавливал — правильно не останавливал:** закупочный оффер для
него законен. **Единственным барьером была парковка**, и она промахнулась.

**Класс — уже известный, но не вычищенный до конца.** `crm.key` у себя это описал и починил:
«звали площадку ПЯТЬЮ способами — fb_sales, fb, facebook — и путь бил в пустоту». Тот же
промах жил в гварде парковки, и цена вышла выше: не пустая карточка, а **сообщение живому
человеку**. Гвард, который можно промахнуть написанием площадки, — не гвард.

**Мой вывод, который дороже самого фикса:** правка, снимающая СЛЕПОТУ автоматики, опаснее
правки, меняющей поведение. Сначала убедись, что ТОРМОЗ ДЕРЖИТ, и только потом делай объект
видимым. Я сделал наоборот.

### Ahmad теперь закрыт ТРОЙНОЙ защитой (одной мало — это и доказано)
| барьер | что делает |
|---|---|
| парковка, **оба** ключа, `unpark_on='boss_decision'` | Дирижёр тред не открывает; новое входящее НЕ снимает |
| приказ в `boss_rulings` (`kind=not_ours`, твоя цитата дословно) | Дирижёр печатает `[ruled]` и проходит мимо |
| → следствие: `review_gate.thread_not_ruled_out=False` | **ЛЮБОЙ** ответ отказан, включая `procurement=True` |

Проверено: `passed=False` в обоих случаях. В очереди Дирижёра Ahmad больше не появляется.
**Новых отправок нет — то сообщение 18:05:58 осталось единственным.** Решение по Tesla за тобой.

---

## 2. ✅ ПРИКАЗ 1 — `lib/screen_hold.py` ЗАКОММИЧЕН

Токеном, как ты разрешил. Правка (срок захвата 90 мин) в `b6963b9`, след
`AUTHORISED` в `commit_gate_pass.log`. Всё запушено: `dcc42b3 → 2d09007`.

## 3. ✅ ПРИКАЗ 2 — OMAR: ПРИКАЗ ТЕПЕРЬ ЧИТАЕТСЯ

`item_records.boss_not_ours` умела читать твои решения **с 15.07** — интейк её просто НЕ
СПРАШИВАЛ. Теперь спрашивает **ПЕРВЫМ**, до всех ключевых слов, и в Дирижёре — **ДО
предохранителя** (зубы кусают невыполнение, а не гвард).

**Живой тик 18:26:58:**
```
[ruled] Omar: РЕШЕНО БОССОМ — приказ Босса (boss voice 2026-08-15T09:58:31Z):
        Omar · dl360 — ЗАКУПКА, мы покупатель; тред не открываю
[ruled] Omar: … Omar · rtx 5090 …; тред не открываю
```
Двое суток ложной эскалации «человеку, который не придёт» — кончились.

🔴 **Отдельно пришлось решить неочевидное.** На интейке **листинг часто НЕИЗВЕСТЕН** (push
несёт только имя), а `boss_not_ours('omar','')` возвращала **False** — приказ проходил мимо.
Добавил суд по ИМЕНИ, но с жёстким тормозом: судить по имени можно **только когда спорить не
с чем**. Есть хоть один приказ «наше» — отказ. Известен хоть один тред этого имени, приказом
НЕ закрытый — отказ. Это спасло **Michelle** (la-z-boy закупка **vs велик продажа**) и
**Robert** (async закупка **vs Giant Rainier продажа**) от глушения по имени. Ошибаться тут
обязано в сторону «не глушить».

## 4. ✅ ПРИКАЗ 3 — РОЛЬ РЕШАЕТ ПОЛОСУ, ДВА ИНТЕЙКА СВЕДЕНЫ

**(а) Полосу решает РОЛЬ, а не площадка.** Корень был в правиле «нет ключевых слов +
площадка продажная ⇒ входящее = ПОКУПАТЕЛЬ». Верное обычно и ложное ровно тогда, когда
первыми писали МЫ. «are you interested» — слово ПРОДАЖИ по словарю и слово ПРОДАВЦА по смыслу.
Теперь роль читается в ЕДИНОМ чокпойнте всех детекторов: **seller → Боря, buyer → Сеня.**

**(б) Два интейка сведены.** Дирижёр брал лидов из `fb_waiting.json`, push-демон писал в
`commitments.json` — Ahmad **не видел ни один разбирающий механизм** три четверти часа.
Живой тик: `[intake] досыпано из ledger'а (push-путь, свип их не видел): ahmad samarah`.

**(в) Продажный ответ продавцу запрещён на границе отправки.** `review_gate` теперь САМ
определяет, кто мы в треде, и блокирует расхождение. **Флагу `procurement` от вызывающего
веры нет** — ошибается именно тот, кто ошибся полосой (тот же принцип, что «не доверять
своему же ключу»). Прежние гейты это НЕ ловили: `GONE_REPLY` «sorry, it's not available» не
называет ни цены, ни города, поэтому фактчек молчал.

### 🔴 Что вскрылось попутно и было бы хуже, чем исходный баг
**Аудит 57 карточек `role='seller'` сразу после включения гейта.** 55 — реально машины
проекта cars_ev, роль верна. Но у **George** стоит `role='seller'` при
`item='Giant Rainier 26in Mountain Bike'` — **НАШЕМ велике**. Слепо поверив полю, новый гейт
увёл бы ЖИВОГО ПОКУПАТЕЛЯ нашего велика в закупку — то есть **сам стал бы новым способом
терять покупателей**. Теперь **канон товара сильнее устаревшей роли**: `role=seller` +
канон уверенно говорит «наш товар» ⇒ считаем ПОКУПАТЕЛЕМ. На `uncertain` не гадаем.

Ещё два, найденных при исполнении:
- **`unpark_on` ЗАПИСЫВАЛСЯ, но не читался НИКЕМ** — парковку «до решения Босса» снял бы
  первый же новый DM. Теперь `unpark_lane` знает триггер.
- **`crm.card_path` вырезал всё, кроме `[a-z0-9]`** — ЛЮБЫЕ два кириллических имени давали
  ОДИН файл `crm/fb_messenger.json`. Раз полосу теперь решает `role` из карточки, общая
  карточка = роль одного человека в треде другого. Для латиницы путь **байт-в-байт прежний**
  (сверено на всех 138 живых карточках), ущерба не было — ловушка была заряжена.

---

## 5. ✅ ПРИКАЗ 4 — ПУТЬ ПУБЛИКАЦИИ ПРОГНАН ЦЕЛИКОМ. СТЕНА НАЙДЕНА И СНЕСЕНА

Ты просил не ждать, пока упрёшься. Стена была — и **не там, где ожидалось: публикация
работала, разваливалось ПОСЛЕ неё.**

🔴 **`senya_sell.create` писал ТОЛЬКО задачу.** Вещь оставалась канону `item_records`
НЕ ИЗВЕСТНА, а канон определён **В КОДЕ** — рантайм-пути завести вещь **НЕ СУЩЕСТВОВАЛО
ВООБЩЕ**. Доказано стендом:

> `review_gate` → `item_identified: False`,
> «reply states item facts but the item could not be identified from CRM — **fail-closed**»

То есть: **объявление живёт, покупатели пишут, ответить им НЕЛЬЗЯ.** Плюс `listing_coverage`
строит матрицу из канона → новой вещи в покрытии не было бы вообще, а ни `listing_alias`, ни
`listing_coverage` после публикации не обновлялись.

### Звенья — по одному, как ты просил
| звено | было | стало |
|---|---|---|
| команда → интент | ✅ работало | ✅ |
| **имя вещи** | 🔴 «Dyson V8» → **«Dyson V»**: регексп цены вырезал ЛЮБУЮ цифру | ✅ цена срезается только там, где она цена; V8 / A1-PRO / EL-CAC60 / RTX 5090 целы |
| «sell this for $45» | 🔴 товар с именем **«for»**, дефолт `or "вещь"` | ✅ один вопрос «что продаём?» |
| **вещь в канон** | 🔴 **пути не было** | ✅ `add_item()` + `items_dynamic.json`, мержится в `_load()` |
| фото Telegram → телефон | ✅ | ✅ **проверено ЖИВЬЁМ**: `adb push` → MediaStore индексирует СРАЗУ (гонки нет) |
| сколько фото | 🔴 хардкод **2** — ты присылаешь три, публиковались два, молча | ✅ сколько прислал, столько и цепляем |
| детекция формы FB | ✅ гибрид uidump+ЗРЕНИЕ (тот баг, что 2 недели парковал sofa/desk/мультиварку, уже починен 21.07) | ✅ |
| **реестр из улик** | 🔴 не трогался вообще | ✅ `listing_alias` + `listing_coverage`, **требуя КАДР публикации** |
| вердикт | — | ✅ **`draft`, НЕ `live`**: `ok=True` от постера = «форма отправлена», а FB держит новое объявление «In Review» |
| **ответ покупателю** | 🔴 **МОЛЧАНИЕ** | ✅ отказа нет, `facts_match_item=True` (цена и город сверены с каноном) |
| Craigslist | 🔴 ack обещал, кода НЕТ | ✅ ack честен |

**Про Craigslist отдельно:** ack обещал «FB Marketplace **(+ Craigslist)**», хотя
`post_to_marketplace` умеет ТОЛЬКО FB, а обещанный `scripts/cl_post_giveaway.py` существует
лишь как **осиротевший `.pyc` — исходника НЕТ**. Обещать было нечем. Теперь ack обещает FB и
честно говорит: **на CL автопостинга нет, скажи — выложу отдельным заходом руками** (по
рецепту, которым 17.08 легли шесть объявлений). Закон «СКАЗАТЬ = СДЕЛАТЬ».

⚠️ **Почему отдельный файл `items_dynamic.json`, а не `items.json`:** `sync()` ПЕРЕЗАПИСЫВАЕТ
`items.json` из кода. Вещь, дописанная туда, исчезла бы при первом же `sync()` — молча и вместе
с историей покупателя. Проверено контрактом: `sync()` динамику не убивает.

⚠️ **Алиасы — по ФОРМЕ слова, а не по словарю.** Первая версия браковала родовые слова списком
и пропустила «пылесос»: список НИКОГДА не будет полным, а каждая дырка = чужой тред, опознанный
как наш. Теперь отличительно то, что имеет цифру, написано с заглавной или идёт парой слов.
Одинокое строчное существительное алиасом не станет.

---

## 6. ЧТО ПРОВЕРЕНО

| проверка | итог |
|---|---|
| Контракты | ✅ **37 / 37 зелёных** (добавлены `test_intake_reads_role_and_ruling.py`, `test_sell_path_ready.py`) |
| Живой тик Дирижёра | ✅ `[ruled] Omar` ×2, `[intake] досыпано из ledger'а`, Ahmad не в очереди |
| Регрессы | ✅ роль неизвестна → прежнее поведение; латинские CRM-пути байт-в-байт; обычному покупателю ничего не сломано; Боря по-прежнему может торговаться |
| Живой канон | ✅ не тронут тестами (`items_dynamic.json` не создан, тестовые карточки убраны) |
| git | ✅ `b6963b9`, `0854dee`, `2d09007` — запушены, дерево чистое |
| **Титан** | ✅ pid не менялись: 2538048, 2538053, 3282345, 3647921 |
| **Solana** | ✅ pid те же, что после ЧУЖОГО рестарта 14:58:30 (не мой) |

---

## 7. 🔴 ЧЕСТНЫЙ ОТКРЫТЫЙ СПИСОК

1. 🔴 **Оффер $8500 продавцу Tesla ОТПРАВЛЕН и не отозван.** Отзывать я не стал: ещё одно
   сообщение — это снова решение о машине. **Решай ты.** Если скажешь — напишу ему что угодно
   (отзыв, другая цена, отказ) или не напишу ничего.
2. **Мультиварка на FB — всё ещё «In Review».** С 15:51 плашка не проверялась: нужен заход
   глазами. В реестре честно `draft`.
3. **Алиасы новой вещи генерируются автоматически и бывают ШИРЕ ручных.** Для «Giant Rainier
   26in Mountain Bike» получается и `mountain bike` — такое может зацепить чужой тред. Для
   вещей, где это важно, алиасы стоит вычитать глазами; ручной канон (`_BUILTIN`) по-прежнему
   сильнее динамики.
4. **Название из одного родового слова неисправимо широко.** «продай самокат за 90» даст
   алиас `самокат` — отличительного там просто нет. Называй вещь с брендом/моделью.
5. **`select_photos` берёт ПЕРВЫЕ N миниатюр, а не наши файлы по имени.** Свежий push —
   новейший в галерее, поэтому обычно попадает верно, но гарантии по имени НЕТ.
6. **Michelle: `role='seller'` при `item='giant'`.** Канон по «giant» даёт `uncertain`, роль я
   НЕ переписываю (гадать нельзя) — значит её тред уедет в закупку к Боре. Если это покупатель
   велика, скажи, и я поправлю карточку.
7. **Craigslist для новой вещи — руками.** Автопостинга нет (исходник удалён).
8. **`item_records` остаётся каноном ИЗ КОДА.** Динамика живёт рядом; при конфликте id
   выигрывает КОД. Вещь, заведённую на ходу, стоит потом перенести в `_BUILTIN` руками.
9. **Полоса procurement теперь РАБОТАЕТ** (Боря отправил оффер — механизм жив), но именно это
   и стоит помнить: она **автономно торгуется**. Пределы её автономии заданы только парковками
   и приказами `boss_rulings`.
10. **Твои три сообщения не прочитались.** Пришли как `гююи же б`, `б ) тот эжж б`, `б ю` —
    похоже на случайные нажатия. Если там было что-то важное, повтори: я на них не реагировал.

---

**Итог:** три приказа исполнены и доказаны живьём, путь публикации доведён до
ОТВЕЧЕННОГО покупателя (стена была на записи в канон — её больше нет). Но по ходу мой
код отправил продавцу Tesla оффер, которого ты запретил: барьер был один, и он
промахнулся написанием площадки. Теперь барьеров три, и они проверены. Титан и Solana
я не трогал.
