# fb_inbox_sweep остановлен НАМЕРЕННО + пинг живости подтверждён замером: 0.12 МЛН/ч

**29.08.2026, 19:20 UTC.**

## 1. fb_inbox_sweep — НЕ сломан. Остановлен намеренно 26.08

В crontab строка **есть**, но снята с пометкой:

```
#STOPPED26AUG_EVENTDRIVEN 12,42 * * * * … projects/sales/fb_inbox_sweep.py
```

Это та самая партия из десяти строк опроса, снятых 26.08 при **переходе на событийность**.
Молчание ровно 3.3 суток = ровно с 26.08. Совпадение дат — не совпадение.

Рядом в кроне стоит **прямая пометка, запрещающая его трогать**:

```
⚠️ FB-патруль (fb_inbox_sweep) НЕ ТРОГАТЬ: FB прислал 0 писем о Маркетплейсе за 10 мес.
⚠️ ВЕРНУТЬ: файл projects/sales/fb_inbox_sweep.py НА МЕСТЕ, ничего не удалено —
   строка возвращается расскомментированием.
```

Записанная причина снятия: патруль **дублировал** push-детекцию и **дрался за экран** с
прогревом и продажами. И остаточный риск был **назван и ПРИНЯТ Боссом**: «FB перезаписывает
уведомление треда плашкой (кейс Raghavender). Буфера-правды (экрана) больше нет; ловим то,
что пуш донёс.»

### 🔴 Поэтому я его НЕ перезапускаю

Пункт 2 задания сработал бы только «если он реально мёртв, а не по приказу Босса». Он —
**по приказу**: снят осознанно, с записанной причиной, с принятым риском и с явным
«НЕ ТРОГАТЬ» в том же файле. Вернуть его сейчас значило бы молча отменить архитектурное
решение и вернуть драку за экран, которую 26.08 убирали намеренно.

### Что я проверил вместо этого — жива ли ЗАМЕНА

Настоящий вопрос за «не сломан ли» — работает ли то, что его заменило. Проверено живьём:

| механизм | состояние |
|---|---|
| `inbox_watcher` (push-детекция FB) | ✅ ЖИВ, heartbeat обновлён **0.2 мин назад** |
| `mail_idle_watch` (IMAP IDLE, OfferUp/CL) | ✅ ЖИВ |

Замена на месте и дышит. Дыра, если она есть, — не в остановленном патруле, а в лоссёвости
пуша, и она уже названа и принята.

## 2. Пинг живости — ЗАМЕРЕНО, прогноз подтверждён

Окно **18:15 → 19:15 UTC**, ровно час, я в это время машину не трогал.

> ### liveness-ping: **0.12 МЛН за час** — 2 пинга
> Прогноз был 0.12. Совпало в точности.

Сравнение «яблоки к яблокам» — то же окно длиной в час, та же частота, то же число пингов:

| | пингов за час | цена пинга | итог за час |
|---|---|---|---|
| **до** (тихий час 14:10–15:10) | 2 | ~95К | **0.19 МЛН/ч** |
| **после** (18:15–19:15) | 2 | **60К** | **0.12 МЛН/ч** |

**−37 %, и цена пинга больше не растёт** — каждый пинг это новая сессия.

### ⚠️ Честно: час НЕ был стерильным

В окне, кроме пинга, работали ГЛАЗА — **0.68 МЛН, 24 вызова**. Разобрал цели: это плановая
публикация в группу **«SoCal Electric & Hybrid Deals»** (3 разбора текста поста, 2 — вход в
группу, 1 — строка создания поста). Это **плановая работа роста, а не холостой ход** и не
покупатель. Холостую часть она не загрязняет: liveness считается отдельной строкой.

Итого честная формулировка: **холостой ход (пинг живости) = 0.12 МЛН/ч, подтверждено
замером.** Полного простоя в этот час не было — дом работал по расписанию.

### ⚠️ И вторая честность: сам замер стоил дороже измеряемого

В том же окне `interactive` — **3.49 МЛН**: это мои собственные ходы, которыми я ждал
закрытия часа. Один мой ход стоит ~290К, то есть ожидание обошлось примерно в **29 раз
дороже**, чем весь измеренный за час холостой расход. Ждать надо было молча и одним
фоновым ожиданием, а не опросом часов — я так и сделал во второй половине окна, но первые
шесть проверок уже были оплачены.

## Итог одной строкой

* `fb_inbox_sweep` — **жив как файл, снят как крон, по приказу**; не трогаю. Замена дышит.
* Пинг живости — **0.19 → 0.12 МЛН/ч, замерено**, прогноз подтверждён точно.
