# Детекция и петля эскалации — проверка фактом (22.08.2026, 12:30 UTC)

**Коротко.** Экономия зрения работает как задумано и **не пропустила ни одного события**.
Детекция работает. **Ответ покупателю — НЕТ: прямо сейчас двое ждут — yunus sağlam 16 часов
(OfferUp) и Mario 3 суток 8 часов (FB, посреди сделки).**
Босс прав по петле эскалации, но причина не та, что кажется: петля не «застревает после
починки» — **её никто не запускает**. Единственный рот, умеющий заводить задачу Коле, молчит
с 17.08 23:15, а все остальные шесть сторожей физически не умеют это делать.

---

## ЗАДАЧА 1 — РАБОТАЕТ ЛИ ДЕТЕКЦИЯ ПОСЛЕ ПРАВКИ 19.08

### 1.1 Судья причины: ЖИВ, экономия РЕАЛЬНА

Окно 20.08 12:00 → 22.08 12:25 UTC, источник `.kola_state/fb_inbox_sweep.log`:

| | |
|---|---|
| тиков крона (`12,42`) | **97** |
| **СМОТРЕЛ** | **15** (15.5 %) |
| НЕ смотрел («причины нет») | 82 |
| причины взгляда | `net` × 12, **`push` × 3** |

Было 48 взглядов/сут — стало **7.5/сут**. Строка `[sweep] НЕ СМОТРЮ: причины смотреть нет`
(признак живой экономии из отчёта 19.08) присутствует. Сеть 3 ч натягивается ровно как
спроектировано: 01:12 … 03:42 пропуск, 04:12 «смотрю — net: 205 мин назад (порог 180)».

### 1.2 Пропустил ли судья реальное событие? — НЕТ

Три взгляда за 48 ч были подняты **пушем**, а не часами. Оба реальных входящих за окно
поймал первый канал (ухо `inbox_watcher`), а не свип:

| платформа | последнее РЕАЛЬНОЕ входящее | детект | ответ (пруф) |
|---|---|---|---|
| **FB Marketplace** | **Turq**, 21.08 16:12:14Z | `kind=route` 16:12:14 (crm_role=buyer) | ✅ `CARRIED turq` 16:19:44, повтор 16:34:16, `RESOLVED` 17:23:48 |
| **FB Marketplace** (старее) | **Mario**, 19.08 04:47:38Z (писал 18.08 23:15 и 19.08 04:47) | `kind=route` оба раза | 🔴 **ПРУФА НЕТ. 3 суток 8 часов** — см. 1.6 |
| **OfferUp** | **yunus sağlam**, 21.08 20:45:12Z — «Hi! Is this still available?» | `kind=route` 20:45:12 | 🔴 **ПРУФА НЕТ. 16 часов молчания** |
| **Craigslist** | 21.07 (велосипед) — с тех пор входящих нет | — | инбокс пуст, читано с экрана 22.08 12:03 |

**Ни одного события, пропущенного судьёй, не найдено.** Задержка детект→ответ у Turq — 7 мин 30 с.

### 1.3 ЖИВОЕ ЧТЕНИЕ ЭКРАНА — КТО ЖДЁТ ПРЯМО СЕЙЧАС

**OfferUp** (`inbox_sweep.py --quiet`, 22.08 12:28:52 UTC, экран Pixel):
```
"fresh": ["yunus sağlam (16h ago): Hi! Is this still available?"]
```
🔴 **Живой покупатель ждёт 16 часов.** Остальные треды — «1mo ago», все с префиксом `You:`
(отвечены нами).

**Craigslist** (`cl_chat_watch`, 22.08 12:03:26 UTC, читано с экрана):
`инбокс ПУСТ («no conversations») — тредов 0`. Виджет OK, вкладка ACTIVE — это НЕ слепота.

**FB Marketplace** (`fb_inbox_sweep.py`, принудительный обход `FB_SWEEP_NET_S=1`,
22.08 12:28:53 → 12:32:20 UTC, кадр `fb_sweep_inbox.jpg`):
```
[unread] сырых непрочитанных тредов: 0 (—)
[sweep] тредов=104 ЖДУТ=26 отвечено=38 закрыто=40
"fresh": []
```
**Нового входящего на FB нет.** Но «ЖДУТ=26» — не ноль, и среди них Mario (1.6). И тот же
обход прямо у меня на глазах снова поставил флаг Moore (1.5).

**Итог живого чтения трёх экранов: ждут ДВОЕ — yunus sağlam (OfferUp, 16 ч) и Mario (FB, 3 сут 8 ч).**

### 1.4 🔴 СТОРОЖ ОТВЕТА ОСЛЕП — ОКНО ПРУФА ЗАБИТО ШУМОМ OfferUp

`answer_guarantee` каждые 5 минут печатает **13 НАРУШЕНИЙ** и «ступень уже отправлена»:
`Turk Mehmet, Mehmet, Marie, Alejandro Alacio, Shawn Tang, Shawn, Seymour Unpingco, Seymour,
Mario Hv, Mario, Julie Hail, Turq, yunus sağlam`.

Но **Turq ОТВЕЧЕН** — пруф в `watcher.log` от 21.08 16:19:44. Почему сторож его не видит:

`lib/reply_proof.py:133` — `has_proof(..., tail_lines=4000)` читает **последние 4000 строк**
`watcher.log`. Замер прямо сейчас:

| | |
|---|---|
| строк в `watcher.log` | 21 557 |
| окно пруфа (последние 4000) покрывает | **22.08 01:14 → 12:26 = 11.2 часа** |
| из них строк шума OfferUp | **3 996 из 4000 = 99.9 %** |
| строк пруфа (`CARRIED`/`RESOLVED`) в окне | **0** |

С **20.08 18:26** OfferUp шлёт пуш `«OfferUp: Comics and Suitcase related items for you!»`
каждые ~27 с (5 287 повторов). Объём `watcher.log`: 17.08 — 353 строки/сут, **21.08 — 3 804,
22.08 за 12 ч — 4 415** (≈8 800/сут). Окно в 4000 строк, задуманное как «14 суток истории»
(`LOOKBACK_S = 14*24*3600`), сжалось до **11 часов**.

**Последствие двустороннее и опасное:** отвеченный Turq выглядит брошенным (ложная тревога),
а реально брошенный yunus неотличим от 12 ложных. Канал тревог Боссу забит на 92 % ложью —
усталость от алертов сконструирована механически. Это ровно тот класс, что описан в шапке
самого модуля: «судья, чьё ОКНО делает часть фактов невидимыми».

### 1.5 🔴 ПРИКАЗ БОССА ПРО Moore НЕ ДОШЁЛ ДО ПУТИ ФЛАГА

Босс 19.08 19:52 закрыл Moore (Kia Niro) и Alaine F. (Kona) как НЕ НАШЕ, навсегда.
Приказ соблюдается в `lead_router._boss_ruled_out` и `review_gate` — **ответ действительно не
отправляется**. Но путь флага экрана его не спрашивает:

`projects/sales/fb_inbox_sweep.py:869-885` строит держателей из `ours` + `proc_leads` и
проверяет **только паузу полосы** (`_dc.is_paused`), приказа Босса не спрашивает вообще.
Результат — на КАЖДОМ обходе:
```
[flag] + SET buyer_waiting:moore → Moore::2023 Kia Niro EV [полоса Боря/procurement ЖИВАЯ]
[ALARM] Боссу: 1 ждущих > 20 мин
```
**20 раз с 19.08 15:16**, из них все после приказа — Moore держит экран телефона и вытесняет
прогрев, будучи закрытым навсегда. Тот же класс, что 17.08 (Ahmad/Tesla): **стоп-защита не
бывает ОДНИМ механизмом** — приказ живёт в одном судье и не живёт в соседнем.

### 1.6 🔴 Mario: ЛОЖНЫЙ `RESOLVED` ПО БЕЙДЖУ ЗАКРЫЛ ЖИВУЮ СДЕЛКУ

Mario торговался по велосипеду Giant Rainier — уже договаривались о встрече на парковке
Target Irvine Spectrum. Полная выписка:

| время | событие |
|---|---|
| 18.08 14:45:03 | ✅ `CARRIED Mario: Sin problema, Mario, entiendo lo del transporte…` — **наш последний ответ** |
| 18.08 23:15:52 | 🔵 `kind=route` — **Mario написал снова** |
| 19.08 04:47:38 | 🔵 `kind=route` — **Mario написал ещё раз** |
| 19.08 14:14:50 | ⚠️ `RESOLVED Mario: живая проба инбокса — у треда Mario НЕТ метки непрочитанного` |
| 22.08 12:32:19 | 🔴 живьём: `[flag] + SET buyer_waiting:mario → Mario::Giant Mountain Bike 26in` |

Тред закрыт **по отсутствию бейджа непрочитанного**, а бейдж снимается **ЧТЕНИЕМ, а не
ОТВЕТОМ** — это ровно тот пункт, который отчёт 19.08 оставил в «ещё не закрыто». Он не
остался безобидным: он **заглушил сторожа по живой сделке на 3 суток**. Свип с ним не
согласен и каждый обход честно пишет, что Mario ждёт — два судьи по одному факту, и
победил неверный.

---

## ЗАДАЧА 2 — РАБОТАЕТ ЛИ ПЕТЛЯ ЭСКАЛАЦИИ

### 2.1 Техзадач за 48 ч: НОЛЬ

`.kola_state/tech_queue.log` — последняя запись **2026-08-17T23:15:37Z**. За 48 часов (и за
4 суток 13 часов) не заведено НИ ОДНОЙ задачи. Каталог `.kola_state/tech_tasks/` не менялся
с 17.08 23:15.

### 2.2 Две задачи от 17.08 ВИСЯТ ДО СИХ ПОР

| id | заведена | статус | сессия Коли | починено | работа возобновлена |
|---|---|---|---|---|---|
| `0b171334059b` (ui_unreachable, снять Writing Desk с FB) | 17.08 23:15:09 | **`new`** — не взята никем | ❌ «сессия Коли уже идёт — ждёт очереди» | ❌ | ❌ |
| `199d63730fb5` (infra, снять Sectional Sofa с FB) | 17.08 23:15:35 | **`in_progress`** | ✅ поднята 23:15:37, `session_attempts=1` | ❌ | ❌ |

Обе висят **4 суток 13 часов**. У обеих `resume = {}` — **пустой возобновитель**: даже если
их закрыть, возобновлять нечем. Причина остановки 199d63730fb5 записана честно:
`«спросить маршрут было НЕ У КОГО: модель недоступна (cli_rc:1: You've hit your session limit)»`.

### 2.3 Петля НЕ сломана — 17.08 она отработала ПОЛНОСТЬЮ

Опровержение гипотезы «застревает после починки», из того же журнала:
```
19:47:52 ЗАВЕДЕНА 55b88ac34f5b [capability_missing] CL-постинг: нет кнопки continue
19:48:14 СЕССИЯ КОЛИ поднята под 55b88ac34f5b (попытка 1/3) → in_progress
20:33:43 ЗАКРЫТА 55b88ac34f5b (kolya): Стена вскрыта и починена
20:33:43 ВОЗОБНОВЛЕНА работа по 55b88ac34f5b: … scripts/_resume_cl_probe.sh
```
За вечер 17.08: **8 задач заведено, 9 закрыто, 3 работы возобновлены**. Механизм жив и
доказан. Значит вопрос не «почему не возобновляется», а **«почему больше не заводится»**.

### 2.4 🔴 КОРЕНЬ: ЗАВОДИТЬ ЗАДАЧУ УМЕЕТ РОВНО ОДИН ПУТЬ, И ЕГО НИКТО НЕ ЗАПУСКАЕТ

`tech_queue.from_solve()` вызывается **из одного места во всей системе** —
`lib/dirigent_commands.py:471`, куда приезжает результат `generic_solve` (то есть работы по
листингу). Проверка остальных сторожей на вызов `tech_queue` — **везде ноль**:

| сторож | что он ловит | заводит задачу Коле? |
|---|---|---|
| `fault_realert` | мёртвые способности (worker_proof, detect_mail, warmup) | **0** |
| `answer_guarantee` | покупатель без ответа | **0** |
| `lead_guarantee` | лид-сирота | **0** |
| `delegation_guarantee` | воркер не поднялся | **0** |
| `coverage_eyes_sweep` | наши лоты не видно | **0** |
| `fb_inbox_sweep` | инбокс/навигация | **0** |

А `generic_solve` / `listing_job` **не стоят в кроне вообще**: 65 активных строк крона
botuser, ни одна не зовёт `listing_job`, `generic_solve`, `tech_queue` или насос очереди
(`tech_pump.log` последний раз писался 15.08). Их запускает только живая сессия — моя или
Босса. Логи подтверждают: `generic_solve.log` и `listing_job.log` последний раз писались
**17.08 23:15**.

**Вывод: стены рта не имеют.** Всё, что ломается вне работы по листингу, доезжает до Босса
в Telegram — и там умирает.

### 2.5 Стены за 48 ч, которые НИКТО не завёл

1. **20.08 21:00 и 21:02** — `coverage_eyes_sweep`: `sense_blind` на ОБЕИХ площадках,
   вердикт `unknown` для **10 из 10** пар товар×площадка. Корень напечатан прямо там:
   `«модель не ответила/упала за 180с — не сужу (fail-closed)»`. Задачи нет.
2. **22.08 00:04** — `fault_realert`: способность **`worker_proof` МЕРТВА**,
   `«назначения тратятся ВПУСТУЮ: lead-offerup-yunus-sa-lam: 4 назначений, ноль CARRIED/RESOLVED»`.
   3 тревоги Боссу (00:04, 01:15, 04:30), лестница исчерпана — **и всё**. Задачи нет.
   Аудит продолжает печатать тот же диагноз каждые 4 ч (04:04, 08:01, 12:00) в пустоту.
3. **19, 20, 21.08, каждый день в 04:00** — `detect_mail` (полоса **money**) умирает,
   в 12:00 «✅ РАЗРЕШЕНО после 8.0ч и 3 напоминаний». Три дня подряд, один и тот же час.
   Снятие — по ре-верификации (способность сама ожила), **не по починке**. Задачи нет ни разу.
4. **22.08 11:16** — `[nav] папка Marketplace НЕ открыта глазами: not_found`. Задачи нет.

### 2.6 Транспорт к модели: 146 отказов за двое суток, починен молча

`.kola_state/claude_cli.log` (пишет ТОЛЬКО отказы):

| дата | отказов | тип |
|---|---|---|
| 17.08 | 4 | `cli_rc:1` (session limit) — **это и завело 199d63730fb5** |
| 18.08 | 10 | `cli_not_found` |
| **20.08** | **100** | `cli_no_start: OSError [Errno 8] Exec format error: 'claude'` |
| **21.08** | **46** | то же, последний в 13:13:58 |
| 22.08 | 1 | `cli_not_found` в 12:21:59 |

Отказы 20–21.08 объясняют пункт 2.5.1: у зрения не было модели, потому свип и ослеп на все
10 целей. Прекратились они в 13:13 21.08, а `/usr/bin/claude` был переставлен **21.08 13:30**
(сейчас `2.1.238`, от botuser запускается, `rc=0`). То есть **транспорт починился побочным
эффектом апгрейда CLI** — ни задачи, ни разбора, ни записи о причине. Отказ 22.08 12:21:59
(того же класса, вызов «eyes of an Android automation worker») **не объяснён**: из логин-шелла
бинарник находится.

### 2.7 Почему yunus не отвечен, а Turq отвечен

Обоим `delegation_guarantee` механически поднял `worker3` после «ни одного запуска воркера
после детекта лида»:
```
21.08 16:34:01 🔴 fb_sales/Turq …→ механически поднимаю worker3 → ✅ поднят
21.08 17:22:02 🔴 fb_sales/Turq …→ механически поднимаю worker3 → ✅ поднят
21.08 20:50:02 🔴 OfferUp/yunus sağlam …→ механически поднимаю worker3 → ✅ поднят
```
У Turq за этим последовали `CARRIED` + `RESOLVED`. У yunus — **4 назначения, ноль пруфов**.
Транспорт в это окно (21.08 13:13 → 22.08 12:21) отказов НЕ логировал, значит дело не в нём:
**ветка отправки OfferUp глотает назначения молча.** Это и есть «слепая способность», которую
назвал аудит — и ровно она не заведена задачей.

---

## ЧТО ЭТО ЗНАЧИТ ОДНОЙ ФРАЗОЙ

Правка 19.08 сделала ровно то, что обещала, и ничего не сломала: **97 тиков → 15 взглядов,
ни одного пропущенного события**. Но она сэкономила на канале, который и так работал.
Деньги теряются на два шага дальше: **детект есть → назначение есть → ответа нет**, и ни один
из шести сторожей, который это видит, не умеет позвать Колю. Босс прав; неверна только
формулировка «не доводит до конца» — система **не начинает**, потому что рот один и он
привязан к работе по листингу, которую крон не запускает.

**Цена прямо сейчас: два живых покупателя без ответа** — yunus sağlam спрашивает «Is this
still available?» 16 часов, Mario ждёт продолжения уже согласованной встречи 3 суток 8 часов.
Оба видны с экрана. Ни по одному не заведено задачи.

## ЧТО Я НЕ ДЕЛАЛ

Ничего не чинил и не отвечал покупателям — задание было «проверить фактом». Титан и Solana
не тронуты. Живое чтение экрана — только чтение (`--quiet`, `cl_chat_watch` read-only,
FB-свип форсирован переменной `FB_SWEEP_NET_S=1` без правки состояния).
