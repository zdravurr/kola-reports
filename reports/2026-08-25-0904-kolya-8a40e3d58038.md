# kolya-8a40e3d58038

_2026-08-25 09:04 UTC_

---

# ФАКТ-пруф свипа после фикса пред-тикового гейта — ПОДТВЕРЖДЁН

**Задача:** 8a40e3d58038 · закрыта · кода не менял (задача была на проверку ФАКТОМ).

Фикс №2 (24.08 08:24, `projects/sales/fb_inbox_sweep.py`: пред-тиковый гейт уступки
`pending_info()` → `others_waiting()`) **работает**. Проверено не по чистому логу, а по
ФАКТ-успехам за ~24 часа тиков крона.

## Три критерия из задачи

**(а) skipped=client_pending по своей метке — ИСЧЕЗ.** За все тики после 24.08 08:24 —
ни одного. Уступки остались только по РЕАЛЬНЫМ живым клиентам:
`fb_sales:david nguyen` (24.08 19:12), `delist:bike_sold` (19:42, 20:12),
`fb_sales:desideria mercado` (22:42). Ровно то поведение, которого добивались:
`others_waiting()` отсекает свою метку и KIND_HOLD, чужого покупателя — пропускает вперёд.

**(б) Скан доходит до конца, waiting[] НЕ пуст.** Четыре полных прохода после фикса:

| тик | тредов | ЖДУТ | отвечено | закрыто |
|---|---|---|---|---|
| 24.08 20:44 | 103 | 19 | 44 | 40 |
| 24.08 23:14 | 103 | 20 | 43 | 40 |
| 25.08 02:44 | 103 | 20 | 43 | 40 |
| 25.08 06:14 | 104 | 21 | 43 | 40 |

Прокрутка «до 12 экранов» отрабатывает целиком. Значит **гипотеза из улик про корень в
`read_marketplace_inbox` СНЯТА** — чтение списка тредов здоровое, дело было именно в гейте.

**(в) Тред George поднялся.** 24.08 10:15 свип его зароутил:
`[flag] + SET buyer_waiting:george → George::2026 giant bike that name`.
Свип роутит и новых: 25.08 06:15 `SET buyer_waiting:rick`.

## Про тики 08:42/09:12, названные в задаче

Сегодняшние 08:12 и 08:42 дали `НЕ СМОТРЮ: no_reason` — это **другой, здоровый** гейт
(событий с прошлого обхода не было, непрочитанных нет), а не тот, что чинили. Ночью тот же
гейт корректно открывал проход по `net: редкая сеть — прошлый обход 207 мин (порог 180)`
(02:42, 06:12) и по `push: живое событие`. Механика уступки жива и различает причины.

## 🔴 Что ФАКТ-пруф вскрыл попутно — заведено ОТДЕЛЬНО (8b4024ae4df7)

**ФАКТ CARRIED по George — НЕТ.** Свип его поднял, но в тред живого покупателя ушла
служебная строка: `You: Not logged in · Please run /login`, и она **`Seen by George A.`** —
покупатель это прочитал. Вместо «100 usd firm, самовывоз Rancho Santa Margarita 92688».
Вхождение в логе ровно ОДНО, только в этом треде → это не артефакт скрейпа экрана, а
реально отправленный текст. Дальше свип паркует тред как отработанный
(`[parked-skip] George — отработан (парковка), строка инбокса НЕ менялась`) и больше его не
роутит — **покупатель заморожен навсегда**. Это дефект пути ОТПРАВКИ ответа, не
пред-тикового гейта, поэтому в текущую задачу не смешивал: завёл 8b4024ae4df7 (нужен гард
«не слать в тред служебные шаблоны» + снять парковку с George и ответить по делу).

## Контракты

113/119. Провалены: `test_boss_grants_a_role`, `test_boss_words_travel_with_intent`,
`test_contracts_never_touch_battle_state`, `test_gone_item_listing_is_watched_and_due_for_takedown`,
`test_gone_item_never_gets_a_phone`, `test_phone_guard_knows_the_item`.
**Не мои** — в этой сессии я не менял ни строки, дерево уже было грязным до меня
(`M lib/agent_capacity.py`, `lib/claude_cli.py`, `lib/eyes.py`, `lib/hold_deadline.py`,
`lib/item_records.py` — чужие незакоммиченные правки). Четыре из шести имён (`gone_item`,
`phone_guard`) трогают путь товара/телефона — на это стоит посмотреть тому, чьи это правки.
