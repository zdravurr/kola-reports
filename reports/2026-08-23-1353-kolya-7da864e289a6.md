# kolya-7da864e289a6

_2026-08-23 13:53 UTC_

---

# 7da864e289a6 [infra] — выравнивание версий openclaw: BLOCKED по правам (подтверждено вторично)

**Итог: НЕ починено. Задача остаётся открытой.** Не потому что не разобрался, а потому что
единственный путь физически закрыт правами сессии. Закрывать как resolve было бы ложью.

## Что проверил своими руками в этой сессии (не по чужим записям)

| Проверка | Результат |
|---|---|
| `npm --version` | **отказ по правам** (вне allowlist сессии) |
| `systemctl --user list-units …openclaw` | **отказ по правам** |
| `openclaw --version` | **отказ по правам** |
| `find` по node_modules (поиск установленной сборки) | **отказ по правам** |
| drop-in `~/.config/systemd/user/openclaw-gateway.service.d/zz-debug-log.conf` | **ЕСТЬ, взведён** (mtime 23.08 13:31, содержимое `[Service] Environment=OPENCLAW_LOG_LEVEL=debug`) |

Т.е. вывод сессии 9229865b06f6 подтверждён независимо: закрыт не один вызов, а **весь класс** —
ни npm, ни systemctl, ни сам бинарь openclaw, ни даже разведка файловой системы через find.

## Почему не обошёл

Осмотрел воркспейс на предмет санкционированной двери: `openclaw_config_sync.py` явно пишет
«Gateway не трогаем» и в живой openclaw.json не пишет; `run_guarded.sh` — гейт cron-джоб по
перегрузу, не привилегированный раннер. Двери нет.

Технический обход существует (протолкнуть `npm -g install` + `systemctl --user restart` через
cron/таймер, минуя allowlist интерактивной сессии). **Я им не пользуюсь сознательно.** Это не
«другой способ достичь цели», а обход того же самого запрета правами — из той же семьи, что
токен обхода commit gate, который мне не передан. Самовольно расширять себе права я не буду.

## Что нужно — ровно один грант, дальше всё есть

Добавить в allowlist сессии Коли два вызова:
1. `npm` (нужен `npm -g install openclaw@2026.7.x`)
2. `systemctl --user` (нужны `daemon-reload`, `restart openclaw-gateway`, `status`)

Ранбук после гранта готов, порядок важен:
1. Бэкап `~/.openclaw/openclaw.json` (снапшот-механика уже есть в `openclaw_config_sync.py`).
2. **СНЯТЬ drop-in `zz-debug-log.conf` ДО `daemon-reload`** — иначе гейтвей уедет в debug-логи
   навсегда. Файл подтверждён на месте, это не гипотеза.
3. `npm -g install openclaw` (2026.7.x).
4. `systemctl --user daemon-reload && systemctl --user restart openclaw-gateway`.
5. Проверка каналов + 15 мин `journalctl` на `unknown method` = пусто.

## Отдельно: чего апгрейд НЕ вылечит

Основной шум — **не** перекос версий. До рестарта 13:28 23.08 работала сборка, которая методы
знает, и она печатала `missing scope: operator.admin` **1694 раза за 3 ч**. После выравнивания
версий останется вопрос scope `operator.admin` для токена диригента. Это **решение о правах**,
не техника — его в этой задаче не трогаю и в неё не подмешиваю.

## Что дальше

Задача оставлена **открытой** с уликами, а не закрыта пустым `derive` (тупика в замысле нет —
замысел верный, закрыты руки). Следующая попытка после гранта прав пройдёт по готовому ранбуку
дёшево и с чистым контекстом.
