# ПУНКТ 13 ПРИМЕНЁН · ОБА БОЕВЫХ ГЕЙТА СНЯТЫ · ЧЕСТНЫЙ СПИСОК ПЕРЕД БОЕМ
**2026-07-29 19:20 UTC** · коммит `d63cb8b` · база `0833f42` · рестарт **18:48:01 UTC**

> **ИТОГ.** Применено, закоммичено, запушено, перезапущено. Баннер **🧪 PAPER, оба флага False**,
> ошибок 0, рантайм = коммит **6/6 по хешу**, константы `CHANGED: только PASSIVE_FILL_RECONCILE_EXISTS`.
> Четыре исхода и фикс причины перепроверены **на применённом коде**.
> **§4 — прямой ответ на твой вопрос. Зелёного света я НЕ даю.**

---

## 1. ПОДТВЕРЖДЕНО ИСПОЛНЕНИЕМ

**Баннер (живой журнал)**
```
Jul 29 18:48:05 titan[4165595]: [ORDER-MODE] 🧪 PAPER — simulated fills only —
                                no order can be sent (LIVE_TRADING_ENABLED=False)
[ORDER-MODE] LIVE_TRADING_ENABLED = False · ORDER_ADAPTER_LIVE = False
[RECONCILE] starting -> done · breakeven_worker started · virtual_trader worker started
traceback/error/exception: 0
```

**Рантайм = коммит, по хешу** — `config` · `order_adapter` · `virtual_trader` · `breakeven_worker` ·
`main` · `gunicorn.conf` → **6/6 MATCH**. Дерево чистое, `HEAD = origin/main = d63cb8b`,
worker `4165680` форкнут `18:48:14` — после рестарта `18:48:01`.

**Константы против `HEAD~1`**
```
compared: 108 · ADDED: none · REMOVED: none
CHANGED: PASSIVE_FILL_RECONCILE_EXISTS False -> True     ← ровно и только гейт
```
Бумага не тронута: `LIVE_FIXED_MARGIN_USDT`, `LEVERAGE`, все пороги каскада, скоринга, SL/трейла,
безубытка, LONG-частички, ярусов и advisor — без движения.

### 🔴 ИЗМЕНЕНИЕ ПОВЕДЕНИЯ, которое ты просил показать отдельно
```
ROUTING_MIGRATED_TO_ADAPTER   = True
PASSIVE_FILL_RECONCILE_EXISTS = True
routing_warning()      = ''
passive_fill_warning() = ''
[ORDER-MODE] 🔴 LIVE ORDERS — REAL MONEY: orders ARE sent to BingX
[ORDER-MODE]   sizing: margin $30 x 5 = $150 notional per entry
✅ RETURNED NORMALLY — отказ БОЛЬШЕ НЕ СРАБАТЫВАЕТ при обоих флагах True
```
**Оба гейта сняты. Структурного препятствия боевому старту больше нет.**

### Четыре исхода — перепроверены на ПРИМЕНЁННОМ коде
```
A) БУМАГА                              -> False, биржа не тронута (exchange=None прошёл насквозь)
B) БОЙ, позиция ОТКРЫТА                -> False
C) БОЙ, позиции нет, стоп НЕ залит     -> False + 🚨 тревога отправлена (13-I)
D) БОЙ, позиции нет, стоп ЗАЛИТ        -> True, финализация из РЕАЛЬНЫХ цены и комиссии
```

### Фикс причины выхода — доказан
```
breakeven_applied=False -> reason = sl
breakeven_applied=True  -> reason = breakeven
```

**Mercury-SOL:** active, PID 1793275, аптайм 8 суток, не тронут.

---

## 2. ЧТО ТЕПЕРЬ ЖИВЁТ В КОДЕ

`read_filled_protective_order` вынесена из `_report_passive_fill` — один код на двух потребителей.
`_do_close` получил `_exit_fill`, поэтому фандинг, close-report, UPDATE строки, кросс-линк в
`trades` и добавка LONG-частички **общие** для активного и пассивного закрытия.
`_reconcile_passive_fill` стоит **первым** в `_process_position`.

---

## 3. НИЧЕГО ИЗ ЭТОГО НИКОГДА НЕ ИСПОЛНЯЛОСЬ ПРОТИВ НАСТОЯЩЕЙ БИРЖИ

Скажу прямо, потому что это фон под всем §4. Первый боевой вход **впервые в истории бота** исполнит:

`market_entry` (боевая ветка) · `_live_fill` · `fetch_order_fee` · `place_stop` ·
`_place_stop_with_retry` · `move_stop_with_race_guard` · `cancel_stop` · `market_close` (боевая) ·
`_execute_close_position(_from_adapter=True)` · `_reconcile_passive_fill` · `_emergency_close`

Всё это проверено **моками**. Моки писал я, по своему же пониманию API. Там, где моё понимание
BingX неверно, тест это не поймает — по построению.

---

## 4. 🔴 ЧТО РЕАЛЬНО ОСТАЛОСЬ МЕЖДУ ТЕКУЩИМ СОСТОЯНИЕМ И ПЕРВЫМ ВХОДОМ НА $150

Категории: **БЛОКЕР** — сломается предсказуемо, чинить до флипа · **ПРИНЯТЫЙ РИСК** — известен,
ограничен, под тревогой · **КОСМЕТИКА**.

| # | что | категория | почему именно так |
|---|---|---|---|
| 1 | **Поведение BingX на `closePosition='true'` НЕ НАБЛЮДАЛОСЬ ни разу** | **БЛОКЕР ЗНАНИЯ** | На нём держатся: стоп закрывает весь остаток после LONG-частички; отсутствие второй позиции; отказ ордера при нулевой позиции. Это **допущение, а не факт**. Не чинится кодом — только наблюдением. Первый бой и есть наблюдение |
| 2 | **Ни один боевой путь не исполнялся живьём** (§3) | **ПРИНЯТЫЙ РИСК ПО ПОСТРОЕНИЮ** | Убирается только выходом в бой. $150 — и есть инструмент для этого |
| 3 | **Пункт 14 — частичные заливки НЕ реализован**; `PARTIAL_FILL_MAX_DIVERGENCE=0.02` записан и **никем не читается** | ПРИНЯТЫЙ РИСК, низкий | 0.0023 BTC против ~1533 BTC видимой глубины — частичная заливка практически невозможна. Размер вниз по потоку берётся ИСПОЛНЕННЫЙ, так что арифметика согласована даже при частичной |
| 4 | **`_live_fill`: `filled == 0.0` трактуется как ПОЛНАЯ заливка** (`executed = filled if filled else amount`) | ПРИНЯТЫЙ РИСК, низко-средний | Отличить «биржа ещё не отчиталась» от «не залилось» код не может. Для маркет-ордера практически всегда первое. Если всё же второе — стоп на нулевую позицию, скорее всего, не встанет → сработает инвариант → аварийное закрытие → строка не пишется. То есть, вероятно, самолечится. **«Вероятно» — честное слово здесь** |
| 5 | **Дыра 8: `_reconcile_side` вешает стоп, не записывая его id в строку** | ПРИНЯТЫЙ РИСК, узкий | Срабатывает только если позиция на бирже осталась БЕЗ стопа. Тогда id в строке устареет, и при заливке нового стопа получится случай C — крик, строка открыта, сторона заблокирована. Худший вариант (id = NULL) ловится 12c ещё на старте |
| 6 | **Случай C блокирует сторону до прихода человека** | ПРИНЯТЫЙ РИСК | Сознательный размен: выдуманный P&L хуже остановки. Тревога громкая — но если Telegram молчит, состояние тихое |
| 7 | **Отмена сиротского стопа может не пройти** | ПРИНЯТЫЙ РИСК | Крик `MANUAL ACTION REQUIRED`, остаток — руками |
| 8 | **Трейл не переживает смерть процесса** | ПРИНЯТЫЙ РИСК, принят тобой | Стоит прибыли, не риска: биржевой стоп на безубытке уже стоит |
| 9 | **Частота отказа `fetch_order_fee` неизвестна** (n=2, отказов 0) | ПРИНЯТЫЙ РИСК | Есть падение на тейкерскую оценку + печать. Оценить частоту по двум наблюдениям нельзя |
| 10 | **`funding_for_close(is_virtual=False)` живьём не исполнялся** | ПРИНЯТЫЙ РИСК, мелкий | Ошибка внутри возвращает 0.0 и не блокирует закрытие |
| 11 | **`_from_adapter` — инвариант, поддерживаемый вручную** | КОСМЕТИКА/сопровождение | Новый вызывающий обязан не забыть флаг; компилятор не проверит |
| 12 | **Legacy-путь входа — мёртвый код (~200 строк), выглядит рабочим** | КОСМЕТИКА | Ловушка для будущего читателя, не для исполнения |
| 13 | **`is_virtual` сменил семантику в середине книги** | КОСМЕТИКА/документировано | При сравнении выборок через границу коммита помнить |
| 14 | **Блокер A вернётся, если кто-то реализует биржевой трейл** | КОСМЕТИКА сейчас | Сегодня недостижим по построению |

### 🔴 ОПЕРАЦИОННОЕ, что легко упустить сегодня вечером

**Флипать флаги нужно, пока бот ПУСТ.** Сейчас открытых позиций **0** — это правильный момент.
Если до флипа откроется бумажная позиция, `assert_single_owner_at_boot` (12c) **откажется
стартовать** (строка с `stop_order_id IS NULL`). Это by design, но выглядит как поломка, если
забыть. Тогда: дождаться закрытия либо архивировать строку руками.

### Мой честный вывод, без смягчения и без зелёного света

**Известного дефекта, который сломается предсказуемо, я не вижу.** Всё, что нашли за сессию,
закрыто или явно принято. Остаточный риск сконцентрирован в **одной точке: поведение BingX на
`closePosition='true'` и на боевых ответах API — допущение, а не наблюдение** (строки 1, 2, 4).

Кодом это больше не уменьшить. Уменьшается только размером и присмотром: **$150 — правильный
инструмент, если ты смотришь на первую сделку живьём**, а не запускаешь и уходишь. Первый вход даёт
ответ на строку 1 и превращает её из допущения в факт.

**Решение твоё. Если хочешь, чтобы риск был ещё ниже перед флипом — единственный содержательный
шаг, который остался: наблюдать одну живую сделку от входа до выхода по стопу, и только потом
считать пункты 11–13 доказанными.** Иначе они доказаны лишь моими моками.
