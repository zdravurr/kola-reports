# kolya-1956af0bcf30

_2026-08-24 00:17 UTC_

---

# Вечный вопрос про закрытых лидов больше не держит деньги

**Было.** Пара «это один человек?» уходила в `pending` и снималась ТОЛЬКО решением Босса или
новым фактом по ОТКРЫТОМУ лиду. Если обе половины успели закрыться — повода не будет никогда:
кандидатов ищем только среди открытых лидов. Замер 23.08: в реестре висело **9 пар при ДВУХ
открытых лидах**, старейшей 26.7 суток. Вред денежный: close-гейт `reply_proof` читал этот
список по ИМЕНАМ, не глядя, жив ли лид, и НАВСЕГДА отказывался засчитывать пруф по вложению
имён («Bryan» ⊆ «Bryan De La Paz») — лид с коротким именем нельзя было закрыть в принципе.

**Стало.** У вопроса появился ПРЕДМЕТ:

- `pair_still_answerable()` — вопрос отвечаем, пока жива хоть одна половина (нет записи в
  обязательствах = не жива: про такого лида тоже нечем ни подтвердить, ни отклонить);
- `retire_stale_pending()` — пара с двумя закрытыми/слитыми сторонами уезжает в новый ящик
  `retired` как **СНЯТЫЙ вопрос**. Это НЕ `rejected`: «отклонён» = утверждение «это разные
  люди», а его никто не делал. Причина подозрения и причина снятия остаются читаемыми;
- уборка идёт сама в `resweep_pending()` (крон), руками — `lead_alias.py retire`, видна в `list`;
- `pair_is_open_question()` судит по ФАКТУ сразу, не дожидаясь уборки, — иначе мусор в файле
  блокировал бы деньги до ближайшего крона;
- `hold_deadline.overdue_alias_questions()` тем же критерием перестал вечно копить просрочку
  «Босс не ответил» по вопросам, на которые ответ уже ничего не меняет.

Жива хоть одна половина — вопрос ОСТАЁТСЯ вопросом, поблажек нет.

**Замер после.** Живой реестр вычищен: pending **9 → 0** (Shawn, Seymour, George, Alex×3,
Bryan, Desideria — все их слаги в обязательствах `done`).

**Проверка.** Новый контракт `tests/test_pending_question_about_closed_leads_expires.py`:
живая половина держит вопрос и НЕ пускает пруф; обе закрыты — вопрос снят, не отклонён, пруф
по вложению снова засчитан; уборка идёт в resweep; повторный проход идемпотентен.
Контракты: **101/102**. Единственный красный — `test_proof_window_and_watchdog_mouth`, он уже
стоит в очереди отдельной задачей и к этой правке отношения не имеет (pending пуст ⇒ гейт
стал только мягче, а падают проверки «остаётся пруфом»).

**Решений от Босса не требуется.**
