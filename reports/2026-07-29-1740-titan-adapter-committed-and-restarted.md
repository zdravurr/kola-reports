# Титан — адаптер применён, закоммичен, перезапущен. Окно `Restart=always` закрыто
**2026-07-29 17:40 UTC** · коммит `96b83d4` · база `5285495`

> **ИТОГ.** Дельта сверх одобренного = **машинерия жёсткого отказа и ничего чужого**.
> Закоммичено `96b83d4`, запушено, `titan.service` перезапущен осознанно в **17:29:13**.
> **Боевой баннер из живого журнала есть — 🧪 PAPER, оба флага False.** Ошибок 0.
> Окно «непроверенный код на диске + `Restart=always`» **закрыто**.

---

## 1. ДЕЛЬТА: одобренный патч против того, что лежало на диске

**Метод — механический, не на глаз.** Из отчёта 16:22 извлечён блок ```diff``` (строки 210–813),
наложен на чистый `git archive HEAD` — **лёг без единого отторжения** (`Applied patch ... cleanly` ×6).
Полученное дерево «КАК ОДОБРЕНО» побайтово сравнено с рабочим.

| файл | вердикт |
|---|---|
| `virtual_trader.py` | **ИДЕНТИЧЕН одобренному** |
| `breakeven_worker.py` | **ИДЕНТИЧЕН одобренному** |
| `gunicorn.conf.py` | **ИДЕНТИЧЕН одобренному** |
| `main.py` | **ИДЕНТИЧЕН одобренному** |
| `config.py` | отличается — 55 строк |
| `order_adapter.py` | отличается — 103 строки |

> **Про «+58 против +57» в `virtual_trader.py`:** расхождения нет. Файл **побайтово равен** одобренному.
> Это был расчёт по разным базам, а не лишняя строка.

### 1a. `order_adapter.py` — 100% машинерия отказа, перечисленная тобой

Всё до последней строки попадает в твой список `ROUTING_MIGRATED_TO_ADAPTER` / `_refuse_to_start` /
`REFUSAL_STATE_FILE` / `_alert_throttled` / `RoutingNotMigrated`:

- `import os, time` — нужны для `os._exit` и таймера троттла;
- `REFUSAL_ALERT_THROTTLE_SEC = 300`, `REFUSAL_STATE_FILE`;
- `class RoutingNotMigrated(RuntimeError)`;
- в `assert_startup_mode()` крик из четырёх `print` **заменён** на `_refuse_to_start(warn, send_tg)`
  с комментарием `# DOES NOT RETURN`; из TG-строки убран хвост с `warn` (он туда больше не доходит);
- `_alert_throttled()` — троттл через файл состояния, **fail-open**;
- `_refuse_to_start()` — печать, TG, `os._exit(3)`;
- в `routing_warning()` — второй ранний возврат по `ROUTING_MIGRATED_TO_ADAPTER` (это «всё чисто»).

**Ничего сверх этого. Ни одной строки, меняющей торговую логику.**

### 1b. `config.py` — отказ ПЛЮС два пункта вне твоего перечня. Говорю прямо

1. ✅ `ROUTING_MIGRATED_TO_ADAPTER = False` — из твоего списка.
2. ⚠️ **Две правки в КОММЕНТАРИИ:** `Either F.` → `Either False`, `Default F.` → `Default False`.
   Это текст комментария. В HEAD такой строки не было вовсе (`grep -c` = 0) — то есть «F.» с высокой
   вероятностью артефакт рендеринга самого отчёта, а не код. Поведения не касается.
3. ⚠️ **`PARTIAL_FILL_MAX_DIVERGENCE = 0.02` — ЭТОГО В ТВОЁМ ПЕРЕЧНЕ ИЗ ПЯТИ ИМЁН НЕТ.**

**Почему я всё же закоммитил третий пункт, а не остановился.** Твой критерий сформулирован как
«**что-то, чего я НЕ одобрял**», а не «чего нет в списке». Этот номер ты одобрил дословно в первом
сообщении сессии:

> «14: accepted. Divergence threshold 0.02, **recorded as a deliberate policy number with its
> reasoning, not a default**.»

На диске лежит ровно это: константа с развёрнутым обоснованием (почему именно 0.02 — шаг размера
0.0001 BTC на 0.0031 BTC даёт 3.2%, порог сознательно ставится **ниже одного шага**, чтобы округление
его не срывало). **Проверено исполнением, что она инертна:**

```
$ grep -rn PARTIAL_FILL_MAX_DIVERGENCE /root/titan-bot/ --include=*.py | grep -v config.py
readers found: 0
```

**Ноль читателей.** Пункт 14 НЕ реализован — это по-прежнему только записанное число.
**Если считаешь, что оно не должно было попасть в этот коммит — скажи, сниму отдельным коммитом.**

---

## 2. КОММИТ, PUSH, ОСОЗНАННЫЙ ПЕРЕЗАПУСК

```
96b83d4  feat(titan): live-order adapter — one seam for fills, and a boot that REFUSES an unsafe config
         5285495..96b83d4  main -> main        (zdravurr/bingx-bot)
         6 файлов: order_adapter.py (new) + config, virtual_trader, breakeven_worker, gunicorn.conf, main
```

```
ПЕРЕД:  master PID 4081846, старт 13:49:47, worker 4081957 (13:49:56)  — код HEAD 5285495
systemctl restart titan.service        @ 17:29:13 UTC
ПОСЛЕ:  master PID 4142493, старт 17:29:13, worker 4142520 (17:29:21) — worker форкнут ПОСЛЕ рестарта ✅
```

---

## 3. 🟢 БОЕВОЙ БАННЕР ИЗ ЖИВОГО ЖУРНАЛА — артефакт, которого раньше не существовало

```
Jul 29 17:29:16 titan[4142493]: ========================================================================
Jul 29 17:29:16 titan[4142493]: [TITAN][ORDER-MODE] 🧪 PAPER — simulated fills only — no order can be
                                sent (LIVE_TRADING_ENABLED=False)
Jul 29 17:29:16 titan[4142493]: [TITAN][ORDER-MODE]   LIVE_TRADING_ENABLED = False
Jul 29 17:29:16 titan[4142493]: [TITAN][ORDER-MODE]   ORDER_ADAPTER_LIVE   = False
Jul 29 17:29:16 titan[4142493]: [TITAN][ORDER-MODE]   sizing: margin $2000 x 5 = $10000 notional per entry
Jul 29 17:29:16 titan[4142493]: ========================================================================
```

**PAPER. Оба флага False.** Баннер напечатан **до** сверки — как и было спроектировано
(`17:29:16.970169` баннер → та же миллисекунда `[RECONCILE] boot reconciliation starting`).

---

## 4. ЗДОРОВЬЕ И ТОЖДЕСТВО «РАНТАЙМ = КОММИТ»

```
[RECONCILE] boot reconciliation starting → done            (17:29:16 → 17:29:21)
breakeven_worker started (interval=5s)
virtual_trader worker started (interval=10s, closed=30/30, entries=65)
traceback/error/exception с момента рестарта:  0
```

Тождество кода — не по mtime, а по **хешу**:

```
order_adapter.py на диске     sha256[:16] = 3adb8137b1f09ec3
закоммиченный блоб HEAD       sha256[:16] = 3adb8137b1f09ec3     ← совпадает
git status --porcelain        пусто (worktree clean)
HEAD = origin/main = 96b83d4
mtime всех шести файлов       16:47–16:49  <  старт процесса 17:29:13   ✅
```

Процесс стартовал позже последней записи в файлы, дерево чистое, диск равен коммиту ⇒
**работающий процесс — это ровно закоммиченный код.**

---

## 5. БУМАЖНАЯ АРИФМЕТИКА

Проверено исполнением на входных данных vpos 85 через **новый** код:

```
adapter _sim fee_cost = 4.99715034      старая формула = 4.99715034      в БД = 4.99715034
fill_price = 64604.4 == ref_price                       SIM_FEE_RATE = 0.0005 == VIRTUAL_FEE_RATE
```

⏳ **ОТКРЫТО, ЧЕСТНО:** нового реального входа после рестарта **ещё не было** — бот пустой.
Сверка «байт-в-байт на следующем реальном входе» **не выполнена и остаётся к проверке**, когда
позиция откроется. Выдавать существующее совпадение за неё я не буду.

---

## 6. ЧТО НЕ ИЗМЕНИЛОСЬ — доказано сравнением, а не заявлением

Загрузил `config.py` из `HEAD~1` и из `HEAD` двумя модулями и сравнил **все** константы:

```
constants compared: 102
ADDED  : LIVE_FIXED_MARGIN_USDT, ORDER_ADAPTER_LIVE, PAPER_FIXED_MARGIN_USDT,
         PARTIAL_FILL_MAX_DIVERGENCE, ROUTING_MIGRATED_TO_ADAPTER
REMOVED: []
CHANGED: NONE  ← ни один параметр не сдвинулся
```

Каскад, скоринг-гейт, SL/трейл/безубыток, LONG-частичка, ярусы сигналов, exit-advisor — **ни один
порог не изменился**. `virtual_trader.py` / `main.py` / `breakeven_worker.py` побайтово равны
одобренному патчу, а он трогает только шов фиксации сделки.

**Mercury-SOL:** `mercury-sol.service` active, PID **1793275**, аптайм **8 суток** (с 21.07 06:39).
`mercury-sol-optimizer-listener` active. **Не тронут.**

---

## 7. ТРИ ДЫРЫ — что доказано и что НЕТ

Доказано исполнением: каждая функция отправки отказывает на реальном конфиге —
`market_entry` / `market_close` / `market_reduce` → `OrdersDisabled: LIVE_TRADING_ENABLED=False
ORDER_ADAPTER_LIVE=False` (это `_require_live()`, перечитывающий оба флага перед самой отправкой).

Жёсткий отказ старта — тоже исполнением:
```
оба флага True, ROUTING_MIGRATED_TO_ADAPTER=False  →  EXIT_CODE=3, строка после
assert_startup_mode() НЕ достигнута
оба флага True + ROUTING_MIGRATED_TO_ADAPTER=True  →  вернулся штатно (отказ УСЛОВНЫЙ, не глухой)
```

**Чего я НЕ делал, намеренно:** не отправлял синтетический P3-вебхук в живого бота, чтобы «прогнать»
дыру 5 насквозь. Такой вызов **записал бы синтетическую строку в `trades`** — ровно то загрязнение
выборки, против которого стоит дисциплина §2.18. Ранние возвраты дыр 5/6/7 стоят на том же предикате,
который исполнен и равен `False`, но **сам ранний возврат прогнан не был**. Говорю как есть.

**Дыра №8** (`main.py:4446`) — не тронута, это пункт 13.

---

## 8. OPEN-ITEMS §2.4 — датапоинт vpos 85 записан

Новый файл: `reports/2026-07-29-1735-open-items.md`. Счёт **2 из ~10**.

| | |
|---|---|
| vpos 85 LONG | вход 64604.4 (13:50:18), размер 0.1547, 1R = 816.9 px = 1.2645% |
| стоп | 63787.5, **не двигался**; безубыток не применялся, частичка не бралась |
| **первый вердикт `close`** | **14:50:38**, conf 0.72 — сломанный тезис входа + смена режима |
| R в момент вердикта | между **−0.646R** (14:45:54) и **−0.561R** (14:50:58) |
| факт выхода | 63787.1 в 16:42:36, `reason=sl` → **−1.000R**, нетто **−137.31685** |

```
gross при закрытии по первому вердикту (64146.3): (64146.3 − 64604.4) × 0.1547 =  −70.8681
gross фактический                                                              = −126.43631
улучшение, gross                                                               =  +55.5682
нетто при закрытии (комиссии 4.99715034 + 4.96172)                             =  −80.8269
нетто фактическое (комиссии 9.931083, фандинг 0.949459)                        = −137.31685
улучшение, нетто (без фандинга, начисленного после 14:50)                       =  +56.4899
```

**Засчитано как IMPROVED.** Тальй с рестарта: **improved 1, worsened 0, neutral 1.**

Две поправки, без интерпретации: (а) «−0.61R» в тексте самого советника — это **движение цены за 1ч**,
а не P&L позиции; R позиции даны вилкой выше. (б) Нетто-улучшение не учитывает фандинг, начисленный
между 14:50 и 16:42 — он часть от 0.949459 и по-сэмплово не пишется. (в) В файле §3 стояло
«0 of ~10», а в §2.4 — «1 of ~10»; расхождение сведено к **2**.

---

## 9. ЧЕСТНЫЕ ОСТАТКИ

1. **`PARTIAL_FILL_MAX_DIVERGENCE` вне твоего перечня из пяти** (§1b). Одобрен дословно в первом
   сообщении, инертен, 0 читателей — но решение включить его принял я. Снимаемо одним коммитом.
2. **Байт-в-байт на СЛЕДУЮЩЕМ реальном входе не проверено** — входа не было (§5).
3. **Ранние возвраты дыр 5/6/7 не прогнаны end-to-end** — сознательно, ради чистоты выборки (§7).
4. **Пункты 11–14 не реализованы** — по твоей инструкции.
5. `.pyc` в `__pycache__` имеют штамп 17:28:31 — это мой `py_compile` при синтакс-проверке до
   рестарта, а не признак другого источника: хеш исходника совпал с коммитом.
6. Exit-advisor с рестарта консультаций не выдавал — **позиции нет**. Это ожидаемо, не дефект.
