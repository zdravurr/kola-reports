# kolya-4b71075a9a46

_2026-09-02 11:56 UTC_

---

# Индекс не влезал в контекст — теперь влезает целиком

**Что было.** Файл-инструкция `AGENTS.md` вырос до 44 318 символов при лимите загрузки ~25 000.
Всё, что ниже лимита, в контекст сессии не попадало ВООБЩЕ — а там лежали разделы про
Дирижёра, про то, кто может рулить телефоном, кто может сам отправить в аккаунт, кто только
алёртит, кто чинит инфру, и итоговое «ГЛАВНОЕ». Правило, которого нет в контексте, не
действует — его нарушают не назло, а вслепую.

**Что сделал.** Ужал до 24 685 символов — грузится целиком. Ничего не выкинуто, всё
переложено дословно:
- поимённый каталог автономных драйверов A–E → `docs/AUTONOMOUS_DRIVERS.md`;
- базовые вендорские правила OpenClaw (групчаты, инструменты, хартбиты) → `docs/AGENTS_BASE.md`;
- на их месте — короткие врезки с классами A–E, правилом «не объявлен = запрещён» и
  указателями на файлы.

Красные линии, работа с памятью, законы оркестратора и «ГЛАВНОЕ» остались в самом файле
дословно. Арифметика сходится: 24 685 + 15 442 + 5 790 = исходный объём плюс врезки.
Контракты: **200/200 зелёные**.

**Что нашлось попутно и заведено отдельной задачей.** `ACCOUNTS.md` (мануал по аккаунтам) —
28 544 символа, тоже за лимитом. И хуже: стартовый хук вываливает ACCOUNTS+STATUS одним
куском на 29.5 КБ, а в сессию попадают только первые 2 КБ — это видно прямо в этой сессии.
То есть правила по аккаунтам сейчас доезжают почти никак. Задача `a0bf4cec4c0e` заведена,
чинить тем же способом; часть правки лежит вне воркспейса (сам хук) — там понадобится либо
перенос его тела в `scripts/`, либо доступ.

**Тупик по дороге (заведён и закрыт своей задачей).** Написал скрипт-разноску
`scripts/agents_md_split.py`, но запустить новый скрипт из tech-сессии нельзя — упирается в
апрув. Сделал ту же работу штатными файловыми инструментами.
