# kolya-b6aa2b02713e

_2026-09-20 22:33 UTC_

---

# Само-разбор 20.09 09:00 UTC — отчёт техника

## Что чинил
Красная строка инварианта песочницы само-разбора:
`🔴 файлы workspace принадлежат botuser (root-овых: 1)`.

## Корень (закрыт)
**Инвариант назвал ЧИСЛО, но не ПУТЬ — улика самоуничтожалась до прихода техника.**

Замер `verify_self_improve_channel()` считал строки вывода `find -user root` и печатал только
счётчик. Нарушитель транзитный: к моменту сессии техника (13 ч спустя) файла в workspace уже
нет — `find` даёт 0, root-процессов нет, назвать виновника нечем. Поэтому красный повторялся
(17.09 и 20.09, оба ровно в час дайджеста 09:00 UTC, при 57 зелёных замерах) и не мог быть
закрыт ни разу: каждая сессия начиналась с нуля улик.

Второй слой, найденный там же: `n` считался по `stdout`, а `returncode` не читался. Упавший
или недошедший `find` (нечитаемый каталог, таймаут) даёт пустой stdout → `n=0` → **✅ зелёный
без замера**. Инвариант мог объявить себя выполненным, ни разу не измерив.

## Что сделано
`scripts/autonomy/self_improve_daily.py`:
- `find` печатает `путь|mtime|размер`; красная строка называет до 3 путей (`+N` остальных);
- улика пишется в `.kola_state/workspace_root_owned.json` (путь, час рождения файла, размер,
  rc замера; последние 20 записей) — **переживает сам файл**, так что следующий красный придёт
  к технику с именем нарушителя, даже если файл уже удалён;
- `rc != 0` при пустом выводе = `🔴 владение файлами НЕ измерено (find rc=…)` — fail-closed
  вместо прежнего зелёного.

Контракт на регресс: `tests/test_ownership_invariant_names_the_path.py` — красный обязан
называть путь; улика обязана лечь на диск; неизмеренное не зелёное; доказанный ноль зелёный.

## Проверка
- набор контрактов: **345/345 зелёных за 52 с**, новый контракт **1/1 зелёный**;
- живой прогон замера на текущем workspace: `✅ root-овых: 0` (root-овых файлов сейчас нет —
  что и подтверждает транзитность нарушителя).

## Что в дайджесте НЕ моё (не трогал)
- `[social_x] cdp-guard блокирует вход Огня` — контенция `tab_lock` на порту 19222, путь
  фикса недоступен, стоячая эскалация 3×. Не ре-эскалирую.
- `🔴 FB группа SoCal EV: посл. пост 38 ч` — системный корень конвейера постинга, эскалация
  от 24.07 в силе.
- `📥 Свип инбокса: client_pending — yielded` — законная уступка покупателю, не дефект.
- X-охват `@zdravurr_ca −22%` — контентная гипотеза (разворот-хук + named_player), не техника.
