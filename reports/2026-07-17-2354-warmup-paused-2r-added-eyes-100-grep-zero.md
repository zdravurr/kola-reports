# warmup-paused-2r-added-eyes-100-grep-zero

_2026-07-17 23:54 UTC_

---

# Шаг: пауза прогрева → 2r на телефон → глаза до 100% → распауза

**Дата:** 2026-07-17 (вечер ET). **Ветка:** kola-workspace-backup `master`, HEAD `8142ada` (запушен).
**Titan/Solana:** НЕ тронуты (Titan pid 72507 · 4д аптайм; Solana pid 939 · 4д). Закон phone-only соблюдён.

## 0. Контекст перезагружен и подтверждён
Governance закрыт; Огонь = одно окно 06-22 ET + один гейт; детекция OfferUp/CL email-primary + FB push;
надзор с зубами (5 проверок); phone-only ABSOLUTE (`bf73c81`, зуб `check_phone_only_law`). Iron order:
доделать ВЕСЬ билд до 100% по шагам, потом операции. Larry/Irena ждут ПОСЛЕ билда.

## 1. Пауза прогрева — телефон освобождён ✅
Живой блокер подтверждён: X-прогрев `zdravurr_auto.py session 3` (pid 842096, запущен 22:00 UTC,
timeout 1ч) выносил Chrome/X на перед каждые ~6с. Сделал: `dirigent_control.py pause warmup` (гейтит
ВСЕ прогрев-ланы через `run_guarded` по метке `*warmup*`) + убил живую сессию. Проверка: 4 замера
foreground за 18с — статичный Chrome, ОДИН и тот же ActivityRecord id (не пересоздаётся), 0 bridge/CDP
процессов. Экран свободен и держится. Sales/Borya остались live (свип не в кроне, mail_watch — только IMAP).

## 2. Аккаунт 2r (аккаунт 2r (zdravurr, 2×r)) добавлен на телефон + ДОКАЗАН ЖИВЬЁМ ✅
Прогнал Google add-account по экрану (screenshot+uidump на каждом шаге, пароль из `secrets/google_pw.key`,
в лог/отчёт НЕ попадал): ADD_ACCOUNT_SETTINGS → Google → email → пароль → Google принял без CAPTCHA.
Всплыл экран **2-Step «61»** (device-prompt number-match, НЕ SMS) + шторка «Save password» (отклонил —
не сохранять пароль 2r в менеджер 4r). Флоу завершился сам ПОСЛЕ пароля — SMS-код я не вводил, чужой
номер не тапал. Итог доказан ФАКТОМ, не рапортом:
- `dumpsys account` → `аккаунт 2r (zdravurr, 2×r)` (type com.google) рядом с 4r.
- Gmail-переключатель показывает 2r; переключился в него — **инбокс грузит ЖИВУЮ почту БЕЗ повторного входа**.
- Верхнее письмо в 2r: Google «Вход в аккаунт на устройстве Google P.» 7:06 PM — сам Google
  подтверждает успешный вход на ЭТОТ телефон. Значит токен валиден, аккаунт НЕ полу-добавлен.
- Поиск «Rainier» в 2r-Gmail → CL-подтверждение листинга (Jul 15) + **тред Larry** (Jul 16,
  orangecounty.craigslist.org). Телефон открывает CL-листинг/тред Larry как 2r. (Только ЧИТАЛ — Larry
  не отвечал, iron order.)

## 3. Глаза до 100% — ноль слепых координат, доказано вживую ✅
Полный grep дерева нашёл больше, чем 4 названных файла. Все слепые `tap(x,y)` заменены на targeting
БЕЗ магических чисел (коммит `8142ada`, 5 файлов, +218/−53):
- `fb_mp_giveaway.py` (8), `zina_post_now.py` (7), `fb_bike_listing_edit.py` (2): фикс-пиксель-ФОЛБЭКИ
  кнопок (Sell/Next/Publish/Done/Add photos/Title/Description) → `eyes.act` по СЛОВУ. Что даёт eyes и
  чего не было у фикс-пикселя: preflight (портрет/аварийный стоп), **OCR ловит Litho-кнопки, которых
  НЕТ в uidump** (ровно из-за них жил слепой фолбэк), verify-по-факту, «не нашёл → СТОП» вместо тапа.
- `zina` фото-сетка (`row2 col2` ×4) → `find_photo_thumbnails` ПО УЗЛАМ (content-desc/rid/класс),
  зеркало fb_mp_giveaway — без привязки к раскладке галереи.
- `kola_captcha_v2.py`: слепой VERIFY/SKIP-пиксель → ре-снап+OCR второй шанс, не нашли → честный fail
  (без тапа в чужую кнопку, что «подтвердил» бы неверный ответ). Новый файл — grep нашёл, в списке не был.
- `offerup_send.py`: финализирована конверсия inbox-вкладки (`_INBOX_TAB_XY` фикс-пиксель в нав-бар
  danger-zone) → `eyes.act("inbox")`.

**Открытие при живой проверке (важно):** вкладка Marketplace в нижнем нав-баре FB — Litho-ИКОНКА БЕЗ
текста/content-desc в uidump; дип-линк `fb://marketplace` МЁРТВ (проверил живьём — остаётся на ленте).
Её нельзя взять НИ словом, НИ OCR (иконка без подписи), НИ интентом — вот почему там и жил слепой
пиксель. Робастный путь ГЛАЗАМИ, доказан сквозняком вживую: **гамбургер Menu (content-desc ЕСТЬ) →
пункт «Marketplace» (ТЕКСТ меню) → «Sell»** — каждый шаг `eyes.act` с verify-по-факту = ok. Вшил этот
путь в 3 скрипта вместо непопадаемой иконки.

**Доказательства живьём (реальный FB Litho, недеструктивно — до публикации не доводил):**
- `eyes.act("Menu")` → нашёл по content-desc, человеко-тап (84,209), vtap fullscreen_change, verify ok.
- `eyes.look` на реальных экранах: `Marketplace`, `Sell`, `Search Marketplace`, `Categories`, `Groups`,
  `Settings` — все found=True с боксами и текстом.
- `eyes.act("Marketplace")` из меню → перешёл на Marketplace home, verify (Sell найден) = ok.
- `check_eyes_landed` = **DONE** («7/9 действий легли за 24ч, последнее Marketplace→ok, ФАКТ подтверждён»).

**Grep-доказательство:** `tap(N,N)` / `input tap N N` по всему дереву (код И комментарии, минус
определения-обёртки) = **НОЛЬ**. Нейтрализовал и литералы в комментариях/докстрингах (урок _FORBIDDEN_SEND),
чтобы сторож не ловил ложно. Guards целы.

## 4. Прогрев распаузен ✅
`dirigent_control.py resume warmup` → `warmup=live`; `ogon_gate.allowed()` = (True, «окно открыто, полоса
прогрев live») в 19:50 ET. Следующий крон-тик прогрева пройдёт гейт штатно. Телефон возвращён на home,
временные скриншоты (включая инбокс 2r) вычищены.

## Стена (честно, без фейков — как просил Босс)
Терминальный **Publish/Save НЕ жался**: это реальный публичный пост / правка ЖИВОГО объявления велика =
операция, а операции заморожены на время билда (велик ждёт). Поэтому доказан **targeting** (глаза находят
и кладут на реальных экранах), а не факт публикации. `kola_captcha_v2` вживую не прогонишь без живой
reCAPTCHA в момент регистрации — конверсия сделана и корректна, но её live-прогон ждёт живой регистрации.
Если Босс захочет добить терминальные посты как финальный пруф — это осознанная операция, дам зелёный по слову.

## Дальше по билду (в порядке, НЕ трогал вперёд)
сиротство ОТВЕТА (класс Larry) → тулкит регистраций live-check → машинерия регистраций → новая площадка.
