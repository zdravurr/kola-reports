# kolya-0a7353de1127

_2026-09-17 22:13 UTC_

---

# 0a7353de1127 [infra] — «способность warmup_output мертва» (×3) ЗАКРЫТА

## Что было
Сторож `fault_realert` третий раз будил техника: «прогрев НЕ ПРОИЗВОДИТ: @zdravurr_ca 0 из 1 постов»,
способность помечена МЁРТВОЙ, лестница и общее восстановление её «не поднимали».

## Корень (не симптом)
Прогрев был ИСПРАВЕН. Он законно уступил экран ДЕНЬГАМ, а судья этого не умел прочитать.

ФАКТ по `warmup_health.log`, слот постов 20:00 UTC @zdravurr_ca:
- 20:00:20 — заход стартовал и сразу уступил money-path: покупатель на телефоне;
- 20:42:48 — заход убит СНАРУЖИ (SIGTERM) на 22.7-й минуте (не крон-таймаут);
- 21:20:09 — снова уступка покупателю; слот помечен **ОТЛОЖЕН, а не потерян**
  (`warmup_resume_pending`, подхват кроном каждые 10 мин).

Фикс от 12.09 (`lawful_slot_loss` — спрашиваем ТОГО, КТО ЗАПИСАЛ ОТКАЗ захода) лёг
**только на ветку комментов**. Ветка ПОСТОВ в `ogon_quota.check` и судья способности
`capability_audit.cap_warmup_output` знали лишь законный ноль ПОЛОСЫ (стоп/пауза Босса).
Пост и коммент теряются одним и тем же способом — а спрашивали владельца отказа только за коммент.
Итог: недобор одного законно отобранного захода читался как «способность мертва» → три сессии техника
на здоровом прогреве.

## Починка
1. `lib/ogon_quota.py` — добавлена норма `POSTS_PER_SESSION` (1 пост = 1 заход, выведено из крона);
   ветка постов перед тревогой спрашивает `lawful_slot_loss` и пишет `v["posts_lawful"]`.
   Недобор больше нормы ОДНОГО захода законным не объявляется (долг прошлых слотов по-прежнему красный).
2. `lib/capability_audit.py::cap_warmup_output` — долгом считается дефицит **без** `posts_lawful`;
   законный недобор возвращается ЖИВЫМ, но с НАЗВАННОЙ причиной (молча не глотаем).

## Проверка
- Судья способности сейчас: `(True, 'недобор законен: заход слота 20:00 UTC законно отобран
  (улика: ⏸ законный отказ — уступил money-path: покупатель на телефоне)')`.
- `run_contracts.py`: **317/317 зелёных** за 54 с.

## Что осталось (не эта задача, красное честно)
Дефицит КОММЕНТОВ @zdravurr_ca = 5 (легло 1 из 6) остаётся красным и правильно: он больше нормы
одного захода, то есть это не один отобранный слот, а накопленный недобор. Его судят свои сторожа.
