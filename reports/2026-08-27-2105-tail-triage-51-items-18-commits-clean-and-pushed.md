# tail-triage-51-items-18-commits-clean-and-pushed

_2026-08-27 21:05 UTC_

---

# ХВОСТ 24–27.08 РАЗОБРАН ПОФАЙЛОВО И ЗАКОММИЧЕН: 51 позиция → 18 коммитов, дерево чистое, запушено

**27.08.2026, 21:0x UTC.** Продолжение сессии «event-driven verified and half-commit completed»
(15:58/16:00). Титан и Solana НЕ ТРОНУТЫ ни одним байтом.

---

## 0. ПЕРВОЕ, ЧТО ПРИШЛОСЬ ИСПРАВИТЬ — АДРЕС РАБОТЫ

`git status` в `/root` показал **ЧИСТО**, коммита `f8820dc` там нет вовсе. Хвост 45 файлов
живёт в ДРУГОМ репозитории: `/home/botuser/.openclaw/workspace`
(origin `zdravurr/kola-workspace-backup`). Разные репо, разные владельцы — `/root` это канон
Титана, воркспейс это канон Коли. Дальше всё — про воркспейс.

## 1. ГДЕ ОСТАНОВИЛАСЬ ТРИАЖ ДО ОБРЫВА (возобновил, не начал заново)

По времени коммитов и mtime файлов видно ровно:

| время | что произошло |
|---|---|
| 15:50–15:51 | `f8820dc`, `8161223` — событийная цепочка и само-разбор (были запушены) |
| **16:11–16:12** | тронуты `zdravurr_auto.py` и его контракт — начало разбора пункта №3 |
| **16:27** | ТРИ коммита: `2a6213f` reply_sanity, `5049d9f` claude_cli, `1395663` wake_turn+wake_delivery_guard |
| дальше | обрыв. Три коммита остались НЕзапушенными (ahead 3) |

То есть до обрыва разобрано 5 файлов из 56. Я продолжил с шестого, ничего не переделывая.

## 2. 🔴 ИСПРАВЛЕНИЕ ПРОШЛОГО ЗАМЕРА: БЫЛО 137/137, А НЕ 136/137

Отчёт 16:00 говорил «136/137, падает `test_missed_post_slot_is_caught_up`». Мой первый прогон
дал ДРУГУЮ пару: 135/137, падают `test_one_warmup_at_a_time` и
`test_empty_inbox_read_is_not_proof_nobody_waits`, а missed_post_slot — ЗЕЛЁНЫЙ.

Корень: **контракты судят того, КТО ИХ ЗАПУСТИЛ.** `test_one_warmup_at_a_time` читает
`crontab -l` — у root там 0 строк прогрева, у botuser 10. Запуск от root даёт ложный провал.
Тот же прогон от botuser: **137/137, все зелёные.**

Класс тот же, что мы ловим везде: *тест мерил не тот вход, о котором говорил* — здесь он мерил
**чей это crontab**, а объявлял «расписание прогревов прочитано».
Практический вывод на будущее: **контракты гонять только `sudo -u botuser`.**

А `missed_post_slot` зелёный потому, что прошлая сессия успела починить его в 16:11 — см. п.3.

## 3. ПУНКТ БОССА «zdravurr_auto.py — доделать ИЛИ откатить»: ДОДЕЛАН ПРАВИЛЬНО

Проверено чтением, не на слово. Починка сделана **не расширением байтового окна**, а снятием
второго судьи — ровно как приказано:

* решение о публикации вынесено из `if __name__ == "__main__"` в функцию
  **`decide_do_post(cron_says_post, posts_today) -> (do_post, reason)`**;
* контракт теперь **импортирует и вызывает ЕЁ** на 8 случаях + 2 инварианта. Прежний тест
  судил РАССТОЯНИЕ в байтах от якоря (правка отодвинула блок с 1703 на 2630 байт → красный при
  живой логике) и держал СВОЮ копию арифметики, которая уже отстала: у неё «слот 16:00
  публикует ВСЕГДА», хотя код обязан молчать на закрытом дне;
* закрыт настоящий баг: 25.08 день получил ДВА поста (10:02 догон + 16:03 свой слот), потому
  что потолок держался ПОЗИЦИЕЙ В КРОНЕ, а не фактом.

⚠️ **Развилка, которую решает Босс, а не умолчание.** При недоступном счётчике постов поведение
стало **fail-open** (идём по ключу крона) вместо прежнего fail-closed. Это сохраняет боевое
поведение и не платит сутками тишины за каждый сбой счётчика, но означает: сломанный счётчик в
слот 16:00 может дать второй пост за сутки. Названо вслух в докстринге и в коммите.

## 4. 18 КОММИТОВ, ПО ОДНОМУ ЛОГИЧЕСКОМУ ИЗМЕНЕНИЮ

Каждый файл прочитан диффом целиком; ни один не взят «на глаз». Проверено дополнительно:
все 41 модуль **компилируются**, все `lib/*` **импортируются от botuser**, ни одного
`TODO/FIXME/pdb/конфликт-маркера` в добавленных строках, и **каждый вызываемый символ существует**
(`turn_session_key`, `inflight_targets`, `pair_still_answerable`, `system_text_reason`,
`_goal_belongs`, `not_inbox_why`, `mark_reached_work`, `understand_detail`, `all_items`,
`is_unpublishable_text`, `is_dead/due_for_reprobe/note_probe` — все на месте).

| # | коммит | суть |
|---|---|---|
| 1 | `44f4c55` contracts | «я репетиция» — метка ОТДЕЛЬНО от «где моё состояние»: 488 фальшивых `kind=review` в боевом журнале с 17.08 |
| 2 | `26df38a` money-path | служебный текст отсекается на ВСЕХ дверях: `kola_hands.type_text` бил в `input text` мимо обоих чокпойнтов |
| 3 | `a1fe0d9` phone | опции SSH-мукса из одного места; заклинивший мастер = 45 с молчания зрения телефона |
| 4 | `c30ab08` inventory | велик `sold` В КАНОНЕ + доклад читает статус с живыми перекрытиями (иначе «for_sale» через сутки после продажи) |
| 5 | `faa95fa` agents | лана автоматики `:auto` (гейтвей перезапускал нашу сессию из-под хода) + жёсткий `timeout -k` (ход 226 мин / 23.2 М при пределе 600 с) + `/proc` как запасной путь `_ps()` |
| 6 | `ad529dd` tech-queue | `mark_reached_work` не звал НИКТО → 6 сессий на задачу вместо 3; бюджет переведён с константы 2.1 М на ЗАМЕР (реально 3.6 М) |
| 7 | `faab52d` screen | свой собственный флаг вытеснял свою же работу — КАЖДЫЙ тик `fb_inbox_sweep` кончался `preempted` |
| 8 | `c1466ca` hands | пока экран взят за лида, в ЧУЖОЙ тред со списка чатов не ходим |
| 9 | `ad4fcdd` sales | счёт без единого входящего СЛОВА снимается; отравленный тред не бывает «отработанным» |
| 10 | `eb427b1` delegation | окно делегирования = окно ЛИДА (потолок 40 мин выкидывал ждущих НАВСЕГДА); техзадача только по факту лечения (было ×29 за 13 мин) |
| 11 | `2fc2e05` nav | «дно списка» объявлялось по `content-desc`, которого на кадре зрения нет ВООБЩЕ |
| 12 | `57fc66d` look | FB-инбокс разбирался только как dict, а приходит КОРТЕЖ → человек парсил список глазами каждый раз |
| 13 | `e39629a` oauth | лестница рубежей ВСЕГДА отдавала 7.0 → один крик 12.08, 7 суток тишины, 19.08 «Not logged in» ушло покупателю |
| 14 | `f7cdbda` gateway | шум журнала — это version skew контрол-панели, а не рогатый процесс |
| 15 | `51c0ac5` staleness | сторож печатал команду починки и не выполнял её: 108 ЧАСОВ, 33 незагруженных модуля |
| 16 | `cff8028` growth | закреплённый пост копил просмотры вечно и выдавал себя за охват; кап @zdravurr 5→8 |
| 17 | `c816808` x | фантомная вкладка `47` (протухла 05.07) отдавалась вслепую; `/json/new` на Android НЕ РАБОТАЕТ (405/500) → `Target.createTarget`; потолок дня в обе стороны |
| 18 | `4425f94` contracts | `tq.pump() is None or True` — тождественная истина, зелёная при любом поведении |

## 5. ЧЕТЫРЕ `.bak-*` УДАЛЕНЫ — НО СНАЧАЛА ДОКАЗАНО, ЧТО ТЕРЯТЬ НЕЧЕГО

Не «оригиналы вроде бы в git», а побайтовое сравнение:

* `loop.sh.bak-interval` — ИДЕНТИЧЕН `projects/dirigent/loop.sh` @ `dad6888`
* `pixel_keepalive.sh.bak-nowake` — ИДЕНТИЧЕН @ `e1ac350`
* `mail_watch.py.bak-unaryplus` — ИДЕНТИЧЕН @ `e1ac350`
* `inbox_watcher.py.bak-eventdriven` — точного коммита НЕТ (это промежуточное состояние, ушедшее
  в `e1ac350` вместе с событийной правкой). Проверено иначе: дифф `.bak` → текущий HEAD-файл
  состоит **ТОЛЬКО из добавлений** (`391a…`, `481a…`, `483a…`, `487a…`), ни одного удаления и
  ни одного изменения. Содержимое `.bak` — строгое ПОДМНОЖЕСТВО закоммиченного.

## 6. ИТОГОВАЯ ПРОВЕРКА

* **Контракты: 137/137 зелёных** (от botuser, полный прогон после всех коммитов).
* **git: чисто.** `git status --porcelain` → 0 строк. Красного не осталось.
* **Запушено:** `8161223..4425f94 master -> master`, `master...origin/master` без расхождения
  (ушли и три вчерашних коммита 16:27, висевших как ahead 3).
* **capability_audit: 1 сломанная способность — `browser_tab`**, и она НЕ от этой работы.

### Про `browser_tab` — честно, без списывания на «оно само»

`/json/version` не отвечает. Но это не поломка кода, а **живая борьба за экран**, и сторож ведёт
себя ПРАВИЛЬНО:

```
20:50:21 ✅ вылечен ЯРУСОМ 2 (форграунд + лечение вкладки)
20:55:03 ok tier0: мост уже проверен рабочим
21:00:16 ярус 2 ПРОПУЩЕН: экран держит ЧУЖОЙ активный драйвер (xwarmup pid=1740956) — не отбираю
21:05:13 ярус 2 ПРОПУЩЕН: тот же pid=1740956 — не отбираю (вернёмся следующим тиком)
21:05:13 🔴 Ярус 3 (перезапуск adbd) НЕ запускался: он сносит ВСЕ форварды
```

Дождался ДВУХ тиков подряд, а не сделал вывод по одному. Экран держит живой FB-прогрев
(`fb_zdravurrrr_warmup.py`, pid 1740956, запущен под `timeout 2700` — то есть может держать
до ~45 мин). Сторож `cdp_bridge_guard` тикает каждые 5 мин, в 20:50 вылечил мост ЯРУСОМ 2, а
в 21:00 и 21:05 сознательно НЕ отбирает экран у чужого активного драйвера. Ярус 3 (рестарт
adbd) не трогаю: именно он раньше и ронял мост, и требует явного `--allow-adbd-restart`.
Это ровно тот класс, который у нас уже записан законом: **rc=143 / чужое приложение на экране =
ОТОБРАННЫЙ ЭКРАН, а не падение.** Мост поднимется следующим тиком после прогрева; насильно
отбирать экран ради зелёной строчки в аудите — это чинить термометр.

---

## 7. ЧТО ОСТАЛОСЬ НА СТОЛЕ (не спрятано)

1. **Развилка fail-open в `decide_do_post`** (п.3) — решение Босса, не моё.
2. **Кап @zdravurr 5→8 фолловов/день** применён по приказу 26.08 «упор на подписчиков»; это
   боевое изменение поведения, а не рефакторинг — называю отдельно.
3. **`browser_tab`** — ждёт конца прогрева; сторож живой, вмешательства не требует.
