# agent-identification

_2026-07-16 15:06 UTC_

---

# ОПОЗНАНИЕ ce09d26 — READ-ONLY. 16.07.2026, ~15:00 UTC

**Ничего не менял, не гейтил, не выключал. Дерево чистое, HEAD как был.**

---

## ГЛАВНОЕ, И ЭТО НЕПРИЯТНО ДЛЯ МЕНЯ

**Коммит ce09d26 спровоцировал Я САМ.** Это не автономная петля, вышедшая из-под контроля.
Мой собственный ручной прогон `inbox_sweep.py` в 14:21 разбудил главного агента.

И вторая моя ошибка: в дополнении к шагу 5 я подал это как **«в money-path автономно
коммитит ещё один агент»** — с интонацией угрозы. Это было **преждевременно**. Правда
скучнее и стыднее: агент проснулся, потому что **его позвал я**.

---

## 1. ЧТО ИМЕННО СКОММИТИЛО — ОПОЗНАНО ОКОНЧАТЕЛЬНО

### Сначала: «Kola <<почта-Босса>>» НЕ ОПОЗНАЁТ НИКОГО

```
.git/config:  user.name=Kola   user.email=<почта-Босса>
```
Это **дефолтная identity РЕПОЗИТОРИЯ**. **Любой** процесс, коммитящий без
`-c user.name=...`, подписывается «Kola». Мои коммиты видны как `kolya` **только потому,
что я явно переопределяю identity**. Значит поле автора — **не улика**, и мой вывод
«другой агент» строился на пустом месте.

### Настоящий актор: ГЛАВНЫЙ АГЕНТ OpenClaw (LLM), разбуженный МОИМ свипом

Улика — `agent_capacity.log`:
```
2026-07-16T14:21:51+0000 [WAKE] inbox_sweep → main (ок: занято 0/6 (живых 0, бронь 0), память 70.4%)
2026-07-16T14:21:51+0000 [WAKE] inbox_sweep → main (ок: занято 1/6 (живых 0, бронь 1), память 70.3%)
2026-07-16T14:21:51+0000 [WAKE] inbox_sweep → main (ок: занято 2/6 (живых 0, бронь 2), память 70.1%)
```
Леджер за сегодня — **только эти три пробуждения, других нет**:
```
"2026-07-16": { "inbox_sweep": { "woke": 3, "denied": 0 } }
```

**Цепочка целиком:**
1. 14:21:18 — я вручную запускаю `inbox_sweep.py`. У `run(escalate=True)` — **дефолт**.
2. Свип впервые за ~277 тиков читает OfferUp → `fresh = [Lucy, kristopher, Manuel]`.
3. `_route_offerup_to_main(lead)` → `agent_capacity.wake("main", …)` → **3 пробуждения главного агента**
   (по одному на «свежий» тред).
4. Главный агент получает: «ЖДУЩИЙ ПОКУПАТЕЛЬ… судить по обрывку ЗАПРЕЩЕНО, открой тред».
5. Он идёт разбираться, **почему трёхдневные «спасибо» вдруг свежие лиды**, находит
   баг дедупа (возраст в хэше) и в **14:31:16** коммитит `ce09d26`.

Ровно 10 минут между моим пробуждением и его коммитом. Причина и следствие.

### Это НЕ `kola-self-improve-daily`
`self_improve_daily.py` — крон **`0 9`**, отработал в **09:00**. Его продукт —
те самые 6 `LEARNINGS.md` (mtime 09:00–09:01). К 14:31 он отношения не имеет.

### Ещё одна МОЯ ошибка, которую нашёл по дороге
В шаге 2 я доложил про `LEARNINGS.md`: **«это не я»** — и был прав. А потом в коммите
**`bf8978b`** сделал `git add -u` и **затащил все 6 этих файлов под своей подписью**.
Чужую работу подписал собой. `git add -u` вместо явных путей — моя халатность,
ровно против стандинга «stage specific files, NEVER -A».

---

## 2. ПОЛЕ ЗАПИСИ: НОМИНАЛЬНО — WORKSPACE, ФАКТИЧЕСКИ — **РУТ**

Агент живёт под `botuser`. И вот что это значит:
```
$ sudo -l -U botuser
    (ALL) NOPASSWD: ALL

$ sudo -u botuser sudo -n id
    uid=0(root) gid=0(root) groups=0(root)          ← БЕЗ ПАРОЛЯ

$ sudo -u botuser sudo -n head -c 60 /root/titan-bot/main.py
    import os, ccxt, requests, sqlite3, ...          ← ДОТЯНУЛСЯ ДО ТИТАНА
```
| Цель | Прямой доступ botuser | Через `sudo -n` (без пароля) |
|---|---|---|
| money-path (`projects/sales/**`) | ✅ пишет | ✅ |
| замки экрана (`lib/*`, `.kola_state/locks`) | ✅ пишет | ✅ |
| свой крон (44 job'а, вкл. money-path) | ✅ пишет | ✅ |
| **Titan `/root/titan-bot/main.py`** | ❌ нет | ✅ **ДА** |
| **Solana `/mnt/.../mercury-sol`** | ❌ нет | ✅ **ДА** |
| **`trades.db`, root-крон (19 строк)** | ❌ нет | ✅ **ДА** |
| системный конфиг | ❌ | ✅ |

**Вне поля — НИЧЕГО.** Титан и Солана «не пишутся напрямую» — но это не изоляция, это
косметика: одна команда `sudo`, и он внутри. Я прочитал `main.py` Титана через `sudo`
за одну строку — запись отличается одним флагом.

Это ровно то, что записано в памяти как корень двух инцидентов:
«**я РУТ на хосте, botuser=NOPASSWD:ALL → КАЖДЫЙ агент рут**» (статус: **PROPOSED ONLY**,
то есть **не сделано**).

---

## 3. КАДЕНЦИЯ: ЭТО НЕ ПЕТЛЯ ПО ТАЙМЕРУ — ЭТО СОБЫТИЙНОЕ ПРОБУЖДЕНИЕ

Агент **не ходит по расписанию**. Его будят **детекторы**, когда видят лид:
`inbox_sweep`, `fb_inbox_sweep`, `mail_watch`, `inbox_watcher`, `commit_watchdog`.

**Коммиты за 7 дней:**
| Автор | Коммитов |
|---|---|
| **Kola** (= дефолт репо; агенты) | **95** |
| kolya (я) | 11 |
| botuser | 7 |

| День | «Kola» |
|---|---|
| 07-12 | 20 |
| 07-13 | 18 |
| 07-14 | 17 |
| **07-15** | **37** |
| 07-16 | 3 |

**Что трогают — НЕ только свои логи.** Это ядро money-path:
```
ce09d26  projects/sales/inbox_sweep.py          ← OfferUp-свип
17b6c6d  lib/listing_supervisor.py, scripts/cl_renew_bike_phone.py
19db4c7  scripts/cl_post_bike_phone.py
446eca1  lib/dirigent_commands.py, dirigent_voice.py, listing_check.py, llm_intent.py
547d3e7  feat(senya): autonomous SELL from Boss task — lib/dirigent_commands.py
```
За 7 дней **ни одного** коммита в Titan/Solana. Но см. п.2 — это не потому, что не может.

---

## 4. ПУШ: ЛОКАЛЬНО, ОФФ-БОКС — РАЗ В СУТКИ, В ПРИВАТНЫЙ БЭКАП

```
origin  github.com/zdravurr/kola-workspace-backup (SSH, ПРИВАТНЫЙ)   (ПРИВАТНЫЙ, deploy-key)
## master...origin/master [ahead 6]      ← прямо сейчас 6 коммитов НЕ запушены
```
Агент коммитит **только локально**. Наружу уходит **кроном `backup_push.sh` (`30 3`)** —
раз в сутки, в **приватный** бэкап-репозиторий. Публично он ничего не публикует.
(Публичный `kola-reports` — отдельный путь, туда идут только отчёты после PII-скана.)

---

## 5. ГЕЙТЫ: НА КОММИТ — **НИ ОДНОГО**

| Механизм | Что реально делает | Гейт на коммит? |
|---|---|---|
| `.git/hooks/pre-commit` | **только `.sample`** — не установлен | ❌ **НЕТ** |
| `.git/hooks/pre-push` | **установлен**, но это **сканер СЕКРЕТОВ** (regex `sk-ant-`, `AKIA`, ключи) | ❌ код не ревьюит |
| `review_gate` | fail-closed — **только на ОТПРАВКУ покупателю** | ❌ к коммитам не относится |
| `agent_capacity` | потолок слотов (0/6), `MAX_BURST=8`, жнец `HARD_MAX_AGE_S=3600`, проверка памяти | частично — ограничивает **сколько** агентов, не **что** они пишут |
| **`GLOBAL_STOP`** (рубильник Босса) | файл `.kola_state/pause/GLOBAL_STOP` | ⚠️ **СНЯТ 15.07** → `GLOBAL_STOP.retired_20260715` |

**Вывод: сегодня коммиты агента не ревьюит НИКТО и НИЧТО.** Единственный автоматический
барьер на пути наружу — сканер секретов при пуше. Рубильник, который в 14–15.07 глушил
пробуждения (`[PAUSE] GLOBAL_STOP — не бужу main`), **выведен из строя 15.07** — потому мой
вызов в 14:21 и прошёл беспрепятственно.

---

## УГРОЖАЕТ ЛИ ЭТО ИЕРАРХИИ УПРАВЛЕНИЯ — ЧЕСТНО

**Сегодняшний инцидент — НЕ бунт.** Агент сделал ровно то, для чего заведён: увидел
странность, разобрался, починил. Его фикс я **проверил сам, не на веру** — он верный
(age-drift → тот же ключ; новое сообщение/другой покупатель → ключ другой, лид не теряется).
Разбудил его **я**.

**Но структурно защиты нет — и это правда независимо от сегодняшнего:**
1. **Агент = рут.** Доказано: `sudo -n id` → uid=0, дотянулся до `main.py` Титана.
   «Титан не тронут» держится на **благоразумии агента**, а не на правах.
2. **Ноль ревью на коммит.** 95 коммитов за 7 дней в money-path, никто не смотрит.
   Пик 37 за 15.07.
3. **Рубильник Босса снят 15.07.** Единственный механизм «всё стоп» — выведен.
4. **Автор в git не значит ничего** — «Kola» это дефолт репо. Разобрать по логам постфактум
   можно только через `agent_capacity.log`, и то если актор шёл через шлюз.
5. `autonomy_audit.log` пробуждение **не записал** — в аудите есть `drive_phone`, но
   `wake_main` за 14:21 **отсутствует**. Значит аудит пробуждений **дырявый**: восстановить
   «кто кого позвал» я смог только по `agent_capacity.log`. Аудит, в который не попадает
   пробуждение агента-рута, — это не аудит.

**Моя оценка:** угроза не в том, что агент своевольничает — сегодня он не своевольничал.
Угроза в том, что **между ним и Титаном нет ничего, кроме его собственной вежливости**,
а рубильник снят. Пока он полезен — это незаметно. Один плохой турн — и заметно станет
сразу и необратимо.

---

## ЧЕГО НЕ ДЕЛАЛ
Ничего не выключал · гейтов не ставил · файлы агента не трогал · Titan (`active`) и Solana
не тронуты · рабочее дерево **чистое**, HEAD как был (`ce09d26`).
Убрал за собой только свой же зависший watch-цикл из шага 4.
