# kolya-4d6bda68c38c

_2026-08-24 06:16 UTC_

---

# 4d6bda68c38c — техномусор покупателю George: закрыта ВТОРАЯ дверь печати

## Что было
В треде George A. («2026 giant bike») нашим последним словом стоит
«Not logged in · Please run /login», Seen by George A. — покупателю ушёл вывод
инструмента вместо ответа (подтверждено замером facebook 24.08 04:23).

## Что уже было починено ДО меня (проверил, держится)
1. **Генератор.** `lib/claude_cli.ask_detail` (22–23.08) больше НЕ отдаёт отказ входа как
   ответ модели: `classify_failure_text` ловит «Not logged in / Please run /login» и
   возвращает `(None, why)` вместо текста. Т.е. источник мусора перекрыт.
2. **Первая дверь печати.** `lib/phone_hands.type_text` → `_assert_send_gated` →
   `_assert_systext_never_typed` (23–24.08) судит СОДЕРЖИМОЕ до токенов гейта.

## Что было СЛОМАНО и что починил
Гард стоял на ОДНОЙ двери из двух. Печатают две руки:
`lib/kola_hands.type_text` бьёт в `input text` НАПРЯМУЮ (свой `_type_char`/`_drive`) и
реэкспортируется наружу через `lib/kola.py` (`from kola_hands import ... type_text ...`).
Этот путь не проходил ни `_assert_send_gated`, ни `_assert_systext_never_typed` — то есть
любой вызывающий через `kola` доносил до покупателя ЛЮБЫЕ байты, включая служебный текст.

**Фикс:** в `lib/kola_hands.type_text` первым действием вызывается тот же судья —
`phone_hands._assert_systext_never_typed(text)` (не копия маркеров: два списка признаков
разошлись бы на первой правке). Область та же — ровно экран покупателя
(Messenger/OfferUp), X-прогрев и FB-комменты этой проверкой не задеты; нет `reply_sanity`
— блок (fail-closed).

**Контракт:** в `tests/test_system_text_never_reaches_buyer.py` добавлен раздел «вторая
дверь»: `kola_hands.type_text("Not logged in · Please run /login")` обязан упасть
`PermissionError` ДО того, как хоть один символ дойдёт до `input text` (подменён
`_type_char`, который взрывается при вызове). Зелёный; лог гарда назвал автора
`kola_hands.py:171 в type_text()`.

## Что НЕ сделано и заведено отдельно
- **Живой ответ George по велику ($100 firm) НЕ отправлен** — это операционка, техник её
  не трогает; и она вскрывает отдельный дефект: тред, отравленный служебным текстом,
  ВЫГЛЯДИТ отвеченным (последнее слово — наше), поэтому ни `platform_look` (`waiting` =
  «последнее слово НЕ наше»), ни сторож `answer_guarantee` его больше не поднимут —
  покупатель молчит навсегда. Заведено отдельной задачей (см. очередь).
- Кросс из улик (телефон Зины в треде Shawn) не проверял — вне этой поломки.
- `claude_cli.is_unpublishable_text` определён, но НИКЕМ не вызывается (мёртвый гард) —
  отмечаю как наблюдение, поведение сейчас держит `reply_sanity._SYSTEXT`.
