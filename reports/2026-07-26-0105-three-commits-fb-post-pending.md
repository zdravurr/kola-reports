# three-commits-fb-post-pending

_2026-07-26 01:05 UTC_

---

✅ ТРИ КОММИТА ПО ПРИКАЗУ БОССА. Пост в EV-группу — ЧЕСТНО ЕЩЁ НЕ УШЁЛ (экран занят X-прогревом).

━━━ 1. inbox_watcher — ЗАКОММИЧЕНО ━━━
Коммит: b85e265
Гейт принял токен (KOLA_COMMIT_GATE_TOKEN из kola_secrets:commitGateToken):
  «🔓 COMMIT GATE: авторизованный коммит в защищённые пути (токен принят)»
Рабочее дерево по файлу чистое.

Что чинилось: `new listings?\b` БЕЗ якоря в УЖЕ ЗАКОММИЧЕННОМ коде (строка 179).
_NOISE_RE применяется к ТЕКСТУ входящего (is_noise, строка 230), а не только к пуш-титулам:
  «I saw your new listing, is it still available?» → is_noise=True → лид НЕ доезжал до main
Проверка после фикса: 9 шумовых строк — все HIT, 10 покупательских — 0 ложняков. PASS.
Заодно внесена staged-правка от 24.07 (4 паттерна против витрины OfferUp) — своих ложняков не даёт.

━━━ 2a. СТОЛКНОВЕНИЕ КРОНОВ — РАСПИСАНИЕ ПЕРЕСТАВЛЕНО ━━━
Было:  0 */2 * * *                      (каждый чётный час — лоб в лоб с X-прогревом)
Стало: 15 0,3,6,9,12,15,18,21 * * *
Бэкап старого crontab: /tmp/kola_crontab_backup_20260725T162330Z.txt

Машинная проверка разбега по КАЖДОМУ слоту (занятость: xwarmup 10/16/22 до 3600с,
xwarmup_ca 13/20/23:30 до 1800с, fbwarmup 11:30/17:30/23:30, evwarmup 19:00, coverage 14/21):
  00:15 свободно, 585 мин до следующего  → ✅ пост может идти
  03:15 свободно, 405 мин                → монитор (вне окна EV)
  06:15 свободно, 225 мин                → монитор
  09:15 свободно,  45 мин                → монитор
  12:15 свободно,  45 мин                → монитор
  15:15 свободно,  45 мин                → ✅ пост может идти
  18:15 свободно,  45 мин                → ✅ пост может идти
  21:15 свободно,  45 мин                → ✅ пост может идти
Ни одного занятого слота; минимальный разбег 45 мин при нужде ~20 мин.
Окно EV-поста = 15:00–03:00 UTC (08:00–20:00 PT), отсюда 4 постовых слота и 4 «только монитор».

Улика провала ДО фикса (досмотрел живьём):
  {"ts":"2026-07-25T16:15:04Z","kind":"session_screen_busy","err":"tab32 lock busy >900s"}
Причём это НЕ новость: fb_ev_group.json хранит BLOCKER_X_CRON_CONFLICT (10.07) и
..._AGAIN (12.07, «прождал до 20:30Z, потеряно ~20 мин»). Грабли лежали 2 недели.

━━━ 2b. ДВОЕНИЕ ТЕКСТА — ЗАКОММИЧЕНО ━━━
Коммит: 0da4841

Дефект №1 — очистка поля мерилась ТЕМ ЖЕ слепым read-back:
  _clear_field брал длину из _typed_text(), который в litho-композере отдаёт "" →
      cur="" → min(0+8,900)//25 + 1 = ОДНА пачка = 25 DEL
  На 370 символах стиралось 25, оставалось 345, и _type_raw дописывал текст ЦЕЛИКОМ.
  Ровно это глаза и доложили 24.07 19:10: «the tip text is DUPLICATED and merged with a glitch».
  Арифметика до/после (посчитана, не на глаз):
      316 симв: было 25 DEL → остаток 291 (ДВОЕНИЕ) | стало 375 DEL → остаток 0
      370 симв: было 25 DEL → остаток 345 (ДВОЕНИЕ) | стало 425 DEL → остаток 0
  Теперь: select-all через `input keycombination KEYCODE_CTRL_LEFT KEYCODE_A` (проверил —
  устройство поддерживает, есть в `input` usage) + DEL; фолбэк считает длину от ОЖИДАЕМОГО
  текста; если read-back зрячий — дожимаем до пустого и логируем остаток.

Дефект №2 — игла для зрения не существовала на экране:
  собиралась из ОТФИЛЬТРОВАННЫХ слов (len>3) и склеивалась обратно:
      «Free tip that saves EV buyers thousands…» → игла «Free that saves buyers»
      (выпали «tip» и «EV»)
  Проверено: старая игла `in text` = False, новая («первые 6 слов подряд») = True.
  То есть зрение НЕ МОГЛО спасти правильно набранный пост — ложный mismatch был неизбежен,
  дальше перепечатка и двоение (дефект №1).
Плюс: доклад зрения «текст на месте, но ЗАДВОЕН» больше не молчит — пишем text_typed_duplicated.

━━━ 4. ЛОЖНЫЙ ДИАГНОЗ «НОДА МЕРТВА N СУТОК» — ЗАКОММИЧЕНО ━━━
Коммит: acd1b12

Корень структурный, а не «LLM сглючил»:
  1) в analyze() летят ТОЛЬКО строки-ошибки (ERROR_RE). Успехи «[bridge] already up» не
     попадают туда НИКОГДА → мост, моргнувший и починившийся сам, выглядит мёртвым.
     Отсутствие успехов было артефактом фильтра, а читалось как улика отказа.
  2) в промпт кладётся хвост ВЧЕРАШНЕГО LEARNINGS с «мертва N суток» → вывод переписывается
     и инкрементится без перепроверки. Вчерашний вывод = сегодняшняя улика, счётчик растёт сам.

Фикс — bridge_liveness(): машинный замер (успехи моста в окне + живая проба _cdp_alive())
идёт в промпт отдельным блоком ФАКТ для social_x/fb_social/infra, с правилом: «нода мертва»
допустимо ТОЛЬКО когда за окно НЕТ НИ ОДНОЙ успешной активности моста; handshake-ретрай с
последующим успехом — самолечение. Плюс явно сказано, что отсутствие успехов в блоке ошибок —
артефакт фильтра, а не улика.

Прогон на ЖИВЫХ логах:
  успешных строк моста за окно: 27
  переходных ошибок хендшейка:   1
  живая проба CDP прямо сейчас: ЖИВ
  ВЕРДИКТ: МОСТ ЖИВ
27 успехов против 1 блипа — вот что 9 суток подряд докладывалось как «нода мертва».

━━━ 3. ЖИВОЙ ПРУФ ПОСТА — ЕЩЁ НЕТ, И Я ЭТО НЕ ПРЯЧУ ━━━
На 16:46 UTC экран держит X-прогрев (PID 2840403, 46 минут из возможных 60, timeout 3600).
Замок pixel.lock.owner = его. Журнал fb_active после 16:00 содержит ровно одну запись —
тот самый session_screen_busy. Постов сегодня НЕТ.

Скрин поста в ленте НЕ предъявляю, потому что поста НЕТ. Старый пруф не подставляю.

Что дальше (сторож взведён на освобождение экрана):
  • X-прогрев обязан закончиться не позже 17:00 UTC (жёсткий timeout 3600).
  • Как только отпустит — запускаю ШТАТНУЮ обёртку ev_group_recurring_post.sh (тот же путь,
    что дёргает крон) и снимаю скрин ленты группы.
  • Если не выйдет — следующее чистое окно: плановый крон 19:00 UTC, затем слот 21:15.
  • Слот 18:15 в этот раз промолчит: у self-heal кулдаун 3ч от запуска 16:00.

Важно: оба дефекта, валившие пост (2a и 2b), уже в коде. Следующий заход идёт по починенному
пути, а не по старому.

Титан/Солана не тронуты.
