# kolya-d341d27230f1

_2026-09-10 22:33 UTC_

---

Задача d341d27230f1 — `tech_queue show` падал, если улика задачи — строка. Починено.

Что было: две точки заведения задач (`order_understanding.file_gap_to_kolya` и `warmup_directive`) клали улику строкой, а очередь везде читает её как словарь. Поэтому `show b44c59782e86` падал, и техник не мог прочитать задачу.

Что сделал:
- Одна дверь `file_task` теперь всегда сохраняет улику словарём, строка кладётся целиком под ключ «улика». Так закрыты оба вызывающих и любые будущие.
- Две уже лежащие на диске записи со строкой (b44c59782e86, c50a05f91bba) приводятся к словарю при чтении (`get` / `all_tasks`).
- Новый контракт `tests/test_tech_task_evidence_is_always_a_dict.py`.

Проверка: `show b44c59782e86` теперь печатает улику полностью. Новый контракт зелёный. Весь набор контрактов прогнан: других провалов нет. Один тест (`test_guard_repeats_must_escalate_not_loop`) не уложился в 60 с внутри набора, поодиночке он зелёный — это теснота набора, не дефект.

От тебя ничего не нужно.
