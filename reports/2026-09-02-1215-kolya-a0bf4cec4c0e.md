# kolya-a0bf4cec4c0e

_2026-09-02 12:15 UTC_

---

# ACCOUNTS.md влез в лимит загрузки + написан дайджест для startup-хука

**Было:** ACCOUNTS.md = 28544 симв при лимите ~25000 — низ мануала по аккаунтам в контекст не попадал. Плюс SessionStart-хук печатал ACCOUNTS.md+STATUS.md целиком (~29.5КБ), а харнесс режет вывод хука на 2КБ — в tech-сессиях правила аккаунтов не действовали почти целиком.

## Что сделал

**1. ACCOUNTS.md ужат 28544 → 23325 симв** (−5219, под целью <24000). Историю/детали вынес ДОСЛОВНО в тематические файлы memory/, в мануале оставил стоячие правила + указатели:
- `project_x_startup_known_bugs.md` — полный разбор всех 8 багов старта/X-работы (сон Pixel рвёт CDP, dwell≠зависон, утечка преамбулы/самоответы, картинки EV, ложный «Post: OK», батарея). В ACCOUNTS.md вместо 7КБ текста — выжимка «что делать всегда»: x_bridge_ensure + x_preflight перед постингом; факт поста только через live-поиск; не тестировать на живом аккаунте Босса; фикс — сразу в оба скрипта; все посты с картинкой.
- `project_fb_active_warmup_details.md` — fb_active.py, баг прикрепления картинки в katana, ASCII-ввод.
- `project_fb_group_socal_ev_setup.md` — создание группы SoCal EV, обложка, TODO ведения.

Все три проиндексированы в memory/MEMORY.md. Ни одно правило не потеряно — только переехало из «грузится всегда» в «грузится по ссылке».

**2. Написал замену телу хука: `scripts/session_start_digest.py`** (в воркспейсе, как и требует граница). Печатает выжимку вместо дампа: вердикт аудита + ВСЕ не-зелёные строки (🔴/🟡/❔) + оглавление разделов ACCOUNTS.md + куда читать полностью. Сам гонит `kola_boot_status --if-stale 600` (тот же префикс, что был в хуке). **Замер: 1593 байта < 2048** — влезает в окно харнесса целиком, а не огрызком. Есть страховка: при переполнении режет оглавление с конца, вердикт и не-зелёное держит всегда.

## Что осталось и почему (заведено отдельной задачей `f3ea0080d953`)
Само тело хука лежит ИНЛАЙНОМ в `~/.claude/settings.json` — вне воркспейса, tech-сессии туда писать не разрешены (проверил: чтение/правка блокируются). Скрипт-замена готова и проверена; нужна одна строка — заменить команду хука на `timeout 60 /usr/bin/python3 /home/botuser/.openclaw/workspace/scripts/session_start_digest.py`. **Это требует апрува либо сессии с правом писать вне воркспейса.**

До этого шага хук всё ещё льёт файлы целиком, но главное уже вылечено: ACCOUNTS.md теперь целиком грузится памятью (23325 < 25000), т.е. правила аккаунтов действуют полностью, даже пока хук режется.

**Контракты: 200/200 зелёных.**
