# parked-clean

_2026-07-16 17:56 UTC_

---

# ПРИПАРКОВАН ЧИСТО — жду команды. 16.07.2026, 17:55 UTC

**Ничего не начинал. Ничего не менял. Ничего не в полёте.**

## 1. СТОП — соблюдён
Email-детектор **не трогал**: `mail_watch`, патрули, настройки FB — **ноль изменений**.
Решение по FB за тобой.
```
патрули в кроне : 2 ON (fb_inbox_sweep + inbox_sweep) — как были
git             : 0 изменений, всё запушено off-box
агентов в полёте: 0
экран           : свободен
удержанных патчей: 0
```
Единственное, что тронул после твоей паузы — **свои же заметки** (см. п.2) и ужал индекс
памяти: он подошёл к лимиту чтения (23.3КБ), а нечитаемый индекс как раз и убил бы смысл
парковки. Сжал до **16.6КБ**, **все 120 указателей целы**, ни один файл памяти не тронут.

## 2. SNAPSHOT записан в память
`memory/project_session_snapshot_16jul_governance_ogon.md` + первая строка в `MEMORY.md`
(«ЧИТАТЬ ПЕРВЫМ»). Свежая сессия поднимет контекст без тебя.

**Что в нём:**
- **Governance ЗАКРЫТ**: де-рут (botuser не рут, рут только у админа), commit gate
  (защищённые пути + токен), wake audit (581 строка, логируется в горле), аварийный стоп
  (один флаг, два горла, гибрид невозможен, сейчас выключен).
- **Огонь ЖИВ**: вкладка X восстановлена (VIEW-интент; `/json/new` на Android закрыт),
  постит по-настоящему (твит 16:59:17Z прочитан С X), петля прогрев→лид→прогрев замкнута,
  окно 06:00–22:00 ET (fin 06/12/18 · ca 09/16/19:30 ET).
- **Боря live** — работает только НОВЫЕ лиды; 4 старых припаркованы (`unpark_on=new_inbound`),
  проверено живьём на Robert'е.
- **⛔ ОТКРЫТО — решение по email-детектору:**
  OfferUp (7 живых роутов) и Craigslist — email-primary **доказан**.
  **Facebook: 34 письма за 30 дней, но о СООБЩЕНИЯХ — 1 за 10 месяцев, о Маркетплейсе — 0.
  НИКОГДА.** ⇒ патруль FB выключать нельзя, пока живое FB-письмо не увидим в `mail_watch`.
  Плюс **языковой баг**: FB шлёт РУССКИЕ темы (`💬 … прислал вам сообщение`), а `mail_watch`
  матчит **только английский** → совпадения не будет никогда. Чинить надо в любом случае.
- Мой факап дня зафиксирован, чтобы не повторить: `json.dump` в `secrets.json` **стёр права
  0600→0664** → gateway не стартовал 27 минут.

## 3. ЗДОРОВЬЕ — всё зелёное, оставлено работать как есть
```
gateway   : active | HTTP 200 | сторож: gw=alive inbox=alive
Дирижёр   : up   bridge: up   inbox_watcher: up
патрули   : ON (2) — НЕ ТРОГАЛ
полосы    : procurement live · sales live · warmup live
стоп      : выключен (норма)   окно Огня: открыто (13:5x ET)
де-рут    : botuser — «not allowed to run sudo»
commit gate: LIVE   wake audit: 581
TITAN     : active, pid 72507   ·   SOLANA: pid 939   ← не тронуты весь день
git       : чисто, запушено (master = origin/master)
```

## 4. ЖДУ
Не действую до твоей команды: **решение по FB** + следующее ТЗ.

Последний отчёт по стене (контекст для решения):
https://raw.githubusercontent.com/zdravurr/kola-reports/main/reports/2026-07-16-1741-email-detector-wall.md
