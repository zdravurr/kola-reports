# 26.08 11:57 UTC — ПАРОЛЬ ROOT ЗАМЕНЁН НА ПАРОЛЬ БОССА

Короткая операционная запись. Продолжение отчёта 11:50
(`2026-08-26-1150-internal-ruled-out-password-changed-and-22M-is-normal.md`).

## ЧТО СДЕЛАНО

Пароль `root` заменён на присланный Боссом. Сгенерированный мной час назад (11:41) больше
не действует.

```
ПРУФ (только дата, сам пароль намеренно НЕ приводится):
    passwd -S root   →  root P 2026-08-26
    chage -l root    →  Last password change : Aug 26, 2026
```

## КАК СДЕЛАНО — ЧТОБЫ ПАРОЛЬ НИГДЕ НЕ ОСЕЛ

* подан в `chpasswd` **через stdin**, а не аргументом командной строки — в `ps` он не
  появлялся ни на секунду (аргументы процесса видны любому пользователю системы);
* **временный файл не создавался**; прежний `/root/.newrootpw` был уничтожен `shred -u`
  ещё в 11:41 — проверено, файла нет;
* проверка на осадок: `grep -rl` по `/root`, по воркспейсу `botuser` и по репозиторию
  `kola-reports` — **совпадений ноль**;
* в этот отчёт, в Telegram-сообщение и в git пароль не попал и не попадёт.

## 🔴 ДВЕ ВЕЩИ, КОТОРЫЕ ОБЯЗАН НАЗВАТЬ, А НЕ ПРОМОЛЧАТЬ

**1. Пароль пришёл текстом в переписку.** В задании было сказано «отдельным каналом», но
фактически он прислан обычным сообщением и теперь лежит в истории чата. Канал переписки —
не хранилище секретов. **Считать пароль известным и сменить его сразу после того, как
заработает вход по ключу.**

**2. Шаблон «слово + год + знак» перебирается первым.** Это самая частая форма пароля в
словарях подбора. При этом вход `root` **по паролю у нас всё ещё открыт в интернет**:
`fail2ban` (bantime 1 ч, 5 попыток за 10 мин) перебор только ЗАМЕДЛЯЕТ, а не закрывает.
Пока нет публичного ключа Босса, **этот пароль — единственная защита сервера**: за ним
подписка Claude, ключи Титана/SOL, `.env` и секреты продаж.

## ЧТО ЭТО НЕ ЗАКРЫВАЕТ

Смена пароля закрывает ровно один сценарий — «старый пароль у кого-то был». Она НЕ отвечает
на вопрос, кто открыл сессию 26.08 02:00–02:52 (разбор — в отчёте 11:50: внутренняя версия
исключена полностью, соединение внешнее, личность недоказуема при входе по паролю).

Работает сторож, поставленный в 11:45: `/root/infra/ssh_login_alert.py` (крон root `*/2`) —
**каждый успешный вход теперь сам докладывает Боссу** (когда, кто, откуда, каким способом),
и курсор двигается только после подтверждённой доставки. Если кто-то войдёт — будет видно
в течение двух минут, без участия сессии.

## ОТКРЫТО — ЖДЁТ БОССА

1. **Публичный ключ** (`ssh-ed25519 AAAA…`). Порядок дальше жёсткий: положить ключ →
   проверить вход **второй** сессией, НЕ закрывая первую → и только после успешной проверки
   `PermitRootLogin prohibit-password` + `PasswordAuthentication no`. Выключать пароль
   раньше нельзя: рабочего ключа у `root` нет (консольный ключ DigitalOcean истёк 19.08),
   Босс останется снаружи.
2. **Решение по `kola-reports`** — делать приватным или нет. Приватный репозиторий ломает
   ВСЕ ссылки на отчёты, включая эту (`raw.githubusercontent` потребует токен). Три варианта
   перечислены в отчёте 11:50.
3. **Событийная цепочка покупателей** — `lib/lead_event_chain.py` написан и проверен
   синтаксически, но НЕ подключён и живьём НЕ проверен; `promise_executor` не начат.
   Ответы покупателям автоматически не уходят.

Титан и Solana не тронуты.
