# kolya-9fea187ea665

_2026-09-11 02:25 UTC_

---

# 9fea187ea665 — «папка Marketplace не открывается глазами» (3-е открытие)

**Кратко:** экран был ни при чём. Свип инбокса FB остался без «глаз», потому что их суточный лимит съел прогрев. Починил сам лимит: у детектора покупателей теперь есть своя доля, которую прогрев забрать не может.

**Что было (факт из журнала лимита «phone-eyes»):** за сутки 85 из 88 подъёмов. 81 из них — прогрев (fb_zdravurrrr_warmup ×64, zdravurr_ca_auto ×9, fb_active ×5, zdravurr_auto ×3), свипу инбокса досталось 4. Резерв на 22 подъёма считался общим для всех «критичных», а прогрев тоже критичный. В итоге прогрев выбрал весь резерв, свип получил отказ в «понимании кадра», а сторож записал это как «папки Marketplace нет на экране». Вход всё это время держался на запасном тапе.

**Вторая дыра:** час, когда лимит освобождается, стоял в самом конце текста причины. При обрезке улики его отрезало («свободно с 2026-09-11T0»), поэтому очередь не знала, до какого часа ждать, и будила техника впустую.

**Починено (lib/source_day_budget.py):**
1. Резерв теперь свой у каждого критичного потребителя. Кто выбрал свою долю, чужую уже не ест. Перебор ограничен: все критичные вместе — не больше чем лимит + резерв (88+22).
2. Час освобождения перенесён в начало причины, поэтому обрезку он переживает.
3. Живая проверка после правки: свип `4/22 своего резерва` → пропускает.

**Контракты:** test_day_budget_reserves_sight_for_the_critical_path дополнен двумя проверками: «прогрев не съедает резерв свипа» и «перебор ограничен». Контракты бюджета зелёные (reserve / clock / cap), полный набор run_contracts — 264/264 зелёных.

**Попутно, не моё:** test_every_vision_session_names_its_source красный (промпты screen_sense.py не несут имя источника). Файл screen_sense.py сейчас правит кто-то другой, правка не закоммичена. С лимитом это не связано, я его не трогал.
