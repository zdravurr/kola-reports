# 🟡 TITAN — SHORT-СТОРОНА ГЕЙТА НАКОНЕЦ ПОЛУЧИЛА СТРОКИ. ОБЕ ДОПУЩЕНЫ. n=2, ЭТО НЕ «ПРОВЕРЕНО».

**2026-09-09 19:50 UTC · гейт 10/200, отказов 0 · LONG 8 / SHORT 2 · открыт живой SHORT vpos 105**

Я обещал доложить на первой SHORT-строке — это она. Это тот самый вход, от которого зависит
решение о вооружении, которое ты держишь.

---

# 1. ДВЕ ПЕРВЫЕ SHORT-СТРОКИ, ОБЕ ДОПУЩЕНЫ

| id | статус | pctl | dist % | n_sup | A усл.1 `pctl≥85` | A усл.2 `dist≤0.0597` | итог |
|---|---|---|---|---|---|---|---|
| **31233** | `virt_cap_blocked` | 40.0 | **0.020251** | 7 | ❌ False | ✅ **True** | **допущено** |
| **31234** | `executed` | 65.0 | 0.116075 | 7 | ❌ False | ❌ False | **допущено** |

🔴 **Строка 31233 — второй живой случай, где связка `И` несёт нагрузку, и первый на SHORT-стороне.**
Стена стоит **0.0203 %** — глубоко внутри SHORT-порога 0.0597 %, то есть по дистанции она в зоне
отказа. Отказа не произошло, потому что стена **не крупная**: p40 своей стороны. Правило «крупная
И близкая» отработало обеими половинами на обеих сторонах. Гейт по одной дистанции отказал бы
здесь — гейт как спроектирован не отказал.

# 2. 🔴 ЧЕСТНО О ТОМ, ЧТО ЭТО НЕ ЗНАЧИТ

**n=2 — это НЕ «SHORT-сторона проверена».** Это две строки против 105 в исторической калибровке.
Ни одна из них не подошла к порогу перцентиля ближе, чем на 20 пунктов (max p65 против p85).
**Наблюдаемых отказов по-прежнему ноль на обеих сторонах, 0 из 10.**

Предрегистрация ждала 4.26 % LONG / 3.81 % SHORT. На 10 строках ожидание — **меньше половины
одного отказа**. Ноль отказов на этой выборке **ничего не опровергает и ничего не подтверждает**.

Моя рекомендация от прошлого доклада **не меняется, а усиливается**: вооружать по НАБОРУ строк на
SHORT-стороне, а не по календарю и не по факту «строки появились».

# 3. ЖИВОЙ SHORT ОТКРЫТ — И ЭТО ПЕРВАЯ СДЕЛКА ПОСЛЕ ОТКАТА

Строка 31234 исполнилась: **vpos 105, SHORT 0.0019 BTC @ 78 263.2**, стоп **79 295.1**, риск
1.9607 USDT. Оба зонда BingX согласны (`unified+raw`, ошибок нет), стоп живёт на бирже с
`closePosition=true`.

⚠️ **vpos 105 НЕ входит в популяцию каскадного эксперимента.** Популяция — vpos 95…104, она
закрыта на десяти. 105 открыта **после** отката (14:52:36 UTC) и живёт уже под восстановленной
конфигурацией. Не приплюсовывать её к −3.6595.

# 4. СОСТОЯНИЕ, КОТОРОЕ Я ПРОВЕРИЛ, А НЕ ПРЕДПОЛОЖИЛ

**Откат каскада выполнен не мной** — коммит `6fa5d45`, перезапуск **14:52:36 UTC**, `NRestarts=0`.
Выполнен ровно по выверенной процедуре: **три значения, ноль удалений**, имя
`HTF_REARM_FROM_15M_ENABLED` сохранено с пометкой «main.py imports it». Обе ловушки, которые я
нашёл утром, обойдены.

```
CONFLUENCE_FLAT_THRESHOLD       = 5.0    (было 3.0)
HTF_REARM_FROM_15M_ENABLED      = False  (было True, имя сохранено)
HTF_NEUTRAL_REQUIRE_15M_DRYRUN  = False  (было True — ENFORCE)
```

🔴 **Книжный гейт откатом НЕ затронут и мной НЕ тронут:**
`BOOK_GATE_ENABLED=True · BOOK_GATE_DRYRUN=True · CLAUSE_A=True · CLAUSE_B=False` — прочитано
импортом, а не глазами. Гейт по-прежнему не отказывает ничему.

| | |
|---|---|
| оценок гейта | **10 / 200** (LONG 8 / SHORT 2) |
| отказов | **0** |
| ошибок гейта / Traceback | **0** |
| строк `[BOOK-GATE]` в журнале | **0** — допущенные молчат по устройству |
| каскад | **закрыт на 10, ΣR −3.6595**, откачен |
| открытых позиций | **1** (vpos 105 SHORT) |

⚠️ **Окно «перезапуск из плоского» закрыто** — позиция открыта. Следующее окно будет на её закрытии.

---

**Итог: SHORT-сторона ожила и обе её строки допущены корректно; связка «крупная И близкая»
подтвердилась на второй стороне; отказов по-прежнему 0 из 10, и это ожидаемо, а не тревожно.
Вооружение по-прежнему за тобой, и n=2 его не приближает.**

Титан `6fa5d45` (гейт `3b075fd`).
