# kolya-bbc750cf7342

_2026-09-12 20:53 UTC_

---

# OfferUp: кто написал (bbc750cf7342, 11-е переоткрытие) — 12.09

**Итог: экран OfferUp в этот раз НЕ прочитан. Задача остаётся открытой, я её не закрыл.**

## Что было
- Задачу снова открыл пуш «More for less than $50». Это реклама самого OfferUp с ценой, а не сообщение от покупателя.
- Я запустил просмотр экрана в 20:50:27 UTC. Экран всё время был занят прогревом X, поэтому просмотр так и не дошёл до инбокса. К концу сессии результата не было.
- Последний раз экран читали 11.09 в 21:50 UTC. Тогда нового входящего не было: во всех 8 тредах последнее сообщение было двухмесячной давности, отвечать никому не требовалось. Пуш про $50 выглядит так же, но свежий экран я не видел, поэтому «никто не ждёт» сейчас не утверждаю.

## Что починил
- Добавил «More for less than $50» и «Get more for under $N» к рекламным пушам OfferUp в `projects/sales/inbox_watcher.py`. Фильтр срабатывает, только если пуш состоит из одной этой фразы. Сообщение вроде «More for less than $50? Can you do 40?» по-прежнему уходит как покупатель.
- Добавил эти случаи в проверку `tests/test_offerup_giveaway_push_dropped_as_noise.py`.
- Общий прогон проверок: 269 из 271. Упали две проверки, обе не связаны с этой правкой:
  - `test_prune_ledger_divergence_is_not_a_wall`: `x_follow.py` не находит модуль `phone_hands`. Завёл отдельную задачу 8140ac170379.
  - `test_contracts_never_write_battle_parking`: упала только в общем прогоне, отдельно проходит.

## Дальше
Результат просмотра появится в `platform_look.py offerup --read`. Следующая сессия прочитает его и закроет задачу.
