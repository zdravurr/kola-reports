# stop-catcher-armed-two-walls

_2026-07-17 11:41 UTC_

---

# СТОП НА ВТОРОМ ПУНКТЕ — КАК ТЫ И ПРИКАЗАЛ. ДИРИЖЁР НЕ ЗАКРЫТ

17.07. Ты сказал: «If any single item hits a real wall, STOP at that item and tell me exactly
why — don't fake, don't rush it». Стены две, обе настоящие. Докладываю, а не изображаю.

---

## ✅ СДЕЛАНО И ДОКАЗАНО ЖИВЬЁМ: EV ПОД overload_guard

Обе EV-строки обёрнуты в `run_guarded.sh evwarmup`. Метка `*warmup*` → заодно второй рубеж
паузы полосы (первый — `ogon_gate` внутри кода).

Живой прогон с НАСТОЯЩИМ флагом перегруза:

    ДО (нагрузка ок):     ✅ EV запустился
    ФЛАГ soft (load 9.9): is_paused() -> soft | should_pause() -> True
      run_guarded evwarmup → echo НЕ напечатался, exit=0
      run_guarded xwarmup  → echo НЕ напечатался, exit=0
      лог: SKIP evwarmup — overload pause active
           SKIP xwarmup  — overload pause active     ← EV ведёт себя ТОЧНО как X
    ФЛАГ СНЯТ:            ✅ EV снова проходит

Крон: 44 активных строки до и после. Бэкап `.kola_state/pause/crontab.botuser.pre_evguard_20260717T113534Z.bak`.

---

## ⛔ СТЕНА 1: ПАТРУЛЬ НЕ ТРОНУТ — ЛОВЕЦ ВЗВЕДЁН

**`--peek` НЕ ЗАПУСКАЛ. Стэш НЕ трогал. Крон НЕ менял.**

Ловец = `kola-inbox-watcher.service` → `inbox_watcher.py`: жив 16ч10м, опрашивает
уведомления Pixel каждые ~26с, последний опрос секунду назад (11:39:30). Он ЖДЁТ твоё
тестовое сообщение. `--peek` поднимает `com.facebook.orca` — это ровно то, что ты запретил.

**НО ТЫ ДОЛЖЕН ЗНАТЬ ВОТ ЧТО.** Пока ловец ждёт, **`fb_inbox_sweep` сам открывает Messenger
каждые 15 минут** — `ph.launch("com.facebook.orca")`, последний заход 11:33. То есть запрет
«не открывать Messenger» нарушает не мой `--peek`, а твой собственный действующий крон.

Риск при этом НЕ высокий, и вот почему: ловец опрашивает каждые 26с, свип — раз в 15 мин.
Пуш, прилетевший в момент T, будет пойман ловцом за ~26с, задолго до следующего свипа.
Сгореть тест может только если сообщение придёт ровно в те секунды, когда свип держит
Messenger открытым. Но это лотерея, а не гарантия.

**Решение за тобой (я НЕ трогал):**
1. Ждать пуш → как поймается, сразу `--peek` и дальше по плану. Безопасно.
2. Погасить свип на время теста (`crontab` комментарий) → окно теста чистое. Но свип —
   money-path, гасить его вслепую я не буду без твоего слова.

## ⛔ СТЕНА 2: «УБРАТЬ ПАТРУЛЬ НАСОВСЕМ» ПРОТИВОРЕЧИТ ТВОИМ ЖЕ УЛИКАМ

В аддендуме: «remove FB patrol for good», «FB push sole detector + hardened ✅».
В памяти (твой же вывод, кейс Raghavender): **«Пуш = первичный + ЛОССИ (плашка перезаписывает
покупателя) → свип = ПРАВДА, буфер правды = ЭКРАН»**. FB перезаписывает уведомление треда
generic-плашкой — это поведение FB, не наш баг.

Убрать патруль = сделать ДОКАЗАННО лоссивый детектор ЕДИНСТВЕННЫМ. «Harden the push against
banner-overwrite» — это не тюнинг на полчаса: мы не управляем тем, что FB кладёт в
уведомление. Пока не показано ЖИВЬЁМ, что перезапись побеждена, снятие патруля — это
регрессия money-path, и я её молча не сделаю.

Плюс расхождение приказов: первое сообщение — **прорядить** (`*/20 --tick`, peek-first),
аддендум — **убрать насовсем**. Это разные вещи. Скажи какую.

---

## ⚠️ СТЕНА 3: СТОРОЖ «ХОД БЕЗ ОТЧЁТА» РОДИЛСЯ БЫ СЛОМАННЫМ

Не построил — нашёл блокер раньше, чем написал строку.

**У отчётов ДВА пути, и ledger видит только один.**
* `full_report.send_full_report` (путь, который предписывает CLAUDE.md) — в ledger **НЕ пишет**
  (`grep outbox|deliveries full_report.py` → пусто).
* `report_outbox` / `notify_boss` — пишет.

Последняя запись `deliveries.jsonl` — **09:20**. Оба моих сегодняшних отчёта (10:46 и 11:25,
оба с raw-ссылками, оба доставлены) там **отсутствуют**.

Сторож, построенный на этом ledger'е, сегодня **дважды заорал бы «Коля закрыл ход без
отчёта»**, пока я отчитывался со ссылками. Это ровно та болезнь, что уже описана в коде
`report_outbox.watchdog`: *«сторож, орущий вечно, — это шум, а не сторож: Босс перестаёт
читать канал»*. Построить его сейчас = гарантированно отравить канал ложными тревогами.

**Сначала надо свести пути** (научить `full_report` писать в ledger) — и вот загвоздка:
`full_report.py` лежит в **`/root/titan-bot/`**. Ты сказал «Titan untouched». Он не торговый
(общий отправитель), но это твоя граница, и я её без спроса не перехожу. Скажи — правлю.

Существующие сторожа (`commit_watchdog`, `report_outbox.watchdog`) оба висят на том, что я
САМ зарегистрирую задачу/обещание — то есть на моей памяти. Ровно то, что ты хочешь убрать.
Механическая улика, которой не нужно моё сотрудничество, — **git**: коммит есть, отчёта со
ссылкой после него нет → ор. Это правильный дизайн, но он требует единого ledger'а.

---

## 🔍 ВИНОВНИК АВТО-ПОВОРОТА: СИЛЬНАЯ УЛИКА, НО НЕ ПРИГОВОР

**Наш код чист** — подтверждаю твою запись: ни одной записи `accelerometer_rotation=1`
у нас нет (есть только `=0` в `x_bridge_ensure.sh`). Виновник — снаружи.

**Сносы идут по расписанию, с шагом ровно 15 минут, на минутах :02 / :16 / :32 / :46:**

    04:32 04:46 05:02 05:16 05:32 05:46 06:02 06:16 06:32 06:46 07:02 07:16
    07:32 07:46 08:02 08:16 08:32 08:46 09:02 09:04 09:16 09:32 09:46 10:02
    11:02 11:06 11:16 11:32 11:34 11:38
    распределение по минуте: :32 ×10, :02 ×10, :46 ×9, :16 ×9

`portrait_lock` ходит `*/2`, значит ставится оно на :00/:15/:30/:45 — **сетка `*/15`**.
На этой сетке сидят: `fb_inbox_sweep` (открывает Messenger!), `mail_watch`, `buyer_watchdog`,
`oauth_refresh`.

**Естественный эксперимент — сам подвернулся:** между **10:02 и 11:02 свип не крутился
ни разу** (в логе только 10:02, потом 11:02). И сносов в этом окне — **НИ ОДНОГО**
(10:16/10:32/10:46 чисты, хотя все предыдущие 6 часов шли как часы). Свип молчит → поворот
не слетает.

**Второе совпадение:** снос в **11:06** — ровно мой прогон `fb_active`, поднявший
`com.facebook.katana` (browse в 11:06:55). Другое FB-приложение — тот же эффект.

**Гипотеза:** авто-поворот включают САМИ приложения Facebook (orca/katana) при запуске.

**Почему НЕ приговор, честно:** снос в **11:32:01** случился на 1.5 минуты РАНЬШЕ свипа
**11:33:34**. Причинность для этого экземпляра не сходится — либо на сетке `*/15` сидит
кто-то ещё, либо логи свипа неполные. Дожать можно только логом самого сеттера на телефоне
(кто/какой uid пишет настройку) — а это работа с телефоном, которую я не начинаю, пока
взведён ловец.

---

## НЕ НАЧАТО

* **Харденинг commit-gate** (`--no-verify` обходится, у botuser есть пароль). Не трогал.
* **Харденинг пуша против плашки** — см. Стену 2.

## СОСТОЯНИЕ СИСТЕМЫ

* Полосы: `procurement: live`, `warmup: live`. Гейт Огня: 🟢 окно открыто, полоса live.
* Ловец: **ВЗВЕДЁН**, жив, ждёт (последний опрос 11:39:30).
* Патруль: `*/15`, крутит ПРОВЕРЕННЫЙ файл. Стэш `stash@{0}` не тронут.
* Git: `HEAD == origin/master`, `0de48b0`. Незакоммичено только чужое (авто-`LEARNINGS.md`).
* **Титан/Solana:** `titan.service active`, изменений 0. Root-крон не редактировался.
  `full_report.py` в titan-bot **не тронут** (см. Стену 3 — жду твоего слова).

## ЧТО МНЕ НУЖНО ОТ ТЕБЯ (3 решения)

1. **Пуш-тест:** ждать, или гасить свип на время теста?
2. **Патруль:** прорядить (`*/20 --tick`) или убрать насовсем? Если убрать — чем бьём
   доказанную лоссивость плашки, до того как снимем буфер правды?
3. **`full_report.py` в titan-bot** — можно править (свести ledger)? Без этого сторож
   «ход без отчёта» рождается ложно-орущим.
