# ТИТАН: СТРОКА `Volume:` СПАРЕНА — ЧЕТВЁРТЫЙ И ПОСЛЕДНИЙ ЭКЗЕМПЛЯР. КЛАСС ЗАКРЫТ ФОРМАЛЬНО

**2026-08-31 15:35 UTC** · `openitems_guard` — **EXIT=0 до и после**. HEAD `9c40a4f` → **`f5d3542`**.
🔴 **Правка ПРИМЕНЕНА. Только ярлыки и значения — ни порога, ни инструкции, ни совета.**
🔴🔴 **Mercury-SOL НЕ ТРОНУТ: в дерево не входил, не читал, не импортировал.**
Титан **ПЛОСКИЙ** (0 открытых) при правке и при рестарте.

**Основание — мой отчёт 15:30, §4.** Я его нашёл и оставил Боссу. **Решение принято: чинить.**

---

# §1. ПРАВКА

Строка `Volume:` стоит **внутри** блока `Regime at ENTRY vs NOW`, но несла `vol_1h` и `vol_15m`
**только за СЕЙЧАС** — без входной пары и без пометки о её отсутствии. Явной дельтой был
только третий элемент строки.

**`trades.srv_vol_ratio_1h` и `srv_vol_ratio_15m` — 84 из 84, пустых строк 0.** Лежали в той же
строке БД и просто не выбирались. **Ничего не выдумано.**

## 1a. NULL ПЕЧАТАЕТСЯ СЛОВАМИ, СОСЕДНИЙ ТФ НЕ ПОДСТАВЛЯЕТСЯ

```python
def _vp(entry_v, now_v):
    _n = f"{now_v:.2f}"  if isinstance(now_v,  (int, float)) else 'not recorded'
    _e = f"{entry_v:.2f}" if isinstance(entry_v,(int, float)) else 'not recorded'
    return f"entry {_e} -> now {_n}"
```
**Идиома та же, что в блоке книги (`entry x7.9 -> now x7.3`), и тот же приём, что в правке трендов.**

## 1b. ДО И ПОСЛЕ НА ДВУХ ПОЗИЦИЯХ КЛАССА

```
vpos 90
  БЫЛО   Volume:   vol_1h=0.86 vol_15m=0.43 ATR change vs entry=-4.7%
  СТАЛО  Volume:   vol_1h: entry 2.65 -> now 0.86   vol_15m: entry 5.60 -> now 0.43   ATR change vs entry=-4.7%

vpos 96
  БЫЛО   Volume:   vol_1h=3.37 vol_15m=2.21 ATR change vs entry=+15.0%
  СТАЛО  Volume:   vol_1h: entry 2.75 -> now 3.37   vol_15m: entry 1.78 -> now 2.21   ATR change vs entry=+15.0%
```

> 🔴 **И ВОТ ЧТО ЭТО ОТКРЫВАЕТ, ЧИСЛОМ. У vpos 90 объём на 15m УПАЛ С 5.60 ДО 0.43 —
> в ТРИНАДЦАТЬ РАЗ. Советнику показывали «0.43» и ни одной точки отсчёта.**
> Он не мог знать, что объём обвалился: для него это было просто маленькое число.
> **Это ровно тот дефицит, который порождал весь класс — не ошибка модели, а молчание промпта.**
> (У vpos 96 наоборот: объём **вырос** 2.75 → 3.37 и 1.78 → 2.21, чего тоже не было видно.)

## 1c. `ATR change vs entry` НЕ ТРОНУТ
Он и был явной дельтой. **Оставлен байт в байт.**

---

# §2. ЧТО НЕ ИЗМЕНИЛОСЬ

| проверка | результат |
|---|---|
| **2a `_CLOSE_SYSTEM_RICH`** | `3d709571e17ff4051362c5a50496c74c0223b29a062eb2c94b2a5de09655670b` — **совпадает с эталоном задания** |
| **2b порог / инструкция / совет** | **нет.** Добавлены два входных числа и слова `entry … -> now …`. Ни слова о позиции |
| **2d AST `main.py`** | изменена **одна** функция `_build_exit_context`; добавлены вложенные `_vr`, `_vp` |
| **2d AST `claude_advisor.py`** | 🔴 **НЕ ИЗМЕНЁН ВООБЩЕ** — `changed=[] added=[] removed=[]` |
| **`git diff`** | **один файл.** `config.py` вне диффа |
| **восемь охраняемых констант** | **все на месте, ни одна не тронута** |
| **`.bak`** | `main.py.bak-volpair-20260831-153005`, `claude_advisor.py.bak-volpair-20260831-153005` |
| **контракты** | ✅ **все зелёные** |

## 2c. 🔴 ВСЕ ПЯТЬ ФАКТОВ — ПОБАЙТОВО ТЕ ЖЕ

Полный дифф промпта через **настоящий код**, `_call` подменён, **обращений к API ноль**:

| позиция | дифф |
|---|---|
| **vpos 96** (не взведена) | **ОДНА строка — `Volume:`. Больше ничего.** |
| **vpos 100** (ВЗВЕДЕНА) | **ОДНА строка — `Volume:`. Больше ничего.** |

| блок | идентичен? |
|---|---|
| **Trailing-stop arithmetic** (5-й факт, 31.08) | ✅ **ДА** |
| **Cost of closing** (факт «a», 30.08) | ✅ **ДА** |
| **If you HOLD / STOP_MARKET** (факты «b», «c») | ✅ **ДА** |
| **Since the previous consultation** (факт «d») | ✅ **ДА** |

---

# §3. ПРИМЕНЕНО

**Плоский — 0 открытых, проверено в секунду перед рестартом.** Останавливаться было не о чем.

```
titan.service : ActiveState=active  NRestarts=0  старт 2026-08-31 15:31:49 UTC
boot line     : [TITAN][ORDER-MODE] 🔴 LIVE ORDERS — REAL MONEY: orders ARE sent to BingX
загружено ли  : mtime main.py < старт процесса ⇒ True — поднят НОВЫЙ файл
журнал        : ошибок и трейсбеков 0
guard         : EXIT=0 ПОСЛЕ
коммит        : f5d3542, запушен
```

---

# §4. 🔴 КЛАСС ЗАКРЫТ ФОРМАЛЬНО

Внесено в канон как **§0.PROMPT-PAIRING — CLASS CLOSED**.
**Один класс дефекта, найден и закрыт целиком за один день:
«заголовок обещает сравнение, которого в значениях нет».**

| # | экземпляр | что обещал заголовок против того, что нёс | коммит |
|---|---|---|---|
| **1** | **ADX15m** — на входе только ADX1h, сейчас ADX1h **и** ADX15m | модель взяла единственный «entry ADX» — часовой. vpos 90: «спайк» раздут с +9.6 до +26.5 | **`c66a900`** |
| **2** | **перцентили стен** — четыре ТЕКУЩИХ значения четырёх РАЗНЫХ объектов подряд | vpos 96: «supporting wall 72→70th pct» через две разные стены. Корень: `NOTE` велит судить по перцентилю, а движение есть только в множителях | **`c66a900`** |
| **3** | **строки трендов** — вход 1d/4h/1h, сейчас 15m/5m, **пересечение ПУСТОЕ** | `1h=bear` рядом с `15m=neutral` читается как разворот, которого не измеряли | **`9c40a4f`** |
| **4** | **строка `Volume:`** — в том же блоке, `vol_1h`/`vol_15m` только за сейчас | у vpos 90 объём 15m обвалился 5.60 → 0.43, а показывали «0.43» | **`f5d3542`** |

🔴 **ВО ВСЕХ ЧЕТЫРЁХ ВХОДНЫЕ ДАННЫЕ УЖЕ ЛЕЖАЛИ В ТОЙ ЖЕ СТРОКЕ БД И ПРОСТО НЕ ВЫБИРАЛИСЬ:**
`srv_adx_15m` 84/84 · `trend_15m`/`trend_5m` 82/84 · `srv_vol_ratio_1h`/`_15m` 84/84.
**Ни в одной из четырёх правок ничего не выдумано.** NULL печатается как `not recorded`.
Там, где пары не может быть в принципе — 1d/4h/1h (313/313 без колонки), противоположная стена,
все перцентили — **отсутствие НАЗВАНО, а не опущено.**

**СВЁРТКА ЗАВЕРШЕНА.** Промпт отрендерен целиком и каждый блок перечитан с одним вопросом.
После четвёртого экземпляра **больше ни одного не осталось**: блок книги, блок перцентилей,
стоимость закрытия, тождество MFE/giveback, тиры входа, структура 5m и блок защиты — чисты.

## ⚠️ МЕНЬШАЯ ЗАМЕТКА — ЗАПИСАНА, НЕ ИСПРАВЛЕНА

`Since the previous consultation (1.0h into the position)` печатает `prev_age_h` — **возраст
ПРЕДЫДУЩЕГО замера**, а не промежуток между двумя. Читатель может принять это за интервал.
🔴 **Это двусмысленность, а не ложное утверждение: величины под ней верны.** Поэтому записана
рядом с классом как меньшая заметка и **НЕ ИСПРАВЛЕНА** — к классу выше не относится.

---

# ВЕРДИКТ

1. 🔴 **СТРОКА `Volume:` СПАРЕНА. Четвёртый и последний экземпляр класса закрыт.**
2. **Не новые факты: `srv_vol_ratio_1h`/`_15m` — 84/84 в той же строке, не выбирались.
   NULL → `not recorded`. `ATR change vs entry` оставлен нетронутым.**
3. 🔴 **Открылось скрытое: у vpos 90 объём 15m обвалился В 13 РАЗ (5.60 → 0.43), и советник
   этого видеть не мог — ему показывали «0.43» без точки отсчёта.**
4. **`_CLOSE_SYSTEM_RICH` побайтово равен эталону. `claude_advisor.py` НЕ ИЗМЕНЁН.
   Одна функция, два вложенных помощника. Все пять фактов байт в байт — дифф РОВНО ОДНА
   СТРОКА на каждой из двух реальных позиций. Восемь констант целы. Контракты зелёные.**
5. **Применено из ПЛОСКОГО, рестарт 15:31:49, файл старше процесса, журнал чист, guard EXIT=0.**
6. 🔴 **КЛАСС ЗАКРЫТ В КАНОНЕ: четыре экземпляра, четыре коммита, все за 31.08. Свёртка
   завершена, новых экземпляров нет.** Двусмысленность `prev_age_h` записана как меньшая
   заметка и намеренно не тронута.

---

**Происхождение.** `trades.db` Титана — `mode=ro`, только SELECT: покрытие `srv_vol_ratio_1h`
и `srv_vol_ratio_15m` (84/84, пустых строк 0), значения входа и текущего замера для vpos 90, 96, 100.
Диффы промптов — прогон настоящего кода на строках позиций 96 и 100 с подменённым `_call`:
обращений к API ноль, ордеров ноль. Сравнение версий — `ast.dump` по каждой функции;
системный промпт — `sha256` литерала через `ast.literal_eval`. Mercury-SOL не открывался.
