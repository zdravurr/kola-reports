# 25.08 17:00 — СТЕНД ЗЕЛЁНЫЙ, ДВЕ ПОЧИНКИ В ДЕРЕВЕ, ПО GEORGE — СТОП

_2026-08-25 17:00 UTC · сессия Босса · Титан/Solana не трогались_

---

## 1. BASELINE: БЫЛО 7 КРАСНЫХ → СТАЛО 122/122 ✅

Задача b176c7ec3b9f называла 6; живой прогон дал **7**. Шесть из семи падали **без единого
дефекта** — контракт судил не свой вход, а живую витрину.

| контракт | почему краснел |
|---|---|
| `test_gone_item_never_gets_a_phone` | брал ЖИВОЙ SKU `bike_giant_rainier` как образец «вещь в продаже» |
| `test_boss_grants_a_role` | то же |
| `test_phone_guard_knows_the_item` | то же |
| `test_gone_item_listing_is_watched_and_due_for_takedown` | то же + изоляции состояния НЕ БЫЛО ВОВСЕ |
| `test_boss_words_travel_with_intent` | то же (матрице снятия стало нечего отличать) |
| `test_escalation_loop_no_boss_courier` | подмена STATE на боевой тащила БОЕВОЙ суточный счётчик сессий — 18/18, спавн уходил в отказ по бюджету ДО захвата задачи |
| `test_contracts_never_touch_battle_state` | `test_one_human_one_lead…` тянул answer_guarantee в боевую очередь без изоляции (0c889f1a8a4e) |

**Корень пяти:** велик продали 24.08, канон в `item_records._BUILTIN` честно стал `sold` —
и пять зелёных контрактов покраснели ОДНОВРЕМЕННО. Фикстура умерла вместе с вещью.

**Починка:** `tests/_item_fixture.py` — контракт прибивает статус образца в СВОЁМ
изолированном состоянии. С замком: **в боевое состояние не пишем НИКОГДА** (иначе опечатка
вернула бы проданный велик в `for_sale`, и мы снова начали бы предлагать несуществующую вещь).
Замок проверен живьём — попытка записи в `.kola_state` падает с отказом.

Коммит baseline: **`e68fca5`**.

---

## 2. РЕШЕНИЕ 1 — ЯРЛЫК ЗАДАЧИ ЕДЕТ ВМЕСТЕ С УЛИКОЙ

`lib/tech_queue.py`. Личность задачи — `signature`, а не покупатель; один id держит все стены
одного сторожа (это правильно, иначе история рвётся). Цена была в том, что `title` и
`blocked_what` писались ОДИН РАЗ, а `evidence` обновлялась каждый повтор.

Теперь `_refresh_label()` освежает ярлык **на повторе И на переоткрытии**. Пустое значение
НЕ затирает названное (сторож, промолчавший в этот раз, не стирает то, что назвал прошлый).
Прежний ярлык сохраняется в `label_was` / `blocked_was` — история не теряется.

Контракт `test_task_label_travels_with_its_evidence` воспроизводит инцидент целиком —
и в изолированном прогоне задача получила **тот же id `84ae341db946`**, что подтверждает
разбор личности задачи. Отдельно проверено, что в брифе техника больше нет протухшего Seymour.

## 3. РЕШЕНИЕ 2 — ЗАКРЫТИЕ РУКОЙ ОСТАНАВЛИВАЕТ СЧЁТ, НО ОТВЕТОМ НЕ СТАНОВИТСЯ

`lib/reply_proof.py` + `lib/answer_guarantee.py`. Сделано **новым вопросом, а не послаблением
старого** — это принципиально:

- `has_proof()` — «ушёл ли человеку ТЕКСТ». `force-done` не засчитывает **никогда**, без изменений.
  Послабление здесь молча похоронило бы живого покупателя (дело David 24.08).
- `is_settled()` — «продолжать ли ЭСКАЛАЦИЮ». Засчитывает: кто-то посмотрел на тред и сказал
  «здесь закончено», повторять это решение каждые 5 минут смысла нет.

Судья сличения имён вынесен в `_person_matches()` и переиспользуется обоими — второй
экземпляр правила (оплаченного дырой в close-гейте 28.07 и делом Bryan 23.08) разъехался бы с первым.

**Замер на живых данных до/после:**

| покупатель | `has_proof` | `is_settled` |
|---|---|---|
| bryan de la paz | True | True |
| bryan | False | **True** |
| rick | False | **True** |
| desideria | False | **True** |
| george | False | **True** |

Контракт `test_admin_closure_stops_the_count_but_is_not_a_reply` держит все рубежи, включая
два важных: закрытие «Michael Green» **не** снимает счёт с «Michael Brown», и **новое входящее
позже закрытия снова открывает счёт** (иначе «закрыто рукой» стало бы вечной глушилкой).

**Прогон: 122/122 зелёных.** Коммит: **`dfe0ccf`**.

### Про токен гейта

Токен не понадобился и **не применялся**. Гейт срабатывал только потому, что `git add -A`
загребал ЧУЖИЕ защищённые файлы (`item_records`, `phone_hands`, `reply_sanity`,
`screen_priority`, `lead_router`, `agent_capacity`) — они лежали в дереве грязными до меня.
Мои шесть файлов в защищённый список не входят; коммит с точной областью прошёл штатно.

⚠️ **Со-резидент, о котором надо знать:** `lib/answer_guarantee.py` и `lib/tech_queue.py`
несут НЕЗАКОММИЧЕННУЮ правку прошлой сессии (**a5b2c043a31b**, «второй признак принадлежности
стены»). Git не делит файл по авторам — она уехала этим же коммитом. Дерево при ней зелёное.
В дереве осталось ещё ~40 грязных файлов от прошлых сессий — я их не трогал.

---

## 4. 🔴 GEORGE — НЕ ОТПРАВИЛ. ПРИКАЗ СТОИТ НА ФАКТЕ, КОТОРОГО НЕТ

Приказ был: «отправить George настоящий ответ (велик $100 firm, самовывоз RSM 92688)».
**Посмотрел на тред ГЛАЗАМИ, прежде чем писать живому человеку — и отправлять нечего.**

Пруф: `_george_thread_live.jpg`, снят **сегодня 09:48:54**, за минуту до закрытия треда.

Что на экране:

1. Шапка листинга: **«SOLD - 2026 giant bike that name giant»** — Facebook сам метит вещь
   проданной. В каноне `bike_giant_rainier` = **`sold`** (с 23.08), `offerable = False`.
2. **Настоящий ответ George УЖЕ ПОЛУЧИЛ**, полностью по делу: «…this one is a Medium and fits
   riders about 5'6" to 5'10", so a bit big for an 11-year-old at 4'11"… It is still available
   at $100 if he grows into it… Take care!» — и **поставил на него ❤️**.
3. George ответил: **«Thanks Alex, you too!»** — тепло попрощался. Он **сам отказался**:
   рама Medium велика его сыну (4'11").
4. Дальше системная плашка FB «You can now rate each other» — не вопрос покупателя.
5. И только ПОСЛЕ этого — наш служебный пузырь **«Not logged in · Please run /login»**.

**Даты в треде — JUL 28.** То есть разговор закрыт ~4 недели назад, а «не менялась с 24.08»
из задачи 03dd9e9ed557 относится к строке ИНБОКСА (окно наблюдения свипа), а не к дате сообщений.

**Поэтому отправка сейчас означала бы:** предложить **ПРОДАННУЮ** вещь человеку, который
месяц назад вежливо отказался и попрощался, заново вскрыв закрытый тред. Это ровно тот класс,
против которого стоит phone-guard — тот самый, чьи контракты я час назад возвращал в зелёное.
Отправлять такое без твоего слова я не стал: письмо живому покупателю не отзывается.

**Что осталось настоящего:** служебная строка — последнее, что видит George в треде.
Варианты, решение твоё:
- **(а) ничего не слать** — тред закрыт по-доброму, вещь продана (моя рекомендация);
- **(б) одна короткая человеческая строка-извинение** за случайный служебный текст, **без**
  всякого предложения товара;
- **(в)** если считаешь, что где-то велик всё ещё висит живым — скажи, проверю витрины.

**Технический корень я закрою в любом случае, он от треда не зависит:** гард
`reply_sanity` стоит **только** в `craigslist_chat_send.py`. В `messenger_app_send.py` (FB —
именно там утекло), `offerup_send.py` и `answer_buyer.py` его **НЕТ**. Это и есть дыра из
задачи 8b4024ae4df7: служебный текст уходит в тред покупателя без проверки.

---

## 5. ОГОНЬ — СЛЕДУЮЩИЙ, ЕЩЁ НЕ СДЕЛАН

Тупик зрения (`[sense] тот_экран=False → back` → `[post] FAILED` → прогон **не прерывается**,
а уходит в `[dwell] скролл`) и уборка экрана за money-path сессией — в работе следующим шагом.
Пока не сделано — говорю прямо, а не «почти готово».

---

## 6. ПРО «23 ИЗМЕНЁННЫХ ФАЙЛА» — ЭТО НЕ МОЯ ПРАВКА

Число 23 пришло из АЛЕРТА ГЕЙТА, а не из моего коммита. Я загрёб `git add -A`, и гейт честно
перечислил всё грязное, что лежало в дереве. Мой коммит `dfe0ccf` — **6 файлов**.

**Мои 6 (что каждый чинит):**

| файл | что чинит простыми словами |
|---|---|
| `lib/tech_queue.py` | заголовок задачи и «что заблокировано» писались один раз при заведении и больше не обновлялись — только улики под ними. Поэтому 84ae341db946 трое суток слал техника чинить «тест для Seymour». Теперь ярлык едет с уликой; пустое не затирает названное; прежнее сохраняется |
| `lib/reply_proof.py` | добавлен ВТОРОЙ вопрос. «Ушёл ли текст покупателю» по-прежнему не признаёт `force-done`. Новый «продолжать ли эскалацию» — признаёт как точку остановки. Судья имён сведён в одну общую функцию, чтобы две копии не разъехались |
| `lib/answer_guarantee.py` | сторож теперь задаёт второй вопрос вместо первого. Это и обрывает вечную ступень 5: пересоздание лида уносило настоящий пруф за горизонт, после него оставались только закрытия рукой — 590 повторов |
| `tests/test_task_label_travels_with_its_evidence.py` | новый: воспроизводит инцидент Seymour целиком, включая проверку брифа |
| `tests/test_admin_closure_stops_the_count_but_is_not_a_reply.py` | новый: закрытие рукой останавливает счёт, но ответом не становится; не течёт между Michael Green и Michael Brown; новое входящее счёт открывает заново |
| `tests/test_answer_ladder_survives_answered_message.py` | его заглушке `reply_proof` понадобился новый метод, иначе сторож падал |

Плюс в baseline-коммите `e68fca5`: `tests/_item_fixture.py` (новый) и пять контрактов,
бравших живой SKU велика как образец «вещь в продаже».

**Остальные 17 — НЕ МОИ.** Это незакоммиченная работа прошлых сессий в `lib/`
(~1051 добавленная строка): `claude_cli`, `agent_capacity`, `wake_turn`, `screen_priority`,
`phone_hands`, `reply_sanity` и другие. Я их не писал, не коммитил и оставил как есть.
Два из них — `answer_guarantee.py` и `tech_queue.py` — я тоже правил, поэтому чужая правка
уехала моим коммитом: git не делит файл по авторам. Это помечено в сообщении коммита.

---

## ИТОГ

- Стенд: **7 красных → 122/122 зелёных**, два новых контракта.
- Решение 1 и решение 2 — **в дереве и закоммичены** (`e68fca5`, `dfe0ccf`), токен не понадобился.
- **George: стоп до твоего слова** — приказ опирался на «он не получил ответа», а глазами
  видно обратное: ответ получен, отреагирован сердцем, человек отказался сам, вещь продана.
- Огонь — следующим шагом.
