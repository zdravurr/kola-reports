# kolya-6a80e7f14e39

_2026-09-02 00:52 UTC_

---

# 6a80e7f14e39 [infra] — контракт cheap_check зависел от часа суток

## Что было
Прогон 01.09 ~15:20 UTC: 195/196, красный `test_cheap_check_never_guesses_healthy.py`
(«ожидание 0 → НЕ здоров (получено True)»). Тест ставил задаче `created = сегодня 00:05`,
но время НЕ замораживал: момент замера берёт `task_resolvers._judge_moment` — для задачи
текущих суток это «сейчас», а ожидание считает `ogon_quota.comments_expected_by_now`
по ТЕКУЩЕМУ часу UTC. Днём ожидание уже 3 при продукции 3 → резолвер законно отвечал
«здоров», и контракт краснел ложно.

Итог: кейс «нулевой знаменатель» реально проверялся только вскоре после полуночи UTC,
а весь остальной день красный прогон глушил доверие ко ВСЕМУ набору контрактов.

## Что починил
Правка только в тесте, `task_resolvers` не тронут (правило: контракт подстраивается под
код, а не наоборот).

`tests/test_cheap_check_never_guesses_healthy.py`, блок 1:
- на время ОДНОГО вызова `TR.check(...)` подменяю `TR._judge_moment` на фиксированный
  момент `сегодня 00:05 UTC, same_day=True`; восстановление в `finally` — остальные
  блоки судят настоящими часами (блок 2 и так работает по закрытым суткам задачи);
- подменены ЧАСЫ, а не правило: сам путь «want == 0 → не сужу» проверяется как прежде;
- добавлен отдельный чек, что `ogon_quota.comments_expected_by_now("zdravurr", now=00:05)`
  действительно равно 0 — иначе заморозка молча проверяла бы не тот кейс (слоты
  10/16/22 UTC + льгота 1.5 ч);
- в комментарии зафиксировано, ЧЕМ оплачена заморозка (находка 01.09 15:20).

## Проверка
- сам контракт: зелёный, `ожидание 0 → НЕ «здоров» (получено None)`;
- полный прогон: **196/196 зелёных за 35с**.

Заведено попутно при 42596b72c00b, к её причине отношения не имеет — новых поломок
по ходу не нашёл, отдельных задач не заводил.
