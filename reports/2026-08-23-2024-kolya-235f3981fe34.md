# kolya-235f3981fe34

_2026-08-23 20:24 UTC_

---

# 235f3981fe34 [infra] — красный контракт auto_collapse: «вопрос Боссу не уходит»

**Итог: чинить было нечего — красный снялся сам, и я это проверил, а не поверил. Контракты 98/98. Кода не трогал. По ходу нашёл отдельную настоящую дырку — завёл задачей.**

## Что было
Прогон 23.08 19:47 дал 93/94; единственный красный — `test_auto_collapse_only_on_hard_evidence.py`, случай [4] «два разных человека на одном товаре». Подозрение в улике: схлопывание правильно остановлено, но вопрос Боссу не доставлен (`status=none`) → лид зависает молча.

## Что оказалось
Красный — это **окно между двумя правками**, а не поломка доставки:
- 19:31 — `lib/lead_alias.py` перестал считать связку заголовка FB «X **to** Y» уликой родства (`_HEADER_STOP={"to"}`, `same_thread_names` сравнивает стороны строго);
- 19:47 — прогон контрактов со старым ожиданием («вопрос должен уйти») → красный;
- 19:56 — контракт приведён к новому поведению: у такой пары `status=none`.

`none` здесь — не немой лид, а **усиление запрета**: пара «Adan V. to Adan» ⟷ «Gil D. Reyes to Gil» теперь даже не становится кандидатом, потому что единственный «общий токен» у них — служебное слово «to», по которому пересекается ЛЮБАЯ пара FB-заголовков.

## Путь доставки вопроса — проверил отдельно, как просили улики
Он **жив**: `check()` → непустой `fresh` → `_ask_boss(fresh)` → `notify.notify_boss(..., channel="dirigent")`. Случай [5] («Michael» ⟷ «Michael S.», настоящее общее имя) даёт `status=pending`, и вопрос уходит — это в контракте закреплено.

Живой реестр `.kola_state/lead_aliases.json` тоже чист: 9 pending, мусорных «to»-only — **0** (раньше их было большинство).

## Что нашёл по ходу и завёл отдельно — `67e72f594bbf`
Пара, однажды попавшая в `pending`, **больше никогда не пересматривается** на жёсткую улику: ветка `if k in reg["pending"]: status="pending"; continue`. Значит пары, зависшие ДО правила 18.08, не схлопнутся, даже когда факт налицо. В реестре прямо сейчас две такие:
- «Shawn» ⟷ «Shawn T. to Shawn» — 7.5 суток,
- «Seymour» ⟷ «Seymour U. to Seymour» — 6.2 суток.

Имя = сторона заголовка треда, то есть ровно та форма, которую контракт признаёт ФАКТОМ. Они висят вопросом, на который ответа нет, а покупатель тем временем рискует получить два ответа по одному велику — ровно вред случая Mario 18.08. Путь денежный, поэтому лечить это отдельной задачей с чистой головой, а не наспех в конце сессии.

## Проверка
`run_contracts.py` → **98/98 зелёных**.
