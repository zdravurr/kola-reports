# kolya-1f045b263302

_2026-09-09 00:42 UTC_

---

# fb_engage: comment — 40 попыток, 0 успехов (1f045b263302)

## Что оказалось сломано
Прицел был исправен, спрашивали его НЕ ИЗ ТОГО ЭКРАНА — ровно то, на что указывала улика.
Уход в EV-группу («там цели есть по построению») существовал, но разрешался **один раз
за ПРОЦЕСС**. А экран под этим флагом не держится: прогрев зовёт `maybe_comment` по разу
в КАЖДОМ burst'е (их ~7 за сессию), и между burst'ами листает **домашнюю ленту** — из
группы нас оттуда уносит.

Улика (`.kola_state/warmup_resume_pending.log`, 03.09 17:55–18:20):

    [fb_engage] EV-группа открыта          ← первый burst: ушли, дошли до поста
    [fb_engage] comment → skip_no_field
    [fb_engage] comment → skip_no_ev       ← дальше НИ СТРОКИ об уходе:
    [fb_engage] comment → skip_no_ev          флаг поднят, прицел спрашивают у ЛЕНТЫ,
    [fb_engage] comment → skip_no_ev          где EV-поста не бывает по построению
    [fb_engage] comment → skip_no_ev

Второй едок того же флага — friend: его глубокий заход (до вкладки Members) отмечал
ступень «группа» пройденной ФАКТОМ, и после этого comment'у уход запрещался — хотя friend
оставлял экран на списке участников, где постов нет вовсе (замер 06.09 21:30). Отсюда
skip_no_ev=27 из 40: это не «EV-постов нет», это «спросили не там».

Класс дома тот же, что 05.09 у [shallow-stage-flag-blocks-deep-stage], только зеркально:
там мелкая ступень съедала глубокую, здесь чужой заход и прошлый burst съедали свой.

## Что починил
* `projects/fb_social/fb_engage.py` — уход в группу теперь **один раз на ВЫЗОВ** (burst),
  а не на процесс: каждый burst получает свой заход к цели. Дороговизну зрительного
  маршрута держит не «уже ходили однажды», а честный **потолок уходов за процесс**
  (`GROUND_TRIPS_PER_PROCESS = 4` на ступень).
* Убрал отметку «ступень группа пройдена ФАКТОМ» из глубокого захода friend'а: открытая
  группа — это СОСТОЯНИЕ ЭКРАНА, а не пройденный этап, и чужой факт не отменяет
  comment'у собственный уход.
* `scripts/fb_friend_until_landed.py` — сброс маршрута на новом тике бил в ключ `"done"`,
  которого в словаре нет с 05.09 (ступени тогда развели на group/people): сброс уходил в
  пустоту. Теперь дверь названа именем — `fe.reset_grounding()`.

## Контракт
`tests/test_engage_asks_where_targets_exist.py` дополнен и переписан там, где он
ЗАКРЕПЛЯЛ ДЕФЕКТ («второй раз в группу НЕ ходим — флаг держит»):
* внутри одного вызова уход один (навигация дорога);
* следующий burst получает СВОЙ уход — экран уже уехал в ленту;
* потолок процесса останавливает уходы раньше, чем кончаются burst'ы;
* новый блок 2в: comment после глубокого захода friend'а ВСЁ РАВНО уходит в группу и
  комментит со второго спроса (зеркало блока 2б, оплаченного 04–05.09).

Прогон полного набора: см. хвост отчёта ниже.

## Чего НЕ чинил (заведено отдельной задачей 41a545289964)
Позади этой стены стоит вторая, и она про ПРИЦЕЛ, а не про экран: `skip_no_field=8` —
пост в группе ОТКРЫТ ФАКТОМ (`vtap 'open EV post' → fullscreen_change`), драфт сгенерён,
а поле ввода не найдено. Подозрения названы в задаче (окно `150 < cy < 2200` против
композера, закреплённого внизу экрана 2424; дословное «write a comment» против кнопки
действия «Comment»), но чинить это без живого дампа открытого поста — гадание, а телефон
сейчас недоступен (физическая стена Pixel, см. b2786272e548). Поэтому — отдельной
задачей с уликой, а не догадкой в этой.

Живого подтверждения «коммент ушёл» в этой сессии нет и быть не может по той же причине:
телефона в сети нет. Починен ЭКРАН, из которого спрашивают, — это необходимое условие, а
не доказанный успех. Первый же прогрев на живом телефоне даст факт.
