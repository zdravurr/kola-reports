# razreshenie-polucheno-provodka-i-money-path-primeneny-zhivoy-progon-idet

_2026-08-15 16:58 UTC_

---

# Разрешение получено: проводка и два money-path коммита применены, живой прогон ИДЁТ

**15.08.2026, 16:57 UTC.** Босс разрешил явно. Применил всё, что ждало слова, и запустил
живое снятие. **Отчёт публикуется ДО завершения прогона** — коммиты уже есть, значит и
ссылка обязана быть уже сейчас. Итог живого прогона уедет отдельным отчётом.

---

## 1. Что применено (и как именно)

### Патч проводки — `1f53e0d`
`lib/dirigent_commands.py`: интенты `remove_listing` / `mark_sold` больше не упираются
в плоский отказ. Порядок стал обратным: **сначала попытка на экране** (`generic_solve`),
и только если она честно не дошла — отказ, с местом остановки и кадрами.

Отказ перестал быть первым словом и стал последним.

`authorized=True` передаётся потому, что в эту ветку управление попадает ТОЛЬКО по прямому
приказу Босса на это самое действие. Общего «разрешено всё» не появилось: без команды сюда
не войти, а сам решатель без `authorized` экрана не касается вообще.

Исход берётся у решателя, а не у намерения: `did=True` возможен ТОЛЬКО когда факт прочитан
с экрана.

### Парный контракт — в том же коммите, и это не подгонка под код
`test_no_capability_substitution` требовал: «в ответе есть НЕ УМЕЮ». Верное требование для
мира, где попытки не было. После честной попытки верный ответ другой — **«НЕ СДЕЛАЛ, вот
где встал, вот кадры»**. Контракт теперь стережёт именно это, плюс невозможность `did=True`
без факта. И он ПОДМЕНЯЕТ решатель: без подмены прогон контракта лез бы на живой телефон.

### Два money-path коммита — `9d2637e`
`inbox_agent.py` и `listing_gen.py` на единственный транспорт `claude_cli`. Модели сохранены
побуквенно; `_reject_buyer` (гвард money-path) как звал без `--model`, так и зовёт —
менять поведение гварда заодно с транспортом нельзя.

### 🔓 Гейт НЕ обойдён — пройден авторизованно
Оба коммита прошли **штатным авторизованным путём** гейта (`KOLA_COMMIT_GATE_TOKEN`), и он
это напечатал сам:

```
🔓 COMMIT GATE: авторизованный коммит в защищённые пути (токен принят)
```

Это не `--no-verify` и не снос хука. Гейт остался на месте и сработал как задумано: без
слова Босса — держал (16:06, алерт 7020), со словом — пропустил и записал, что пропустил.

**Контракты 15/15 зелёные** — прогнаны ПОСЛЕ применения патчей, а не до.

---

## 2. Живой прогон — идёт прямо сейчас

Запущен через **боевой командный путь**, а не в обход: смысл проверки в том, что фраза
проходит ВСЮ цепочку.

```
ФРАЗА : сними объявление кофейного столика — его уже отдали
ИНТЕНТ: remove_listing            ← 16:55:46
        → dirigent_commands.execute → generic_solve → экран
```

### Цель опознана по реестру, а не по памяти
```
item_id  : zina_coffee_table
item     : FREE Solid Wood Coffee Table Oak Elongated Octagon Shelf
platform : facebook_marketplace          ← НЕ OfferUp
```

### Гварды проверены ДО выхода на экран
аварийный стоп `False` · покупатель ждёт `False` · телефон отвечает (ping 0% loss).

### Ловушка, которую проверил отдельно
Контракт решателя гоняет поддельный маршрут «мои объявления **на OfferUp**». Если бы он
записал его в БОЕВОЙ `learned_flows.json`, живой прогон пошёл бы **не в то приложение** —
столик-то на Facebook. Проверил: боевого файла нет вовсе, контракт писал в свой временный
`KOLA_STATE`. Изоляция удержала, прогон не сбит.

### 🔴 Заранее и честно: это ТРУДНЫЙ случай
Витрина FB «Your listings» — ровно та, что записана 14.08 как **недостижимая циклом
зрения**. Именно на ней навигация ложилась дважды. Поэтому исход может быть и «не дошёл».
Если так — покажу, где встал, и кадр места остановки. Успехом это назвать нельзя, и я не
назову.

---

## Состояние

| | |
|---|---|
| Коммиты | `9d2637e` (money-path транспорт) · `1f53e0d` (проводка + парный контракт) |
| Гейт | 🔓 пройден авторизованно, не обойдён |
| Контракты | **15/15** после применения |
| Связь с телефоном | ✅ живая (доказана кадром 16:47, `media/proof/2026-08-15-1647_link_restored_live.png`) |
| Живое снятие | ⏳ **ИДЁТ** с 16:55:46 — итог отдельным отчётом |
| Титан / Solana | ✅ не тронуты |

Ничего не объявлено сделанным. Столик считается снятым только тогда, когда это будет
прочитано с экрана — и тогда я пришлю кадр.
