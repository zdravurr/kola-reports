# kolya-66581f60b0c3

_2026-08-24 01:33 UTC_

---

# 66581f60b0c3 [infra] — контракт test_proof_window_and_watchdog_mouth: 4 красных проверки

## Итог
Контракт ЗЕЛЁНЫЙ. Полный прогон: **106/106**. Правки в код не потребовались — причина уже устранена, я её нашёл, проверил и подтвердил, что контракт при этом НЕ ослаблен.

## Что было
Прогон 23.08 23:5x дал 99/100. Падали 4 проверки, все про одно:
- «исходящий акт SENT воркером остаётся пруфом»
- «обоснование НАЛИЧИЕМ ответа в треде остаётся пруфом»
- «входящее ДО акта тем же пруфом закрыто»
- «контемпоранный RESOLVED судится по времени записи»

Смысл поломки был не косметический: живой пруф ответа переставал закрывать входящее → уже отвеченный покупатель мог пере-обрабатываться и долбиться повторно.

## Причина
Не дефект кода в истории, а **снимок недоделанной правки в рабочем дереве**. В момент прогона `lib/reply_proof.py` был модифицирован незакоммиченной работой соседней задачи `3e7d434506fe` — там резали `force-done` как пруф («административное закрытие ≠ ответ», подделанный счёт по David). Промежуточное состояние резало границу ПО ТОКЕНУ, и под нож попали заодно живые исходящие акты — «SENT воркером» и «ответ уже в треде».

Соседняя сессия довела правку до конца сама: коммит `791f27d` (24.08 00:23) переносит границу **на СОБЫТИЕ, а не на токен** — `force-done` больше не пруф, а «SENT воркером» и «ответ уже в треде» пруфом остаются. В сообщении коммита эти самые 4 утверждения названы явно.

## Как проверил, что это не «зелёное по слабости теста»
`git log` по `tests/test_proof_window_and_watchdog_mouth.py`: последняя правка — `2864038`, то есть **ДО** поломки. Коммит-починка `791f27d` тест не трогал. Значит зелёный получен ужесточением кода под неизменившийся контракт, а не подгонкой контракта под код.

Прямой прогон файла: все 25 проверок ✅, включая 4 бывших красных. Полный `run_contracts.py`: 106/106.

## Класс, который стоит помнить
Красный контракт может быть снимком чужой правки НА ПОЛПУТИ, а не регрессом в истории. Прежде чем чинить — сверить рабочее дерево с историей: чинить уже починенное дороже, чем посмотреть `git log` по файлу.

## Открытых хвостов нет
Эскалации не требуется, новых поломок по ходу не найдено.
