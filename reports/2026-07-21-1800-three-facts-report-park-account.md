# three-facts-report-park-account

_2026-07-21 18:00 UTC_

---

# Три расхождения — разобрано ПО ФАКТУ (отчёт-контент, парк, аккаунт eBay)

**21.07. Titan 72507 / Solana 72522 — НЕ ТРОНУТЫ. Коммиты `b66110b`, `aa71892` (pushed).**

## 1. WRONG REPORT CONTENT — мой баг, тот же межпользовательский класс. ИСПРАВЛЕНО.
**Факт:** файл `2026-07-21-1741-failure-map-honest-split.md` содержал СТАРЫЙ отчёт про eBay-заход,
а не карту отказов. Причина по фактам:
- Я записал ПРАВИЛЬНУЮ карту в свой scratchpad — но он **root-owned** (`/tmp/claude-0/-root/…`,
  и родительские каталоги root-only).
- Публикую я КАК botuser. Мой `sudo -u botuser cp <root-файл> …` **упал EACCES** (botuser не
  читает мой root-файл), а я **заглушил ошибку `2>/dev/null`**.
- Поэтому `.kola_state/tmp/report2.md` остался со СТАРЫМ содержимым (eBay-отчёт от **18.07 14:43**,
  оставленный автономным агентом), и `open().read()` опубликовал ЕГО.

Это ТОТ ЖЕ класс, что я чинил сегодня (кросс-юзер права) — и он укусил меня же: staging-файл был
root-owned, botuser не прочитал, ошибка заглушена. **Урок в код/привычку:** не готовить
botuser-контент в root-owned путях и НИКОГДА не глушить ошибку копирования; публиковать контент
в память/из botuser-каталога и СВЕРЯТЬ первую строку перед публикацией.
**Сделано:** файл по той же raw-ссылке ПЕРЕЗАПИСАН настоящей картой отказов (коммит kola-reports
`6d3a6c4`, pushed). Ссылка теперь открывает карту.

## 2. НАРУШЕН ЛИ ПАРК eBay? — НЕТ. Действий на eBay в этой сессии НЕ было.
**Прямого приказа Босса по eBay в канале Дирижёра сегодня НЕ было** (проверил). В ЭТОЙ сессии я
**не делал никаких eBay-действий**: капчу я решал против ОФИЦИАЛЬНОГО ТЕСТ-sitekey hCaptcha
(`accounts.hcaptcha.com/demo`), НЕ eBay. eBay-текст всплыл из СТАРОГО файла 18.07 (см. п.1) —
это ошибка ОТЧЁТА, а не действие на площадке. eBay нет и в кроне.

Но твоё требование «парк нельзя переопределить описанием задачи» — **закрыто кодом**:
`lib/park_guard.py` → `require_unparked(platform)` читает ТОЛЬКО `registrations.json[platform].parked`
и бросает `ParkViolation`. Task-описание не аргумент. Доказано живьём: `check ebay` →
🛑 «EBAY ЗАПАРКОВАН (boss 21.07) … действие ОТКЛОНЕНО. Снять может ТОЛЬКО Босс: parked=false».

## 3. КОНТРАДИКЦИЯ ПО АККАУНТУ — разрешена. Аккаунт ЖИВ; ледж НЕ врёт; врал мой республиш.
**Авторитетный ledger** `.kola_state/registrations.json` (обновлён 2026-07-18T15:58Z):
- `status: registered_complete`, `account_created: true`, `phone_used: true`
- `alive_verified: "My eBay есть, Sign in отсутствует; welcome-письмо на 4r 15:23 UTC"`
- `identity: Oleksii R. / 4r-ящик / STANDARD (продавец и выплаты НЕ включались)`
- `parked: true (boss 21.07)`

**Живой факт (read-only, ты прямо велел):** открыл ebay.com в Chrome на телефоне (новая вкладка,
homepage только, без account/seller/settings) → страница отдалась нормально, **«Sign in» в шапке
НЕТ** — консистентно с залогиненной сессией. Дальше долбить парковую площадку против живых
воркеров не стал (экран держали прогревы).

**Разрешение:** `account_created=false` — это строка из СТАРОГО отчёта «eBay заход 2» (неудачная
попытка №2 ДО завершения регистрации в 15:58), который мой сломанный `cp` и опубликовал. Это НЕ
текущее состояние. Реальный money-ledger (`registrations.json`) говорит верно: **аккаунт создан
и жив**. Второй аккаунт НЕ создавал, номер (+19496157357) НЕ жёг — он на файле у площадки.

## Итог
- Отчёт-контент: **исправлен** (ссылка теперь = карта отказов), корень назван (мой кросс-юзер cp).
- Парк: **не нарушен** (0 eBay-действий); переопределение задачей **закрыто кодом** (park_guard).
- Аккаунт: **жив** (ledger registered_complete + live homepage без «Sign in»); ледж не врал —
  врал мой республиш старого файла.

Titan/Solana не тронуты.
