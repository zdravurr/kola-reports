# 🔴 TITAN — КНИЖНЫЙ ГЕЙТ ПРИМЕНЁН И ЖИВ. В DRYRUN. НЕ ОТКАЗЫВАЕТ НИЧЕГО.

**2026-09-07 13:45 UTC · коммит `3b075fd` · перезапуск из плоского состояния 13:29:26 UTC**
`openitems_guard` — **EXIT=0 на входе**, **EXIT=0 на выходе** (между ними он честно поймал мой
собственный коммит и потребовал обновить шапку канона — обновил, коммит `3afc645` в kola-reports).

---

# 🔴 ГЛАВНОЕ, СРАЗУ И ЧЕСТНО — ЧЕГО ПОКА НЕТ

**Пункт 6 НЕ ЗАКРЫТ ЖИВЬЁМ. Ни одной оценки гейта после перезапуска ещё не было.**
`[BOOK-GATE]` в журнале: **0 строк**. Строк с заполненными `book_gate_*`: **0**.
Я НЕ выдаю это за работающее — сторож на первую такую строку **висит и ждёт прямо сейчас**.

Почему так, и почему это ожидаемо, а не поломка: гейт стоит **последним из дешёвых гейтов** —
после HTF-каскада, EMA-конверта, планки score, списка фильтров и риск-гейта. За 35 суток до
него добрались **199 сигналов ≈ 5.7/сутки, крайне неровно** (14 за 06.09, **1** за 02.09,
**0** за несколько дней). После перезапуска пришло 4 сигнала — все умерли раньше:
`htf_blocked`, `htf_blocked`, `exit_unarmed_noop`, `confirm_recorded`. До гейта не дошёл ни один.

## 🔴 И ОТДЕЛЬНО — ДЕФЕКТ В САМОЙ ФОРМУЛИРОВКЕ ПУНКТА 6, КОТОРЫЙ НАДО ЗНАТЬ

Ты просил «первую `[BOOK-GATE]` оценку **на ДОПУЩЕННОЙ строке**». **Такой строки не будет
никогда — по устройству патча, а не по недоделке.** `print` стоит ВНУТРИ ветки отказа:

```
if _book_verdict.get('refuse'):          # main.py, обе точки входа
    _bg_line = (f"[BOOK-GATE] {'DRYRUN would-refuse' ...}")
    print(f"[TITAN]{_bg_line}", flush=True)
```

**Допущенная строка пишет шесть колонок МОЛЧА и не печатает ничего.** `[BOOK-GATE]` в логе
появится только на would-refuse — по замеру это ~4 % строк, т.е. примерно **раз в 4–5 суток**.
Значит доказательство на допущенной строке — **это сами шесть колонок в БД, а не строка лога**.
Сторож ждёт именно колонки. Ждать строку лога как признак «работает» — значит ждать месяц
и решить, что гейт мёртв.

## 🔴 ЧТО ПРИ ЭТОМ УЖЕ ДОКАЗАНО ЖИВЬЁМ, А НЕ ПРЕДПОЛОЖЕНО

**Живой воркер выполнил пропатченный `init_db` и САМ создал шесть колонок.** Это не косвенное:
колонки `book_gate_clause / opp_mult / opp_pctl / opp_dist_pct / lean / n_supporting` стоят
позициями **149–154, то есть последними в таблице** (добавлены `ALTER TABLE` последними), а
единственный код во всём дереве, который их создаёт — `init_db` в `main.py`, которого до патча
не существовало. `grep` по дереву даёт ровно три вхождения `book_gate_clause`, все в `main.py`.
До перезапуска этих колонок не было, после — есть. Их сделал **процесс**, не я.

---

# 1. ПЛОСКОЕ СОСТОЯНИЕ — ПОДТВЕРЖДЕНО ДВАЖДЫ, ОБОИМИ ЗОНДАМИ, БЕЗ ОШИБОК

Проверено **до** применения патча и **повторно непосредственно перед** перезапуском:

| проверка | результат |
|---|---|
| `virtual_positions status='open'` | **0** |
| `exit_pending` | **0** |
| `breakeven_jobs` | **0** |
| BingX позиции, зонд 1 `fetch_positions` | **{}** |
| BingX позиции, зонд 2 raw `swapV2 UserPositions` | **{}** |
| BingX открытые ордера, оба зонда | **{}** |
| ошибки зондов | **[] — пусто** |

🔴 Пустой словарь при **пустом списке ошибок** — это «плоско». Пустой словарь при непустых
ошибках означал бы «МЫ НЕ ЗНАЕМ», и я бы остановился. Ошибок не было ни в одном из четырёх
вызовов. Сам бот подтвердил то же самое при загрузке своим голосом:

```
[TITAN][RECONCILE-XDB] ✅ exchange and DB agree for BTC/USDT:USDT: 0 exchange position(s), 0 open row(s)
[TITAN][STOP-CLEANUP] no orphaned orders for LONG BTC/USDT:USDT
[TITAN][STOP-CLEANUP] no orphaned orders for SHORT BTC/USDT:USDT
```

---

# 2. ПАТЧ ПРИМЕНЁН — ЧЕТЫРЕ ФАЙЛА, ВСЕ НА МЕСТЕ

```
 titan-bot/config.py           | 141 +++++++++++++
 titan-bot/main.py             | 186 ++++++++++++++++++
 titan-bot/skip_attribution.py |   7 +-
 titan-bot/book_gate.py        | 245 +++++++++++++++++++++++++  (НОВЫЙ ФАЙЛ)
 4 files changed, 578 insertions(+), 1 deletion(-)
```

`book_gate.py` **существует в дереве Титана**: `/root/titan-bot/book_gate.py`, 12 750 байт.
Все четыре компилируются (`py_compile` чисто).

## 🔴 ОДНА ПОПРАВКА К ТВОЕЙ КОМАНДЕ, ЧТОБЫ ОНА НЕ ПОДВЕЛА В СЛЕДУЮЩИЙ РАЗ

Ты написал «`git apply -p2` из `/root`». **Это ровно та комбинация, которая НЕ работает** —
единственная из трёх:

| откуда | ключ | результат |
|---|---|---|
| `/root` | `-p1` | ✅ **0** |
| `/root/titan-bot` | `-p2` | ✅ **0** |
| `/root` | `-p2` | ❌ `error: config.py: No such file or directory` |

Пути в диффе — `a/titan-bot/config.py`. `-p2` срезает `a/` и `titan-bot/`, значит рабочим
каталогом обязан быть `titan-bot`. Из `/root` эквивалент — `-p1`. Оба зелёных варианта дают
**побайтово одинаковый результат**; я применил `-p2` из `/root/titan-bot`. Ничего не потеряно,
но формулировку стоит запомнить исправленной.

---

# 3. 🔴 ФЛАГИ В РАНТАЙМЕ — ИЗ ЗАГРУЖЕННОГО БАЙТКОДА, НЕ ИЗ ФАЙЛА

**Все четыре — как заказано:**

```
BOOK_GATE_ENABLED          = True
BOOK_GATE_DRYRUN           = True     ← ОТКАЗЫВАЕТ НИЧЕГО
BOOK_GATE_CLAUSE_A_ENABLED = True
BOOK_GATE_CLAUSE_B_ENABLED = False    ← разоружён по замеру, vpos 94
```

**Как именно это прочитано, и почему это байткод, а не файл.** Значения дизассемблированы
**из `__pycache__/config.cpython-312.pyc`** — не из `config.py`:

* `.pyc` написан **13:27:02**, воркер стартовал **13:29:32**, то есть **позже**;
* заголовок `.pyc` содержит `(mtime, size)` исходника и **сходится побайтово** с текущим
  `config.py` (`mtime=1788787617 size=76580`), магия `cb0d0d0a` совпадает с интерпретатором 3.12.3;
* следовательно CPython при загрузке **признал `.pyc` валидным и выполнил ИМЕННО ЕГО**, не
  перекомпилируя — иначе он бы переписал файл и mtime стал бы 13:29:xx. Это буквально тот
  байткод, который живёт в процессе.

То же проделано для `main.pyc`, `book_gate.pyc`, `skip_attribution.pyc` — все четыре валидны
против своих исходников.

**Вторая, независимая проверка того же:** `main.py` на уровне модуля делает
`from config import BOOK_GATE_DRYRUN` и `import book_gate`. Отсутствуй любое из имён — воркер
упал бы с `ImportError` и не поднялся. Он поднялся и обслуживает. Значит **в живом процессе оба
имени есть**.

🔴 **Чего я НЕ делал и почему.** `gdb`/`pyrasite` на этой машине нет, а ставить и цеплять
отладчик к процессу, который держит РЕАЛЬНЫЕ деньги, я не стал: `gdb` останавливает процесс
сигналом. Замораживать живого торгового бота ради красивой строчки в отчёте — плохой обмен.
Цепочка выше даёт то же утверждение без остановки процесса.

---

# 4. 🔴 НЕ ТРОНУТО — ПО AST И ПО РАНТАЙМУ

## 4a. Самое сильное доказательство — структурное

**Во ВСЁМ диффе удалена РОВНО ОДНА строка**, и та — закрывающая скобка кортежа:

| файл | удалённых строк |
|---|---|
| `config.py` | **0** |
| `main.py` | **0** |
| `skip_attribution.py` | **1** — `'failed')` → `'failed', 'book_blocked')` |

Ноль удалённых строк в `config.py` и `main.py` означает, что **ни одна существующая строка не
изменилась** — патч чисто добавляющий. Это сильнее любого поимённого перечисления.

## 4b. AST-дифф всех функций `main.py` (84 до → 84 после)

```
ADDED  : NONE
REMOVED: NONE
CHANGED: _handle_5m_trigger, _handle_state_machine, init_db
```

Ровно три: **две точки входа** (куда и встал гейт) и **init_db** (шесть колонок). AST **ни одной
другой функции не сдвинулся**.

## 4c. Полный дифф конфига — все 116 имён до против 124 после

```
ADDED   : BOOK_GATE_CLAUSE_A_ENABLED, BOOK_GATE_CLAUSE_B_ENABLED, BOOK_GATE_DRYRUN,
          BOOK_GATE_ENABLED, BOOK_GATE_LEAN_FLOOR, BOOK_GATE_MIN_SUPPORTING,
          BOOK_GATE_WALL_DIST_PCT, BOOK_GATE_WALL_PCTL
REMOVED : NONE
CHANGED : NONE
```

*(Первый прогон пометил `CONFIRMED_REVERSAL_IDS` и `OBSERVE_REVERSAL_IDS` — это `set`, их
`repr` перемешивается от `PYTHONHASHSEED`. Сравнение по ЗНАЧЕНИЮ: `equal=True` для обоих, и в
диффе `config.py` этих имён нет вовсе. Ложная тревога инструмента, не изменение.)*

## 4d. Поимённо, как ты просил — AST из исходника + рантайм

| значение | AST | рантайм |
|---|---|---|
| `SL_ATR_MULT` | **2.25** | 2.25 |
| `TRAIL_MULT_ATR` | **1.6875** | 1.6875 |
| EMA-конверт: `GATE_ENABLED` / `TFS` / `REQUIRED_DIR` / `FAIL_OPEN` / `REFUSAL_CARD` | True / `('1h','15m')` / `'Expanding'` / True / False | то же |
| каскад (a) `HTF_NEUTRAL_REQUIRE_15M_DRYRUN` | **True** | True |
| каскад (b) `CONFLUENCE_FLAT_THRESHOLD` | **3.0** | 3.0 |
| каскад (c) `HTF_REARM_FROM_15M_ENABLED` / `COOLDOWN` / `DRYRUN` | True / 60 / False | то же |
| `LONG_PARTIAL_ENABLED` | **False** | False |
| `CONFLUENCE_SCORE_THRESHOLD` | **3.0** | 3.0 |
| `EXIT_ADVISOR_DRYRUN` | **False** | False |
| `LIVE_TRADING_ENABLED` | **True** | True |
| `ORDER_ADAPTER_LIVE` | **True** | True |
| размер позиции `LIVE_FIXED_MARGIN_USDT` × `LEVERAGE` | **30.0 × 5** | 30 × 5 |

**Оба промпта советника: `claude_advisor.py` ПОБАЙТОВО ИДЕНТИЧЕН.**
sha256 `ca14e959…a1b8c6c6` до патча и после — файл в патче не участвует вообще. Из 38 `.py`
файлов Титана хеш сменился ровно у трёх + добавился новый `book_gate.py`.

**Число мест вызова в `main.py` до/после:** `ema_envelope` 9/9, `SL_ATR_MULT` 15/15,
`TRAIL_MULT_ATR` 4/4, `HTF_NEUTRAL_REQUIRE_15M` 4/4, `CONFLUENCE_FLAT_THRESHOLD` 6/6,
`EXIT_ADVISOR_DRYRUN` 5/5, `LIVE_TRADING_ENABLED` 5/5, `ORDER_ADAPTER_LIVE` 1/1,
`active_fixed_margin` 2/2. **Реальных вызовов `consult_for_entry(` — 2/2** (текстовых вхождений
стало 6→10, но все четыре новых — прозаические упоминания в комментариях патча, не код).

---

# 5. ПЕРЕЗАПУСК ИЗ ПЛОСКОГО — СТРОКА ЗАГРУЗКИ

```
Sep 07 13:29:32 titan[169385]: [TITAN] ====================================================
Sep 07 13:29:32 titan[169385]: [TITAN][ORDER-MODE] 🔴 LIVE ORDERS — REAL MONEY: orders ARE sent to BingX
Sep 07 13:29:32 titan[169385]: [TITAN][ORDER-MODE]   LIVE_TRADING_ENABLED = True
Sep 07 13:29:32 titan[169385]: [TITAN][ORDER-MODE]   ORDER_ADAPTER_LIVE   = True
Sep 07 13:29:32 titan[169385]: [TITAN][ORDER-MODE]   sizing: margin $30 x 5 = $150 notional per entry
Sep 07 13:29:32 titan[169385]: [TITAN] ====================================================
```

Мастер `169385`, воркер `169402`, служба `active`. **Этот баннер печатает сам процесс из своего
загруженного конфига** — то есть это ещё и рантайм-подтверждение трёх значений из §4d.

`openitems_guard` **EXIT=0** после. Он же перед этим честно отказался (**EXIT=1**,
`doc='652bb10' runtime='3b075fd'`) — шапка канона обновлена, снова зелено.

---

# 6. КОД ГЕЙТА ПРОВЕРЕН НА РЕАЛЬНЫХ КНИГАХ — ПРЕДРЕГИСТРАЦИЯ СОШЛАСЬ ТОЧНО

Пока живой сигнал не пришёл, я прогнал **применённый** `book_gate.evaluate` (файл
`/root/titan-bot/book_gate.py`, тот самый) по **199 настоящим книгам** из `advisor_book_json`:

| величина | предрегистрировано 11:45 | получено на применённом дереве |
|---|---|---|
| отказ LONG | 4.26 % | **4.26 % (4/94)** |
| отказ SHORT | 3.81 % | **3.81 % (4/105)** |
| перекос сторон | 1.12x | **1.12x** |
| clause A / clause B | 100 % / 0 % | **A: 8, B: 0** |
| `evaluate()` бросил исключение | 0 | **0** |
| строк с непрочитанной книгой | — | **0 из 199** |

Совпало **до второго знака по всем строкам**. Это не «должно работать» — это применённый код,
прогнанный по данным, которых он раньше не видел, с заранее записанным ответом.

🔴 **Напоминание из самого патча, которое я не смягчаю:** это **ДИСЦИПЛИНА, А НЕ ИЗМЕРЕННОЕ
ПРЕИМУЩЕСТВО.** Гейт **НЕ отказал бы** ни vpos 102, ни vpos 103 — тем двум стопам, из-за которых
он и появился. Если будущий замер не найдёт денег — это **ОЖИДАЕМЫЙ** результат и **НЕ повод
его снимать**. Поводы снять: срабатывает слишком часто, срабатывает перекошенно по сторонам,
отказывает сделкам, которые трейдер бы взял.

---

# 7. 🔴 ТОЧКА РЕВИЗИИ: **200 ОЦЕНОК ГЕЙТА — В СТРОКАХ, НЕ В ДНЯХ**

**Счётчик стартует с объявленного нуля.**

| | |
|---|---|
| счёт на момент перезапуска 13:29:26 UTC | **0 / 200** |
| строк с непустыми `book_gate_*` в `trades` | **0** |
| единица измерения | **СТРОКИ. НЕ ДНИ.** |
| ожидаемый темп | ~5.7 строк/сутки, неровно ⇒ **порядка 5 недель** |

Календарная ревизия оценила бы выборку, которая ещё не пришла: 199 строк набирались **35 суток**.
Считать надо строки.

Что мерить на 200-й: доля отказов **по каждой стороне отдельно** и **отношение сторон**.
Порог тревоги задан заранее и не подлежит тихому смягчению: **выше 5 % на любой стороне или
отношение сторон выше 2x** — это находка о калибровке, **пере-резать по строкам, дошедшим до
гейта, по сторонам, и показать оба среза**. n=94/105 ⇒ **одна строка это ~1 процентный пункт**;
к тому же четыре LONG-отказа включают три выборки **одной и той же секунды книги**
(26330/26331/26332), так что по различимым книгам LONG-популяция = **74, а не 94**.
Проценты не читать до второго знака.

---

# 8. КАСКАДНЫЙ СЧЁТЧИК — НЕ СДВИНУТ ЭТИМ ПРОХОДОМ

| правило `§0.CASCADE-STOP` | порог | до прохода | ПОСЛЕ |
|---|---|---|---|
| ΣR ≤ −5.0R | −5.0 | −4.2353 | **−4.2353** ❌ не сработало |
| 10 закрытых позиций | 10 | 9 | **9** ❌ не сработало |

Последнее закрытие — **vpos 103, 2026-09-07 03:37:25 UTC**, то же самое. Открытых позиций
сейчас **0**. Остаётся запас **0.7647R** и **одно** закрытие.

🔴 **И это ровно то, почему применение сегодня было безопасным.** `BOOK_GATE_DRYRUN = True`
меняет **НОЛЬ решений**: шесть колонок пишутся **ДО** ветки dryrun, отказа не происходит,
карточки нет, `book_blocked` в БД нет, записи в `skip_attribution` нет. Телеметрия копится с
первого же сигнала, эксперимент не запачкан. Ждать было бы платой телеметрией ни за что.

# 🔴 9. ВООРУЖЕНИЕ — ОТДЕЛЬНОЕ РЕШЕНИЕ, ВСЁ ЕЩЁ ЗА ТОБОЙ

**Я НЕ вооружал гейт и не буду без твоего слова.** `BOOK_GATE_DRYRUN` остаётся `True`.
Решение о `False` берётся **после того, как разрешится каскадный эксперимент** — как ты и сказал.
Clause B остаётся `False`: его единственный исторический отказ по LONG — **vpos 94, +0.8650R,
крупнейший выигрыш живого батча**. Числа лежат в конфиге, чтобы вооружить в тот день, когда
популяция скажет иначе; смягчать пол ради этой сделки нельзя — потребовался бы пол ниже
наблюдаемого минимума, то есть no-op под именем порога.

---

# 10. MERCURY-SOL — НЕ ТРОНУТ НИ ОДНИМ БАЙТОМ

`/mnt/volume_nyc1_1780480650620/mercury-sol` **не открывался, не читался, его БД не запрашивалась,
процесс не перезапускался.** Его мастер `3442516` как работал с **Thu Sep 3 19:45:14**, так и
работает — тот же PID, эксперимент `FLAT_ADX_GATE_DRYRUN` до 2026-09-17 идёт нетронутым.
Единственное, что я трогал на этом томе — репозиторий `kola-reports`, это другой каталог.

---

# ИТОГ

| пункт | статус |
|---|---|
| 0. `openitems_guard` на входе | ✅ EXIT=0 |
| 1. плоско, оба зонда, без ошибок | ✅ |
| 2. патч применён, 4 файла, `book_gate.py` в дереве | ✅ |
| 3. четыре флага из загруженного байткода | ✅ |
| 4. не тронуто — AST + рантайм + 0 удалённых строк | ✅ |
| 5. перезапуск из плоского, строка загрузки, guard EXIT=0 | ✅ |
| 6. **живая оценка гейта на допущенной строке** | ⏳ **ЕЩЁ НЕ ПРИШЛА — сторож ждёт** |
| 7. точка ревизии 200 оценок, старт с нуля | ✅ объявлено, счёт **0/200** |
| 8. каскадный счётчик не сдвинут | ✅ 9 / −4.2353 |

**Не закрыт один пункт — шестой, и он не может быть закрыт волей: он ждёт сигнала, который
переживёт каскад, EMA-конверт, планку score, фильтры и риск-гейт.** Сторож на первую такую
строку висит; как появится — доложу отдельно, с шестью колонками как есть.

Коммиты: Титан **`3b075fd`**, канон kola-reports **`3afc645`**.
