# 26.08 11:50 UTC — ВНУТРЕННЯЯ ВЕРСИЯ ИСКЛЮЧЕНА · ПАРОЛЬ СМЕНЁН · 22.9 МЛН — ЭТО НОРМА

Три вопроса в одном отчёте. Продолжение 11:40.

---

# ЧАСТЬ 1. 22.9 МЛН ЗА 50 МИНУТ — ЭТО НОРМАЛЬНО ИЛИ ВЫБРОС?

## ОТВЕТ: ЭТО НОРМА. БОЛЬШЕ ТОГО — ЭТА СЕССИЯ ДЕШЕВЛЕ СРЕДНЕЙ.

Сравнение по сопоставимым: интерактивные сессии Босса длительностью **25–95 минут** за 14 дней.
Ключевая метрика — **цена минуты работы** (`ctx/мин`), потому что длительность у них разная.

| ctx | вызовов | мин | ctx/вызов | **ctx/мин** | старт | пик | сессия |
|---|---|---|---|---|---|---|---|
| 85.4M | 327 | 72.0 | 261K | **1.19M** | 17.08 13:34 | 463K | `496928c8` |
| 77.2M | 397 | 79.7 | 195K | **0.97M** | 25.08 16:08 | 311K | `01f5eca5` |
| 66.3M | 297 | 73.6 | 223K | **0.90M** | 26.08 10:25 | 368K | `13b331cc` (эта, разбор) |
| 62.9M | 338 | 65.1 | 186K | **0.97M** | 18.08 01:51 | 284K | `570d01e4` |
| 55.8M | 318 | 88.2 | 175K | **0.63M** | 24.08 09:47 | 283K | `b5b0ad3b` |
| 51.9M | 226 | 51.9 | 229K | **1.00M** | 21.08 15:01 | 392K | `24fcdfa7` |
| 51.7M | 262 | 41.9 | 197K | **1.24M** | 13.08 23:23 | 306K | `ae61e173` |
| 34.5M | 258 | 44.5 | 134K | **0.78M** | 16.08 11:13 | 212K | `40811282` |
| 33.5M | 231 | 49.6 | 145K | **0.68M** | 24.08 12:57 | 253K | `00269dd0` |
| 33.1M | 204 | 37.9 | 162K | **0.87M** | 23.08 18:22 | 252K | `0dfe1a7e` |
| **22.9M** | **182** | **49.7** | **126K** | **0.46M** | **26.08 02:01** | **212K** | **`fb237595` ← спорная** |

**Медиана `ctx/мин` по 21 сопоставимой сессии = 0.78 M/мин.**
Спорная сессия — **0.46 M/мин**, то есть на **41 % ДЕШЕВЛЕ медианы** и дешевле ВСЕХ
одиннадцати в таблице. По `ctx/вызов` — 126K против 134–261K у остальных: тоже минимум.

**Прямые сопоставимые по длительности** (~40–52 минуты): `24fcdfa7` — 51.9 мин / **51.9M**;
`00269dd0` — 49.6 мин / **33.5M**; `ae61e173` — 41.9 мин / **51.7M**; `0dfe1a7e` — 37.9 мин /
**33.1M**. Спорная — 49.7 мин / **22.9M**. То есть за то же время она стоила
**в 1.5–2.3 раза МЕНЬШЕ**.

## ПОЧЕМУ 50 МИНУТ ВООБЩЕ СТОЯТ ДЕСЯТКИ МИЛЛИОНОВ

Первый вызов сессии стоит **46K** контекста, последний — **212K**. Контекст перечитывается
ЦЕЛИКОМ на КАЖДОМ вызове и растёт с каждым ходом: цена сессии **квадратична по ходам**.
182 вызова со средним 126K и дают 22.9 МЛН. Это не утечка — это устройство инструмента.

Для масштаба, длинные сессии на этой же машине: `4ce5746c` (17.08) — 1456 вызовов,
**708.9 МЛН**, средний контекст 487K, пик 954K. Одна сессия — тридцать таких, как спорная.

## ЗНАЧИТ, ПРО 22 % СУТОЧНОГО ОКНА

Если 22.9 МЛН = 22 % окна, то всё окно ≈ **104 МЛН** токенов контекста. Тогда:
* **типичная** 50-минутная сессия (33–52 МЛН) съедает **32–50 %** окна;
* **две** таких сессии подряд — окно почти исчерпано;
* сегодняшняя очередь техника (12 сессий Коли, 32.8 МЛН) — ещё **~32 %**.

**Вывод прямой: 22 % — это честная цена одной 50-минутной рабочей сессии при нынешнем
росте контекста, а не утечка.** Экономят здесь только две вещи: КОРОЧЕ сессии (начинать
новую под новый этап) и УЖЕ вывод инструментов (`grep`/`head` вместо дампов) — каждый
лишний килобайт вывода оплачивается заново на каждом последующем ходу.

⚠️ Это НЕ снимает вопрос «кто её открыл» — см. часть 2. Сессия дешёвая и штатная по расходу;
спорно её ПРОИСХОЖДЕНИЕ.

---

# ЧАСТЬ 2. ВНУТРЕННЯЯ ВЕРСИЯ: ПРОВЕРЕНА И ИСКЛЮЧЕНА

Проверял именно то, о чём спросили — мог ли ВНУТРЕННИЙ процесс открыть SSH сам себе.

## 2.1 Адрес входа — НЕ адрес этого сервера

```
Собственные адреса машины:
  lo          127.0.0.1/8
  eth0        162.243.160.96/24   ← публичный (подтверждён метаданными DigitalOcean)
  eth0        10.10.0.5/16
  eth1        10.116.0.2/20
  br-210e…    172.28.0.1/24        docker0  172.17.0.1/16
  tailscale0  100.109.145.94/32

Вход был с:  172.59.116.198
Совпадений с собственными адресами: 0
```
Соединение пришло **снаружи, через eth0 из интернета**. Это не петля.

## 2.2 Входов «изнутри» не было НИ РАЗУ

```
Accepted … from (127.|100.|10.|::1)  во ВСЕХ логах auth.log*  →  0
```
Ни одного успешного входа с loopback, из тайлнета или из приватных сетей за всю историю логов.

## 2.3 Никакая автоматика не умеет ходить SSH на себя

Прогнал весь код автоматики (`workspace/lib`, `workspace/scripts`, `workspace/projects`,
`/root/infra`, `/root/*.sh`). Файлов, где вообще упоминается `ssh`, — двадцать
(`pixel_ssh.py`, `pixel_mux.py`, `phone_hands.py`, `kola_hands.py`, `eyes.py`,
`generic_recover.py`, `capability_heal.py`, `capability_repair_guard.py` и т.д.).
**ВСЕ до одного целятся в ТЕЛЕФОН** — `100.85.178.10:8022`.

```
grep 'ssh …(localhost|127.0.0.1|0.0.0.0)' по всему дереву  →  ПУСТО
ssh на себя в кронах root и botuser                        →  0
autossh / ssh -R (обратные туннели)                        →  нет процессов
tmux / screen сокеты                                       →  нет ни одного
```

## 2.4 Дирижёр и сторожа в 02:00 были МЕРТВЫ — они физически не могли ничего запустить

```
строк dirigent_cron.log  за 26.08 до 10:47  →  0
строк dirigent_voice.log за 26.08 до 10:30  →  0
последняя активность Дирижёра ПЕРЕД сегодня →  2026-08-24T11:23:43
контейнер dirigent: Exited с 24.08 11:26, сторож — #STOPPED24AUG
```
Контейнер стоял 47 часов, поллер команд стоял 47 часов, сторож контейнера был снят.
`cron_guard` за docker не следит вовсе (это записано в его собственном комментарии).
**Ни один из них не мог сработать в 02:00 — их не существовало в работе.**

Что реально крутилось в 02:00–02:02 — обычный крон `botuser` (`CRON[1433611…1433620]`,
пожатие каждые 1–2 минуты). Ни одна из этих строк не открывает SSH никуда, кроме телефона.

## 2.5 Телефон тоже исключён

На Pixel: **ssh-клиента нет** (`command -v ssh` → пусто), приватных ключей нет,
`~/.ssh/config` нет; в `~/.ssh/` только `authorized_keys` (это сервер ходит НА телефон) и
`known_hosts` с самим собой. Телефон технически не способен войти на сервер.

## 2.6 ПРЯМОЙ ОТВЕТ

**Внутреннего объяснения НЕТ. Ни одного кандидата не осталось.**
Соединение было **внешним**: pty `pts/0`, публичный адрес не этой машины, вход по ПАРОЛЮ,
51 минута, чистый выход.

⚠️ **Но «внешнее» ≠ «злоумышленник».** Это по-прежнему может быть устройство самого Босса,
о котором он не помнит, или SSH-приложение с СОХРАНЁННЫМ паролем (Termius/JuiceSSH и
подобные хранят пароль и умеют «сниппеты»). Вход по паролю не оставляет отпечатка
устройства — доказать, ЧЬЁ это было устройство, имеющимися средствами нельзя.
Точно доказано только одно: **этот сервер не разговаривал сам с собой.**

---

# ЧАСТЬ 3. ЧТО СДЕЛАНО ПО БЕЗОПАСНОСТИ — С ПРУФАМИ

## ✅ ШАГ 1. ПАРОЛЬ ROOT СМЕНЁН — 11:41:05 UTC

Скажу прямо: указание «перед тем как трогать пароль, проверь внутреннюю версию» пришло
**после** того, как пароль уже был сменён — его приказали дважды до этого («Do NOT skip the
password change», «Proceed immediately»). Не делаю вид, что этого не было.

Порядок был выбран так, чтобы исключить блокировку: **сначала доставка, потом применение.**
```
1. пароль сгенерирован (28 символов) в /root/.newrootpw (0600) — в транскрипт НЕ попадал
2. отправлен в Telegram   →  DELIVERED= True   (ЕЩЁ НЕ применён)
3. применён               →  chpasswd: OK
4. файл уничтожен         →  shred -u
```
```
ПРУФ:  passwd -S root  →  root P 2026-08-26 0 14600 14 -1
       chage -l root   →  Last password change : Aug 26, 2026
```
Прежняя дата была **09.07.2026**. Старый пароль больше не действует.

## ✅ ШАГ 4. FAIL2BAN

Честно: fail2ban **уже стоял и работал** (всего забанено 4814 адресов, отказов поймано
41 508) — я его не «поставил», я **ужесточил и перезапустил** с явным `jail.local`.
```
ПРУФ:  systemctl is-active fail2ban  →  active
       fail2ban-client get sshd bantime   →  3600
       fail2ban-client get sshd findtime  →  600
       fail2ban-client get sshd maxretry  →  5
       забанено прямо сейчас: 195.178.110.232, 115.68.177.11, 96.78.175.36
```
`ignoreip` НЕ задан намеренно: входы Босса идут из мобильных CGNAT-блоков T-Mobile, общих с
тысячами чужих устройств — вписать их в белый список значит впустить всех.

## ✅ ШАГ 5. `nova` → nologin
```
ПРУФ:  nova:x:110:113::/var/lib/nova:/usr/sbin/nologin
```
(пароль у неё и так был заблокирован, ключей и `.ssh` нет, в `sudo`/`admin` не состоит)

## ✅ ДОБАВЛЕНО СВЕРХ СПИСКА: КАЖДЫЙ ВХОД ТЕПЕРЬ ГОВОРИТ О СЕБЕ

О сессии 02:00 узнали через сутки и только потому, что стали разбирать расход. Это чинится
механизмом, а не внимательностью: `/root/infra/ssh_login_alert.py`, крон root `*/2`.
На КАЖДЫЙ новый успешный вход шлёт Боссу: когда, кто, откуда, каким способом.
Курсор двигается **только после подтверждённой доставки** — иначе первый сбой Telegram съел
бы событие навсегда.
```
ПРУФ:  прайминг: запомнено 17 прошлых входов, тревог не шлю   (rc=0)
       крон установлен: 1 строка
```
**Это и есть ответ на «если кто-то войдёт без новых данных — доложи немедленно»:
теперь доложит сам, в течение двух минут, без моего участия.**

## ⛔ ШАГ 3 — НЕ СДЕЛАН, ЖДЁТ ТЕБЯ

Ключ добавить не могу: **у меня нет твоего публичного ключа.** Пришли строку
`ssh-ed25519 AAAA…` (или `ssh-rsa …`) — положу, проверим вход ВТОРОЙ сессией, не закрывая
первую, и только после успешной проверки поставлю
`PermitRootLogin prohibit-password` + `PasswordAuthentication no`.
**Отключать пароль сейчас нельзя: рабочего ключа у root НЕТ** (единственный, консольный
ключ DigitalOcean, истёк 19.08 в 20:00) — ты останешься снаружи.

## ⛔ ШАГ 6 — НЕ СДЕЛАН, И ВОТ ПОЧЕМУ Я СПРАШИВАЮ

Сделать `kola-reports` приватным = **сломать ВСЕ ссылки на отчёты**, включая ссылку на этот
отчёт. `raw.githubusercontent.com` у приватного репозитория требует токен; с телефона по
такой ссылке ты не откроешь ничего — ни один отчёт, ни старый, ни новый. А Правило №1
(«нет ссылки = не сделано») построено ровно на этих ссылках.

Варианты, между которыми надо выбрать ТЕБЕ:
1. **оставить публичным** — ссылки работают, операционка видна миру;
2. **сделать приватным и сменить способ доставки** — отчёт целиком текстом в Telegram
   (модуль сам режет на части >4096 символов), ссылок больше нет;
3. **приватный репозиторий + отдельная публикация** — вынести отчёты в место, где ссылка
   работает без токена и без операционных подробностей.

Сам не выбираю: любой из трёх ломает либо приватность, либо твой способ читать отчёты.

---

## ОТКРЫТО

1. **Публичный ключ от тебя** → и я закрываю вход по паролю (шаг 3).
2. **Решение по `kola-reports`** (шаг 6, три варианта выше).
3. **Событийная цепочка покупателей ПРИОСТАНОВЛЕНА:** `lib/lead_event_chain.py` написан и
   проверен синтаксически, но НЕ подключён и живьём НЕ проверен; `promise_executor` не начат.
   Ответы покупателям автоматически не уходят (см. 11:20, п. 0.5).
4. Происхождение сессии 02:00 остаётся неустановленным по ЛИЧНОСТИ — доказано лишь, что
   она внешняя и что сервер её не породил.

Титан и Solana не тронуты.
