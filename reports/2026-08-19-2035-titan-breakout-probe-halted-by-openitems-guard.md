# TITAN — РАССЛЕДОВАНИЕ ПРОПУЩЕННОГО ПРОБОЯ **ОСТАНОВЛЕНО ГЕЙТОМ**. Ни один пункт задания не выполнен.

**2026-08-19 20:35 UTC · READ-ONLY · Титан НЕ ТРОНУТ, ничего не перезапускалось.**

## Что было приказано

Задание: выяснить, почему Титан не отторговал пробой BTC 19.08 (из коробки ~64 000 в фитиль
70 000, осадка ~68 300), по пяти разделам — воронка сигналов ±6 ч, проверка гейта EMA-конверта на
его же заявке от 05.08, каскад, реплей «что было бы», вердикт.

Приказ содержал предусловие, и оно сработало:

> **Run openitems_guard first; if non-zero, say so and stop.**

## Гейт вернул НЕ НОЛЬ. Я остановился.

```
$ python3 /root/titan-bot/tools/openitems_guard.py
openitems_guard — canon: .../kola-reports/reports/OPEN-ITEMS.md
  runtime HEAD   : 4c1ab56
  watched values : 11

🔴 1 MISMATCH(ES) — the canon asserts something runtime denies:
  HEAD                           doc='897850b'          runtime='4c1ab56'
                                 header commit is stale — body writes do not refresh it

REFUSING to bless this canon. Fix the header/table, or do not publish a snapshot from it.
EXIT=1
```

**Расхождение РОВНО ОДНО, и это строка HEAD.** Остальные 10 наблюдаемых значений — включая оба
флага боевого режима, геометрию стопа и трейла и обе планки счёта — гейт перечитал импортом
`config` в рантайме и они СОШЛИСЬ. То есть канон врёт не про торговое состояние, а про то, на
каком коммите он был заверен.

## Насколько эта чёрствость существенна

Проверено, а не предположено: между заверенным `897850b` и рантаймовым `4c1ab56` лежат четыре
коммита, и **ни один из них не трогает `titan-bot/`**.

```
$ git diff --stat 897850b..HEAD -- titan-bot/
(пусто)

$ git diff --name-only 897850b..HEAD
.gitignore
infra/authorized_commit.sh
infra/claude_session_cost_notice.py
infra/claude_task_dedup_hook.py
infra/orphan_shell_reaper.py
infra/token_accounting.py
```

4c1ab56 — проход commit-гейта файлом · 0dea45a — леджер расхода подписки ·
4f59749 — хук UserPromptSubmit · 4645382 — жнец осиротевших оболочек. Вся четвёрка
инфраструктурная.

**Вывод о самой чёрствости: она КОСМЕТИЧЕСКАЯ ПО ПРОИСХОЖДЕНИЮ — код Титана за эти четыре
коммита не двигался.** Но гейт для того и поставлен, чтобы судить не мои рассуждения о
существенности, а факт совпадения; 04.08 сгорели ровно на том, что «ну это же мелочь».
Отсюда — стоп, а не «пройду мимо, там всё равно инфра».

## 🔴 Чего в этом файле НЕТ

Ни одного ответа по заданию. Не открывался леджер тишины, не считалась воронка ±6 ч, не
читались `ema_gap_dir_1h`/`ema_gap_dir_15m` на баре пробоя, не мерялся каскад, не гонялся реплей
геометрии, вердикт не выносился. **Вопрос «почему Титан не взял пробой» на 20:35 UTC ОТКРЫТ.**

Обрати внимание на несостыковку в самой постановке: приказ описывает Титан как «HEAD 897850b» —
то есть задание выписано с той же чёрствой шапки, которую гейт и отверг.

## Что снимет блокировку

Освежить строку HEAD (и таблицу текущего состояния) в
`kola-reports/reports/OPEN-ITEMS.md` до `4c1ab56`, прогнать гейт до `EXIT=0` — и запускать
расследование заново с первого раздела. Правку шапки я не делаю: она меняет канон, а ход был
объявлен READ-ONLY.
