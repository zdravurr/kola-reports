# report

_2026-07-18 09:03 UTC_

---

🧠 САМООБУЧЕНИЕ 18.07 09:00 UTC (разбор 24ч)
⚙️ Свои баги — чиню сам, до автоматизма, без костылей и без запроса добра.
Здоровье: 🟢0 🟡3 🔴3
🔴 Красные: fb_social, sales, social_x

♻️ Повторы:
• [cars_ev] kola-self-improve-daily пишет в LEARNINGS.md неидемпотентно (14 блоков '— green' подряд, 2026-07-03 записан трижды). Фикс предложен 13.07, повторён 16.07 и 17.07 — внедрение не подтверждено, 4-й цикл долга
• [cars_ev] Само-разбор не может верифицировать собственный фикс: попытка найти хендлер режется пермишенами (подтверждено в этом прогоне — grep/ls по каталогу проекта требует апрува)
• [fb_social] subprocess.TimeoutExpired на su -c через SSH (25с) — 4-е сутки подряд (06-08.07, 16.07, 17.07, 18.07); те же вызовы: monkey launch, input keyevent, uiautomator dump. Единственный класс ошибок в логе.
• [fb_social] Нулевой исход постинга: ни одного post_PUBLISHED за сутки, долг растёт.
• [fb_social] Мета-регресс: фиксы от 16.07 и 17.07 (гейт по post_PUBLISHED, форс-портрет, удалённый timeout, health-check) не применены — в логе те же PIXEL_SSH без BatchMode/ServerAliveInterval и su -c без timeout. Разбор генерит планы, которые никто не вносит в код.
• [infra] boot_status: all_ok=True при node=stale, regressed=[] и repaired=[] — node не входит в конъюнкцию агрегата; фикс обещан 16.07 и 17.07, не сделан (3-й день)

🔧 Трив-фиксы (применю в сессии):
• [cars_ev] Разрешить хендлеру kola-self-improve-daily читать LEARNINGS.md и делать ls/grep по каталогу проекта без интерактивного апрува — иначе само-разбор физически не может проверить внедрение своих же фиксов и будет писать одну рекомендацию бесконечно. Это блокер для всех остальных фиксов ниже.
• [cars_ev] Идемпотентная по дате запись: если блок за сегодня уже есть — перезаписать, не аппендить; при чистом прогоне обновлять единственную строку 'последняя проверка: <дата> — green' вместо новой секции
• [cars_ev] Разделить хартбит и результат: строка-хартбит с датой пишется ВСЕГДА при старте прогона, секция с уроками — только при yellow/red
• [cars_ev] Разово схлопнуть хвост 01.07–12.07 (14 блоков '— green') в одну строку-хартбит
• [fb_social] Обернуть удалённую команду собственным таймаутом: su -c 'timeout 15 <cmd>', локальный subprocess timeout держать строго больше (25>15). В PIXEL_SSH добавить -o BatchMode=yes -o ServerAliveInterval=5 -o ServerAliveCountMax=2. Локальный timeout рвёт только ssh-клиента, su на Pixel остаётся жив и блокирует следующие вызовы — отсюда каскад.
• [fb_social] Ранний health-check перед сессией: su -c 'timeout 5 id'. Не прошёл → пишем device_unreachable, сессия не стартует, слот ротации НЕ сжигается. Сейчас прогон лезет в UI на мёртвом su и заканчивается ложным post_nav_fail.

🛠 В сессии (нужен разбор):
• [fb_social] СНАЧАЛА обеспечить применимость фиксов: из песочницы /home/botuser нет доступа к /mnt/volume и к устройству, поэтому правки fb_active.py физически не вносятся. Либо смонтировать/дать путь к коду в сессию, либо назначить внешнего исполнителя. Без этого дальнейшие пункты — снова план, а не фикс.
• [fb_social] Верифицировать наличие гейта по post_PUBLISHED (ts > T0) прямо в коде, а не в плане: ev_recurring_last и анти-дубль на 8ч двигаются только по факту записи в fb_active_log.jsonl. Если гейта нет — слоты продолжают сгорать вхолостую.
• [infra] Ветка auto-repair не вызывается, пока regressed[] пуст: считать stale-компоненту regressed, рестартить (pixel_ssh/eyes/hands/node) и писать поднятое в repaired[]; при N подряд неудачах → --notify
• [provisioning] Передавать разбору реальные пути к LEARNINGS.md и скрипту self_improve_daily (или запускать его в рабочей директории проекта) — без доступа к файлам ни один trivial-фикс не может быть применён, и цикл предложений не разорвётся
• [sales] Эскалация в post_watchdog: если по каналу N подряд тиков 🔴 и счётчик часов монотонно растёт — прекратить повторять тот же алерт, пометить канал СЛОМАН и отправить ОДИН отдельный алерт Боссу «постинг X не работает N тиков / M часов». 12 одинаковых строк в сутки = шум, читаемый как «я вам уже говорил»
• [sales] Из main-окружения (не из песочницы) выяснить, почему FB группа SoCal EV молчит ~8 суток: перевзводится ли client_pending после снятия 13.07, или у growth-сбора вторая причина скипа. Линейный рост счётчика без единого успешного поста = скип на входе, а не флаки-отправка

📈 X-контент (разбор 12 постов за сутки):
• Лучший охват: [zdravurr_ca] запуск дилерства через макро-рамку «Tesla = хедж против печатного станка» — 178 views, в 3.6x выше лучшего fin-поста; связка «личное объявление + макро-тезис» дала пик
• Что заходит: Работает формула «Это не A, это B» + НАЗВАННЫЙ игрок и структурный сдвиг (Circle/банки=49, Stripe/PayPal=14, Empery/mNAV=15). Проваливаются посты про уровень цены BTC: $63K повторён в 4 постах → 23/22/8/1 view, аудитория выжжена повтором одного тезиса
• Посты завтра: Завтра: максимум 1 пост про цену BTC, остальные — про конкретные компании/регуляторов с последствием для читателя. И убрать число $63K из хука: первые 8 слов должны нести имя игрока + глагол действия («Circle получил чартер — банки только что...»), а не котировку
• Комменты завтра: Комментить в первые 10–15 мин под крупными аккаунтами своей темы (fin: макро/стейблкоины; EV: SoCal-дилеры, кредиты) той же формулой «не X, а Y» + один свой факт с лота/тейпа. Без ссылок, без общих согласий типа «this», 1–2 предложения — заход в чужой охват сейчас единственный источник новых глаз при базе 1–50 views

📥 Свип инбокса не удался: client_pending — yielded

🧩 Стек генерации:
• ✅ генерация: Opus 4.8 (CLI/подписка)
• ✅ Gemini=только память (генерация закрыта, HTTP 429)

🔧 Фиксы 04.07:
• ✅ notif_watch = dumpsys-first
• ✅ translate_ru placeholder-гейт
• ✅ FB-capture мукс (kola_tap)
• ✅ inbox_sweep thread-guard
• ✅ X CDP-мост alive
• ✅ кроны: inbox_sweep + bridge-keepalive
• 🔴 inbox-watcher service active
• ✅ orca: авто-снос restore-модалки
• ✅ orca: общий предохранитель go_home_generic
• 🔴 покупатель: draft всегда есть (не дёргаю Босса по 'available?')

📮 X-постинг (таб+dedup+свежесть):
• ✅ X: живой page-таб есть (не sw.js)
• ✅ X: find_tab фильтрует type==page
• ✅ X: dedup порог ≥3 (моно-ниша не глохнет)
• ✅ X: нет оборванных постов за 24ч (гейт завершённости)

🚦 Прогревы (гейт перед стартом дня):
• ✅ X @zdravurr (фин): посл. пост 11ч (порог 14)
• 🔴 X @zdravurr_ca (EV): посл. пост 33ч (порог 30)
• 🔴 FB группа SoCal EV: посл. пост 199ч (порог 28)
• ✅ X CDP-мост (Chrome на Pixel)
• ✅ FB прогрев (Alex Man) guard=yellow

(код из крона сам не правлю — фиксы применяю в сессии; журнал в LEARNINGS)
