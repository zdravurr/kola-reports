# kolya-e7e033f624a8

_2026-09-03 12:56 UTC_

---

# 🧠 Само-разбор infra 03.09 (окно 24 ч) — задача e7e033f624a8

**Итог трека infra: 🟡 yellow.** Регрессов нет, но ФАКТ-успеха по своей ветке нет — зелёным не зову.
**Что реально починено сегодня: ложная тревога ВНУТРИ рубежа №3.**

## Корень, найденный по уликам самой задачи

Задача пришла с заголовком «🔴 Коля — REPORT-Telegram НЕ ПРОШЁЛ (проверь @Orchetkoyyyyy_bot!)».
Telegram не падал. Этот текст стоял **темой по умолчанию** в `notify_boss()` — то есть его
получал каждый вызыватель, тему не задавший, включая совершенно успешный ежедневный
само-разбор.

Куда тема доходит: единственный её потребитель — рубеж №3 (`boss_gate`, поставлен 02.09),
который кладёт её в улику `исходный subject` заведённой техзадачи. Обе отведённые
03.09 задачи само-разбора (`3106c26b0dd0`, `e7e033f624a8`) открывались словами о провале
доставки при живом канале. Техник читает ярлык как диагноз и идёт чинить сбой, которого
не было ни секунды, — ровно тот класс ложной тревоги, против которого рубеж и строился.

Побочно: `verdict()` классифицирует по blob = `subject + text`. Константа в теме
подмешивалась в **каждый** разбор бес-темной тревоги: совпади в ней хоть один шаблон
`BOSS_KEEP` — и вся такая техника молча поехала бы Боссу мимо белого списка. Мины не
сработало (шаблоны не совпали), но заряд лежал.

## Что сделано

- `lib/notify.py` — тема по умолчанию снята: `subject=None`. Тема это **ярлык, а не
  диагноз**; в `boss_gate` уходит `subject or ""`.
- `lib/boss_gate.py` — улика обязана быть фактом: пустая тема больше не выдаётся за
  настоящую, пишется `— (вызыватель тему не задал)`. Закрыто с двух сторон, на случай
  любого будущего вызывателя без темы.
- `tests/test_alarm_without_subject_wears_no_failure_headline.py` — новый контракт,
  12 проверок: нет диагноза в умолчании; без темы улика честная; настоящая тема доходит
  дословно; тема не меняет приговор (COMMIT GATE и неопознанное по-прежнему Боссу).
- **Контракты: 208/208 зелёные** (было 207 + новый). Боевое состояние тестом не тронуто
  (журнал рубежа 25 строк, задач 198 — как до прогона).

## Рубеж №3 за первые сутки — работает

25 событий: техника уходит в очередь (`ОТВЕДЕНО` — молчащий детектор, два само-разбора),
сообщения о уже существующих задачах гасятся на месте (`ПОГЛОЩЕНО … дубль`) и второй
записи не плодят. Ни одного «ОТВЕСТИ НЕ СМОГ».

## Повторы трека — по стоячей дисциплине, без ре-эскалации

- `pid1274433` (удалённый `zdravurr_ca_auto.py`): рецидив монотонного роста до 109 ч →
  очистился сам, хвост «все пути живые». Хронический не-рестарт при деплое; фикс
  авто-рестарта предложен 17.08, не применён — **не ре-эскалирую**.
- `boot_status`: `node=stale` вне конъюнкции `all_ok`, `regressed=[]`/`repaired=[]`,
  авто-починка не зовётся — трив, эскал. 25.07.
- `launcher_path_audit`: двойной блок + «найдено N (🔴0)» без деталей — трив, эскал. 25.07.

Путь к применению этих трёх недоступен с 25.07; держу молча, как договорено.

## Решения от Босса не требуется

Ничего не жду. Красный трек `social_x` (tab-каскад / фокус вкладки) — не эта задача,
он идёт своим треком со стоячей эскалацией.
