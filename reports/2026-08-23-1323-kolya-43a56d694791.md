# kolya-43a56d694791

_2026-08-23 13:23 UTC_

---

# 43a56d694791 — клиент question.list на dirigent: опознан транспорт, имя клиента — нет

## Что сделано
Сессия НЕ была заперта песочницей (в отличие от прежнего захода): journalctl и ~/.openclaw читались. Пошёл прямо по уликам.

## Факты (добыты, переоткрывать не надо)
- Гейтвей слушает **только 127.0.0.1:18789** (ss -tlnp, pid 558811).
- Шумные соединения приходят **через tailscaled**: сокеты 127.0.0.1:54486/54492 лежат в cgroup `/system.slice/tailscaled.service` (ss -tne), т.е. tailscale serve с tailnet. Значит клиент физически на ДРУГОМ узле (dirigent), не на этой машине.
- Прежняя гипотеза «петля переподключений, conn каждый раз новый» **не подтвердилась сейчас**: conn стабильный — один `conn=bc4d78b1…0034` даёт 306 вызовов за 30 мин (~10/мин). Это **опрос**, а не реконнект-петля.
- Гейтвей на текущем logLevel **не пишет ни одной строки про открытие ws-соединения**: за 6 ч в журнале только `res ✗ question.list … conn=… id=…`. Полей peer/token/user-agent там нет вообще — предложенный путь «вытащить поля из журнала» упирается именно в это.
- Пары: `~/.openclaw/devices/paired.json` — 4 записи. linux `clientId=cli`, role=operator, **scopes=[operator.admin]**, lastSeen свежий; три android-ноды (Galaxy Tab A11+ 5G, Pixel 10a ×2) со scopes без admin. Аутентификация гейтвея: `gateway.auth.mode=token`, общий токен из файла `gatewayAuthToken`; trustedProxies=[127.0.0.1,::1].
- Наш код на dirigent question.list не зовёт: `projects/dirigent/loop.sh` — цикл раз в 300 с, `dirigent.py`; ни `18789`, ни `ws://`, ни openclaw-клиента там нет. Значит зовёт **клиентская сторона самого openclaw** на узле dirigent.

## Почему не погашено
Дотянуться до узла dirigent из этой сессии физически нельзя: docker API — permission denied, `tailscale` CLI и `ssh 100.76.85.17` — не в allowlist. Имя клиента без строки ws connect не выводится однозначно, а гадать и на глазок резать токен — нельзя.

## Что дальше (уже заведено задачей, Босса ждать не надо)
`derive` от этой задачи: временно поднять logLevel гейтвея → снять строку ws connect для `conn=bc4d78b1…0034` (clientId/deviceId/publicKey/remoteIp/ua) → сматчить deviceId с paired.json. Нет в paired → входит по общему токену гейтвея, гасится ротацией/отзывом токена на стороне dirigent; есть → гасить процесс на dirigent. Потом logLevel вернуть.

## Вопрос прав (не решаю, фиксирую)
Выдача `operator.admin` этому токену — решение Босса, и задавать его **всё ещё нельзя**: имя клиента не названо.

Кода не менял — контракты не гонял (нечего проверять).
